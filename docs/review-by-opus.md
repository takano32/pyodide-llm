# レビューを Fable から Opus xhigh に替えるときの懸念と、うまくいかせる決まり

2026-09-25、持ち主の問い「いまは Opus medium が実装して Fable high がレビューしているが、節約のために Opus medium + Opus xhigh にしたい。何が懸念で、どうすればうまくいくか」への Fable の検討。根拠はこの日までの実例（TODO.md の T72・T77・T82・T93・T96・T98〜T100・T109・T110）。

## 結論

**できる。ただし「読むだけのレビュー」にしないこと。** この 1 日の実例で、レビューが実際に捕まえたものの大半は「読んで気づいた」のではなく「走らせて・境界値を入れて・CI の記録を読んで」見つかっている。同じモデルが実装と審査の両方をすると、読むだけのレビューは実装と同じ思い込みを共有して素通しになる。だから決まりは 3 つ:

1. **レビューは別の会話で、実装の文脈を持たずに始める**（同じ会話の続きで「自分の書いたものを見直す」形にしない）。
2. **OK と言う前に必ず動かす**（pytest・smoke・forward-check に加えて、ページや Worker を触る変更は **CI の実ブラウザで本番相当を 1 回**）。
3. **設計の判定は「数字と式と、判定を覆す条件」を書く**（読めば持ち主が追える形にする）。

Fable は残す。ただし呼ぶのは下の「Fable に回すもの」だけにする。

## 懸念（実例つき）

### 1. 同じモデルは同じ思い込みを共有する

- T72（2026-09-21）: 変換器が `rotary` と `parallel_residual` をエンジンに渡していなかった。**テストは全部通り、ブラウザで「動くが文章が壊れる」形で出た。** テストが `Llama()` を直接呼んでいたので、実装した側の前提（設定はちゃんと渡る）を試験も共有していた。
- T77・T85: `layout()` と engine に `bias` / `arch` を足したのに `checkpoint_dtype()` と `convert_hf.py` に足し忘れた。**「`arch` を受け取る関数を足したら呼び出し側を全部探す」**という機械的な確認で捕まる種類だが、実装した側は「足した」と思っているので探さない。
- 今日の T109（Fable 自身の変更）: helper.js の先頭に本当に待つ `await import()` を置いたら、ブラウザは最初のメッセージを取りこぼしてページが止まった。**Node の試験は全部通った**（Node はメッセージを溜める）。捕まえたのは CI の画面撮りと本番確認で、レビューではない。Fable でも同じ思い込み（Node で通れば動く）を持った。

**同じモデルにすると、この種の素通しが増える**と見るのが妥当。対策は「別の会話」「動かす」「機械的な確認の一覧」で、モデルの賢さで埋めない。

### 2. 設計の判定は「読み方」に価値がある

今日の判定で、Fable が足したものは次の 3 つ。Opus xhigh にもできるはずだが、**「書き方を決めておく」ことで質を保つ**。

- T98: Opus の表は「llama.cpp の Q4_K_M は +3〜5%」で止まっていた。Fable はそれが名前に反して**大半 5 ビット + 一部 6 ビット**（dim が 256 の倍数でないため）だと読み、「4 ビットは届かない、5 ビットの一様な形式なら届く見込み」に変えた。表の数字ではなく、数字がどの条件から出たかを読んだ。
- T100: Opus は受け入れ率と費用を測って報告した。Fable はそれを 1 つの式（1 回で進むトークン数 ÷（確かめる費用 + 下書きの費用））に入れ、いちばん有望な組でも 0.95 倍と出して「実装しない」と判定した。**測ることと、測った数字で決めることは別の仕事**。
- T101: `asc` の CLI が 32 ビット固定でも、コンパイラの公開 API で `Target.Wasm64` を選べることを読んで、C への書き直しでなく「小さなドライバ」の道を書いた。

**判定の書式を決める**: (a) 使う数字と出どころ、(b) 式、(c) 判定、(d) **判定を覆す条件**（「これが測れて X 以上なら逆」）。(d) があると、持ち主が判定を追えるし、次の回で見直せる。

### 3. 「見た目」と「訪問者に見える文」の判断

描画のタスク（T75・T76・T88）を Fable に寄せていたのは、見た目の判断のため。今日、**CI で画面を撮って持ち主が見る仕組み**（`preview.yml`・`tests/screenshots.mjs`）ができたので、これは Opus でよい。決まりはそのまま: コミットの前にスマホ幅と PC 幅の画面を撮って持ち主に見せる、訪問者に見える日本語は先に文面を出す。

### 4. レビュー担当が「OK」を出しやすくなる

同じモデルの xhigh は、実装した側の説明（TODO.md の記録）を読んで納得しやすい。今日のレビューで must-fix が出たのは 6 件中 3 件（T74・T82・T99）で、どれも**記録に書かれていないところ**（境界、失敗の経路、戻り値）を突いて出た。「記録を読んで確かめる」のではなく「記録に無いところを探す」のが役目。

## うまくいかせる決まり

### レビューの手順（OK と言う前に全部やる）

1. **別の会話で始める。** 入力は TODO.md の項と `git diff`、AGENTS.md。実装した会話の文脈は持ち込まない。並列にするならサブエージェントを**新規**で（fork でなく）起こし、同じ入力だけを渡す。
2. **動かす。** `.venv/bin/python -m pytest tests -q`（リポジトリの中の venv、docs/dev-setup.md。system の python3 では 62 件が skip になる）、`node tests/smoke.mjs`、`node tests/forward-check.mjs`（共有と `--plain` の両方）、スレッドに触るなら `node tests/threads-check.mjs`。**ページか Worker か forward.js か helper.js に触る変更は、`models.yml` を本番に対して走らせて通るまで OK と言わない**（今日の T109 の止まりは、これだけが捕まえた）。
3. **境界を自分で入れる。** レビュー担当は「記録にある確認」を信じず、記録に無い入力を 1 つ以上足す（今日の例: `to_f16` の 326 個の境界値、`attention_f16` の half の全数、GPT-2 の外れ値の列 × 下書き）。リポジトリに入れるかは別として、やったことを記録に書く。
4. **機械的な確認の一覧を回す**（AGENTS.md の落とし穴から）: `arch` / `bias` / `dtype` を受け取る関数を足したら呼び出し側を全部探す、legacy 形式を変えたら 3 か所 + Cache の鍵、Worker が読むファイルを足したら `?v=`、モジュール Worker の先頭で await しない、`git diff --cached --stat`、AGENTS.md と TODO.md が実装と合っているか。
5. **失敗の経路を読む。** 成功の経路は実装した側が確かめている。容量切れ・短い書き込み・中止・2 回目の読み込み・別のブラウザ、を 1 つずつ問う。
6. **書く**: 「レビュー（Opus xhigh、日付）: 判定。直したもの。確かめて問題なしだったもの。あれば良い（やらない）」の 1 行。must-fix が 0 の回が続いたら、それ自体を持ち主に言う（見つけられていない可能性のほうが高い）。

### 判定の手順（設計を決めるとき）

- 上の書式 (a)〜(d) で書く。数字は測ったものだけ（推測は「未計測」）。
- 選択肢が 2 つ以上あって、どちらも筋が通るときは、**選ばずに持ち主に出す**（今日の T98 の「+3〜5% を許すか」は持ち主が決めた）。
- **方針（AGENTS.md の番号つきの方針）に触る判定は持ち主**。これは今までどおり。

### Fable に回すもの（短い一覧）

- 実装とレビューの意見が割れたとき。
- 形式を変える設計（legacy 形式、Cache の鍵、Worker とページの契約）で、後から直しにくいもの。
- レビューが 3 回続けて must-fix を出さなかったとき（1 回だけ Fable に読ませて、見落としが無いか確かめる）。
- 持ち主が「数字の読み方を確かめたい」と思ったとき（T84・T95 の公開する表）。
- **GPU のシェーダ**（速さの核。T146 から、2026-09-26 の持ち主の判断。effort は max）: タイルの形、共有メモリ、packed int8 の量子化、端の扱い、端末ごとの違い。

### 費用について

xhigh はトークンあたりの費用が高いが、レビューと判定の会話は短い（今日の 6 件のレビューはサブエージェント 3 本で合計 30 分弱）。費用の大半は実装の会話で、そこは medium のまま。**節約になるのは「Fable を常駐させない」こと**で、レビューを薄くすることではない。上の手順 2 の CI の走行は無料ではないが、今日の T109 の止まり（本番を約 40 分止めた）を防ぐ費用としては安い。

## この文書の置き場

AGENTS.md の「役割」から参照する。決まりが変わったら（Opus xhigh に替えた日、Fable に回した件）、ここに日付つきで足す。

## 最初の回（2026-09-25 の夜、Opus xhigh）で分かったこと

- **must-fix は 5 件**。うち 2 件は**動かして見つけた**: 記録に無い `?wide=on&coi=off` を CI で試したら、隔離されていないページでは int8 のモデルが全部読み込めなかった（`SharedArrayBuffer` という名前がブラウザには無い。T110 から、T113 のフォールバックも巻き込んでいた）。同じページでモデルを替える経路（`E2E_THEN` を足した）を試したら、tiny-lm の後の Pythia 1B が読み込めなかった（T96）。残り 3 件（ベンチの後始末、URL と画面、transformers 5 の `rope_parameters`）は読んで見つけ、CI かこの開発機の transformers で確かめた。
- **Node で通る試験は、ブラウザにしか無い条件を素通しにする**（T109 の await と同じ種類）。今回は `SharedArrayBuffer` の有無: `tests/engine.mjs` が `--plain` のときその名前を消すようにした。**`models.yml` は 1 モデル 1 ページ**なので、訪問者がいちばんよくやる「既定のモデルの後に別のモデルを選ぶ」は試されていなかった。
- 並列の仕方: 新規のサブエージェント 4 本（描画、Worker と Service Worker、エンジン、Memory64）に読ませ、試験を回すのは本体だけにした（この開発機のメモリ）。サブエージェントは偽物の Cache・fetch・Memory の上で worker.js と coi.js の関数をそのまま動かす試験台を作り、境界（全体を返すサーバ、短い 206、共有メモリの取れない端末）を突いた。must-fix の候補は本体が CI かコードで確かめてから直した（7 件の主張のうち 1 件は T96 の「3 つ目のメモリ」で、CI では再現しなかったので、参照を切るだけにした）。

## 2 回目の回（2026-09-26、Opus xhigh）で分かったこと

- **must-fix は 3 件で、3 件とも「本物」と突き合わせて見つけた**: rinna の改行（本物の sentencepiece と ID の列で）、読んだ書式の古い日付（本物の transformers の `apply_chat_template` と 1 ターンで）、Python の中で投げた JavaScript の例外（CI の WebKit で記録に無い 7B を選んで、画面の文を読んで）。どれも記録にある確かめ方（合成のモデル、Node の試験、Chromium の本番）には出ない。**書式とトークナイザは、本物のライブラリ（transformers・sentencepiece・`tokenizers`）がこの開発機にあるので、新規のサブエージェントに突き合わせさせるのがいちばん効く**（1 本で数百件）。
- 直した後にもう一度同じ突き合わせを回す。T127 の日付の直しは、直した直後の再比較で 1 件（日付で計算するテンプレート）が新しく「違う」に出た。
- 見積もりは CI で確かめてから書く。sarashina2.2 3B は Safari で入らないと見積もって note を直しかけたが、見積もりに relaxed SIMD の補正を入れていた（WebKit には無い）。CI の WebKit で入って答えた。

## 3 回目の回（2026-09-26、Opus xhigh、a1-free での最初の回）で分かったこと

- **must-fix は 6 件で、どれも動かして見つけた**: T134 の 3 件は CI の bench.yml の記録を読んで（WebKit だけ回線の絞りが 1.2〜5 倍ずれていた）と、わざと壊した版を 9 通り走らせて（デバイスを失う、突き合わせが例外）。T139 の 2 件は新しい写しで文書の手順を書いてあるとおりに回して。DeepSeek-R1 は本物の BOS と今の BOS で perplexity と greedy の文を比べて。読んだだけで見つけたものは無い。
- **決まり 1 を守れない形が出た**: レビューの会話が、同じ日に T139 の段 6 を実装していた。T139 だけ新規のサブエージェントに見させ、判定をそのまま書いた。そのサブエージェントは、自分の文脈に自動で入った AGENTS.md が会話を始めた時点の古い版だったと報告した。**サブエージェントには、ファイルを読み直させる**（頼みに「最初に AGENTS.md を読む」と書く）。
- **待ちが終わらないと、CI が遅く見える**: サブエージェントの `until ! pgrep -f …` が自分自身に一致して 52 分回り、持ち主には「CI がとてつもなく遅い」と見えていた（GitHub の記録では数分）。CI は `tests/ci.mjs` で ID を待ち、手元の計算は PID か印のファイルで待つ（T142、AGENTS.md の落とし穴）。サブエージェントへの頼みにも書く。
- **サブエージェントの作業ファイルの置き場を頼みに書く**: 頼みに `~/tmp/review-…` と書いて約 28GB を `$HOME` の下に置かせていた。いまはリポジトリの中の `.tmp/`（持ち主の指示）。
- **担当 2 人の判定が割れたら選ばない**: `?hf=` の特殊トークンは、T124 の担当が must-fix、T138 の担当が「次に `CONVERTER` を上げるとき」。直すと訪問者の保存が全部取り直しになるので、T143 にして持ち主に出した。

## 4 回目の回（2026-09-26、Opus xhigh）で分かったこと

- **must-fix は 2 件で、どちらも動かして見つけた**: GitHub のログインが長い報告を落とす件は curl で飛び先を追って（前の回は「302 が返った」を「通った」と読んでいた。**状態の番号でなく、飛び先の中身まで見る**）。GGUF の行の NaN は、CI の表にすでに「0 (nan at row 8)」と出ていたのを担当が読んで見つけた。**CI の表の変な値（nan、0 なのに注記つき）は、通っていても読む。**
- 記録の数字の誤りが 2 つあった（format_check の「16 が 9/9」は 15、既知の違いの名前 2 つ）。数えた本人の記録でも、レビューで数え直す。
- この回の途中で、持ち主が T135 を今から並行で始めると決めた。実装は新規のサブエージェントにワークツリーで渡し、レビューの会話では仕様を書くだけにした（決まり 1: 実装とレビューを同じ会話で混ぜない。T135 の第 1 段のレビューは次の回で）。
- 並行の数: レビューの担当 5 本と実装 1 本、CI を 12 本。a1-free のメモリは、担当ごとに 3GB の枠（`systemd-run`）で足りた（bench-check の GPU の節だけ 3GB を超えた）。
