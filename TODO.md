# TODO

このプロジェクトの作業台帳。背景・方針・落とし穴・検証手順は [AGENTS.md](AGENTS.md) にあるので、先にそちらを読むこと。

**いまの状態と次にやること**は、下の「Opus の回とレビューの回」の節の「次の回」と、そのあとの「レビューの回」の記録にある。その節の「Opus の回（次）」と、下の「いまの勧める順番（2026-09-25）」は当時の記録（その頃の開発機ではブラウザを動かさなかった）。

- **進め方（2026-09-25 の夕方から、持ち主の決定）: 基本の作業は Opus（medium）が進め、Opus（xhigh）が別の会話でレビューと判定をする。** 手順と懸念は [docs/review-by-opus.md](docs/review-by-opus.md)（別の会話で始める、OK の前に必ず動かす、判定は数字と式と覆す条件を書く）。Fable を呼ぶのはそこにある短い一覧のときだけ（意見が割れた、形式を変える設計、must-fix が 3 回続けて出ない、公開する数字の読み方）。見た目のタスク（`[描画]`）も Opus で、コミットの前に `preview.yml` で画面を撮って持ち主が見る。2026-09-24〜25 は「Opus が実装、Fable がレビュー」だった（完了の記録の「レビュー（Fable、日付）」はそのとき）。
- 状態は `未着手` → `進行中` → **`レビュー待ち`** → 完了。Opus は実装・検証・文書を終えて push したら、状態を `レビュー待ち` にして「完了したタスク」の末尾に `- [x]` で移し、結果の要点（実測値・理由）を書く。**レビュー担当はその項に「レビュー（Opus xhigh、日付）: …」を 1 行足す**（OK、または直したこととそのコミット）。それまで完了とは呼ばない。
- **レビューの観点（T77 で決めた 5 つ）**: (1) 形式やオプションの増やし方がこの先も耐えるか、(2) 抽象がちょうど良いか（足りない・多すぎない）、(3) 速度に効く場所に分岐が入っていないか、(4) テストが本当に回帰を捕まえるか（参照との一致・ストリーム変換・options の経路・**実物のモデルで人が読む**）、(5) AGENTS.md と TODO.md が実装と合っているか（**台帳の壊れも含む**）。小さい直しは Fable がそのまま入れてよい（理由ごとにコミットを分ける）。大きい作り直しは持ち主に相談する。
- 詳しい知見は AGENTS.md に足す。**作業と同じコミットで更新する。**
- タスク番号は通し番号で、再利用しない。分割や取りやめで使わなくなった番号も欠番のまま残す（T40 は T41 と T42 に、T61「Llama 以外のモデルにも対応する」は T63・T64・T65 に分けたので欠番）。
- タスクの題名の先頭に、漢字 2 文字の分類を付ける: `[追加]`（動くモデルを増やす）、`[計測]`（測る道具と、測った数値の公開）、`[性能]`（tok/s やメモリ）、`[描画]`（ページの見た目と操作）、`[文書]`、`[運用]`（CI、デプロイ、リポジトリ）。完了したタスクには付けていない（2026-09-20 から）。
- 未着手のタスクは、依存のあるものを除いて単独で着手できる。**いまの勧める順番（2026-09-25、持ち主の判断）: 公開する計測は性能の改善の後に取る**（先に測ると、公開した数字がすぐ古くなる）。(1) 性能と機能の改善（若い番号から）: T105（分割された safetensors）→ T106（Llama 3 系）→ T107（取得の速さ）→ T108（プロンプトのまとめ処理）→ T98（int4 の重み）→ T99（OPFS）→ T100（投機的デコード）→ T109（attention をスレッドで）→ T110（float16 の KV）→ T111（Pyodide の保持。持ち主の判断待ち）→ (2) T101（Memory64 の判定）→ (3) **不具合の調査はここでまとめて**: T96・T97（機能を足せば不具合も増えるので、改善が終わってから一度に調べるほうが効率がよい。2026-09-25、持ち主の判断）→ (4) T81（足せるモデルの調べ直しと追加）→ (5) T84（梯子の表）・T95（wllama との比較）→ (6) T83（gist）。描画の T75・T76・T88 と、T91（T76 の後）は Fable の回で。**レビュー待ち（Fable）**: T82・T74・T86・T89・T93。保留: T102〜T104（T94 は 2026-09-25 に採用して仕様を詰めた）。（2026-09-24 の Fable の順番は git の履歴にある。そこに並んでいた T82〜T91 は、この 2 日で済んだかこの順番に入った）

## Opus の回とレビューの回（2026-09-25 に持ち主が決めた進め方。レビューは 2026-09-25 の夕方から Opus xhigh）

**Opus の回（次）**: T81 の前にやることのうち、Opus でできるところを順に全部。タスクごとに実装・検証・文書を終えてコミットと push、デプロイの後で本番を確かめてから次へ（AGENTS.md の決まり）。この開発機ではブラウザを動かさず、実ブラウザの確認は CI で（`browsers.yml`・`threads.yml`・`coi.yml` は Opus が走らせてよい）。メモリの多い計測は `systemd-run --user --scope -p MemoryMax=…` の中で。各項の「担当（Fable の切り分け）」に書いた**止まる点**で止まり、そこまでを「レビュー待ち」にして完了の欄へ移す。

| 順 | タスク | Opus がやる範囲 | 止まる点 |
|---|---|---|---|
| 1 | T105 分割された safetensors | 最後まで | なし |
| 2 | T106 Llama 3 系 | 第 1 段: Llama-3.2-1B-Instruct の `config.json` と `tokenizer.json` を読み、断られる理由と足す量を書く | 実装の前で止まる（レビュー担当の判定） |
| 3 | T107 HF からの取得の速さ | 最後まで（計測のワークフロー、表、既定の値） | なし |
| 4 | T108 プロンプトのまとめ処理 | 最後まで | なし |
| 5 | T98 int4 | 第 1 段: Q4_0・Q4_1・Q4_K の読み手（計測用）と品質の表 | 表を出して止まる（レビュー担当が形を決める） |
| 6 | T99 OPFS | 最後まで（事前判断のとおり） | なし |
| 7 | T100 投機的デコード | 第 1 段（受け入れ率）と第 2 段（まとめて計算する forward の費用。T108 の部品で） | 費用を出して止まる（レビュー担当が組み合わせと k を決める） |
| 8 | T109 attention をスレッドで | 最後まで（先に測って、効かなければ取りやめ） | なし |
| 9 | T110 float16 の KV | 最後まで（perplexity で +0.1% を超えたら取りやめ） | なし |
| 10 | T111 Pyodide の保持 | 効きの計測だけ（CI で 2 回目の準備完了を保持あり・なしで） | 数字を出して止まる（持ち主の判断） |
| 11 | T101 Memory64 | 判定の計測（作れるか、共有と両立するか、32 対 64 の速さ、対応） | 報告して止まる（レビュー担当と持ち主） |
| 12 | ~~T96・T97 不具合の調査~~ | **この回からは外した**（2026-09-25、持ち主の判断: Fable のレビューを通ったコードで試してから調べる） | — |

**持ち主が決めること → 2026-09-25 に決まった**（持ち主の判断）: (a) **T111 は採る**（オフラインでも使えるようにする。方針 2 を改めた）、(b) **T101 は加える**（4GB を超えるモデルは Safari で動かなくてよい。方針 6 に足した）、(c) **T98 は +3〜5% の品質の低下を許す。ただしコードをきれいに保つことを優先する**（方針 5 に足した）、(d) **どのモデルも入れる**（非商用の Qwen2.5-3B も、Llama 3.2 を unsloth の写しから取るのも可。T81・T106）。

**T101 の後に Opus xhigh のレビューの回（2026-09-25、持ち主の決定）**: T101 が本番で確かめられたら、Opus xhigh の会話に切り替えて「レビュー待ち」を全部見てもらう。T97・T84・T95・T81・T83 はその後。

**Opus xhigh のレビューの回 — 済（2026-09-25 の夜）**: レビュー待ちの 12 件（T75・T76・T88・T91・T96・T98・T101・T106・T111・T112・T113・T114）を見た。手元の試験（pytest・smoke・forward-check の共有・`--plain`・`--wide`・threads-check）と、`models.yml` で本番に対して記録に無い境界（同じページでのモデルの切り替え、`?coi=off`、`?wide=on&coi=off`、`?bits=6&without=kernels`、`?wide=on&bits=6`、HF のモデルのオフライン、WebKit・Firefox、gemma）を走らせた。**must-fix は 5 件で、どれも直した**: 隔離されていないページで int8 のモデルが全部読み込めなかった（T110 から。T113 のフォールバックも）、T96 の使い回すメモリに次のモデルの補正が入らなかった、ベンチが失敗すると状態が戻らなかった、URL と画面の食い違い（T75・T88）、transformers 5 の config.json の RoPE を読まなかった（T106）。ほかに直したもの: `?revision=`、前分割、Range の 200 と短い 206、効いていない待ちの確かめ、deploy の 64 ビットの試験、`browsers.yml` の WebKit。11 件を完了に移し、T112 は持ち主の Safari の確認が残る。**直したほうがよいもののうち残りは T115〜T120 に採番した**（持ち主の指示）。

**T81 の前にやること（2026-09-25、持ち主の指示「T81 の前にやるべきことは採番して台帳にまとめる」）**: T115（32 ビットと 64 ビットの判定と使い回しの判定。T81 は 3B 級を数えるので最初）→ **T123（変換と読み込みをマルチコアで。2026-09-25、持ち主の指示「マルチコア対応いれよう」。カーネル 2 つで済み、スレッドは取りやめ）** → T116（保存の鍵にビット数と変換器の版）→ T119（画面の小さな直し）→ T120（止められたソフトウェアスレッドを待ち続けない）→ T97（Windows の Firefox）→ T117（済、CI で確認）→ **T81** → T84・T95 → T83。

**Opus xhigh のレビューの回 — 済（2026-09-26）**: レビュー待ちの 9 件（T115・T116・T117・T118・T119・T120・T123・T97・T81）を見て、9 件とも完了に移した。手元の試験（pytest・smoke・forward-check の共有・`--plain`・`--wide`・threads-check・kept-check・coi-js-check・models-check）と、新規のサブエージェント 3 本（worker.js を Node の `vm` で偽の fetch と Cache API の上で動かす試験台、forward.js の確保を全部なぞる再生と SIGSTOP の試験、T81 のモデルを本物の Jinja と `tokenizers` で突き合わせ）、`models.yml` で本番に対して記録に無い境界（フォルダの経路 `local`・`hf` を 3 つのエンジンで（models.yml が走らせられるようにした）、`?coi=off` でのモデルの切り替え、Firefox の 7B（64 ビットのメモリ 9.7GB）、Chromium の 7B の construct、2 シャードの 3B、Windows の Firefox、オフライン、HF の断りの文）。**must-fix は 2 件**: T81 の TinySwallow のライセンスの文、T120 まわりの「手放した後に立ち上がるソフトウェアスレッドが残り、次のモデルで 10 秒止まって 1 本になる」。ほかに直したもの: T116 の init と load の競合、T97 の短く終わる部品と壊れた保存、T119 のシャードごとに戻る進捗、T120 の凍ったタブでの誤った諦め、T81 の書式と生成の設定と int8 の大きさ。残りは T129（取得と読み込みの境界）・T130（共有を断られたときの KV）・候補 T131（BOS と先頭の空白）に採番し、持ち主の指示で T128（一覧の並び）を積んだ。**持ち主の判断（2026-09-26）**: (a) 大きいモデル（3B 級と 7〜8B）も入れる「試さないと分かんない」→ T132、(b) 候補 T124〜T127 は全部採る「最初は有名なのはぜんぶ入れよう」、(c) 自動のビット数は勧めのとおり、64 ビットのメモリのあるブラウザでは int8「オススメで様子見」→ T133、(d) 英語だけのモデルは「はじめはぜんぶ表示しよう、あとで少なくする」→ T128（並びは持ち主が「日本語か英語の小 → 大、英語のみの小 → 大」と決めた）。**次の Opus の回の順番（レビュー担当の提案。持ち主が変えてよい）**: T133 → T132 → T126 → T125 → T127 → T124（形式の設計を先に決める）→ T128（モデルが増えた後に並べる。持ち主の並び: 日本語が使える小 → 大、英語だけの小 → 大）→ T84・T95 → T83。 **持ち主の追加（2026-09-26）**: T94 を完了にし、総合的なベンチマーク（GPU の計測を含む）を T134、WebGPU で速くするのを T135 に採番した（T128 の後に T134 → T135）。HF などからの取得を速くするのを T136 に採番した（持ち主の指示、まず計測。順番は持ち主が決める）。T129・T130 は持ち主の端末で見られる回に。

**次の回（2026-09-26、持ち主の決定）: 開発機を移したら（T139）、レビューの回から再開する**。新しい機械の最初の会話で T139 の段 6（`docs/dev-setup.md` の用意と試験、AGENTS.md の「この開発機では」の書き直し）を済ませ（**済、2026-09-26。T139 はレビュー待ち**）、次に別の会話の Opus xhigh でレビュー待ち（T124・T138・T134・T136 の段 ①・T139）と、仕様（T136 の段 ②）と、レビューで決める点（T138 の記録にある DeepSeek-R1 の書式、AGENTS.md の T136 の 7B の桁あふれの警告）を見る。持ち主の端末の確認（T134 の `/benchmark/` を 3 台で、T137 の第 0 段）は持ち主の都合で。

**Opus xhigh のレビューの回 — 済（2026-09-26 の 2 回目）**: レビュー待ちの 6 件（T133・T132・T126・T125・T127・T128）と、設計・仕様の 3 件（T124・T136・T137）を見た。手元の試験（pytest・smoke・forward-check の共有・`--plain`・`--wide`・6 つの検査）と、新規のサブエージェント 4 本（テンプレートの読み手を本物のテンプレート約 480 件と transformers で突き合わせ、手で書いた書式とライセンスと大きさを transformers と HF の API で、T137 の再開を試作し T124 の前提を洗う、T136 の GGUF の候補を見出しと標本で）、この開発機で一覧の sentencepiece のモデル 16 個を本物の sentencepiece と、`models.yml` で本番に対して記録に無い境界（同じページで 32 → 64 → 32 ビットのメモリ、WebKit の 3B と 7B、`?hf=` の Mistral の 2 つ、`?hf=` の Qwen2.5-32B、テンプレートを読むモデル 4 つ、rinna の 3 つ）。**must-fix は 3 件で、どれも直して本番で確かめた**: Python から呼ばれた JavaScript の例外がトレースバックで出ていた（Safari の 4 GB の断りと HF のモデルのメモリ不足。T133）、rinna の 3 つで改行とタブが「った」「する」になっていた（sentencepiece の正規化と `[UNK]`。T126）、読んだ書式の日付が変換の日のまま保存されていた（T127）。ほかに直したもの: `filled()` の `$`（T132）、テンプレートの `or`・`and`、コメントの後の `{%-`、エスケープ（T127）、台帳（T133 の誤った一文、AGENTS.md の `browsers.yml` の組）。`CONVERTER` は 6 まで上げた（保存した変換は作り直される）。6 件を完了かレビュー済みに（T132 は持ち主のデスクトップでの確認が残る）、設計・仕様の 3 件は進めてよい（足すものを各項に書いた）。**残りは T138（書式を本物に寄せる残り）に採番し、T129 に (7)、候補 T131 に 4 つ足した**。**持ち主に聞いたこと → 決まった（2026-09-26、どれもレビュー担当の勧め）**: (1) T125 (3) の文面は 3 か所に同じ並びで Mistral を足す、(2) T131 は測ったうえで却下（「やらないと決めたこと」へ）、(3) T136 の (5)（変換済みを持ち主の HF に置く）はやらない。**次の Opus の回の順番（レビュー担当の提案。持ち主が変えてよい）**: T124（qwen3、設計は済み）→ T138（小さい）→ T136 の段 ①（今の読み手で 8 項目を GGUF に、日本の回線で取得がほぼ半分）→ T137（第 0 段の計測から）→ T134 → T135 → T84・T95 → T83。T129・T130 は持ち主の端末で見る回に。

**Opus xhigh のレビューの回 — 済（2026-09-26 の 3 回目、a1-free での最初の回）**: レビュー待ちの 5 件（T124・T138・T134・T136 の段 ①・T139）と、仕様の T136 の段 ②、決める点 2 つ（DeepSeek-R1 の書式、7B の桁あふれの警告）を見た。T139 の段 6 はこの会話で実装したので（docs/review-by-opus.md の決まり 1）、T139 は新規のサブエージェントの判定をそのまま書いた。手元の試験（`.venv` の pytest 471・6 つの検査・smoke・forward-check の 3 つ・threads-check）、新規のサブエージェント 5 本（項目ごとに 1 本）、CI（models.yml で記録に無い境界 5 本: 同じページで tiny-lm → Qwen3 0.6B → Qwen3 4B、Qwen3 4B の 6 ビット、Firefox の Qwen3 0.6B、WebKit で GGUF の Qwen2.5 1.5B の 6 ビット、GGUF の TinySwallow の 2 回目の訪問。bench.yml を 3 つの OS で直す前と後）。**must-fix は 6 件で、5 件を直した**: T134 の 3 件（WebKit で回線の節の絞りが壊れていた、GPU のデバイスが失われても done、シェーダの突き合わせが落ちても ok）、T139 の 2 件（レビューの手順の pytest が system の python3 で 185 件が走らない、Ubuntu 26.04 で WebKit は動かない）、DeepSeek-R1 の BOS と書式（T138 の範囲外。perplexity が本物の 2.5〜2.7 倍）。残る 1 件（`?hf=` の書式の special でない追加のトークン）は担当 2 人の意見が割れたので、T143 にして持ち主の判断に。判定: T124・T138・T139 は完了。T134 は持ち主の 3 台の数字待ちで、その前に直すとよい should が 3 つ。T136 は段 ① 済、桁あふれは害なし、段 ② は仕様に 10 点を足して進めてよい。**この回の間の持ち主の問いから 2 つ**: CI が遅く見えたのは待ちが終わらなかったため（T142、`tests/ci.mjs`）。本当に止まっていたのは browsers.yml の Windows の WebKit の `browser.close()`（T141）。venv は `.venv`、作業ファイルは `.tmp/`、`$HOME` に黙って作らない（持ち主の指示）。

**次の回（2026-09-26、Opus の提案。持ち主が変えてよい）**: 持ち主に決めてほしいのは T143（いつ `CONVERTER` を上げるか）。Opus medium の回: (1) T134 の should 1・2・4（節の期限、報告のモデルの帰属、Issue の URL の長さ）を、持ち主が 3 台で測る前に → (2) T136 の段 ②（仕様の 10 点を足して）→ (3) T144 → T129・T130。持ち主の端末: T134 の `/benchmark/` を 3 台で（(1) の後）、T137 の第 0 段。そのあと Opus xhigh のレビューの回（T141・T142 も）。

**レビューの回（その次。2026-09-25 は Fable がやった。次からは Opus xhigh）**: (1) Opus の回で「レビュー待ち」になったものを全部レビューする（観点は冒頭の 5 つ）。(2) 止まっているものの判定: T106（前分割と RoPE の表の確認 → 実装の仕様）、T98（採る量子化の形、形式とカーネルの仕様）、T100（組み合わせと k → 仕様）、T101（採るか。持ち主と）、T111（持ち主に諮る材料をまとめる）。(3) 描画の T75・T76・T88。**その後の Opus の回**: レビューを通ったコードで**不具合の調査（T96・T97）をまとめて**、T111 の実装（オフライン）、T106・T98・T100 の実装、T81（調べ直しと追加）、T91、公開する計測（T84・T95）、T83 の下書き。

## Fable の回（2026-09-24。Fable を何回かに分けて走らせ、1 回ごとにまとめて進める）

タスクは 1 つずつのまま（コミットも T 番号ごと）。ここはどの回にどれをやるかの割り振りだけ。1 回の中は上から順に。回を終えたら、その回の見出しに「済（日付）」と足す。

**1 回目: レビューと判断（コードはほとんど書かない）**
- T87・T90・T85 のレビュー。それぞれの項に「レビュー（Fable、日付）: …」を足す。 — 済（2026-09-24）
- T92 の切り分け（どの行列の入力で 7 ビットが崩れるか）を測り、対処を決める。実装が小さければこの回で入れる。— 済（2026-09-24、実装まで）
- T74 の突き合わせの手順（`tests/gguf_check.py`）のレビューと、GGUF の読み手に進むかの判定。合格の線と取得元もここで決める。 — 済（2026-09-24。進める。線と取得元は T74 の項）

**2 回目: 性能の判断** — 済（2026-09-25。T93 は仕様まで、T94 は着手の順を決めた）
- T93 の判定: まず Node の `worker_threads` で共有メモリの試作を測る（2 Worker で 1.5 倍を超えるか）。出るなら (A) `coi-serviceworker` を CI で確かめ、方針 6 の変更を持ち主に諮る。
- T71 は T93 の結果しだい（`SharedArrayBuffer` が取れなければ、分類器だけの分け方の試作へ）。往復の判定は済（2026-09-24）。


**次の回（2026-09-25 に持ち主が決めた。いちばん先に）: 残タスクの担当の切り分け**
- 残りの全タスク（T96・T97・T98・T99・T100・T101・T81・T84・T95・T83・T75・T76・T88・T91、保留の T94・T102〜T104）について、**どこまで Opus でできるか、どこから Fable が要るか**を決め、各項の「担当」を書き直す。決める観点: 設計の判断（形式を変えるか、何を採るか）、訪問者に見える見た目と文面（描画）、数字の読み方、実装の量と壊したときに気づけるか（`forward-check.mjs`・固定値・perplexity の道具で捕まえられるか）。Opus が進められる範囲は、Fable の判断を待たずに着手できるよう、仕様（どこで止まって何を報告するか）まで書く。
- あわせて、レビュー待ちの T82・T74・T86・T89・T93 のレビュー。

**3 回目: 描画**
- T75（最適化のスイッチを画面から）と T76（ベンチの結果を見せる）。どちらも T52・T45 の画面側なので、同じ回で見た目を揃える。
- T88（任意の HF のリポジトリを画面から開く）。

**持ち主が `browsers.yml` を走らせた後の回（どの回に混ぜてもよい）**
- T82 のレビュー（表と artifact が出るか、T70 の WebKit が 600 秒で打ち切られて先へ進むか）。T87・T90 の実ブラウザでの出方もその結果で見る。

**時期未定**: T81（足せるモデルの調べ直し）。

## これからのタスク

### T129 [運用] 取得と読み込みの境界の残り（2026-09-26 の Opus xhigh のレビューから）— 状態: 未着手（規模 小。**読み込みの経路に触るものは持ち主の端末で見る回に**）
- 根拠: レビューのサブエージェントが worker.js を Node の `vm` で偽の fetch・Cache API・ストリームの上で動かす試験台を作り（T97・T118・T119、約 70 件）、次を見つけた（直したものは各項のレビューの行）。
- (1) T118: 版の解決（`resolvePyodideVersion`、data.jsdelivr.com）に期限が無い。つながったまま答えないと「Loading Pyodide」のまま止まる。案: 包み（`watchArrivals()`）を先に入れ、版の解決も段の 1 つにする（そうするとモデルの全部品が数えられ、Pyodide の止まりの発見はモデルの取得が終わってから 30 秒後になる）。
- (2) T118: `integrity` を付けた fetch（Pyodide の `loadPackage`）は本文を読み終えるまで解決しないので、NumPy の段で wheel の到着は数えられない。先読み（`integrity` なし）が wheel を読めば済むが、読めない（lock の形が変わる、キャッシュに残らない）と 0.8 Mbps より遅い回線で 30 秒で諦める（試験台で再現）。案: 先読みが読めたかを返し、読めなかったら NumPy の段は長い経過時間の期限にするか、`loadPackage(…, { checkIntegrity: false })`（持ち主の判断）。
- (3) T97: 1 つの部品が最終的に失敗した後も、ほかの接続がモデルの残りを取り続ける（次の読み込みの中止まで）。内側の AbortController で止める。`sink` の書き込みの失敗は取り直さない（`final` に）。
- (4) T97: サイトの部品の 5xx を取り直さない（HF の取得は 3 回）。3 度目の失敗の文に部品の番号を。切れた後に進捗が少し戻る（見た目だけ）。
- (5) T119: 429（HF の回数制限）を「しばらく待って」の文に。408 は取り直す。
- (6) T118: 包みに try/catch が無い（どこかのブラウザで `TransformStream` か `new Response` が投げると、読み込み中の fetch が全部失敗する。今の 3 つのエンジンでは通る）。
- (7) T133 のレビュー: 64 ビットのメモリの上限（Chromium の 16GiB）を超えるモデル（`?hf=` の Qwen2.5-32B、int8 で約 37GB）を、Worker は見出しの時点で断らず、65GB の取得を始めて 7.8GB のところで書き込みが失敗した（RangeError: Invalid typed array length: 4188160。メモリがなぜ作れたのか、なぜ 7.3GiB で止まったのかは未調査）。`weightsBuffer()` で、チェックポイントと forward が置くものが 64 ビットの上限を超えるなら、Safari の断りと同じく取得の前に分かる文で断る（文面は持ち主に）。
- 手順: 試験台を `tests/worker-check.mjs` として入れ（`vm` と偽物の上で `download()`・`fetchRange()`・`inOrder()`・`watchArrivals()` をそのまま動かす）、(1)〜(6) をそこで確かめる。deploy でも走らせる。
- 完了条件: 試験台が deploy で走る。`slow.yml` の `slow` と `stall` が今までどおり。持ち主の端末で読み込みが通る。

### T130 [性能] 共有のメモリを断られたときも、KV が文脈の終わりまで入るようにする — 状態: 未着手（2026-09-26、T115 のレビューから。規模 小〜中）
- 根拠: `weightsBuffer()` は forward が置くもの（`after`）を共有のメモリ（KV は float16、T110）として測るが、`weightsMemory()` が共有を 3 つとも断られると共有でないメモリ（KV は float32）になり、KV の分だけ足りない。Qwen2.5-3B の int8: 見積もり 3.818 GiB（32 ビット）に対して実際は 4.029 GiB で、位置 2048 の最後の倍化でメモリ不足。Llama-3.2-3B の int6（Chrome の自動の選択）: 3.664 → 4.320 GiB。上限を下げて取れた共有（使える幅が 1GiB だけ）でも Llama-3.2-3B の int6 は同じ（1070.7 MiB 要る）。断られやすいのはモデルを替えた直後（古いメモリが GC される前、T96）。
- 案: (a) 共有を断られたら共有でないメモリとして測り直し、32 ビットに入らなければ 64 ビットにする（Safari では分かる文で止める）、(b) 共有でないメモリでも入らないときだけ KV を float16 にする（1 本では attention が遅い、T110: 位置 4000 で 38 → 21 tok/s）、(c) KV をその場で伸ばして峰を下げる（古い塊と新しい塊の和でなく、差の分だけ取って後ろから詰め直す。Qwen2.5-3B の共有なしが 4.029 → 3.888 GiB で 32 ビットに入る）。
- 完了条件: forward-check に「共有を頼んで断られた」経路の試験。`models.yml` の `long` で 3B 級が文脈の終わりまで書く。

### T134 [計測] 総合的なベンチマーク: CPU の計測と GPU の計測を 1 つに — 状態: **`/benchmark/` は公開済み（2026-09-26）。GPU の項目に足す計測（T135 の前）も公開済み（T140 の前に画面を確かめて本線へ、0affb08・ce4ae46）。レビュー済み（must-fix 3 つを直した、2026-09-26）。持ち主の 3 台の数字待ち**（2026-09-26、持ち主の指示「gpu test じゃなくて、総合的な benchmark を作って、それに gpu test をふくませるのがよくないか？」「T94 復活させて WebGPU でのパフォーマンス向上を目指そう」。**T128 の後**（Opus の提案。持ち主が変えてよい）。規模 中）
- 根拠: いまの計測は 2 つに分かれている。`?bench=1`（T45・T76: そのページのモデルでカーネルなど 4 段を外して測る、Markdown と Issue の報告 T91）と `/gpu-test/`（T94 の第 0 段: アダプタ、int8 の行列 × ベクトルの GB/s、1 トークンぶんの仕事）。GPU の計測はバッチ 1 の生成だけで、**GPU を生かす形を測っていない**（T94 の完了の記録）。
- 作るもの: 1 つのページ（場所は設計で決める: `?bench=` を広げるか、`/bench/` を新しく作って `/gpu-test/` を移すか）で、同じ端末の CPU と GPU を同じ表に出し、T91 と同じ形で Issue に報告できる。**モバイル Safari を含む全部のブラウザで動く**こと（持ち主「モバイル Safari でも有効なテクノロジーがいい」: WebGPU は Safari 26 から。Memory64 や relaxed SIMD の無いブラウザでは、その行を「無し」と書く）。
- GPU の項目に足す計測: (1) **プロンプトのまとめ処理**（N トークンを行列 × 行列で。CPU の T108 の 16 トークンずつと比べる）、(2) **固定費の内訳**（空のディスパッチ 240 回だけ、読み戻し 4 バイトと logits 全部）、(3) **融合したカーネル**（層の小さな処理を行列積に寄せ、ディスパッチを数分の 1 に）、(4) **GPU でのサンプリング**（読み戻しをトークン ID だけに）。CPU の項目は今の `?bench=1` の段と、スレッドの本数。
- 止まる点: ページの見た目は `[描画]` と同じく `preview.yml` で撮って持ち主が見る。訪問者に見える文は先に文面を出す。**速さは持ち主の端末で測る**（PC の Chrome、Android の Chrome、iPhone の Safari）。CI は正しさだけ（SwiftShader のアダプタ、いまは `bench.yml`）。
- **設計（案、2026-09-26、Opus medium。持ち主の問い「OPFS や WebGPU のテストを別々にするのは面倒なので /benchmark などにまとめて一回でテストできるようにしたい」。実装はまだ）**
  - **いま 3 つに分かれている**: `?bench=1`（モデルのページの中。tiny-lm などをカーネルの 4 段を外して測る、`src/bench.js` 116 行、Issue のラベル `benchmark`、`tests/reports.mjs` が集める）、`/gpu-test/`（T94、`bench.js` 411 行、ラベル `gpu`）、`/opfs-test/`（T137 の第 0 段、ラベル `opfs`）。持ち主が端末で測るには 3 回開いて 3 つ Issue を書く。
  - **案 A（勧め）: `/benchmark/` を 1 枚作り、節を順に走らせて 1 つの表と 1 つの Issue にする**。節: ① 端末（UA、コア数、`deviceMemory`、隔離されているか、Memory64・relaxed SIMD・WebGPU・OPFS の有無）、② CPU（`forward.js` のカーネルを直に: int8 の行列 × ベクトルの GB/s を 1 本・2 本・4 本で。Pyodide は要らない）、③ モデル（今の `?bench=1` の 4 段。tiny-lm を `worker.js` で。`src/bench.js` をそのまま使う）、④ GPU（`/gpu-test/` の中身: アダプタ、行列 × ベクトル、1 トークンぶん。T134 で足す行列 × 行列・固定費の内訳もここ）、⑤ 保存（`/opfs-test/` の中身、既定 256 MiB）、⑥ 回線（サイトの部品と huggingface.co の Range の MB/s と最初の 1 バイトまでの待ち。T136 の「持ち主の回線で測る」がここで済む）。「Run all」1 つと、節ごとの「Run」。無いもの（WebGPU の無いブラウザ、Safari の Memory64）は行に「無し」と書いて先へ（モバイル Safari を含む全部で最後まで走る）。
    - **メモリの決まり**: 節は 1 つずつ Worker の中で走らせ、終わったら Worker を閉じる（WebAssembly のメモリは Worker と一緒に消える。T96: Chromium は 1 ページに 3 つ目のメモリを作らせなかった。スマホで GPU のバッファとモデルと OPFS の大きなファイルを同時に持たない）。GPU のバッファは `destroy()`、OPFS のファイルは消す（今の 2 つのページと同じ）。
    - **報告**: 1 つの Markdown（節ごとの表）と 1 つの Issue（ラベル `benchmark`、雛形の 3 つの問いはそのまま）。`tests/reports.mjs` は節ごとに表を起こす。`/gpu-test/` と `/opfs-test/` は `/benchmark/?only=gpu` などへの転送にする（リンク済みの Issue の URL が生きる）か、消す（持ち主が決める）。
    - **CI**: `bench.yml` 1 つ（`gpu.yml` と `opfs.yml` と `browsers.yml` の `?bench=1` の段を寄せる）。OS × ブラウザで節ごとの正しさ（GPU は SwiftShader、OPFS は WebKit が無い・失敗するのを「無し」と書けるか）。速さは持ち主の端末で。
    - **大きさの見込み**: 新しい Astro のページ 1 枚と節ごとの JS（今の `gpu-test/bench.js`・`opfs-test/writer.js` を移す）。モデルのページからは `?bench=1` を残し、`/benchmark/` へのリンクを足す（案 B の良いところ）。所要は Run all で数分（未計測。③ が tiny-lm の 4 段で約 1 分、④ が 1B の形で数十秒、⑤ が数秒〜数十秒、⑥ が 30 秒の見込み）。
  - **案 B: `?bench=` を広げる（モデルのページの中で GPU と OPFS も）**。良いところ: 訪問者が知っている入口のまま、ページは 1 つ。悪いところ: 何を測るにも Pyodide とモデルの読み込み（tiny-lm で約 10 秒）が先に要る。モデルのメモリが生きているページで GPU の大きなバッファと 256 MiB〜1 GiB の OPFS のファイルを持つことになり、スマホでメモリの予約が入らない形（T96、2026-09-25 の固まり）に近づく。`index.astro` は 1100 行を超えていて、表示の分岐がさらに増える。
  - **案 C: 今の 3 つを残し、`/benchmark/` はそれぞれを iframe で順に開いて表を集めるだけ**。いちばん小さいが、iframe の中の Worker と隔離（COOP/COEP）の扱いが増え、Issue を 1 つにする仕事は結局要る。
  - **勧め: 案 A**。数字の根拠: 持ち主が 3 台で測る手間が 3 ページ × 3 台 = 9 回の操作と 9 つの Issue から、3 回と 3 つに。メモリの危険は案 B が最大（モデルと GPU と OPFS が同じページ）で、案 A は節ごとに Worker を閉じるので最小。**覆す条件**: 節を Worker に閉じても、同じページで GPU の後にモデルを読むと固まる端末が出たら（持ち主の iPhone で試す）、その節の前に再読み込みを挟む形にする。
  - **持ち主に決めてほしいこと**: (1) 案 A でよいか、(2) 場所の名前（`/benchmark/`）、(3) `/gpu-test/`・`/opfs-test/` は転送か削除か、(4) ⑥ 回線の節を入れるか（T136 の持ち主の回線の計測を兼ねる）。見た目は `[描画]` と同じく `preview.yml` で撮って見てもらい、訪問者に見える文は先に文面を出す。
  - **持ち主の判断（2026-09-26）**: (1) **案 A でよい**（`/benchmark/`）、(2) **集約して元のページ（`/gpu-test/`・`/opfs-test/`）は消す**（`gpu.yml`・`opfs.yml` と `tests/gpu-check.mjs`・`tests/opfs-check.mjs` も `bench.yml` に寄せる）、(3) **回線の節も入れる。擬似的にトラフィックを絞れたらなおよし**。絞り方の案: ページは回線そのものを遅くできないが、応答の本文を決めた速さでしか読まない（読む → 待つ → 読む）と、ブラウザは受け取りを止め、TCP の受信窓で送り手も遅くなる。これで「指定した速さ（例 1・4・8 MB/s）の回線で、取得が何秒か」と「実際の回線の速さ」の両方を出す（`slow.yml` のプロキシの代わりにはならない: 絞れるのはこのページが読む取得だけ）。**止まる点**: 実装したら `preview.yml` で画面を撮り、訪問者に見える文と一緒に持ち主に見せてからコミットを本線に入れる。
  - **実装（2026-09-26、Opus medium、ブランチ）**: `src/pages/benchmark.astro`（ページ）と `public/benchmark/`（節の Worker: `sections.js` が機能・CPU・回線、`gpu.js` と `waiter.js` が `/gpu-test/` の中身、`storage.js` が `/opfs-test/` の中身）。モデルの節は `worker.js` をそのまま `init` → `bench` で動かす（`src/bench.js` の `ROUNDS` / `FULL_ROUNDS`）。**CPU の節は `forward.js` の forward そのもの**（`createForward` に、Python の代わりに `sections.js` が作った計画を渡す。Llama 3.2 1B の幅で 2 層・語彙 32000 の作り物の int8、1 トークン 211MB。`setThreads()` で 1・2・4 本）。報告の Markdown はモデルの表が先頭（`parseReport()` と `tests/reports.mjs` はそのまま読める、`tests/bench.mjs` に試験）。Issue の雛形の説明を `/benchmark/` に。モデルのページの「About」に 1 行「Benchmark this device」。消したもの: `public/gpu-test/`・`public/opfs-test/`・`tests/gpu-check.mjs`・`tests/opfs-check.mjs`・`gpu.yml`・`opfs.yml` → `tests/bench-check.mjs` と `bench.yml`（落とすのは結果が正しくないときと、端末に依らない節（機能・CPU・モデル）のエラーだけ）。`preview.yml` がブランチの `dist/` で `/benchmark/` を Chromium で走らせ、画面も撮る（前と「Run all」の後）。`browsers.yml` の `?bench=1` の段はそのまま（モデルのページの吹き出しはまだある）。**この開発機で確かめたこと**（ブラウザは動かさない）: Node で `sections.js` の機能と CPU の節（1 本: 15.8 ms / トークン・13.3 GB/s、logits は有限）、回線の節（huggingface.co を 1 MB/s に絞って 1.02 MB/s）、`tests/bench.mjs`、models-check、`npm run build`。**未確認**: 実ブラウザのすべて（ソフトウェアスレッドの 2・4 本、GPU の節の新しい置き場、WebKit と Firefox）、スマホ、本番の GitHub Pages の gzip の部品（回線の節はサイトの部品を Range なしで読む）。
  - **GPU の項目に足す計測の実装（2026-09-26、Opus medium、ブランチ。持ち主の「GPU 計測実装開始」）**: GPU の節（`public/benchmark/gpu.js`）に 4 つ。(1) **プロンプトを行列 × 行列で**: 1 つの重みの読み出しで 8 トークンぶんを掛けるシェーダ（`BATCHED`、トークンのタイルはディスパッチの 3 つ目の次元）で、CPU の節と同じ作り物のモデル（Llama 3.2 1B の幅の 2 層、分類器なし）に 1・16・64 トークンを一度に通し、1 トークンあたりの ms。CPU の側は CPU の節に「プロンプトのトークンを 16 ずつ（T108 の `forwardMany`）」の列を足した（ソフトウェアスレッドの本数ごと）。(2) **重みのほかの費用**: 何もしないディスパッチ 240 回を 1 回の submit で、submit 1 回（待たない・待つ）、読み戻し 4 バイトと logits 全部（128256 個）。(3) **ディスパッチを減らす**: 同じ入力を読む行列を 1 つに（q・k・v、gate・up）して層あたり 4 つ、小さな処理は層あたり 7 → 3（norm は次の行列の入力の読みへ、残差の足しは前の行列へ畳んだ形の回数だけ。計算の中身は畳んでいない: 数えるのは「回数」の効き）。(4) **次のトークンを GPU で選ぶ**: logits の最大の位置（同じ値なら小さい番号、JavaScript と同じ）を 1 ワークグループで探し、4 バイトだけ読み戻す。(3)(4) とその両方を Llama 3.2 1B の 1 トークンで、元の形の行と並べる（表に「dispatches」の列）。**正しさ**: 行列 × 行列（300 行 × 512、11 トークン: タイル 1 つと端）と最大の位置（語彙 128256 と、同じ値が 2 つある 1000 個）を JavaScript と比べ、合わなければ節は「WRONG」（`bench-check.mjs` はそれで落ちる）。**数字は SwiftShader（CI）のものしか無い**（持ち主の端末は未計測）。
- **レビュー（Opus xhigh、2026-09-26）: must-fix 3 つを直して OK。残りの should は、持ち主が 3 台で測る前に直すとよいものがある（下）。** 確かめ方（新規のサブエージェント）: HEAD の写しを Playwright の Chromium（SwiftShader）で、わざと壊した版を 9 通り（シェーダの添字、同値の最大の位置、行を 128 ずつに切る、デバイスを失う、突き合わせが例外、`Atomics.waitAsync` なし、CPU の NaN など）走らせ、CI の bench.yml の記録 3 回ぶんを読み、GitHub の新規 Issue の URL の長さの上限を測った。**直したもの**: (1) **WebKit で回線の節の絞りが壊れていた**（069d38a）: 1・4・8 MB/s を頼んで 1.24〜1.50・4.45〜14.77・19.5〜39.1 MB/s と出て「within 10%」が no（3 つの OS の WebKit、Chromium 系と Firefox は 0.97〜1.00 倍）。WebKit は読まれなくても受け取り続けて大きな塊で渡し、最後の塊を予定時刻まで待たずに抜けていた。直した後の bench.yml で WebKit は 3 つの OS とも 1.00・3.99〜4.00・7.98〜8.00 MB/s。(2) **GPU のデバイスが失われても節が done のまま**（00b7585）: 失われたデバイスは後の待ちにすぐ答えるので、プロンプトの表が 0.72 / 0.03 / 0.01 ms と偽の数字になっていた（T135 の判定 (a) が読む表）。いまは節を failed にし、「The device was lost …: the times measured after it are not the GPU's」と書く。(3) **シェーダの突き合わせが例外で終わると節が ok のまま、その行が報告から消え、bench-check も通っていた**（00b7585）: いまは failed にして「not checked (…)」と書き、bench-check も落ちる（壊した版で exit 1 を確かめた）。**確かめて問題なし**: BATCHED のタイルの端（11 トークン）と行を切った場合、同値の最大の位置、誤って WRONG になる余裕（float32 の順を模して 600 回、最悪 6.1e-4 で線は 1e-3）、fused は同じ 1.39GB を読んでディスパッチ 225 → 113、CPU の計画と forward.js の契約、6 つの節とも `finally` で Worker を閉じる（waiter を除く）、Worker の `?v=`、保存の短い書き込みの検出、`parseReport()` と `reportsTable()` が CI の報告を読む。
  - **残した should**（持ち主が 3 台で測る前に直すとよいのは 1・2・4）: (1) **節ごとの期限が無い**: GPU の Worker に `Atomics.waitAsync` が無いと、ブリッジで「Run all」が終わらない（240 秒で done にならず、保存と回線は走らなかった。当たるのは WebGPU があって waitAsync の無い Firefox 141〜144）。回線の `measure()` にも止まりの期限が無い（T118 の `watchArrivals()` に当たるもの）。案: 進みの無い N 分で error、Worker を閉じて次へ。(2) **報告のモデルの帰属**: GPU だけ走らせた報告も先頭に選んだモデルの名前と空の表が出て、`reportsTable()` が `? | ? | …` の行を作る。モデルの節のエラーは報告に入らない。モデルを選び直すと前の回の行が新しい名前で載る（`modelRows`）。(3) CPU の節の正しさは「有限か」だけ: 本数ごとに最後のトークンの logits を比べれば T101 の「4 本だけ食い違う」を捕まえられる。spawn の reject で行が消える、`engine.lostThreads`（T120）が表に無い。(4) **Issue の URL の長さの余裕が少ない**: ログインしていない人は約 7,000 文字から GitHub が 500 を返し（6,799 は 302）、約 8,100 文字から誰でも 414。preview の実物の報告は 5,701 文字、`?full`・`?large`・エラーの行を入れた最悪の組み立てで 6,342 文字。案: 長ければ本文を短い案内にして全文はクリップボードへ。(5) 保存の節の読み戻しは各部品の先頭の 1 語だけ（`storage.js:113`）。中断すると最大 1 GiB のファイルが次に最後まで走るまで残る。(6) `bandwidth()` にエラースコープが無い（T94 から）。(7) 文面: 3B の段はチェックしたときだけ、回線の節が huggingface.co から約 85 MB を取ることが書かれていない、GPU のプロンプトの行は attention を含まない（T135 の判定 (a) にその旨を添える）。(8) `?run=` の知らない名前は黙って無視（`run=foo` は何も走らず done）、`run=all,device` は device だけ。bench-check は 6 つの節が全部結果を持つかを見ない。
- 完了条件: 持ち主の 3 台の表が出て、T135 でどこを GPU に置くか（生成・プロンプト・両方・置かない）を数字で決められる。

### T135 [性能] WebGPU で速くする — 状態: 未着手（2026-09-26、持ち主の指示「T94 復活させて WebGPU でのパフォーマンス向上を目指そう」。**T134 の後**。規模 大。段階ごとに止まる点がある）
- 設計の下地は T94 の記録（完了の欄）: `forward.js` と同じ契約の `gpu.js`、同期と非同期の橋渡し（生成ループが GPU の待ちを `yield` する第一案）、重みはチェックポイントと同じ並びで GPU へ、`device.lost` で CPU に戻る。
- 何を GPU に置くかは T134 の数字で決める。見込み（未計測）: プロンプトのまとめ処理は行列 × 行列なので、固定費が小さく見えて GPU が勝ちやすい。生成（バッチ 1）は、融合で固定費を数分の 1 にできたときだけ。
- **決める数字（T134 の `/benchmark/` の GPU の節、持ち主の 3 台で）**: (a) **プロンプト**: 「A prompt」の表の 16・64 トークンの「GPU ms a token」と、CPU の節の「ms a token of a prompt, 16 at once」（いちばん速い本数）。GPU が有意に小さければ、プロンプトを GPU に置く段から。(b) **生成**: 「a token」の表の Llama 3.2 1B の 4 行（元・fused・chosen on the GPU・両方）の GPU ms と、CPU の節の 1 トークンの ms（本数ごと）。両方を足した形でも CPU に届かなければ、生成は置かない。(c) **固定費の内訳**: 「besides the weights」の表で、ディスパッチの回数・submit の待ち・読み戻しのどれが T94 の 18〜20ms の大半かを見る（どこを削るべきかの順番）。**判定の書式**は docs/review-by-opus.md のとおり（数字・式・覆す条件）。
- 止まる点: 各段の判定は Opus xhigh、既定にするかは持ち主。**開発機の結果で既定を決めない**（各端末で測って選ぶ）。
- 完了条件: 持ち主の端末のどれか 1 つ以上で、GPU に置いたところが CPU より有意に速く、既定の選び方が決まっている。

### T136 [性能] Hugging Face などからの取得を速くする — 状態: **段 ① は済（8 項目を GGUF に、レビュー済み、2026-09-26）。段 ② は仕様に 10 点を足して進めてよい（レビュー、2026-09-26）。本番の前と後の数字はまだ**（**仕様はレビュー済み、2026-09-26。(2) を段に分けて進めてよい**）（2026-09-26、持ち主の指示「すぐにできそうなら着手、無理そうなら仕様をいったんレビューする」。計測したら、この開発機の回線ではすぐ効く手が無かった）（2026-09-26、持ち主の指示「Hugging Face などからのダウンロードの高速化を台帳に追加したい」。**まず計測から**。順番は持ち主が決める（Opus の提案: T128 の後、T134・T135 と並べて）。規模 小〜中）
- 根拠: 大きいモデルの準備完了の大半は取得。日本の回線（この開発機）で huggingface.co から 8.3 MB/s（16 MiB × 6 本、T123）、1B 級で約 300 秒、7〜8B（15〜17GB）では約 30〜35 分。CI（米国）では部品の大きさが効き（Qwen2.5 0.5B が 4 MiB で 23 秒、16 MiB で 8.3 秒）、並列数は 4 → 12 で 1 割前後（T107）。**その値は米国の CI で決めたもので、日本の回線では測っていない**。
- 試す手（どれも未計測）: (1) **日本の回線で部品の大きさと並列数を振る**（`?hfParts=&hfConnections=` と `fetch.yml` の表。持ち主の回線か、日本の開発機の Node で取得だけを測る）。(2) **小さい取り込み元**: 原本と同じ重みの Q8_0 の GGUF なら取得は bf16 の約半分（T74: SmolLM2 135M で 269MB → 145MB、int8 の結果は +0.13%）。モデルごとに `tests/gguf_check.py tensors` で原本と同じ重みか先に確かめる（Qwen2.5 の公式 GGUF は別の重みだった）。(3) **途中からの再開** → **T137 に分けた**（2026-09-26）。(4) **取得先**: HF の CDN（xet の保存など）で、Range 要求の待ちが短い道があるか調べる。サイトのモデル（GitHub Pages）の部品の取得（T68: 8 MiB × 8 本）も同じ目で見る。
- 完了条件: 日本の回線での取得の速さが数字で出て、採った手で準備完了が縮んだことを本番で示す。効かなかった手は数字と理由を書いて取りやめる。
- **計測（2026-09-26、この開発機（日本）、`node tests/fetch-node.mjs`、Qwen2.5 1.5B の先頭 256MB、1 回ずつ）**: 部品 8・16・32・64 MiB × 接続 4・6・8・12・16 の 20 通りが **7.1〜9.4 MB/s**（最速は 16 MiB × 16 本の 9.4、今の既定の 16 MiB × 6 本は 8.0）。**同じ回線で日本の JAIST のミラーは 1 本 9.3 MB/s、4 本合わせて約 9 MB/s**。つまり**この回線は約 9 MB/s が上限で、HF はすでにそこまで出ている**。部品の大きさ・並列数・取得先（HF は日本からも米国の CDN `us.aws.cdn.hf.co/xet-bridge-us` に振り向ける）を変えても、この回線では速くならない。ほかに分かったこと: huggingface.co の 302 は約 0.24 秒、CDN への 1 回目は約 0.8 秒（新しい接続込み）。CDN は `Access-Control-Allow-Origin: *` で `Content-Range` も見せるので、ページが CDN の URL へ直接頼むことはできるが、署名つきの URL は**約 1 時間で切れる**（7B の日本からの取得より短い）。Node の fetch は HTTP/1.1 でブラウザ（HTTP/2）と違うので、ブラウザでの値は未計測。
- **仕様（レビューに出す）**: この回線で効くのは「取るバイトを減らす」だけ。**(2) を先に**: 一覧のモデルごとに、原本と同じ重みの Q8_0 の GGUF があるかを調べ（`tests/gguf_check.py tensors` を通したものだけ、T74 の決まり）、あるものは取り込み元を GGUF に替える（取得は bf16 の約 53%、int8 の結果は +0.1% 前後。SmolLM2 135M で実績）。候補の数と、通るかは未調査（公式の GGUF は Qwen2.5 のように別の重みのことがある）。**(1) の残り**: 持ち主の端末（光回線など、上限の高い回線）で `?hfParts=&hfConnections=` を振る（上限の高い回線では部品と並列数が効く見込み、CI では効いた）。**(4) の CDN への直接**は、302 の 0.24 秒 × 部品の数（1B で約 130 回、6 本で割って約 5 秒ぶん）の見込みで、署名の切れを扱う手間に比べて小さいので後回し。**(5) 新しい案（持ち主の判断が要る）**: 変換済みの int8 を持ち主の HF のアカウントに置き、それを取る（取得は int8 の大きさ = bf16 の約 56%、変換も要らない）。方針 1（バイナリをリポジトリに入れない）には触れないが、重みの再配布になり（ライセンスごとに確かめる）、置く手間と置き場の容量が要る。
- **この回で入れたもの**: `tests/fetch-node.mjs`（取得だけを測る Node の道具。部品 × 並列数の表、最初の 1 バイトの待ち）。
- **レビュー（Opus xhigh、2026-09-26、仕様）: 計測の読みは正しい。(2) を段に分けて進めてよい。** 確かめ方: 新規のサブエージェントが一覧の HF の 41 項目の Q8_0 の GGUF を探し、見出し（Range）を今の読み手の関数（`gguf_read` → `gguf_model` → `check_config` → `gguf_tokenizer`、`Conversion.from_gguf`）に通し、重みを標本で原本と比べた（F32 の norm 3 つはビット単位で同じ、Q8_0 の 3 行は int8 の 99.8〜100% が原本の量子化と同じ）。取得は見出しと標本の約 371MB だけ。**`tests/gguf_check.py tensors`（T74 の決まり）はまだどれにも通していない**。(a) 数字: **今の読み手のまま 8 項目**（Qwen2.5 の 0.5B・1.5B・3B・7B・Coder 0.5B は bartowski、TinySwallow・SmolLM2 360M は公式、SmolLM2 1.7B は bartowski）で取得 33.70 → 17.95GB。トークナイザを原本のリポジトリから取る形を足すと **+16 項目**（sarashina・CAT・Rakuten・llm-jp・Swallow-MS・Mistral・zephyr・TinyLlama、118.13 → 62.80GB）。GGUF 自身の sentencepiece の語彙は unigram か BPE かと正規化を持たず、llm-jp の GGUF は点数が全部 −1000 なので使えない。`rope_freqs` を読む（または原本の config.json の `rope_scaling` を使う）と +3（Llama 3.2 の 2 つ、Swallow 8B、24.96 → 13.28GB。表はエンジンの `rope_frequencies()` と 2.7e-7 で同じ。Swallow の GGUF の eos は 128009 だけなので止まりも原本から）。前分割の名前（`deepseek-r1-qwen`）と NFC と BOS で +1、gptneox の読み手で Pythia +5。GGUF の無いもの 6（5.87GB）。**公式の Qwen2.5 の GGUF（0.5B・1.5B・3B・Coder）は原本と別の重み**（T74 の 0.5B と同じ。norm が 0.28〜1.1 違う）、bartowski のものは原本と同じ。(b) 式: 取得の時間 ≒ バイト数 ÷ 回線（この回線で約 9 MB/s）。Qwen2.5 7B は約 28 分 → 約 15 分。(c) 判定: 進めてよい。段: ① 8 項目を、それぞれ `gguf_check.py tensors` を通してから GGUF に替える（大きいものは CI で。この開発機のメモリでは 1B 級まで）→ ② GGUF の重みに原本のリポジトリのトークナイザと config.json（止まり・`rope_scaling`）を合わせる形（16 + 3 項目）→ ③ 残り。6 ビット（Safari）は Q8_0 から量子化し直すことになるので、1 つ perplexity を測る（未計測）。取り込み元を替えると保存の名前（`keptName` のリポジトリと版）が変わり、保存した変換は取り直しになる。(1) は持ち主の回線で。**(5)（変換済みの int8 を持ち主の HF に置く）はやらない（2026-09-26、持ち主の判断、レビュー担当の勧め）**: 取得の量は GGUF とほぼ同じ（bf16 に対して int8 は約 56%、Q8_0 は約 53%）で、変換そのものは取得に比べて短い（1B 級で約 5 秒対約 300 秒）。それに対して、5 種類のライセンスの再配布の義務（告知、Llama の「Built with Llama」、Gemma の使い方の制限）、100GB を超える置き場（無料の公開容量は best-effort）、変換して上げる CI と書き込みのトークン、出どころの表示が、ずっと続く手間になる。一覧のモデルがブラウザの中で変換しなくなることも、このプロジェクトの趣旨から外れる。(d) 覆す条件: 持ち主の回線かブラウザ（HTTP/2）で、部品の大きさや並列数で 9 MB/s を大きく超えるなら (1) を先に。`gguf_check.py tensors` が落ちた GGUF はその項目だけ替えない。

- **段 ① の結果（2026-09-26、Opus medium）**: 8 項目とも `tests/gguf_check.py tensors` を通り（6 つは手元、Qwen2.5 3B・7B は CI の `gguf.yml`）、GGUF に替えた（97f903d、c250669）。取得は 33.7 → 17.9 GB。詳しくは AGENTS.md の「取り込み元を Q8_0 の GGUF に」。本番の準備完了の前と後は下に。
- **段 ② の仕様（2026-09-26、Opus medium。レビューに出す。実装はまだ）**: GGUF の重みに、原本のリポジトリの語彙と config.json を合わせる。
  - **対象**: 語彙が sentencepiece の 16 項目（sarashina2.2 の 4 つ、CAT-Translate の 2 つ、llm-jp-3 980M・3.1 1.8B・4 8B、RakutenAI の 2 つ、Swallow-MS 7B、TinyLlama、Mistral v0.2・v0.3、zephyr）で取得 118.1 → 62.8 GB、`rope_freqs` の表を持つ Llama 3 の 3 項目（Llama 3.2 1B・3B、Swallow 8B）で 25.0 → 13.3 GB（T136 のレビューの見出しの調べ）。**今の読み手が GGUF の語彙を断る理由**: GGUF の sentencepiece の語彙は unigram か BPE かと正規化を持たず、llm-jp の GGUF は点数が全部 −1000。どちらも原本の `tokenizer.json` か `tokenizer.model` を使えば今と同じ語彙になる。
  - **一覧の形**: `hf` に語彙の出どころを足す: `hf: { repo, revision, weights: "*.gguf", vocabulary: { repo, revision, tokenizer } }`（原本の固定したリビジョン。`original` は今までどおり出典の表示）。worker は重みの見出しを GGUF から、`config.json`・語彙・`tokenizer_config.json`・`chat_template.jinja` を `vocabulary` のリポジトリから取る（今の safetensors の経路と同じ取り方）。保存の名前（`keptName`）は重みのリポジトリと版のまま。
  - **変換器**: `Conversion.from_gguf(head, config=, tokenizer=, tokenizer_name=, tokenizer_config=, chat_template=)`。形（次元・層・head・語彙の数）は GGUF の見出しから、それ以外（止まり、bos、`rope_scaling`、`rms_norm_eps`、書式、特殊トークン）は原本の config と語彙から。**両方から分かるものは突き合わせて、違えば断る**（次元・層・head・kv head・`vocab_size` と埋め込みの行数・head_dim）。語彙は原本のもの（`sentencepiece_pieces` / `tokenizer_json_pieces`）なので、options は safetensors の経路と同じになる。
  - **`rope_freqs`**: 読み飛ばすと素の RoPE で文が壊れる（落とし穴）ので、原本の `rope_scaling` から `rope_frequencies()` で作った表と GGUF の `rope_freqs` を比べ、相対 1e-5 を超えて違えば断る（レビューの調べで 2.7e-7 で同じ）。一致すれば表は捨てて、今までどおりエンジンが `rope_scaling` から作る。
  - **試験**: pytest で「合成の GGUF（sentencepiece の Llama、`rope_freqs` 付きの Llama 3）+ 原本の語彙と config」が safetensors の経路とチェックポイントのバイト（スケールの丸めを除く、T74 と同じ見方）と options と書式で同じになること、形が違う原本・`rope_freqs` の違う原本を断ること。実物は `tests/gguf_check.py tensors`（19 項目。3B 級までは新しい開発機、7〜8B は `gguf.yml`）、`tests/format_check.py`（語彙と書式は原本のままなので同じ結果のはず）、本番の `models.yml`。
  - **ライセンス**: GGUF のリポジトリのカードから写す。**カードにライセンスの書いていない GGUF は使わない**（別の候補を探すか、原本のまま）。
  - **段 ③ に残すもの**: GPT-2 と GPT-NeoX の GGUF（読み手の arch を増やす、Pythia 5 つと GPT-2 で 7.0 → 3.4 GB）、DeepSeek-R1（前分割の名前）、GGUF の無い 6 つ。
  - **判定の書式**: (a) 数字: 19 項目で取得 143.1 → 76.1 GB（約 53%）。(b) 式: 取得の時間 ≒ バイト数 ÷ 回線。(c) 勧め: 進める。変える場所は変換器の `from_gguf` と worker の取り方と一覧の形の 3 つで、エンジンと forward は変えない。(d) 覆す条件: 原本の語彙と GGUF の重みの語彙の順（ID）が違うモデルが出たら、そのモデルは替えない（形の突き合わせでは捕まえられないので、`gguf_check.py tensors` の語彙の比べで見る）。
- **レビュー（Opus xhigh、2026-09-26）: 段 ① は OK（must-fix なし、should 4）。7B の桁あふれは害なし（記録だけ直した）。段 ② は下の 10 点を仕様に足してから進めてよい。** 確かめ方（新規のサブエージェント）: 8 項目の 2 つの経路（safetensors の `Conversion` と `from_gguf`）を見出しだけで組んで並べ、7 個の int・int8 と int6 の大きさ・options が全部同じ（違うのは tokenizer.bin の詰め物の文だけ）。`gguf_check.py tensors` を手元で Qwen2.5 0.5B（worst 0.0082、不一致 0）と TinySwallow 1.5B（0.0071、0）。`download` は 8 つとも HEAD の大きさと 1 バイトまで同じ、ライセンスは GGUF のカードと合う。6 ビット（Safari の経路）の Qwen2.5 0.5B の perplexity は safetensors から 24.089、GGUF から 24.053（int8 は 23.749）。CI（このレビューの回）: WebKit で GGUF の Qwen2.5 1.5B を `?bits=6` で開いて答え、Chromium で TinySwallow の 2 回目の訪問が OPFS から 5.8 秒。
  - **段 ① の should（やるかは持ち主）**: (1) **古い保存がはぐれる**: 替えた 8 項目の前の保存は `serves()` に外れるが、`outdated()` は `CONVERTER` しか見ないので消されず、About に同じ名前と大きさの行が 2 つ並ぶ（7B なら 8.6GB が残り、新しい保存が容量で失敗しうる。未計測）。案: 一覧の項目の id を持つのにその項目の `keptNames` に入らない保存を「置き換えられた」として消す（T116 の前例は自動）、最小ならリポジトリ名を About の行に出す。(2) `format_check.py` は GGUF の 9 項目を比べない（T139 の記録）。(3) **Q8_0 の広げ（`llama2_convert.q8_0()`）が NumPy で遅い**: Node の Pyodide で Qwen2.5 0.5B の `feed()` が safetensors（988MB）3.2 秒に対して GGUF（531MB）7.7〜11.9 秒、そのうち `q8_0` が 7 割（入力で 45〜69 MB/s、bf16 は約 309 MB/s）。日本の回線（約 9 MB/s）なら取得に隠れるが、速い回線では変換が律速になりうる（未計測）。T123 の `widen_bf16` と同じ形のカーネルにするか、先に本番の前後を測る。(4) 記録: この見出しと AGENTS.md の 6 ビットの文と詰め物の文を直した。**本番の準備完了の前と後の数字はまだ無い**（完了条件）。
  - **段 ② の仕様に足すもの**: (1) **覆す条件 (d) を「重みの行の順」に書き直す**: GGUF の語彙の metadata は llm-jp-4（mmnga-o）で 13 の id の「▁」の有無が入れ替わり、TinyLlama（QuantFactory）は id 3〜770 が Mistral v0.3 の `[INST]`… になっているが、どちらも重みの行は原本の順（標本で）。語彙は原本から取るので、違いは報告だけにする（今の書き方だと使える llm-jp-4 を断る）。TinyLlama は TheBloke のものを使う。(2) **`token_embd` と `output` は行ごとに確かめる**: テンソル全体の誤差の線 0.02 は、Qwen2.5 0.5B の埋め込みで 8 行入れ替えても 0.0122 で通り、26 行で初めて落ちる。行ごと（誤差 > 0.05）なら全部捕まる。(3) **`gguf_check.py` を直す**: `rope_freqs.weight` で `hugging_face_name()` が落ちる（`tests/gguf_check.py:179`、確かめた）ので、表は `rope_frequencies()` と比べる。`tokenizer.model` の語彙も比べる、config は `normalize()` してから比べる（CAT-Translate 1.4B は `rope_parameters`）、段 ② では語彙の違いを不一致に数えない。(4) **`rope_freqs` を比べる場所**: 表は見出しではなくデータにある（Swallow 8B は output.weight の後、558,186,496 バイト目）ので、`from_gguf(head)` では比べられない。流れの中の 1 つのテンソルとして比べて `finish` の前に断るか、流す前にその 256 バイトを Range で取るかを決める。差は 1B で 2.10e-7、3B で 2.75e-7、Swallow 8B で 1.87e-7。(5) **突き合わせの一覧**: 次元・層・FFN・埋め込みの行数・q の幅は `Stream` の形の確かめで今でも断られる。形で捕まらない head と kv head の数（積が同じなら形も同じ）、`tie_word_embeddings` と output.weight の有無（`llama2_convert.py:990` は lm_head が無いと黙って共有にする）、rope_theta と eps（float32 に丸めて）、arch を足す。context_length は窓があるので比べない（Rakuten mini 131072 対 8192 など）。止まりは原本から（Llama 3.x の GGUF の eos は 128009 だけ）。(6) **`keptName` に語彙のリポジトリと版を入れる**（今は重みの `repo@revision` だけ、`kept.js`）。(7) `format_check.py` に GGUF の項目を足す（今のままでは仕様の「format_check で同じ結果」は 19 項目のどれも見ない）。(8) **形の勧め**: `from_gguf(head, config=, tokenizer=…)` で語彙と config の処理を 2 重に持つより、GGUF から「safetensors 風の見出し（dtype と turned つき）と base」だけを作る関数を出し、worker は今の safetensors の枝の `Conversion(header, base, config, tokenizer, …)` をそのまま呼ぶ。(5) の突き合わせは別の関数に。(9) **ライセンス**: mmnga の Swallow 8B の GGUF のカードは `[llama3.3, gemma]` だけなので、「GGUF のカードのライセンスが原本と同じなら、文は原本のカードから写す」と書く。(10) **7B が 6 つ入る前に**、Q8_0 の広げをカーネルにするか本番で前後を測る（段 ① の should (3)）。数字: 19 項目とも Q8_0 の GGUF とカードのライセンスがあり、取得は 143.09 → 76.08 GB（0.532）、見出しはどれも 8 MiB 以内で型は Q8_0 と F32 だけ、語彙は 17 項目が id ごとに同じ。式: 準備完了 ≒ max(取得, 変換)（流しながら変換するので。見立て）。覆す条件: 行ごとの確かめで原本と違う行が 1 つでも出たモデルは替えない。q・k の並びが llama.cpp の回した順でない GGUF は替えない。持ち主の回線で Q8_0 の変換が取得より遅いと測れたら広げのカーネルを先に。GGUF のライセンスが原本と違うものは使わない。
### T137 [性能] 変換のレジューム: 取得と変換が途中で止まっても、次は続きから — 状態: **第 0 段（計測）の CI の数字が出た、2026-09-26。持ち主の端末の数字とレビュー担当の判定を待つ**（**仕様は決まった。実装はまだしない**。2026-09-26 に仕様をレビューし、足すもの 7 つを書いた、2026-09-26、持ち主の指示「変換のレジュームの実装を検討してほしい、実装はまだしない」→「T137 は仕様をつめて台帳を更新」。T136 の (3) をこの番号に分けた。規模 中）
- **根拠**: 日本の回線（8.3 MB/s、T123）で 7〜8B の取得は約 30〜35 分（15〜17GB）、3B 級で約 12〜13 分。今はタブを閉じる・再読み込み・回線が切れて取り直し（`fetchRange` の 3 回）が尽きる、のどれでも最初から。保存（T99）は変換が最後まで済んでから。起きる頻度は未計測。
- **いまの作り（読んで確かめた、2026-09-26）**: 取得は `inOrder()` がファイルの順に `Stream.feed()` へ渡す。`Stream` はテンソルが揃うたびに変換し、`Writer` が出力の位置へ書く（`sink.write(offset, bytes)` → 重みのメモリ）。`Writer` は書き込みのほかに状態を持たない。**テンソルの境目での `Stream` の状態は 3 つだけ**: 次のテンソルの番号（`step`）、そのテンソルの始まり（`steps[step][0]`）、終わった値の数（`done`）。途中まで溜めたもの（`pending`）は境目では空。出力の並び・RoPE の表・見出し・ビット数は見出しと設定から作り直せる。**自動のビット数（T115）は見出しを読んでから決まる**ので、保存の名前（`keptName` はビット数を含む）もそこで決まる: `sink.open(bytes, header, dtype, arch)` がその時点。
- **仕様（案 B。案 A「元のバイトを溜めて最初から変換し直す」は置く量が約 3 倍なので採らない）**
  1. **置き場と形**: 保存（T99）と同じ OPFS のフォルダ（`converted-v1/<keptName>/`）。途中のあいだは `model.bin`（最後の大きさで作り、書けた位置だけ埋まる）と `progress.json` があり、`manifest.json` は無い（今の `keptModels()` は manifest の無いフォルダを数えないので、途中の保存を完成と取り違えない）。`progress.json` は `{ converter: CONVERTER, repo, revision, dtype, max_seq_len, bytes, step, begin, done, total, saved }`（`begin` はファイル（分割ならつないだ位置）の、次に取るバイト）。
  2. **書く**: `model.bin` の同期ハンドルで、`sink.write()` が重みのメモリと同じ位置に書く。**`sink.open()` は Python から同期で呼ばれ、`createSyncAccessHandle()` は非同期**なので（レビュー ②）、取っ手は `Conversion` を作った後で開き、作るときに書いたもの（見出し、float の RoPE の表）を model.bin に写す。`Stream` は境目を越えるたびにその境目（`step`・`begin`・`done`）を覚え、worker は `feed()` の後に境目が進んでいれば `flush()` → `progress.json` を書く（数 KB。書き直しで途中の状態が残らないよう、`truncate(0)` と書き込みと `flush()` を 1 つの同期ハンドルで）。**記録は出力の flush の後**なので、途中で落ちても記録より先の出力は次に丸ごと上書きされる（そのテンソルをやり直す）。**記録する `done` は `feed()` の中で境目をまたいだ時点の値**（レビュー ③。`feed()` の後の値は次のテンソルの行を数えていて、`finish()` が「途中で終わった」と誤る）。最初の境目の前の記録は作ったときの `done`（RoPE の表の分）、全部食わせた後は `step == len(steps)` で `begin` はファイルの終わり。境目ごとに flush すると 7B で約 290 回。回数が重ければ「前の記録から 64 MiB 以上進んだ境目だけ」に（計測で決める）。
  3. **再開の見つけ方**: `convert()` の最初、今の `loadConverted()`（完成した保存）が無いとき、`keptNames(model)` の各名前で `progress.json` を探す。`converter` が今の `CONVERTER` と同じ、`repo`・`revision`・`max_seq_len` が同じなら使う。違えば消す（古い変換器の途中は使えない）。**`?hf=` の版がブランチ（既定の `main`）のときは再開しない**（レビュー ①。版が動くと、形が同じ 2 つの重みを黙ってつなぐ。解決したコミットを記録して比べるなら再開してよい）。**`progress.json` が読めない（書きかけ、空）ときは「途中なし」として消して最初から**（レビュー ④）。ただし別のタブが取っ手を持っているフォルダ（取っ手が取れない = 使用中）は消さない。
  4. **再開**: 見出し・config・tokenizer は今までどおり取り直す（小さい）。`Conversion` に `dtype`（記録のもの。自動の選び直しはしない）と `resume={step, done}` を渡し、`Stream.resume(step, done)` が `step`・`position = steps[step][0]`・`done` を置く（RoPE の表と見出しは初期化で書き直す、同じバイト）。**記録の `total` と `bytes` を新しい `Stream` と比べ、`resume()` は `position` と `begin` の一致を確かめる**（レビュー ⑥。違う位置から食わせても例外にならず黙って壊れる、を試作で見た）。worker は `Conversion` を作った後に `model.bin` の中身を重みのメモリへ読み（完成した保存を読むのと同じ道。`progress` の「8 MiB ずつ」）、取得は `begin` から（分割されたファイルは、つないだ位置からシャードとその中の位置を引く。**シャードの進捗の数えは前のシャードの長さの和から**、レビュー ⑦）。記録のビット数が 64 ビットのメモリを要するのに今のブラウザに無い（別のブラウザで開いた、など）ときは、途中の保存を消して最初から。
  5. **終わり**: `model.bin` はもう全部あるので、`keep()` は tokenizer と manifest を書き、`progress.json` を消すだけ（**今は最後に重み全体をもう一度書いている**: 7B で約 8GB。その書き直しが無くなる）。大きさを読み直して確かめるのは今と同じ。
  6. **止まり方ごと**: タブを閉じる・再読み込み → 次に同じモデルを選ぶと続きから。回線（取り直しが尽きた）→ 今と同じくエラーを出し、選び直せば続きから（同じページの中でも、途中の保存があるので）。別のモデルに切り替えた（中止）→ 途中の保存は残し、戻れば続きから。**中止とエラーで取っ手を閉じる**（レビュー ⑤。開いたままだと次の `open` が失敗し「別のタブ」と誤る。今の `keep()` は失敗でフォルダごと消すが、再開の道ではそうしない）。容量切れ（OPFS の書き込みが失敗・短い）→ 写しをやめて変換はそのまま続け（今の保存の失敗と同じく準備完了の内訳に理由）、途中の保存は消す。
  7. **2 つのタブが同じモデルを変換する**: 同じ `model.bin` に 2 つの同期ハンドルは開けない（2 つ目は例外）。2 つ目のタブは写しを取らずに変換だけする（今の保存の失敗と同じ扱い）。Web Locks は使わない。
  8. **OPFS の無いところ**（Cache API の保存）: 位置を指定して書けないので、レジュームしない（今のまま）。フォルダで開くローカルのファイルもしない（取得が無い）。GGUF はする（`Stream` は同じ）。
  9. **ページ**: 選ぶときの「x GB を取得して変換します。Go on?」は、続きからなら残りの量で言う（「3.2 of 15.2 GB left」の形。文面は実装のときに出す）。進捗は続きの位置から始まる。「kept in this browser」に途中の保存を「途中、43%」の形で出し、消すボタン。
  10. **`CONVERTER`**: 途中の保存にも書き、上がれば捨てる。`Stream` の書く出力は変えないので、この実装で `CONVERTER` は上げない。
- **持ち主の判断（2026-09-26）「T137 の判断はオススメ通りでよい」**: (1) 次の訪問で黙って続きから始めるか、聞くか → **決定: 黙って続きから**（選んだ時点で取得の確認はもう出ている。その文を「残り x GB」にする）。(2) 放っておかれた途中の保存（7B で最大 8GB）をいつ消すか → **決定: 一覧に出して手で消す、に加えて 30 日触られなければ次の訪問で消す**（`saved` の日付で）。(3)（残り）訪問者に見える文（取得の確認、進捗、一覧の「途中」）は実装のときに文面を出す。
- **先に測ること（第 0 段、実装の前）**: OPFS の同期ハンドルの飛び飛びの書き込みの速さ（8 MiB を離れた位置へ、持ち主の端末と CI の Chromium・Firefox・WebKit）。**取得（日本で 8.3 MB/s）より十分速ければ（10 倍以上）進める**。遅い端末があれば、その端末では写しを取らない。
  - **道具（2026-09-26、Opus medium）**: 別ページ `public/opfs-test/`（本番のモデルのページには触らない。`/gpu-test/` と同じ作り）。Worker の中で OPFS の同期の取っ手を開き、既定 1 GiB のファイルに 8 MiB ずつ、(a) 順に、(b) 離れた位置へ飛び飛びに（部品の番号を大きな歩幅で回す並び）書いて flush 1 回、(c) 飛び飛びに書いて 1 つごとに flush（T137 の「境目ごとに flush」の最悪の形）と 1 つごとに `progress.json` 風の小さなファイルを書き直す、(d) 順に読み戻して中身を確かめる。取っ手を開く時間、同じファイルに 2 つ目の取っ手を開くと断られるか（仕様の 7）、閉じた後に開き直せるか（レビュー ⑤）、`navigator.storage.estimate()` も出す。結果は Markdown の表（MB/s と、取得の 8.3 MB/s の何倍か）と Issue のリンク（ラベル `opfs`）。大きさは `?size=`（MiB）、`?run=1` で開いたらすぐ走る。書いたファイルは最後に消す。CI は `node tests/opfs-check.mjs <site> [engines]` と `.github/workflows/opfs.yml`（手動、Linux・macOS・Windows の Playwright の Chromium・Firefox・WebKit、256 MiB）。**Playwright の Firefox の数字は速さに使わない**（落とし穴: デバッガ越しの Firefox。ここは I/O なので wasm ほどは遅れない見込みだが未確認。インストール済みの Firefox（Selenium）での計測は足していない）。
  - **持ち主に頼むこと**: PC の Chrome、Android の Chrome、iPhone の Safari で https://takano32.github.io/pyodide-llm/benchmark/ を開き（T134 で `/opfs-test/` はここの「Storage」の節になった。2026-09-26 の実装まではブランチ）、「Run all」か Storage の「Run」を押して（既定 256 MB。1 GB は選べる、空きが 2 GB ほど要る）、終わったら「Report them as an issue」で Issue にする（機種・OS・ブラウザの 3 行を埋める）。それぞれ数十秒〜数分。**判定（取得の 10 倍以上か）はレビュー担当**。
  - **CI の数字（2026-09-26、`opfs.yml`、本番、256 MiB を 8 MiB × 32、飛び飛びに書いて flush 1 回 / 1 つごとに flush と記録）**: Linux の Chromium 608 / 553 MB/s（取得の 73× / 67×）、Chrome 575 / 535、Edge 557 / 421、Firefox（Playwright）220 / 79（27× / 10×）。Windows の Chromium 371 / 354（45× / 43×）、Chrome 359 / 349、Edge 355 / 336、Firefox 162 / 168（19× / 20×）。macOS（Apple M1）の Chromium 628 / 505（76× / 61×）、Chrome 516 / 527、Edge 468 / 441、Firefox 821 / 1647。どこでも 2 つ目の取っ手は `NoModificationAllowedError` で断られ、閉じた後は開き直せた。**Playwright の WebKit は測れない**: Linux と Windows は Worker に OPFS が無く（`navigator.storage` が無い）、macOS（WebKit 26.4）は取っ手は開くが 256 MiB の書き込みが `UnknownError`（一時プロファイルの容量か。T99 でも Playwright の WebKit の保存は本物と違った）。**Playwright の Firefox の数字は Firefox の速さではない**（落とし穴）。持ち主の端末の数字（とくに iPhone の Safari）は未計測。
- **試験**: pytest で「半分まで食わせて止め、新しい `Stream` を `resume()` して残りを食わせた結果が、止めずに変換した結果と 1 バイトも違わない」（Llama・GPT-2 の c_attn・NeoX・Qwen2 の bias・分割・GGUF・6 ビット、境目の手前・ちょうど・すぐ後で止める）。`kept-check` に途中の保存（見つける・古い変換器なら消す・完成で `progress.json` が消える）。worker の再開の流れは T129 の試験台（`vm` と偽の fetch・OPFS）で。CI の `models.yml` に「途中で読み込み直す」モード（`tests/e2e.mjs` が進捗 40% で再読み込みし、準備完了まで行き、取得したバイトが全体の 7 割未満であること）。
- **判定の書式**: (a) 数字: 7B の取得 15GB・30 分（日本）。失うのは止まった位置のテンソル 1 つ（最大は埋め込みか分類器、7B で約 1GB ≒ 2 分）。置く量は完成の保存と同じ int8 の大きさ。(b) 式: 失う時間 ≒ 止まった位置のテンソルの大きさ ÷ 回線の速さ（今は 止まるまでに取れた量 ÷ 回線の速さ）。(c) 判定: 進める（第 0 段の計測の後）。(d) 覆す条件: OPFS の飛び飛びの書き込みが取得の 10 倍に届かない端末が主になる、または途中で止まることがほとんど無いと分かる（CI と持ち主の報告で）。
- 関係: T136 の (3) をここに分けた。T129 の (3)（1 つの部品が最終的に失敗した後も取得が続く）は、再開の前に止めるので一緒に直す。T124（qwen3）の後でも前でもよい。
- **レビュー（Opus xhigh、2026-09-26、仕様）: 核の主張は正しい。下の 7 つを仕様に足してから第 0 段へ。** 確かめ方: 新規のサブエージェントが `Stream`・`Writer`・`Conversion` の `feed()` の間に変わる状態を全部洗い、リポジトリを変えずに再開を試作した。(a) 数字: 境目の状態は `step`・`position`（= `steps[step][0]`）・`done` の 3 つで、`pending` と `first` は境目で空。GPT-2 の `c_attn` と NeoX の `query_key_value` は 1 つのテンソルの中で溜めるので境目をまたがない。**再開の試作が止めずに変換した結果と 1 バイトも違わなかった**: 選んだ位置（境目の手前・ちょうど・すぐ後・大きいテンソルの中・最初・全部食わせて finish の前）× 2 つの塊の大きさで 216 回（Llama の bf16 / f32 から int8・int6・float16・float32、Qwen2 の bias、GPT-2、NeoX、2〜3 シャード、GGUF の Q8_0）、すべての境目からで 898 回。止まって失うのは、止まった位置のテンソル + 取得中の部品（最大 12 × 16 MiB = 192 MiB、8.3 MB/s で約 23 秒）。(b) 式: 失う時間 ≒（止まった位置のテンソルの大きさ + 取得中の部品）÷ 回線。(c) 判定: 進める。仕様に足すもの: ① **`?hf=` の版がブランチ（既定の `main`）のときは再開しない**（または解決したコミットを記録して比べる）。版が動くと、形が同じ 2 つの重みを黙ってつなぐ。② `sink.open()` は Python から同期で呼ばれ、`createSyncAccessHandle()` は非同期: 取っ手は `Conversion` を作った後に開き、作るときに書いたもの（見出し、float の RoPE の表）を model.bin に写す。再開では作った後に model.bin を重みのメモリへ読む。③ `done` は `feed()` の中で境目をまたいだ時点の値を記録する（`feed()` の後の値は次のテンソルの行を数えていて、`finish()` が「途中で終わった」と誤る）。最初の境目の前の記録は作ったときの `done`（RoPE の表の分）、全部食わせた後は `step == len(steps)` で `begin` はファイルの終わり。④ `progress.json` が読めない（書きかけ、空）ときは「途中なし」として消して最初から。ただし**別のタブが取っ手を持っているフォルダは消さない**（取っ手が取れない = 使用中）。⑤ 中止とエラーで取っ手を閉じる（開いたままだと次の `open` が失敗し「別のタブ」と誤る）。今の `keep()` は失敗でフォルダごと消すので、再開の道ではそうしない。⑥ 記録の `total` と `bytes` を新しい `Stream` と比べ、`resume()` は `position` と `begin` の一致を確かめる（違う位置から食わせても例外にならず黙って壊れる、を試作で見た）。⑦ シャードの進捗の数えは前のシャードの長さの和から。(d) 覆す条件: 仕様のとおり（OPFS の飛び飛びの書き込みが取得の 10 倍に届かない端末が主、または止まることがほとんど無い）に加えて、対象のブラウザの Worker で同期の取っ手が取れないと分かったとき。

### T83 [文書] gist に計測の表（`30-measurements.md`）を足し、`10-overview.md` を今の姿に直す — 状態: 未着手（2026-09-24 採用、Fable の提案。**T81 と、T81 で見つかったモデルの追加の後**（2026-09-25、持ち主の判断）。T84 の表も使う。規模 小〜中、文章）
- 担当（Fable の切り分け、2026-09-25。レビューは Opus xhigh）: **Opus が下書き**（`30-measurements.md` と `10-overview.md` の差分を、gist に載せる前のファイルとしてリポジトリの外、`~/tmp` に置く）→ Opus xhigh が読む → **公開は持ち主が文面を見てから**（外向きで取り消しにくい操作）。
- 根拠: このプロジェクトの価値は「測って**公開する**」こと。先週で測るものは揃った（T52 の分解、T45 のベンチ、21 モデル、スレッドの上限）のに、gist は 4 系統・21 モデルになる前の姿のままで、計測の表（`30-`）は予定のまま無い。**測った数字が gist に無ければ、外から見えるのは古い数字だけ。**
- 手順: (1) `10-overview.md` の「どこまで来たか」「しくみ」「どのブラウザで動くか」を 2026-09-24 の状態に直す（4 系統、21 モデル、chat_template、スイッチ）。(2) `30-measurements.md` を新設: T52 の分解表（Node と Chromium）、T84 の梯子、CI の全体走行から出る OS × ブラウザ × モデルの表（T82 の JSON から）、T68 の読み込みの時系列、スレッドの計測（2 倍と帯域）。**数字はすべて日付と条件（backend、ブラウザの版、機械）つき**。(3) `00-pyodide-llm.md` の目次に足す。
- 決まり: 公開（gist の更新）は外向きなので、**文面を持ち主が見てから**。数値は既存の文書と CI の Summary からの写しだけで、ここで新しく測らない。
- 完了条件: gist の 3 ファイルが今の実装と食い違っていない。表の各行に条件が書いてある。

### T84 [計測] 大きさと速さの関係（Pythia の梯子）— 状態: **進行中（材料を取る仕組みまで）**（2026-09-24 採用、Fable の提案。**性能の改善（T98〜T100）の後に表を起こす**（2026-09-25、持ち主の判断: 公開する数字は改善の後）。規模 小）
- 担当（Fable の切り分け、2026-09-25）: **Opus が最後まで**（表を起こして `kernels/README.md` に載せ、tok/s と大きさの関係を 1 文で書く）。Opus xhigh が載った表を読む。材料は T98〜T100 の後の `browsers.yml` の全体走行の artifact（`node tests/ladder.mjs`）。
- 根拠: T80 で **同じ設計で大きさだけ違う 5 段**（Pythia 70M・160M・410M・1B・1.4B）を揃えたのは、この表を作るため。「WASM Python は大きさに対してどう伸びるか」は、このプロジェクトでいちばん問われる 1 枚になる。まだ誰も測っていない。
- 手順: CI の `huggingface` ジョブ（Linux ランナーの Chromium）で 5 つを同じプロンプト・同じ設定で走らせ、**準備完了までの秒数（取得と変換の内訳つき）・tok/s・ヒープ（`performance.memory` が取れれば）・int8 の大きさ**を 1 表にする。T82 の JSON をそのまま材料にする。可能なら訪問者の報告（T91）から実機の列も足す。
- 見たいこと: tok/s が 1 / パラメータ数 に沿うか（帯域律速なら沿う。AGENTS のスレッドの計測と同じ理屈）、準備完了のうち変換が何割か（T89 の材料）、どこからが「待てない」大きさか。
- 完了条件: 5 段の表が `kernels/README.md` か T83 の `30-` に載り、tok/s と大きさの関係を 1 文で言える。
- **材料を取る仕組み（2026-09-24、Opus 5.5）**: Worker が HF の変換で「Python が変換していた時間」を取得と分けて数え（`seconds.convert`。取得と変換は重なるので、`seconds.download` は両方込みの実時間）、準備完了の報告に WebAssembly のメモリの大きさ（`heap`）を添える。ページはその報告を `window.__ready` に置くだけで、画面は変えていない。`tests/e2e.mjs` はそれを JSON の `load` と `heapMB` に書く。表は `node tests/ladder.mjs <小さい組の results.jsonl> <大きい組の results.jsonl>`（2 つのジョブの artifact）で起こす。列は int8 MB・準備完了・取得と変換・うち変換・tok/s・**int8 MB × tok/s（毎秒読む重みの GB）**・ヒープ。最後の列が梯子を通して一定なら「大きさに反比例」と 1 文で言える。単体テストは `tests/ladder-check.mjs`（デプロイで走る）。**残り**: 持ち主が `browsers.yml` を走らせたら、artifact を取って表を起こし、読み方を書く。

### T95 [計測] wllama と同じ GGUF で比べる — 状態: 未着手（2026-09-25 採用、持ち主の指示。**性能の改善（T98〜T100）の後**（2026-09-25、持ち主の判断: 公開する数字は改善の後）。規模 小〜中）
- 担当（Fable の切り分け、2026-09-25。レビューは Opus xhigh）: **Opus が最後まで**（比較のページ、CI のワークフロー、表、負けた項目の見立て）。Opus xhigh が表と文面を読む。wllama の版は実行時に最新を解決して結果に書く。
- 根拠: wllama は llama.cpp を WebAssembly にしたもので、ブラウザで動く言語モデルの代表。「WASM Python でどこまでできるか」を言うには、いちばん強い相手と**同じファイル・同じブラウザ・同じ機械**で比べるのが早い。T74 で GGUF を読めるようになったので、同じ Q8_0 のファイルをそのまま両方に渡せる。
- 見立て（未計測）: 1 スレッドの演算はこちらの WASM SIMD カーネルがネイティブの NEON とほぼ同じ速さ（AGENTS.md: 10.7〜12.0 対 11.9 G 積和/秒）で、wllama も同じ SIMD の制約の中にいる。バッチ 1 の生成はどちらもメモリ帯域が頭打ちで、Q8_0 とこちらの int8 は読む量がほぼ同じ。だから**生成の tok/s は互角か、こちらが勝ちうる**。負けうるのは、プロンプトのまとめ処理（こちらは 1 トークンずつ、T49 は未着手）と、長い文脈の attention、1 トークンごとに Python の生成ループを通る分。
- 手順: (1) 比べるファイル: SmolLM2-135M-Instruct の Q8_0（一覧の `hf-smollm2-135m-instruct` と同じ bartowski のもの）。余裕があれば 0.5B 級も 1 つ（Qwen2.5 は公式の GGUF が原本と別の重みなので避ける。T74）。(2) wllama は npm（jsDelivr）から読み込む小さな比較のページを `public/` の別ディレクトリに置く（本番のページには触らない）。版は実行時に最新を解決し、結果に版を書く（方針 2 と同じ考え方）。(3) CI の実ブラウザ（`browsers.yml` の OS ジョブか、別の手動のワークフロー）で、**同じプロンプト・greedy・同じトークン数**で、準備完了までの秒数・プロンプトの tok/s・生成の tok/s・メモリを、wllama の 1 スレッドとマルチスレッド、こちらの 1 スレッドと（T93 の段階 2 の後なら）マルチスレッドで測る。wllama のマルチスレッドは COOP/COEP が要るので、T93 の `coi-test` と同じ Service Worker の下で。(4) 出力の文も並べる（量子化の違いで少し変わるのは構わないが、壊れていないことを見る）。
- 完了条件: 表 1 枚（ブラウザ × 実装 × スレッド数 → 準備完了・プロンプト tok/s・生成 tok/s・メモリ）が T83 の gist の計測の表（`30-`）と AGENTS.md に載る。勝ち負けのどちらでも載せる。負けた項目には、何が差を作っているかの見立てを 1 文添える。

### T143 [追加] `?hf=` の書式の特殊トークンと BOS を変換器で合わせる — 状態: 未着手。**次に別の理由で `CONVERTER` を上げるときに、そのコミットの前に Opus medium が入れる**（2026-09-26、持ち主の判断 (2)「Opus medium でなおそう」）（2026-09-26、T124 と T138 のレビューから。規模 小）
- 根拠: 変換器が `specials` に入れるのは `tokenizer.json` で `special: true` の追加のトークンだけ（`public/llama2_convert.py:1716`、そこから書式に書かれているものに絞る）。本物の tokenizers は special でない追加のトークンも 1 つのトークンに読む。`?hf=` で開くと、書式に書かれたそのトークンが綴られる: Qwen3-4B-Thinking-2507 は書式の末尾の `<think>` が `<th`・`ink`・`>` になり 0/9、DeepSeek-R1 Distill Qwen は `<｜User｜>` と `<think>` が綴られて 0/9（ほかの Qwen3 は訪問者が `<think>` と打った場合だけ違う、8/9）。一覧の項目は手で書いた `specials` があるので無事。ほかに同じ種類: (a) config.json と tokenizer の BOS が食い違うモデル（DeepSeek-R1 の系統。変換器は書式の頭で bos_token の ID をもう削っているので、その ID を `options.bos` にする）、(b) GGUF の経路の詰め物の語片の文（T136 のレビュー: 種類 5 を空の文にする 1 行で safetensors の経路と 1 バイトも違わなくなる）。
- 直し: 書式に現れる追加のトークンは special かどうかに関わらず `specials` に入れる。(a)(b) も一緒に。どれも変換器の出すものが変わるので `CONVERTER` を上げる。
- **持ち主に決めてほしいこと**: 上げると、訪問者が保存した変換は全部使われなくなり、次に開くときに取り直しになる（大きいモデルは数分〜数十分）。(1) すぐ直す、(2) 次に `CONVERTER` を上げる別の変更（T136 の段 ② など）と一緒に。レビューの担当 2 人の意見が割れた（T124 の担当は must-fix、T138 の担当は (2)）。Opus の勧めは (2): 一覧のモデルには効かず、効くのは `?hf=` で一覧に無いモデルを開いた人だけ。覆す条件: `?hf=` で開かれるモデルのうち、書式にこの種のトークンを持つものが多いと分かったら (1)。
- 試験: pytest で「special でない追加のトークンが書式にあるとき specials に入る」、`format_check.py` を `?hf=` 相当（一覧の options なし）でも回せるように。
- 完了条件: `?hf=` の Qwen3-4B-Thinking-2507 と DeepSeek-R1 が本物と同じ ID の列。

### T144 [運用] T124・T138 のレビューの残り（試験・道具・エラーの文） — 状態: 未着手（2026-09-26、レビューから。規模 小〜中。T129 と同じ形の寄せ集め）
- 試験（T124 のレビュー。どれも、わざと壊しても今の試験が通ってしまった所）: (1) `Writer` が `sink.open` に渡す head_dim を常に dim/heads にしても pytest が通る（footprint が Qwen3 0.6B の KV を 45% 少なく数える形）→ `tests/test_convert.py` に head_dim の違う場合。(2) GGUF の `attention.layer_norm_rms_epsilon` と `attention.key_length` を読まなくしても通る（GGUF にした Qwen2.5 の 5 つと TinySwallow が、壊れても分からずに 1e-5 に戻る）→ `tests/test_gguf.py` に。(3) デプロイで走る forward-check の 4 モデルに head_dim のあるものが無い → T133 の静的な assert の区画に、Qwen3 0.6B の見出しで `footprint()` の headDim あり・なしを。(4) smoke の Qwen3 の int8 は `backend.startswith("SIMD")` しか見ない（float32 に広がっても通る）→ "int8" を見る。`:140` の「half as wide」は実際は 2/3。
- 形（T124 のレビュー）: (5) head_dim を options に載せる条件が `head_size != dim // heads`（割り切れない dim で取りこぼす）→ `head_size * n_heads != dim`（実在するモデルは見つかっていない）。(6) ファイルから分からない設定を 1 つの辞書で layout・checkpoint_dtype・footprint・sink.open に通す（1 つ足すたびに約 14 か所を触る形をやめる）。
- 道具（T138・T136・T139 のレビュー）: (7) `tests/format_check.py` が GGUF の 9 項目を外している（`:39`）→ GGUF の経路を入れ、`original` にもリビジョンを持たせる。(8) llm-jp の 4 つはカードのシステム文を `SYSTEM` に、sarashina と CAT-Translate の 5 つは本物の Jinja の文を特殊トークンで区切って sentencepiece で符号化して比べる（既知の違い 13 のうち 9 が消える）。(9) `tests/test_llama3.py` が `test_bytebpe.py` を import していて、tokenizers が無いと tokenizers と関係の無い 13 件ごと skip になる → 共有のデータを conftest などに。
- ページ（T138 のレビュー）: (10) `?hf=` のトークナイザの候補が全部断られたとき、最初の候補のエラー（404 など）を出して、変換器の断りの本当の理由が隠れる（`worker.js:1000` の `refusal ??= error`）→ 変換器の断りを優先。フォルダの経路のエラーの文が `spiece.model` を挙げない（`index.astro:910`）。(11) `filled()` の `trim()` の空白の集合を Python の `str.strip()` に揃える（U+FEFF、U+0085、U+001C〜001F。本物の trim のテンプレート 28 件の全部で、この文字のあるプロンプトだけ違う）。(12) DeepSeek-R1 の生成の設定をカードの勧め（temperature 0.6・top-p 0.95）に。
- **済（2026-09-26）**: e2e.mjs と bench-browser.mjs は、一覧に無いモデルの ID（`local`・`hf`・`url`・`hf:…` を除く）をブラウザを開く前に落とす。それまでは `?model=` に渡し、ページが既定の tiny-lm を開いて通っていた（ci.mjs を試した models.yml の `models=no-such-model` が success になって見つけた）。
- 完了条件: 上の試験が、わざと壊したときに落ちる。

## 候補（採否未定）

2026-09-19 に Fable が提案したもの。持ち主が 1 つずつ採用か却下かを決める。採用したら「これからのタスク」へ移し、却下したら理由を添えて「やらないと決めたこと」へ移す。番号はどちらの場合もそのまま。並びは提案時の費用対効果の順。

### T121 [追加] 画像を生成する: まず自己回帰型（画像のトークンを言語モデルで書く）が動くかの判定 — 状態: 候補（2026-09-25、持ち主の問い「画像生成ってできるかな」への Opus xhigh の考察。持ち主が候補に積んだ。規模 判定は小〜中）
- 見立て: **相性が良いのは、画像をトークン列として言語モデルで 1 つずつ書く自己回帰型**。LlamaGen（中身は Llama。ImageNet の 1000 クラスを指定して 256×256 を 16×16 のトークンで。いちばん小さい B は 111M）と Janus-Pro-1B（文章から 384×384 を 576 トークンで。言語モデルの部分は DeepSeek-LLM 系）。今のエンジンと forward がほぼそのまま使え、「言語モデルで画像のトークンを書く」ので目的（言語モデルを動かす実験）の中に収まる。細部（2 次元の RoPE か、トークナイザの形、ライセンス）は着手のときにモデルカードとコードで確かめる（未確認）。
- 足りないもの: (1) 画像の縦横の位置の RoPE、(2) CFG（条件あり・なしの 2 本を同時に回して logits を混ぜる。T108 のまとめ処理で重みの読みを 1 回にできる見込み）、(3) トークンを画素に戻す VQ のデコーダ（畳み込み。**行列×行列で演算律速**なので、今の行列×ベクトルのカーネル（帯域律速、T93）とは別の、ブロックした行列積と畳み込みのカーネルが要る）、(4) ページで画素を描く（Worker が RGBA を送り canvas に。簡単）。
- 速さの見積もり（未計測）: Janus-Pro-1B は今の 1B 級の速さ（CI の 4 vCPU で 20〜26 tok/s）から 576 トークンで 30 秒〜1 分台 + デコーダ。LlamaGen-B は 256 トークンで数秒 + デコーダ。
- 拡散型（Stable Diffusion 系）は別の判断: UNet を何度も回す行列×行列と畳み込み、4096 位置の attention、GroupNorm と、カーネルが一式要る。最小の組は SD-Turbo（1 ステップ）と小さな VAE デコーダ（TAESD）で、512×512 が 1 枚数十秒〜数分の桁の見込み（未計測）。言語モデルの外に出るので、やるなら持ち主の判断。
- 持ち主に決めてほしいこと: 公開サイトで文章から描くモデル（何でも描ける）を置くか。LlamaGen のクラス指定ならその心配は小さい。
- 判定の手順（採るなら）: LlamaGen-B で (1) デコーダの畳み込みの速さを NumPy と SIMD で測る、(2) CFG 込みの 1 枚の時間、(3) 参照（PyTorch。この開発機には無いので CI）と同じトークン列・同じ画素になるか（T86 の固定値のやり方）。報告して止まる。
- T122 と土台（畳み込みと行列×行列のカーネル）を共有する。演算律速になるので、スレッドは帯域でなくコア数で伸びる見込み（T93 の約 2 倍の頭打ちとは事情が違う。未計測）。

### T122 [追加] 音声を合成する（TTS）: 言語モデル型が動くかの判定 — 状態: 候補（2026-09-25、持ち主の問い「TTS ってできるかな」への Opus xhigh の考察。持ち主が候補に積んだ。規模 判定は小〜中）
- 見立て: **言語モデルで音声のトークン（音声コーデックの符号）を書く型**（OuteTTS など。中身は Qwen や Llama 系）なら、今のエンジンにそのまま乗る。足りないのは、符号を波形に戻すデコーダ（畳み込み。T121 と同じ土台）と、ページで鳴らすこと（Worker が波形を送り Web Audio で。逐次の再生もできる）。FFT は NumPy に入っている。文字をそのまま入れる型なら、読みの辞書が要らない。
- 速さの見積もり（未計測）: 音声 1 秒に数十〜百トークン前後（コーデックによる。要確認）。0.5B 級（CI で 40〜55 tok/s）では実時間と同じくらいか少し遅い見込み。
- **日本語の壁は文字の読み**: 小さな専用モデル（Kokoro-82M、Piper など。自己回帰でないので速い）は、文字を発音記号に直す部品（英語は espeak-ng、日本語は OpenJTalk の辞書）が要り、どちらも C のライブラリで今の Pyodide に無い。言語モデルでもないので目的の外。
- 判定の手順（採るなら）: 日本語を読める言語モデル型を 1 つ選び（対応言語とライセンスはモデルカードで確かめる。方針 8）、(1) 1 秒の音声を作る時間（実時間との比）、(2) デコーダの速さ、(3) 参照（CI）と同じ符号列になるか、を測って報告して止まる。

## 保留

### T100 [性能] 投機的デコード（小さいモデルで下書きして、大きいモデルでまとめて確かめる） — 状態: **保留（2026-09-25、持ち主の判断。Fable の判定は見送り）**（2026-09-25 採用、持ち主の指示。**調査から**。**T98 と T99 の後**。規模 大）
- 担当（Fable の切り分け、2026-09-25。判定は済み: 見送り）: **持ち主が覆すなら Opus が実装 → Opus xhigh がレビュー。** 第 1 段（Node だけ、実装なし）: 候補の組み合わせ（llm-jp-3 150M → 440M / 980M、Qwen2.5 0.5B → 1.5B、SmolLM2 135M → 360M、Pythia 70M → 410M / 1B）について、**下書きの受け入れ率**を今のエンジンで測る（小さいモデルの greedy の次トークンが、大きいモデルの greedy の次トークンと一致する割合。同じ文を両方に読ませて位置ごとに数える。T85 の英語と日本語の 1500 トークン）。受け入れ率 r と下書きの長さ k から、期待の伸びを 1 文で出す（大きいモデルの forward 1 回で平均 (1 − r^(k+1)) / (1 − r) トークン進む。まとめて計算する 1 回の費用は 1 トークンの何倍かは未計測なので、第 2 段で測る）。第 2 段（Opus）: `forward.js` に「k トークンをまとめて計算する forward」を足す（行列 × ベクトルの代わりに行列 × k 本。カーネルは `matmul_*` に列数の引数を足すか、k 回回すかを測って選ぶ。出力は 1 トークンずつの forward と 1 ビットも違わないこと）。k = 1・2・4・8 の 1 回の費用を測る。**ここで止まって報告**。第 3 段（Fable）: 第 1 段の受け入れ率と第 2 段の費用から、組み合わせと k を決めて仕様を書く。
- 根拠: 大きいモデルの 1 トークンは重みを全部読む時間で決まる（帯域律速）。下書きの k トークンを大きいモデルが**まとめて 1 回で**確かめれば、重みを 1 回読んで k トークンぶん使える。当たりが多ければ、大きいモデルで 1.5〜2 倍の例が多い（未計測）。出力は大きいモデルだけで作ったものと同じになる（greedy なら一字一句、サンプリングなら同じ分布）。
- 前提: 複数のトークンをまとめて計算する forward（行列 × ベクトルではなく行列 × 行列のカーネル）。これは T49（プロンプトの一括処理、2026-09-19 に「プロンプトが数トークンで効果が見えない」として却下）と同じ部品だが、ここでは**検証に使う**ので理由が違う。できればプロンプトの処理にも効く。
- 組み合わせの候補（語彙が同じであることが条件）: llm-jp-3 の 150M と 440M / 980M、Qwen2.5 の 0.5B と 1.5B、SmolLM2 の 135M と 360M、Pythia の梯子。
- 完了条件: 1 つの組み合わせで、大きいモデル単独と比べた tok/s と出力の一致を示す。伸びなければ理由を書いて取りやめる。
- **第 1 段の報告（Opus、2026-09-25、`tests/draft_check.mjs`、`draft.yml`、CI の Linux x86）。** 小さいモデルと大きいモデルに同じトークンを読ませ、各位置の最尤トークンが一致する割合と、一致が何回続くかから「大きいモデルの 1 回で進むトークン数」（下書き k 個、平均の `min(続く数, k) + 1`）を出した。文は T85 の Wikipedia（1500 トークン）と、大きいモデル自身の greedy の続き（256 トークン。実際の下書きが当てるのはこちらに近い）。

  | 小 → 大 | 文 | 一致 | k = 1 | k = 2 | k = 4 | k = 8 |
  |---|---|---:|---:|---:|---:|---:|
  | llm-jp-3 150M → 440M（instruct3） | 日本語の記事 | 61.5% | 1.61 | 2.00 | 2.43 | 2.69 |
  | 同 | 大の greedy | 93.0% | 1.93 | 2.80 | 4.47 | 7.65 |
  | llm-jp-3 150M → 980M（instruct3） | 日本語の記事 | 55.0% | 1.55 | 1.87 | 2.18 | 2.35 |
  | 同 | 大の greedy | 75.8% | 1.76 | 2.36 | 3.28 | 4.74 |
  | Qwen2.5 0.5B → 1.5B（Instruct） | 英語の記事 | 64.6% | 1.65 | 2.07 | 2.55 | 2.83 |
  | 同 | 日本語の記事 | 61.3% | 1.61 | 2.00 | 2.41 | 2.64 |
  | 同 | 大の greedy（英 / 日の記事の冒頭から） | 88.3% | 1.88 | 2.67 | 4.08 / 4.05 | 6.47 / 6.43 |
  | SmolLM2 135M → 360M（Instruct） | 英語の記事 | 64.9% | 1.65 | 2.09 | 2.62 | 2.98 |
  | 同 | 大の greedy | 88.3% | 1.88 | 2.65 | 3.96 | 5.91 |
  | Pythia 70M → 410M | 英語の記事 | 50.3% | 1.50 | 1.77 | 2.00 | 2.07 |
  | 同 | 大の greedy | 95.3% | 1.95 | 2.87 | 4.65 | 8.09 |
  | Pythia 70M → 1B | 英語の記事 | 48.3% | 1.48 | 1.74 | 1.97 | 2.06 |
  | 同 | 大の greedy | 85.5% | 1.86 | 2.61 | 3.89 | 5.89 |

  **greedy の数字は上限寄り**: ページは temperature 0.7 で引くので、下書きの受け入れは greedy より下がる（未計測）。
- **第 2 段の報告（Opus、2026-09-25）。** `forward.js` の `run()` に「最後の n トークンのロジットを出す」を足し、`forwardEach(tokens, pos)` で最大 16 トークンを 1 回で通して各トークンのロジットを出せるようにした（T108 と同じ経路。新しいカーネルは 0）。**各トークンのロジットは 1 つずつのときとビット単位で同じ**（`tests/threads-check.mjs`、1〜16 本、3 トークンずつの下書き）。1 回の費用（`threads.yml`、llm-jp-3 150M、4 本、1 トークンの何倍か、k = 1 / 2 / 4 / 8 / 16）:

  | 機械 | k = 2 | k = 4 | k = 8 | k = 16 |
  |---|---:|---:|---:|---:|
  | Linux ARM（ubuntu-24.04-arm） | 1.80 | 2.73 | 4.62 | 8.26 |
  | Linux x86（ubuntu-latest） | 1.71 | 3.10 | 5.81 | 11.22 |
  | macOS（Apple M1） | 1.50 | 2.66 | 4.42 | 7.50 |
  | この開発機（ARM、4 本） | 1.40 | 2.19 | 3.72 | 6.81 |

  1 本では k にほぼ比例する（k = 4 で 3.6〜4.1 倍）。**効くのはソフトウェアスレッドのあるときだけ**（T108 と同じ理由）。
- **Fable に判定してほしいこと**: (a) 組み合わせ（llm-jp 150M → 440M が最も有望: greedy で k = 4 なら 1 回 4.47 トークン、確かめる費用は 2.7〜3.1 倍、ただし下書きの小さいモデルの費用が加わる）、(b) k、(c) temperature で引くときの受け入れ（ページの既定は 0.7。受け入れ率は第 1 段の greedy より下がる）、(d) 2 つのモデルを同時に持つメモリ（150M + 440M で int8 約 0.7GB）と、2 つ目のモデルの取得と変換の時間。
- **判定（Fable、2026-09-25）: 実装しない。** 第 1 段と第 2 段の数字を合わせると、いちばん有望な組み合わせでも速くならない。llm-jp-3 150M → 440M、4 本、k = 4: 大きいモデルの 1 回で進むのは greedy の続きで 4.47 トークン。その 1 回の費用は 1 トークンの 2.7〜3.1 倍で、下書きの 150M の 4 トークンぶん（CI の Chromium で 150M は 440M の約 0.44 倍の時間）1.8 を足すと 4.5〜4.9。**4.47 / 4.7 ≈ 0.95 倍**。実際の文（2.43 トークン）では 0.5 倍、k = 8 でも 7.65 / 7.9 ≈ 0.97 倍。ページは temperature 0.7 で引くので受け入れはさらに下がる。1 本では確かめる費用が k にほぼ比例して（k = 4 で 3.6〜4.1 倍）話にならない。**理由**: このエンジンの int8 の行列積は演算で律速されている（T53: 行列の大きさに依らず 10.7〜12.0 G 積和/秒）ので、k トークンをまとめても重みの読み出しを節約する意味が薄く、費用がほぼ k 倍になる。投機的デコードが効くのは「まとめる費用 ≪ k 倍」の、メモリ帯域で律速されたエンジンだけ。**後始末（済み、Fable、2026-09-25）**: 第 2 段で足した `forwardEach` と `threads-check` の下書きの計測は使い道が無いので外した（`run()` はロジットを最後の 1 トークンぶんだけ出す形に戻した。使わないコードを残さない: 持ち主の「コードをきれいに」）。T108 の部品（`forwardMany`）はプロンプトで使うので残る。第 1 段の道具（`draft_check.mjs`・`draft.yml`）は計測の記録として残す。

採用したが、いまはやらないと決めたもの。条件が変わったら「これからのタスク」へ戻す。

### T102 [運用] JSPI で Python の同期ループから非同期の JS を待つ — 状態: **保留（2026-09-25、持ち主の判断）**
- 中身: JS Promise Integration を使うと、Pyodide の中の同期の Python（生成のループ）から、JS の Promise（GPU の結果の読み戻し、ストリームの取得）を待てる。速さではなく作りの自由度が上がる。
- 保留の理由: いまの作りでは要らない（forward は同期の JS）。効くのは WebGPU（T94）の読み戻しの難所を解くときだが、T94 は GPU を別の Worker で動かして `Atomics.wait` で待つ形にした（2026-09-25）ので、要らない。対応は Chrome が中心。
- 再開の条件: T94 を隔離されていないページでも動かすことになったとき。

### T103 [性能] WebNN でブラウザの NPU や GPU を使う — 状態: **保留（2026-09-25、持ち主の判断）**
- 中身: ブラウザからニューラルネット用の演算を NPU や GPU に任せる API。当たれば大きいが、効きは未知数。
- 保留の理由: 一般提供がまだで、試せるのは一部のブラウザのフラグや試験運用の中だけ。GPU のある計測環境も無い（T94 と同じ）。
- 再開の条件: 主要ブラウザで既定で使えるようになり、計測できる環境ができたとき。

### T104 [性能] WebAssembly の FP16（f16x8）で活性値を半精度にする — 状態: **保留（2026-09-25、持ち主の判断）**
- 中身: 半精度の SIMD 命令で、活性値や float16 の重みを広げずに計算する。
- 保留の理由: 提案は実験段階で、既定で使えるブラウザが無い。効くのは活性値の帯域（重みに比べて小さい）と float16 の原本で、見込みは小さい。
- 再開の条件: 主要ブラウザで既定で使えるようになったとき。


## 完了したタスク

古い順。括弧内は対応するコミット。

- [x] **T1 遅さの原因を実測する。** 純 Python 版は 0.26 tok/s。原因は行列積の内側ループをインタプリタが回していること。同じ機械で NumPy 54、素の JS 38、WASM SIMD 170〜190 tok/s。スレッド化は効かない（ネイティブ OpenMP でも同じ）。
- [x] **T2 類似の試みとブラウザ基盤を調査し、レポートを公開する。** llama2.c のブラウザ移植、wllama、WebLLM、Transformers.js、WebGL、モバイルの制約など。gist: https://gist.github.com/takano32/196c6f93979ad44f98cee5712fdd3901
- [x] **T3 WASM Python の土台を決める。** Pyodide を継続。MicroPython の WASM 版は起動 20〜50ms だが計算は 1/3.5（ulab の行列積は 1/11）。Pyodide 内で ctypes 読み込みの SIMD カーネルを使うと 181〜348 tok/s まで出ることを実証（未導入 → T30）。
- [x] **T4 GitHub Pages で成立するか実ブラウザで確かめる。** 特別な HTTP ヘッダなしの静的配信で、wheel のインストールから推論まで Chromium と Firefox で動作。COOP/COEP は不要。
- [x] **T5 http-server の `--cors` の typo を直す。**（755222a）
- [x] **T6 生成物を `.gitignore` に入れる。** バイナリはコミットしない方針。（5b51e0b）
- [x] **T7 GitHub Actions を最新版にする。** checkout v7、configure-pages v6、upload-pages-artifact v5、deploy-pages v5。（210963c）
- [x] **T8 `tokenizer.bin` を llama2.c 本家のものにする。** 旧ファイルは語彙が 204 個重複し、句読点や大文字が未学習の ID になっていた。（54fda6c）
- [x] **T9 Pyodide を常に最新版で読み込む。** 実行時に jsDelivr の API で解決。`?pyodide=<version>` で強制可。固定バージョンやフォールバックは置かない。（4f248fe）
- [x] **T10 NumPy 版のエンジンを書く。** `llama2_numpy.py`。llama2.c の C 実装と 256 トークンまでバイト単位一致、約 50 tok/s。BOS で停止、プロンプト中は分類器を省く、ダミープレフィックス（無いとパープレキシティ +8.5%）。（bdac468）
- [x] **T11 Hugging Face 形式の変換ツールを書く。** `convert_hf.py`、PyTorch 不要。bfloat16、Q/K の並べ替え（あり 4.45 / なし 5.04 の NLL で検証）。（513442c、f1ad824）
- [x] **T12 全モデルを `make models` で取得・変換する。** ローカル・Docker・デプロイで共通。（ff255b9）
- [x] **T13 Web Worker 化、逐次表示、モデル選択。** 日本語モデル tiny-lm を追加（unigram トークナイザは sentencepiece と 16 例一致）。greedy だと反復するのでサンプリング + 繰り返しペナルティ。`<meta charset>` 欠落による文字化けも修正。（a6ea457、6b654fb）
- [x] **T14 ダウンロードの進捗を表示する。** GitHub Pages が `.bin` を gzip で送るため Content-Length が使えず、モデル一覧に展開後の `bytes` を持たせた。（7ab8448、6f5ebb3）
- [x] **T15 入力欄を最下部に固定し、出力だけがスクロールするようにする。**（59f3053、6d38f09）
- [x] **T16 大きいモデルを int8 で配布し、原本も選べるようにする。** `quantize.py`（グループ 32）。品質は原本と区別できず（stories15M +0.04%、tiny-lm 91.3 → 91.1、llm-jp 22.76 → 22.69）、int4 は +16.8% で不可。（ef4c7d2、1b24d49、c36992e）
- [x] **T17 質の高い日本語モデル llm-jp-3-150m を追加する。** パープレキシティ 22.8（tiny-lm は 91）、約 8.5 tok/s、メモリ約 500MB。文脈長を 512 に切り、埋め込み表は int8 のまま保持。（355e93f、0b63548）
- [x] **T18 モデルを 8 MiB の部品に分けて 4 並列で取得し、Pyodide のロードと同時に進める。** 本番 CDN で約 1.8 倍速。ブラウザ側の Range 分割は gzip のため不可。（0b63548）
- [x] **T19 デプロイ直後に新旧のコードが混ざらないようにする。** Worker の URL にコミットのハッシュを付与。（b620c40）
- [x] **T20 既定モデルを日本語でいちばん軽い tiny-lm（int8）にする。** デモは速さ優先。既定プロンプトは「昔々、」。（4f8fd6a、c2c6e57）
- [x] **T21 ページを Astro でチャット風に作り直す。** Node 24 LTS、ライト / ダーク自動、GitHub へのリボンとコーナーをランダム表示。（9d09e80〜2f20fec）
- [x] **T22 プロジェクトを pyodide-llama-py に改名する。** Pages の旧 URL は転送されない。（adbd2c1）
- [x] **T23 引き継ぎ書と台帳を作る。** `AGENTS.md`、`TODO.md`。（268a141）
- [x] **T26 CI のスモークテスト。** `tests/smoke.mjs`: Node 上の最新 Pyodide で、stories260K（float32・GQA）の greedy 出力が参照どおりであることと、tiny-lm（変換 + int8 + unigram）が生成できシードで再現することを確認。約 8 秒、メモリ約 450MB。エンジンを壊すと失敗することを確認済み。デプロイでは `pyodide@latest` を入れ直してから走らせる（ページが実行時に最新版を使うため）。
- [x] **T25 モデルのブラウザ内キャッシュ。** 部品を Cache API（`models-v1`）に保存し、キーに展開後のバイト数を含める。2 回目の読み込みはモデル部品のネットワーク要求が 0 件、準備完了が 11.3 秒 → 7.1 秒（tiny-lm、ローカル）。サイズが変わった古い部品は読み込み後に削除。`navigator.storage.persist()` はページ側から要求（Worker からは呼べない。ヘッドレス Chromium では許可されず false のまま）。
- [x] **T30 SIMD カーネルの導入。** `kernels/*.ts` を `make kernels` でビルドし、`llama2_numpy.py` が ctypes で読み込む（失敗時・GQA・32 の倍数でない int8 は NumPy にフォールバック、`?kernel=off` で NumPy を強制）。float32 は NumPy と同じ出力で 53 → 200 tok/s、int8 は重みを int8 のまま計算して stories15M 351、tiny-lm 422 tok/s（Node 上の Pyodide、greedy）。llm-jp-3-150m は 9.3 → 81 tok/s、WASM ヒープ 897MB → 283MB。Chromium では tiny-lm 43 → 149、llm-jp 8.5 → 47、stories15M 50 → 296 tok/s。Firefox でも動作、Safari は未確認。スモークテストが両経路を確認する。
- [x] **T32 サンプリングの高速化。** `exp` の前に、最有力の 1000 万分の 1 未満のトークンを落とし、llama2.c と同じ厳密な足切り（(1 − top-p)/(n − 1) 未満は nucleus に入らない）で並べ替えの対象を絞り、抽選は累積和と乱数 1 個にした。`sample()` は 2.2 → 0.84 ms（tiny-lm、語彙 51200）。Chromium の tiny-lm: サンプリングあり 263 tok/s（greedy は 318）、繰り返しペナルティ付きは 171 tok/s で目標の 250 には未達 → T34。nucleus の厳密さ・頻度・シードの再現性は検証済み。
- [x] **T33 カーネルの GQA 対応。** `attention` が n_kv_heads と 4 の倍数でないヘッド長（stories3_5M は 26）に対応し、KV キャッシュは `[seq][kv_dim]`。これで全モデルがカーネルで動く。NumPy 版と同じ greedy 出力をスモークテストで確認。Chromium で stories3_5M 141 → 402 tok/s、stories260K 268 → 951 tok/s。
- [x] **T34 サンプリングをカーネルに移す。** 繰り返しペナルティ（`penalize`）と、softmax・足切り・top-p の選択・抽選（`sample`）を `kernels/kernel.ts` に移した。`exp` は SIMD で 4 個ずつ、並べ替えは自前のクイックソートで **nucleus が埋まるところまでしか進めない**（候補は平均 2000・最大 8000 個あるが、必要なのはたいてい数十個）。乱数は Python の `rng.random()` を 1 個渡すので、シードの再現性はそのまま。直近 64 トークンは NumPy 配列をリングとして持ち、1 ステップに 1 個だけ書く。NumPy 版の `Llama.penalize()` / `Llama.sample()` はフォールバックとして残した。ペナルティ + 抽選は 0.92 → 0.29 ms（tiny-lm、Node 上の Pyodide）。Chromium の tiny-lm（既定の生成設定）: 171 → 252〜274 tok/s（3 回計測）、llm-jp-3-150m は 61〜66 tok/s（256 トークンを 4 回。最初に書いた 67 は 61 トークンで終わった 1 回だけの値だったので測り直した）。検証: 40 種の分布で nucleus が厳密、20 万回の抽選の頻度が 5σ 以内、同じ乱数で NumPy 版と 300/300 で同じトークン、ペナルティは NumPy 版と一致、同じシードで同じ文章。要点は `tests/smoke.mjs` に入れた。はまった点: カーネルに渡すだけの作業用配列は Python から参照が消えて解放され、たまに `memory access out of bounds` で落ちた → `self._sampler_buffers` で保持。
- [x] **T24 停止ボタン。**（担当: Opus、レビューと仕上げ: Fable）送信ボタンが生成中だけ停止ボタン（■）になる。要素は増やしていない。Worker の `generate()` を `async` にして `pieces.next()` で回し、イベントループに返して `{type: "stop"}` を受け取る。生成の前後に届いた stop は次の生成を止めない。生成中の `load` は先に止めて待つ。返す間隔は最初「8 トークンごと」だったが、stories260K（900 tok/s）だけ約 5% 遅くなったので **50ms ごと**に変えた（903〜941 tok/s で変更前と同じ、tiny-lm 256〜268）。実ブラウザで確認: 押して 0.13〜0.19 秒で止まり、stats は途中終了でも正しく、直後の生成は最後まで走り、生成中の Enter は二重送信も停止もしない。未確認: Firefox。はまった点（Enter が送信ボタンの click になる）は AGENTS.md。
- [x] **T29 エンジンの単体テスト。**（担当: Opus）`tests/test_*.py` と `conftest.py`。78 件が 7.5 秒、モデルのファイルが無ければ 54 件 + スキップ 24 件。合成チェックポイント 5 構成（MHA、GQA、MQA、分類器が別、head_size 4）で `forward` を Python のループの参照実装と相対誤差 1e-4 以内で比較、`quantize.py` の誤差がグループ最大値の 1/127 以内、BPE / unigram の往復（日本語、絵文字、語彙外、空白類）、`sample` の nucleus が厳密、`generate` のシード再現・停止・`ValueError`。エンジンのバグは見つからず。デプロイのワークフローでも走らせる。カーネルの経路は対象外（`smoke.mjs`）。
- [x] **T31 README に計測結果を載せる。**（担当: Opus）`## Measurements` に実装別 tok/s、Chromium でのモデル別（NumPy / カーネル）、メモリ、int8 の品質、分割ダウンロードの表。文書にある数値だけを使い、Safari は未計測と明記。その過程で文書間の食い違いを直した: llm-jp-3-150m は測り直して 61〜66 tok/s、tiny-lm は llm-jp の「3 倍」ではなく 4 倍、分割ダウンロードは「約 2 倍」ではなく 1.8 倍。
- [x] **T27 生成設定の表示 / T28 計測の表示。**（担当: Opus、レビューと仕上げ: Fable）回答の下の行が `tiny-lm 29M · 90 tokens · 255.0 tok/s · temp 0.7 · seed 1234567890`（greedy のモデルは `… · greedy`）になり、タップすると内訳（最初のトークンまでの時間、プロンプトの tok/s、生成の tok/s と全体の秒数）が開く。ステータス行も準備完了後は開けて、Pyodide のロード / ダウンロード（同時進行なので足さずに並べて表示）/ `Llama()` の構築を出す。開けることが伝わるように行末に ▸ を付けた。シードはサンプリングするモデルのときだけページが `crypto.getRandomValues` で引いて渡す。エンジンの `stats` に `sampled`・`prompt_tokens`・`prompt_seconds`・`prompt_tokens_per_second`・`first_token_seconds` を追加（既存のキーは同じ意味のまま）。実ブラウザで確認: シードを固定した 2 回は同じ文、別のシードなら別の文、390px でページはスクロールしない、途中で止めた回答の行も正しい。速度は変わらず（tiny-lm 250〜274、stories260K 918〜965 tok/s）。実測の例（tiny-lm、キャッシュなし）: Pyodide 8.07 秒・ダウンロード 8.09 秒（同時）・`Llama()` 0.25 秒・最初のトークン 0.03 秒。未確認: Firefox、ライトテーマの見た目。
- [x] **T36 Enter は改行、Ctrl / Cmd + Enter で送信。**（Fable）入力欄を `<textarea>` にして、行数に合わせて 5 行まで伸び、それ以上は欄の中でスクロールする（ページはスクロールしない）。日本語の変換確定の Enter で送信されることがなくなった。スマホは送信ボタンで送る。`tests/e2e.mjs` は Ctrl + Enter で送信する。実ブラウザで確認: Enter と Shift + Enter は改行、Ctrl + Enter と Meta + Enter は送信、生成中の Ctrl + Enter は何もしない、停止ボタンはそのまま動く。
- [x] **T37 ダウンロード中のモデル切り替え。**（Fable）モデルの選択はロード中も有効（無効なのは生成中だけ）。選び直すと Worker が実行中のロードを `AbortController` で中止し、新しいモデルを取りにいく。Pyodide のロードはやり直さない。ロードには番号を振り、Worker の報告（status / progress / ready / error）に付けて、ページは古い番号の報告を捨てる。届き終わっていた部品は Cache API に残るので、元のモデルに戻すと続きから（実測: 21 部品中 6 個が済んでいて、再取得は 15 個）。回線を遅くした実ブラウザで確認: Pyodide のロード中の切り替え、バッファ確保後の切り替え、4 連続の切り替え（最後の 1 つだけが準備完了になる）、切り替え後の生成の出力、エラーなし。メモリ: 中止したロードの後始末を待たずに次のバッファを確保するとヒープが 587MB まで膨らんだ → 後始末を待ってから確保するようにし、モデルを手放すときに `gc.collect()` も呼ぶようにした（同じ操作の後で 266MB。llm-jp を普通に読んだときの約 283MB と同じ水準）。ただし一度 llm-jp のバッファ（171MB）を確保すると、中止してもヒープは 210MB から縮まない（WebAssembly のメモリは縮まない）。
- [x] **T38 ローカルのモデルファイルを使う。**（Fable）モデル選択の右のフォルダのボタン（隠した `<input type="file" multiple>` の `<label>`）か、ページへのドラッグ & ドロップで、チェックポイント + `tokenizer.bin`（+ 任意で設定の `.json`。`src/models.js` の 1 項目と同じ形）を選ぶ。`.json` 以外の大きいほうがチェックポイント。ファイルは `File` のまま Worker に渡し、`file.stream()` でダウンロードと同じ Python バッファへ読む（アップロードなし、Cache API にも入れない）。`dtype` はエンジンの `checkpoint_dtype()` がヘッダとファイルの大きさから判定し、`check_tokenizer()` が語彙の大きさの合わない `tokenizer.bin` を断る（エンジンは大きい語彙の先頭だけを黙って読んでしまうため）。どちらも大きなファイルを読む前に走り、トレースバックなしの文で出る。読んだモデルはコンボボックスに `名前 — local · 33 MB` の一時的な項目として出て、配布モデルとの行き来はダイアログなしでできる。1GB を超えるファイルは `confirm()` で確認する。実ブラウザで確認（Chromium。`node tests/e2e.mjs local` は Firefox でも）: float32（stories260K、期待どおりの出力）、int8 + 設定の JSON（tiny-lm、unigram・NFKC・サンプリング）、ドロップした int8（stories15M）、読み込み中にモデルの取得リクエストが 0 件、トークナイザ 2 つ / 語彙の合わない組 / 1 ファイルだけ、のそれぞれでエラーが出て配布モデルへ戻れる、390px でスクロールしない。手元の全モデル（float32 / float16 / int8 の 12 ファイル）で判定が正しく、トークナイザ 5 つは「チェックポイントではない」と断られる。未確認: 1GB 超のファイル（この機械では試せない）、読み込み中の切り替えによる中止（経路は T37 と同じだが、ローカルの読み込みは一瞬で終わるので試せていない）、スマホ実機のファイル選択。
- [x] **T35 生成設定の変更。**（Fable）入力欄の左端のスライダーのアイコンから、PC ではポップオーバー、640px 以下では下から出るシートで Temperature（0 は greedy）・Max tokens（上限はそのモデルの `seq_len`。Worker が `ready` で知らせる）・Seed（空欄は毎回ランダム、× で空欄に戻す）を変える。top-p と繰り返しペナルティは「More」の中。OK ボタンはなく次の生成から効き、既定値と違う間はアイコンに点が付く。「Reset to defaults」で `model.generation` に戻る。回答の下のシードはボタンになり、押すとそのシードを固定する（行は開かない）。モデルを切り替えると既定値に戻るが、**シードだけは持ち越す**。決めたこと: ハンバーガーメニューにしない、入力欄の上の `<details>` にしない、リロードをまたいで保存しない、パネルはフォームの外に置く（フォームの中だと、シードの欄で Enter を押したときにプロンプトが送信される）。Popover API を使い、ライブラリなし。実ブラウザで確認（Chromium）: 同じシードで 2 回 → 同じ文、ランダムなシードをタップ → tiny-lm（int8）から原本に切り替え → そのシードで生成、リセット、temperature 0 で `greedy` 表示・シードのボタンなし、Esc と外側のクリックで閉じる、シードの欄の Enter で送信されない、390px でシートを開いてもページはスクロールしない。速度は変わらず（tiny-lm 280、stories260K 892〜953 tok/s）。未確認: Firefox と Safari での見た目、スマホ実機でのキーボードとの兼ね合い。
- [x] **入力欄は 1 行 + 送信キーの表示。**（Fable、T36 の続き）いったん `rows="2"` にしたが、空きが間延びして見えるので 1 行に戻した（5 行まで伸びるのはそのまま）。複数行を書けることは、送信ボタンの左の小さなキー表示 `Ctrl ⏎`（Mac では `⌘ ⏎`）で伝える: 送信が Ctrl + Enter だと分かれば、Enter だけなら改行だと分かる。タッチ端末と 480px 以下では出さない（そのキーが無い）。検討してやめた案: プレースホルダに書く（既定のプロンプトが入っているので見えない）、`resize` のつまみ（丸い枠に合わず、自動で伸びる動作とぶつかる）、入力欄の下の説明文（スマホで無意味な行が増える）。
- [x] **T53 1 トークンの時間の内訳を測る。**（Fable）`tests/profile.mjs`（Node / Chromium / Firefox、`PROFILE_THREAD=worker` で Worker の中）と `tests/profile_token.py`。ブラウザの時計は粗く呼び出し 1 回を測れないので、同じモデルの forward を「本物のカーネル」「数えるだけ」「何もしない関数」の 3 通りで走らせた差分と、n = 0 で呼んだカーネル（= ctypes の呼び出し 1 回）のコストから内訳を出す。**結果（Node と Chromium でほぼ同じ）: llm-jp-3-150m は層の行列積 52%、分類器 38%、ctypes 6%（231 回 × 約 3µs）、サンプリング 3%、Python と NumPy 1%。tiny-lm は分類器 54%。** 着手前の私（Fable）の見立ては 2 つとも外れた: (1)「既定モデルはメモリ帯域で頭打ち」ではなかった。int8 の行列積はキャッシュに載る 0.4MB でも載らない 75MB でも 10.7〜12.0 G 積和/秒で、float32 のカーネルが 20GB/s を流すメモリで 13GB/s しか使っていない → 律速は演算で、カーネルに伸びしろがある。(2) 1 トークンで読む量は 160MB ではなく 114MB（llm-jp-3-150m の dim は 768 ではなく 512）。ほかに分かったこと: attention が位置とともに重くなり（llm-jp で位置 8 の 12.6ms → 位置 248 の 16.3ms）、256 トークンの実行が 82 ではなく Node で 70、ページで 61〜66 tok/s になる主因。Worker の中でもメインスレッドでも速度は同じ。次にやる性能のタスクは T54（カーネルの演算）。T47 は既定モデルで最大 7% なので優先度を下げた。
- [x] **T54 カーネルの演算を速くする。**（Fable）**Chromium のページで llm-jp-3-150m（256 トークン）が 61〜66 → 75〜79 tok/s、tiny-lm が 252〜274 → 271〜296 tok/s。** 新旧のカーネルを同じプロセスで交互に走らせた比較では、llm-jp の 1 トークンが位置 8 で 13.3 → 12.1ms、位置 120 で 15.2 → 12.4ms、位置 240 で 17.9 → 12.8ms（1.09 / 1.23 / 1.39 倍）。(1) `attention` を「位置ごとに全ヘッド」で KV キャッシュを 1 回だけ歩く形に書き直した（ヘッドごとだと、CPU のキャッシュに載らない 12 層ぶんの KV キャッシュを飛び飛びに 8 回歩いていた）。スコアは 4 アキュムレータ、softmax は `vexp` で 4 個ずつ、値の加算は 4 位置ぶんを 1 回の読み書きで。作業領域は `heads × (pos + 1)` 個の float になった（エンジンは `heads × seq_len` を確保）。位置 248 での上乗せは 3.7 → 0.7ms。(2) `matmul_q8r` を 4 グループ単位にアンロール（スケールをベクトルで掛ける、アキュムレータ 2 本、補正項を独立した内積に）: 9.8〜11.0 → 11.2〜12.5 G 積和/秒。**ここが上限**: relaxed dot の命令だけを回すループが 13.0 G 積和/秒（アキュムレータ 1 本なら 9.7）なので、V8 のこの命令の速さの 9 割に達している。試して戻したもの: 2 行同時処理は遅くなった（10.4 / 8.8 / 11.1）、`matmul_q8`（relaxed SIMD なしの経路）への同じ工夫は効果なし（7.6〜8.3 のまま）。完了条件の「int8 は変更前と同じ文」は満たせず、理由を調べた: 行列積は新旧とも整数の厳密計算と 1e-6 で一致するが、加算順の違いが 7 ビットの活性値の丸めをまたぎ、llm-jp で logit が最大 1.0 動く。カーネルの経路は以前から NumPy の経路（活性値を量子化しない）と最大 1.7 違っていて、新しいほうが 1.6 とわずかに近い。float32 のモデルは NumPy と一字一句同じ（`smoke.mjs`）。7 ビットの活性値の品質への影響は未計測 → T55。
- [x] **T44 Safari 系（WebKit）を CI でテストする。**（Fable）`.github/workflows/webkit.yml`: macOS ランナーで Playwright の WebKit を入れ、本番サイトに対して `tests/e2e.mjs` を 4 モデル走らせ、結果を実行の Summary に書く。手動（`gh workflow run webkit.yml`）か週 1 回（リポジトリが公開なので macOS ランナーは無料）。`e2e.mjs` はブラウザの版とステータス行（backend）も出すようにした。**結果（WebKit 26.4、Apple M1 の仮想マシン）: 4 モデルとも一発で動いた。int8 のモデルは予想どおり `SIMD kernels, int8`（relaxed SIMD なしの `matmul_q8`）で動き、このフォールバックが初めて実ブラウザで走った。** llm-jp-3-150m 146 tok/s、stories15M（int8）739、tiny-lm 515（20 トークンで終わった回）、stories260K 4271。CPU が開発機よりずっと速いので、ほかの数値とは比べられない。Playwright の WebKit は Safari そのものではなく、iPhone の実機も未確認のまま（T45 のベンチマークモードで集める）。
- [x] **T55 カーネルの経路（int8 の活性値）の品質を測る。**（Fable）`tests/perplexity.mjs` + `tests/perplexity.py`: Node 上の Pyodide で、同じテキスト（日本語 Wikipedia の「富士山」「夏目漱石」「新幹線」の冒頭、1499 トークン、512 トークンごとに文脈をリセット。テキストは実行時に取得し、リポジトリには置かない）のパープレキシティを 3 つの計算経路で測る。**llm-jp-3-150m: float16 の原本 29.976 / int8 の重み + NumPy 29.907（-0.23%）/ + 8 ビットの活性値 29.965（-0.04%）/ + 7 ビットの活性値 30.094（+0.39%）。tiny-lm: NumPy 88.327 / 8 ビット +0.01% / 7 ビット +0.34%。** 7 ビット目（relaxed SIMD）の代償は約 0.5% で、速度は 3 割増し。T54 で見つけた「logit が最大 1.6 違う」は、語彙 10 万個の中の最大値なので大きく見えただけだった。対処は不要。以前の 22.76 → 22.69 は別のテキストでの値なので、絶対値は比べられない（傾向は同じ: int8 の重みは原本よりわずかに良い）。
- [x] **T57 日本語モデルの既定のプロンプトを「これからの流行りは」にする。**（Fable）`src/models.js` の日本語の 4 モデル（llm-jp-3-150m、tiny-lm と、それぞれの原本）の `prompt` を「昔々、」から変えた。プレースホルダーと英語のモデルはそのまま。Chromium で 2 回ずつ生成して確かめた: llm-jp-3-150m は「これからの流行りは「インスタ映え」です。…」「これからの流行りは「スマートウォッチ」。…」と自然に続く。tiny-lm は文としてはつながるが内容は支離滅裂（これまでどおり）。テストは自前で「昔々、」を渡しているだけなので影響なし。追加の依頼で、残りの「昔々、」もすべて変えた: `tests/smoke.mjs` のプロンプト、トークナイザのテストの文と合成語彙（`昔`・`々`・` 昔々` → `流`・`行`・`り`・` 流行り`）、README の設定 JSON の例。この TODO の過去の記録（T20、T49 の却下理由）にある「昔々、」は、その時点の事実なので残してある。
- [x] **T56 既定でモデルを最大限に活用する。**（Fable）(1) 生成の長さの既定を 256 からコンテキストいっぱいに（`src/models.js` とローカルのモデルの既定を `steps: 0`。エンジンは 0 を `seq_len` として扱う）。設定のパネルは既定でスライダーが右端・数値表示（llm-jp は 4096、tiny-lm は 2048）、右端に戻せば「既定値と違う」の点は消える。(2) llm-jp-3-150m のコンテキストを 512 → 本来の 4096 に（持ち主が計測値を見て決定。原本の float16 も同じ長さ）。ブラウザの中での変換（T42）の既定も 4096。(3) その代償の KV キャッシュ（4096 で 200MB）は、256 位置ぶんで始めて届いたら倍にする方式にした（層ごとに伸ばすので、伸ばすときに全体の複製が要らない。NumPy の経路も同じ）。短い文章のヒープは 302MB のまま。ブラウザで停止トークンを切って最後まで書かせた実測: 4096 トークンを 90 秒、全体で 45 tok/s、ヒープ 302 → 362（位置 1024）→ 522MB（位置 2048 以降）、文章は最後まで破綻しない。位置ごとの速度（Node）は 8: 約 85、1000: 65、2000: 50、4070: 35 tok/s。普段は数百トークンで自分で止まる（確認した 2 回は 247 と 392 トークン）。int8 のファイルはサイズが変わらないので `MODEL_CACHE` を `models-v2` に上げた。`tests/e2e.mjs` は設定で 256 トークンに絞って測る（文書の数値と条件を揃えるため）。検証: pytest（伸びるキャッシュで logit が 1 ビットも変わらない）、`smoke.mjs`（キャッシュを 3 回伸ばしながらカーネルと NumPy が同じ文）、e2e 5 種。ほかに既定値での手加減は見つからなかった。
- [x] **T58 できるだけ多くの OS とブラウザで CI テストする。**（Fable）`.github/workflows/browsers.yml`（T44 の `webkit.yml` を広げた）: OS 5 種（Linux x86-64 / Linux ARM64 / Windows x86-64 / Windows ARM64 / macOS Apple M1）× ブラウザ 5 種（Playwright の Chromium・Firefox・WebKit、ランナーに入っている本物の Chrome・Edge。`tests/e2e.mjs` は `msedge`・`chrome` を受ける）= 23 通り（Linux ARM には Chrome と Edge が無い）。最初は 1 ブラウザ 1 ジョブの 23 ジョブにしたが、細かすぎるという持ち主の指摘で **OS ごとに 1 ジョブ（5 ジョブ）**にまとめ、ジョブの中でブラウザを順に回す形にした。手動（`gh workflow run browsers.yml`）か週 1 回、公開リポジトリなので無料。**結果: 23 通りすべてで 4 モデルが動いた。** 表は `kernels/README.md`。分かったこと: (1) WebKit はどの OS でも relaxed SIMD なしで `matmul_q8`。(2) **Firefox はどの OS・どちらのアーキテクチャでも Chromium 系の 8〜15 分の 1**（同じランナーで llm-jp 13 対 107 tok/s、準備完了まで 20 秒対 3〜5 秒）→ 候補 T59。(3) Windows ARM では Playwright の Chromium と WebKit が x86-64 のエミュレーションで、隣のネイティブの Chrome / Edge の 3 分の 1。(4) 1 回目の実行で本物の Edge が `favicon.ico` の 404 をエラーとして報告し、テストが失敗と判定した（モデルは動いていた）→ ページに data URI のアイコンを付けた。ランナーは共有の仮想マシンで CPU もまちまちなので、OS をまたいだ速度の比較には使えない。Safari そのものと iPhone の実機は未確認のまま（T45 で集める）。
- [x] **T59 Firefox が約 8〜15 倍遅い理由を調べる。**（Fable）**結論: 遅いのは Firefox ではなく、Playwright の動かし方だった。** (1) 手元の切り分け: Pyodide と無関係な素の wasm（SIMD の積和ループだけ、474 バイト）でも Playwright の Firefox は Chromium の 13 分の 1（0.43 対 5.7 G 積和/秒、スカラーは 0.07 対 1.2）。ベースラインコンパイラだけを指定しても同じ速度で、最適化コンパイラだけを指定すると「no WebAssembly compiler available」で落ちる。Playwright はデバッガ経由でページを動かし（スタックトレースに `debugger eval code`）、SpiderMonkey はデバッグ対象の wasm をベースラインだけでコンパイルする。`tests/profile.mjs firefox` の内訳も整合: カーネルの中が 13〜15 倍遅く、ctypes の呼び出し（1 回 24µs）は全体の 4%。(2) CI: `tests/stock-firefox.mjs`（Selenium + geckodriver。持ち主の希望どおり、ランナーに入っている Firefox を使う。5 つの OS すべてに Firefox 155 が入っていた）を `browsers.yml` の各 OS のジョブに足した。**同じランナーで llm-jp-3-150m が Playwright の Firefox 13 tok/s、インストール済みの Firefox 106 tok/s（Chromium 107）。** ほかの OS でも 65〜132 tok/s、準備完了まで 4〜6 秒。文書の「Firefox は遅い」という記述をすべて直した。以前の「開発機で 5〜7 倍遅い」も同じ原因。未確認: 人が普通に開いた Firefox が Selenium で開いた Firefox と同じ速さか（Marionette はデバッガを使わないので同じはず）。
- [x] **T60 Hugging Face から検証済みのモデルを取得して、変換して動かす（T39 を吸収）。**（Fable、持ち主の発案）コンボボックスを 3 つのグループ（このサイトのモデル / 量子化前の原本 / Hugging Face から取得してこのブラウザで変換）に分けた。3 つめを選ぶと、Worker が huggingface.co から `model.safetensors` を 8 MiB × 6 並列の Range 要求で取り、**ファイルの順に**変換器へ渡す。変換器は届いたテンソルから int8 にして、最終サイズのバッファの正しい位置へ書く（`llama2_convert.Stream`。T41 の「出力の順に読む」方式は、ここから 1 回 1〜3 秒かかる要求が数百回になるのでやめた。ローカルのファイルも同じ経路）。変換結果は専用のキャッシュ（`converted-v1`）に入り、2 回目は取得も変換もしない。項目名に「kept in this browser」が付き、About の一覧から削除できる。500MB を超える取得は先に `confirm()`。指示モデルは入力を `template` にはめて渡し（1 ターンだけ）、答えにプロンプトを含めない（`generate(echo=False)`）。テンプレートの中の特殊トークン（TinyLlama の `</s>`）はエンジンの `specials` で 1 つのトークンとして読む（`transformers` と 6 / 6 で一致）。一覧に無いものは URL で: `?hf=<owner>/<repo>`（変換器が動かないものを文で断る）、`?checkpoint=<url>&tokenizer=<url>`（llama2.c 形式。T39 の内容）。**実測: この開発機の Chromium で llm-jp-3-150m-instruct3 は取得と変換 38 秒・ヒープ 353MB・81 tok/s、2 回目は 8 秒で準備完了（huggingface.co への要求 0 件、同じシードで同じ答え）。440M instruct3 は 0.9GB を 97 秒・ヒープ 686MB・29 tok/s で、答えの質は 150M よりはっきり良い。CI（`browsers.yml` の `huggingface` ジョブ、Linux の Chromium）で 6 つとも成功: 980M instruct3 は選んでから 53 秒で準備完了・15 tok/s、TinyLlama 1.1B Chat は 38 秒・12 tok/s。** 見積もり（980M 約 10、TinyLlama 約 10、440M 20〜25 tok/s）は、ランナーの CPU が速いぶん上に外れた。分かったこと: 150M の指示モデルは質問を断りがち（「お答えできません」）。ヘッドレスの Chromium は Cache API の割り当てが小さく、503MB の保存が `Quota exceeded` になった（保存をあきらめて理由を表示し、モデルは動く）。未確認: 普通のブラウザでの保存の上限、Firefox / WebKit での HF の経路、スマホ。
- [x] **T39 URL で指定した llama2.c 形式のモデルを使う。** T60 に吸収して実装した（`?checkpoint=<url>&tokenizer=<url>`。Range 要求を並列に出して順番どおりに書く。`dtype` の判定とトークナイザの検査は T38 のまま）。`node tests/e2e.mjs url` が huggingface.co の `karpathy/tinyllamas` の stories260K を読んで、期待どおりの出力になることを確かめる。
- [x] **T62 プロジェクト名を `pyodide-llm` に変える。**（Fable、2026-09-20。持ち主の発案と判断）リポジトリは `takano32/pyodide-llm`、サイトは https://takano32.github.io/pyodide-llm/ 、ページの題は「Pyodide LLM」。理由: 目的はもともと「言語モデルを動かす実験」で Llama に限らない。`-py` は llama2.c → llama2.py という出発点の名残だった。時期は「Llama 以外が動いてから」という Fable の案ではなく、**公開中のサービスなので早いほど損害が小さい**という持ち主の判断で、すぐにやった。やったこと: リポジトリの中の名前（Astro の base、リンク、テストの URL、`browsers.yml` の既定の URL、README、AGENTS.md、`package.json`）を直してコミット → `gh repo rename` → push してデプロイ → 新しい URL で e2e（stories260K と llm-jp-3-150m）が通ることを確認。GitHub Pages は旧 URL を転送しないので、**旧名 2 つ（`pyodide-llama-py`、`pyodide-llama2-py`）に転送用の小さなリポジトリを作った**（`index.html` と `404.html` が、パス・クエリ・ハッシュを引き継いで新しい URL へ飛ばす。実ブラウザで `?model=stories260K` 付きと深いパスで確認）。その代わり、旧名での `git clone` は新しいリポジトリへ転送されなくなった（転送用のリポジトリの README に移転先を書いた）。gist の報告書のリンクも直した。利用者のモデルのキャッシュはパスが変わったので再ダウンロードになる。エンジンのファイル名 `llama2_numpy.py` はそのまま（Llama 構造のエンジンである間は）。ローカルのディレクトリは `~/GitHub/pyodide-llm` に移し、旧名はシンボリックリンクで残した。はまった点: `gh repo rename` は origin を自動で新しい SSH の URL に直してくれるので、自分で `set-url` してはいけない（https に書き換えてしまい、認証で push に失敗した）。
- [x] **T66 [文書] gist を「Pyodide LLM の概要」にする。**（Fable、2026-09-20、持ち主の発案）着手時の調査レポートだった公開 gist（https://gist.github.com/takano32/196c6f93979ad44f98cee5712fdd3901 ）を、プロジェクト全体のまとめにした。題（description）を「Pyodide LLM の概要」にし、概要のファイル（いまは `10-overview.md`）に、目的・到達点（0.26 → 351 tok/s、llm-jp-3-150m が 75〜79 tok/s）・しくみ（ctypes + `dylink.0` のカーネル、int8、1 トークンの内訳）・HF のモデルのブラウザ内変換・23 通りのブラウザ・分かったこと・これから、を日本語で書いた。数値はリポジトリの文書にあるものだけ。元のレポートは同じ gist の別のファイル（いまは `90-survey-2026-09.md`）として残した（URL は変わらない。gist はファイル名の順に並ぶので、概要が先に出る）。大きな成果が出たら概要を更新する。T48（ctypes + `dylink.0` の解説と最小サンプル）は、この概要の「しくみ 1」を掘り下げる続編という位置づけになる。同じ日に、持ち主の案で目次を付けた: gist はファイル名の順に並ぶので `00-pyodide-llm.md`（要約 + 目次。最初の画面がその gist の顔になるので、目次だけにはしなかった）、`10-overview.md`、`90-survey-2026-09.md` と番号を付け、目次からは gist が自動で付けるアンカー（`#file-10-overview-md`、見出しは `#しくみ` など）で飛ぶ。公開後のページを取得して、目次のリンク 10 個すべてに飛び先があることを確かめた。ファイルを足したら `00-` の目次を直す（T48 は `20-how-it-works.md` の予定）。ページの About にも「Pyodide LLM の概要: しくみと計測結果のまとめ」としてリンクを置いた（リンクの文言は飛び先の題と同じにした）。はまった点: `gh gist edit --desc` はファイルが複数あると「どのファイルか」を聞いて失敗するので、説明の変更は `gh api -X PATCH gists/<id>` で行った。
- [x] **T67 [描画] 既定を日本語のいちばん軽いモデルにし、グループの中を言語 → 軽い順に並べ、イントロを充実させる。**（Fable、2026-09-20、持ち主の依頼）(1) 既定は tiny-lm（33MB）。公開サイトで無確認の 171MB は重く、準備完了までが短く、速さがこのプロジェクトの見出し。AGENTS.md の方針 4 を書き換えた（既定は llm-jp → tiny-lm → llm-jp → tiny-lm と変わってきた。質か軽さかの判断で、いまは軽さ）。(2) 取得元ごとの 3 グループはそのままで、中を「日本語の軽い → 重い、英語の軽い → 重い」に: tiny-lm → llm-jp-3 150M → TinyStories 260K → 3.5M → 15M → 42M、原本も同じ順、HF のグループは元からこの順。(3) 補う 3 点（持ち主が決定）: (a) イントロの文中の「llm-jp-3 150M（171 MB）」がボタンで、押すとそのモデルに切り替わる。(b) 選んだモデルを `localStorage` に覚え、次回はそこから始まる（`?model=` などの URL の指定が優先。初めての訪問は必ず tiny-lm。HF から 500MB 超を取り直すことになるモデルは、前回キャッシュに保存できていなければ無確認では始めない。About で保存を消したらその印も消す）。(c) イントロにしくみの説明（Python が層を順に呼び、ctypes で読み込んだ数 KB の WASM SIMD カーネルが計算する）、HF のグループの説明、gist「Pyodide LLM の概要」へのリンクを入れた。最初の文面は 390px の画面の 49% を占めたので削り、42% にした（0.26 tok/s の話は gist に任せた）。モデルごとの tok/s はイントロに書いていない（既定が変わると古くなる）。実ブラウザで確認: 初回は tiny-lm、コンボボックスの並び、イントロのボタンで llm-jp に切り替わる、開き直すと llm-jp から、`?model=` が優先、別のプロファイルでは tiny-lm、390px でスクロールなし。はまった点: 文中のインラインのボタンの前後の空白が詰まる（英語の文で単語がくっついた）ので、英語のボタンには CSS で左右の余白を付けた。同じ日の直し（持ち主の指摘）: 最初の文面は「Python が層を順に呼び」「llm-jp-3 150M が書きます」と、日常的でない言い回しだった → 「処理は Python で書き、時間のかかる計算だけを、ctypes で読み込んだ小さな WASM SIMD カーネルに任せています」「もっと自然な日本語を見たいときは、llm-jp-3 150M（171 MB）に切り替えてください」に直した（ボタンを押すと何が起きるかも伝わる）。自然な文にすると長くなり、日英を並べると 390px の画面の 47% を占めたので、**ブラウザの言語が日本語なら日本語だけ、それ以外は英語だけ**を出すようにした（32% になった。スクリプトが動かなければ両方出る）。訪問者向けの文は、書いたら声に出して読める日本語かを確かめる。さらに持ち主が文面を直した: 「処理は Python で書かれており」「（小さな、は不要）」「既定のモデルはいちばん軽いモデルです」「もっと自然な日本語は llm-jp-3 150M（171 MB）で生成できます」。読点（、）が多すぎるという指摘も受けて減らした（1 文に 1 つまでを目安にする）。いまの文面はこれ。
- [x] **T41 ストリーム変換器。**（Fable）`public/llama2_convert.py`: safetensors を `read(offset, length)` 越しに 100 万値（float32 で 4MB）ずつ読み、最初から最終サイズで確保した出力バッファの正しい位置へ float32 / float16 / int8 で直接書く（int8 は「全層の値 → 全層のスケール」という並びなので、順に追記するのではなく位置を計算して書く）。q・k の並べ替えだけは行列 1 個を丸ごと読む。`config.json` の検証（Llama 以外、RoPE scaling、bias、silu 以外、ヘッドの割り切れなさをトレースバックなしの文で断る）、`tokenizer.json`（Unigram）と sentencepiece のモデルから `tokenizer.bin` と `tokenizer_kind`・`nfkc` を求める関数も同じファイルにある。`convert_hf.py` と `quantize.py` はこのモジュールを使う形に書き直した（PyTorch の pickle を読む部分だけが `convert_hf.py` に残る）。検証: 新しいコードで llm-jp-3-150m と tiny-lm を float32・float16・int8 に変換した 6 個とトークナイザ 2 個が、書き換え前の `make models` の出力と **すべてバイト単位で一致**。メモリのピークは llm-jp-3-150m で「出力 + 13MB」（int8 なら 171 + 13MB、1.7 秒。断片を 4 倍にすると + 52MB で速度は同じ）。Makefile は HF の 2 モデルを int8 へ直接変換するようにした（609MB と 117MB の float32 の中間ファイルが要らなくなった。`make models` をやり直して全出力のチェックサムが一致）。pytest に合成 safetensors での一致（float32 は元のチェックポイントと、int8 は `quantize.py` の出力と一致。F32 / F16 / BF16）、断片読みの確認、対応外の断り方を追加（117 件）。テストが見つけたバグ: 最後のテンソルが RoPE の表のとき進捗が 100% にならなかった。
- [x] **T42 ブラウザの中で Hugging Face 形式を変換して動かす。**（Fable）フォルダのボタン / ドロップで `.safetensors` 1 個 + `config.json` + トークナイザ（`tokenizer.json` か、sentencepiece の `tokenizer.model`・`spiece.model`）を選ぶと、Worker が T41 の変換器（`llama2_convert.py`。初めて要るときに取得）を Pyodide で動かす。読み手は `FileReaderSync` + `File.slice()`（Worker なら同期で読める）で、ファイル全体は読まない。変換は Python のジェネレータで、Worker が断片の合間に進捗（`converting, 42 %`）を出し、50ms ごとにイベントループへ返して、モデルの選び直しによる中止（T37）を受け付ける。既定は int8・コンテキスト 512（KV キャッシュを抑える）。任意の設定 JSON で `conversion: {dtype, max_seq_len}`・`options`・`generation` を変えられる。`tokenizer_kind`・`nfkc`・`rope_theta`・BOS / 停止トークンは `tokenizer.json` / sentencepiece のモデルと `config.json` から求める。**実測（Chromium、この開発機）: llm-jp-3-150m（bfloat16 の safetensors 305MB）を 6.9 秒で int8 に変換、ヒープ 294MB（配布版を普通に読んだときの約 283MB と同水準）、その後 70 tok/s。同じシードで、サイトがビルド時に変換した llm-jp-3-150m と一字一句同じ文を書いた。** 配布版へ切り替えてもヒープは 294MB のまま（リークなし。PyProxy を 1 つでも壊し忘れるとチェックポイントが残るので、すべて `destroy()` している）。断り方も実ブラウザで確認: Llama 以外（`it is a gpt2`）、トークナイザなし / シャード分割、語彙の 9 割に満たないトークナイザ（別モデルのもの。変換は通ってしまい、でたらめを書くので断る）、safetensors でないファイル。変換の 20% でモデルを選び直すと中止され、エラーは出ず、tiny-lm が動く（ヒープ 245MB）。`node tests/e2e.mjs hf` は `tests/make_hf_fixture.py` が stories260K から作る HF 形式のファイル（safetensors + sentencepiece の protobuf）を開き、変換後の出力が元のモデルと同じ書き出しになることを確かめる。未確認: Firefox、1GB 級のモデル、BPE の `tokenizer.json`（非対応と表示する。Llama 3 系の byte-level BPE はエンジンが未対応）。
- [x] **T68 [計測] 準備完了の内訳の「ダウンロード」を、実際に取り終えた時刻で測る。**（Opus 5、2026-09-20、持ち主の指摘）「ダウンロードが Pyodide の完了を待っているように見える」という指摘から測ったところ、本番サイトの Chromium でモデル部品の最初の要求は 1.87 秒・`pyodide.mjs` は 3.17 秒で、tiny-lm は 3.2 秒で 100%、準備完了は 10.2 秒だった。**同時に進んではいたが、内訳の表示が誤解を招いていた**（「Pyodide 9.83 秒 · ダウンロード 9.90 秒（同時に進行）」）。原因は `loadSeconds.download` を `checkpoint.into()` の後で測っていたこと。`into()` は Python のバッファができるまで、つまり Pyodide の完了まで待つので、待ち時間がダウンロードの数字に入っていた。直し: 取得元（`download()`・`readFile()`・`readUrl()`）が自分の秒数を持ち、`download()` は最後のバイトが届いた時刻、ファイルと URL は読み始めてからの時間を返す。「（同時に進行）」は、Pyodide の完了より前に始まったダウンロードにだけ付ける（2 つめ以降のモデルには付かない。`overlapped` を `ready` に載せた）。確認（この開発機の Chromium、ローカルのビルド）: 進捗が 100% に達したのが 0.49 秒で内訳は「Pyodide 7.51 秒 · ダウンロード 0.28 秒（同時に進行）」、2 つめのモデルは「ダウンロード 0.23 秒」で注記なし。`local`・`url`・`hf` の経路と pytest・スモークテストも通る。
- [x] **T69 [性能] NumPy の wheel を先読みする。**（Opus 5、2026-09-20、持ち主との相談）Pyodide は起動してから `loadPackage("numpy")` を呼ぶので、wheel の要求が数秒遅れて出ていた（T68 の計測で、本番では 8.79 秒）。版を解決した直後に `pyodide-lock.json` を自分でも取り、`packages.numpy.file_name` から URL を組んで先に取っておく（`prefetchNumpy()`）。Pyodide も lock を取るので無駄がなく、**手で書くバージョンの定数も増えない**（方針 2 のまま）。新旧を交互に 3 回ずつ（この開発機の Chromium、ローカルのビルド）: wheel の要求 4.9〜7.7 秒 → 0.61 秒、Pyodide 6.8〜9.4 秒（平均 7.9）→ 6.1〜6.3 秒（平均 6.2）、準備完了 7.4〜10.2 秒 → 6.7〜6.9 秒。実行ごとのぶれも小さくなった。失敗しても害がない作り（`try/catch`、失敗は先読みなしに戻るだけ）。先読みの最中に `loadPackage` が走ると同じ wheel を 2 回取るので、`loadPyodide()` の後で先読みの完了を待つ。はまった点（持ち主のマシンを落とした）: 新旧を比べるために `dist` を 2 つ `/tmp` にコピーしたが、この機械の `/tmp` は tmpfs なので 839MB × 2 がメモリに載り、空きが 466MB になった。比較は `dist/worker.js` だけを差し替えて交互に走らせればよい（AGENTS.md の落とし穴に追記）。
- [x] **T63 [追加] byte-level BPE のトークナイザ（→ SmolLM2 が動く）。**（Opus 5、2026-09-21）着手前の判定どおり、**`tokenizer.bin` の形式は変えずに済んだ**: スコアに −順位を書くと、エンジンのいまの BPE のループが HF の byte-level BPE と同じ結果になる。実装は (a) エンジンに `kind="bytebpe"`（GPT-2 のバイト ↔ 文字の表、前分割、復号。`decode()` はもともとバイト列を返すので既存の流れに乗った）、(b) 前分割 `pretokenize()` を `unicodedata` で手書き（`\p{L}`・`\p{N}` は標準の `re` に無い。`gpt2`・`gpt2-digits`・`qwen` の 3 つだけ）、(c) 変換器が BPE の `tokenizer.json` を読む（`tokenizer_json_bpe_pieces()`。知らない前分割・`add_prefix_space`・数字のまとめ方は断る）、(d) `src/models.js` に SmolLM2 135M Instruct（ChatML のテンプレート、`<|im_start|>` と `<|im_end|>` は `specials`）。確認: pytest が本物の `tokenizers` でその場で小さな語彙を学習させ、3 つの前分割 × 23 の文字列で ID の列と全語片の復号を突き合わせる（96 件、CI でも走るよう `deploy.yml` に `tokenizers` と `regex` を足した）。本物の語彙でも一致（SmolLM2-135M-Instruct と Qwen2.5-0.5B-Instruct、66 / 66。復号で違うのは特殊トークンだけで、本物が `skip_special_tokens` で空にするため）。前分割そのものも本物のパターンと 2027 / 2027 一致。**ブラウザ**: SmolLM2 135M Instruct を HF から 269MB 取得して変換し、準備完了 39.2 秒・47.2 tok/s、英語の指示にまともに答えた（Chromium、この開発機）。T64 の Qwen2.5 もトークナイザはこれで読める。
- [x] **T64 [追加] 設定で切り替わる小さな差分（bias など → Qwen2.5 が動く）。**（Opus 5、2026-09-21）Qwen2 は q・k・v に bias があるだけの Llama。**形式 v2 は要らなかった**: bias の 3 本のベクトルを層ごとにファイルの末尾へ足し、ヘッダの 7 個の int はそのまま。bias の有無は変換器が `self_attn.q_proj.bias` の有無で判断して `options` に載せ、engine は `Llama(bias=True)` で受ける（トークナイザの種類と同じ扱い）。計算は既存の `add_inplace` のカーネルで足りるので**新しいカーネルは 0**。はまった点: **bias も wq・wk と同じ並べ替えが要る**（HF は rotate_half の並び。`permute_heads` を 1 次元でも使えるようにした）。確認: 合成した Qwen2 を変換して Python のループの参照実装と一致（3 通りの構成）、bias の無いファイルが 1 バイトも変わらないことをテストで固定、pytest 237 件。**ブラウザ**: Qwen2.5 0.5B Instruct を HF から 0.9GB 取得して変換し、準備完了 108.5 秒・18.7 tok/s、日本語で答えた（Chromium、この開発機）。足した Python は engine 約 15 行・変換器 約 25 行で、歯止め (a)(b)(c) をすべて満たす。`use_sliding_window` が真のモデルは断るようにした。
- [x] **T65 [追加] 別系統の forward（GPT-2 系）。**（Opus 5、2026-09-21、持ち主の判断で歯止め (b) を「パラメータ化なら可」と読み替えて着手）**forward は 1 本のまま**で、`Llama(arch="gpt2")` が LayerNorm・GELU・学習済みの位置・全結合の bias に切り替える。FFN はゲートの無い 2 本。新しいカーネルは **layernorm と gelu の 2 つ**（`simdkernel.so` 5682 → 6307 バイト。GELU の tanh は既存の `fexp` から作れるのでテーブルは要らない）。足した行数は engine +123 / 変換器 +161 / カーネル +35。**速度は落ちていない**（新旧を交互に 3 回ずつ、Node 上の Pyodide: tiny-lm int8 380〜454 対 228〜395、stories15M float32 193〜211 対 199〜212 tok/s）。確認: 合成した GPT-2 を変換して **transformers の計算をそのまま書いた参照と一致**（分類器が共有・非共有の両方）、ファイル順のストリーム変換も同じ結果、カーネル版が Pyodide 上で NumPy 版と一致（`smoke.mjs`）、pytest 242 件。**ブラウザ**: rinna/japanese-gpt2-small が 454MB を取得して 53.3 秒・66.9 tok/s で日本語、openai-community/gpt2 が 548MB で 53.8 秒・60.6 tok/s で英語（Chromium、この開発機）。はまった点（どれも HF 側の都合）: Conv1D で行列が転置されている、q・k・v が `c_attn` 1 つにまとまっている（変換器が 1 つのテンソルから複数の出力を書けるようにした）、テンソル名の `transformer.` が付くモデルと付かないモデルがある、いちばん古い `tokenizer.json` には `model.type` が無い、ブラウザの経路で `config.json` の正規化を忘れていた。**ついでに T64 のバグを 1 つ直した**: ストリーム変換で 1 次元のテンソルを丸ごと変換するとき読む量を間違えていた（Qwen を HF から取ると通る経路。テストも足した）。
- [x] **T72 [追加] GPT-NeoX 系（→ Pythia の梯子と日本語の rinna が動く）。**（Opus 5、2026-09-21）調査どおり T65 の作りにそのまま乗り、**新しいカーネルは 0**（`rope` に「回す本数」の引数を足しただけ）。実装は (a) 部分 RoPE、(b) 並列残差（両方の形が実在する）、(c) 変換器が `query_key_value` を head ごとに割る、(d) `src/models.js` に Pythia 160M と rinna/japanese-gpt-neox-small。確認: 合成した GPT-NeoX を変換して **transformers の計算をそのまま書いた参照と一致**（`rotary_pct` 0.25 / 0.5 / 1.0 × 並列残差あり / なし）、ファイル順のストリーム変換も同じ結果、カーネル版が Pyodide 上で NumPy 版と一致（`smoke.mjs` に 2 通り追加）、pytest 248 件。**ブラウザ**: Pythia 160M は 375MB を取得して 55.6 秒・67.5 tok/s、rinna は 663MB で 74.3 秒・71.3 tok/s、どちらもまともな文章（Chromium、この開発機）。**はまった点**: 回転の並べ替えを v にもかけていた（q と k だけ）、Llama のときに未定義の変数を参照していた、そして **変換器が `rotary` と `parallel_residual` を渡していなかった**（テストは `Llama()` を直接呼ぶので素通りし、ブラウザで「動くが文章が壊れる」として出た。変換器からの経路を見るテストを足した）。残り: Pythia のほかのサイズ（14M・70M・410M・1B）は一覧に入れていない。T45 の梯子の材料にするときに足す。
- [x] **T73 [追加] `chat_template` を読んで指示モデルの書式を自動で決める。**（Opus 5、2026-09-21）**Jinja の一部を自分で書いた**（案 (1)。外部依存は増やさない）。判定のとおり、if を真面目に実装したら `tools` も `is defined` も「偽として飛ばす」で済んだ。**集めた 26 件のうち 23 件を読め、読めたものは本物の Jinja と 1 文字も違わない**。`Conversion` が `tokenizer_config.json` を読んで `options["template"]` に入れ、Worker がそれをページに渡す（エンジンには渡さない。`Llama()` は知らない引数を受けない）。**ページ側の仕組みは変えていない**: 1 ターンぶんだけ描画して `{prompt}` の形に戻すので、`src/models.js` の `template` と同じものになる（あればそちらが優先）。確認: pytest 255 件（本物の 3 つの書式の期待値を直書き。CI に依存を増やさない）、**一覧に無いモデルを `?hf=` で開いて指示に答えることを実ブラウザで確認**（プロンプト 4 → 63 トークン）。**はまった点**: 文字列の中の `|` と `}}` の誤認、`elif` が 2 つ以上で分岐の範囲がずれる、外側の括弧の判定、Jinja が末尾の改行を 1 つ落とすこと。そして**ページの 1 行だけ編集が空振りしていた**のに気づかず、ブラウザで「テンプレートが効かない」形で出た（字下げ違いの置換。以後は置換が当たったか必ず確かめる）。
- [x] **T52 [計測] 最適化を個別に切るスイッチ。**（Opus 5、2026-09-21）`?without=kernels,int8,relaxed,sampler`（`?kernel=off` は今までどおり効く）。エンジンは `Llama(disable=…)` で受け、**すでにフォールバックがあるものだけ**をスイッチにした（新しい代替実装は足していない）。知らない名前は無視せず `ValueError`（打ち間違いで違う条件を測らないため）。ステータス行は動いたものを必ず言い、外したときは末尾に「(without …)」が付く。**分解の結果**（この開発機、Node 上の Pyodide、64 トークン、int8、同じ seed）: tiny-lm 48.6 →（カーネル）187.1 →（int8 のまま）248.1 →（relaxed SIMD）315.3 →（サンプリングもカーネル）415.6 tok/s、llm-jp-3-150m 9.6 → 37.9 → 65.4 → 84.3 → 92.8 tok/s。ブラウザ（Chromium）の tiny-lm でも 44.8 → 82.5 → 175.8 → 193.0 → 334.6 tok/s。`kernels/README.md` に表を載せた。確認: pytest 8 件（全スイッチで参照と一致、backend の文言、知らない名前の拒否）、`smoke.mjs` で float32 の出力がどのスイッチでも変わらないこと、実ブラウザで 5 通り。スイッチを何も付けなければ今までと同じ（242.8 tok/s、backend に「without」は出ない）。**任意の項目だったスカラー版のカーネルは作っていない**（バイナリとテストが増えるため。必要になったら別タスクで）。
- [x] **T43 [計測] 生成設定をクエリパラメータで指定する。**（Opus 5、2026-09-21。持ち主の「簡単そうならやっていい」で T45 から切り離して実施）`?temperature=&topp=&steps=&repetition_penalty=&seed=` を読んで設定の状態に入れる（20 行）。**数値として読めないもの・範囲外は無視**し、リンクで指定した値はモデルを切り替えても残る（リンクが「何で走らせるか」を言っているため。seed と同じ扱い）。既定値から変わっているので設定のアイコンに印が付く。確認（実ブラウザ、Chromium）: `?model=tiny-lm&temperature=0.7&seed=1234&steps=24&topp=0.5&repetition_penalty=1.4` で 5 つの欄が全部その値になり、**同じリンクを 2 回開くと同じ文章**（回答に `seed 1234` が出る）。`temperature=abc`・`steps=-5`・`topp=9`・`seed=99999999999` は無視して既定のまま。T45 は「その設定つきのリンクをコピーする」側を受け持つ。
- [x] **T80 [追加] いまの実装でそのまま足せるモデルを一覧に入れる。**（Opus 5、2026-09-21）調査で条件を満たしたのは 299 件で、そこから 8 つを選んで足した: **Pythia 70M・410M・1B・1.4B**（160M と合わせて**同じ設計の 5 段の梯子**になり、T45 の「大きさと速さ」の表がそのまま作れる）、**Qwen2.5 1.5B Instruct**（小さい指示モデルでダウンロード最多）、**DeepSeek-R1 Distill Qwen 1.5B**（考えてから答える型）、**SmolLM2 360M Instruct**、**Qwen2.5 Coder 0.5B Instruct**。足す前に safetensors のヘッダの dtype を確認した（量子化済みの再配布は読めない。Pythia の U8 は使わないテンソルなので問題ない）。HF のグループは 21 モデルになり、**日本語の軽い → 重い、英語の軽い → 重い**に並べ直した。**CI のジョブを大きさで 2 つに分けた**（1GB 未満の 13 個と 1GB 以上の 8 個。合わせて 20GB を超える取得になり、90 分の 1 ジョブには入らない）。確認: pytest 263 件、ローカルのブラウザで既定のモデルが 307.2 tok/s。**新しい 8 つの確認は CI の `huggingface` ジョブ**（この開発機のメモリでは足りない）。
- [x] **T77 [運用] Opus が実装した範囲を Fable が見直す。**（Fable 5.1、2026-09-24）`llama2_numpy.py`・`llama2_convert.py`・`kernel.ts`・`worker.js`・テストを通読した。**直したもの（コミットは理由ごとに 3 つ）**: (1) `checkpoint_dtype()` が `arch` を知らず、**フォルダのボタンで開いたローカルの Qwen2 / GPT-2 / NeoX のファイルが大きさの判定で拒否されていた**（`layout()` と engine には足したのに 3 か所目を忘れた。Worker の `localOptions` も設定 JSON の `bias` / `arch` を渡すようにし、全アーキテクチャ × 3 dtype で `checkpoint_size()` と一致するテストを足した）。(2) `transformed()` が知らない transform 名を黙って `row` として扱っていた → 名前で拒否。(3) ベンチの後に `disabled` を Worker 自身の URL から戻していて、ページの `?without=` が消えていた → ページの指定を覚えて戻す。**問題なしと判断したもの**: 速度に効く場所の分岐（層ごとに Python の `if` 4 つ、測れない）、transform の抽象（6 種で足りている。GGUF は transform ではなく別の source として入れる）、テスト（3 アーキテクチャとも参照との一致・ストリーム変換・options の経路の 3 本。gpt2 と qwen2 の options の経路は無かったので足した）。**記録だけしたもの**: 「出力 + 13MB」は GPT-2 / NeoX では融合テンソル 1 つぶん多い（AGENTS の表を直した）、RMSNorm の eps が全モデル 1e-5 固定（Qwen2.5 は 1e-6。差は出ない見込みで未計測）。pytest 272 件。**これで T74 のブロッカーは外れた。** (5) の確認で**台帳の壊れを見落とした**: T75 と T76 の項が T73 の項を消したときに巻き込まれて消えており、T45・T48・T78・T79 は完了なのに「これからのタスク」に残っていた（同日中に直した）。
- [x] **T48 [文書] ctypes + `dylink.0` の手法を単体の記事と最小サンプルにする。**（2026-09-21。Opus のサブエージェントが書き、Opus が読んで、持ち主の許可で公開）gist に `20-how-it-works.md`（182 行）を足し、`00-pyodide-llm.md` の目次を直した。最小の再現例（AssemblyScript 22 行、`dylink.0` を前置するビルド 20 行、素の Pyodide + ctypes + NumPy の実行スクリプト）は**このリポジトリの外で動くことを確かめた**（Node 24、Pyodide 314.0.7、`minimal.so` は 207 バイト。`dylink.0` を付けないと `need the dylink section to be first` で落ちることも確認）。記事の数値はすべて AGENTS.md と `kernels/README.md` からの写しで、新しく測ったのは `minimal.so` の大きさだけ。
- [x] **T78 [追加] 1 ファイルだけの分割形式（`model.safetensors.index.json`）を読む。**（Opus 5、2026-09-21）`worker.js` に純粋な関数 `singleShard(index)` を置き、HF の経路で weights が 404 なら index を取って `weight_map` の値が 1 種類ならそのファイル名に差し替える。2 ファイル以上は「この形式は分割が 2 ファイル以上で、いまは読めない」と断る。フォルダのボタンの経路は元から「`.safetensors` が 1 つ」で選ぶので、そのままで通る。**実ブラウザでの確認は CI 待ち**（この開発機ではブラウザを動かさない。Playwright の `page.route()` で huggingface.co を偽装する手も試したが、Worker からの要求では `Content-Range` が消えて範囲取得が動かない。落とし穴に記録）。
- [x] **T79 [追加] 日本語の 0.5B（sarashina2.2）を一覧に入れる。**（Opus 5、2026-09-21）`sbintuitions/sarashina2.2-0.5b` と `-instruct-v0.1`（T78 の形式）を `src/models.js` に足した（どちらも取得 1.59GB → int8 約 0.6GB、desktop only）。`browsers.yml` の `huggingface` ジョブにも足した。**実ブラウザでの確認は CI 待ち**（1.6GB はこの開発機では試さない）。
- [x] **T45 [計測] ベンチマークモード。**（Opus 5、2026-09-21）`src/bench.js`（素の ES モジュール。測る組み合わせ `ROUNDS` = カーネルあり / なし、`FULL_ROUNDS` = T52 の 5 段、`benchMarkdown()` と `environmentOf()`）、Worker の `{type: "bench"}`（組み合わせごとにモデルを作り直し、同じプロンプトを greedy で 64 トークン生成して測る。各ラウンドはモデルの切り替えと同じ扱いで中止と後始末をする）、ページの `?bench=1` / `?bench=full`（結果はコンソールと `window.__bench`。画面に出すのは T76）。確認: `tests/bench.mjs`（Node だけ、通過。表の形、値が無いときの「?」、`NaN` や `undefined` を出さない）。**実ブラウザの確認は `tests/bench-browser.mjs` を `browsers.yml` に足して CI で行う**（着手中に 2 度この機械のメモリを使い切った。以後ブラウザは CI のみ）。はまった点: `parameters` より前で `benchmark` を読んでいた（宣言の順番）、ベンチのラウンドに中止用の `signal` を作っていなかった。
- [x] **T82 [運用] CI の結果を読めるようにする。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。`tests/e2e.mjs` に **1 回ごとの持ち時間**（`E2E_TIMEOUT`、超えたら timed out と記録して exit 2。待ちは全部この持ち時間に任せ、個別の 30 分や 10 分の待ちは外した）、**失敗時の保存**（`E2E_ARTIFACTS` にスクリーンショット・DOM・コンソールの最後の 200 行。止まったページが応えなくても 15 秒で諦めて先へ進む）、**JSON の結果**（`E2E_RESULTS` に 1 行追記。ブラウザと版・OS・準備完了の秒数・tok/s・backend・失敗の理由）。投げられた例外（`page.goto` の失敗など）も失敗として記録する（ESM のトップレベルの `await` の失敗は `uncaughtException` で拾えることを確かめた）。`tests/summary.mjs` が JSON 行を 1 枚の表にし、`browsers.yml` の両ジョブで `if: always()` の表と artifact（14 日）を足した。持ち時間は OS のジョブ 600 秒、HF は 900 秒と 1800 秒。ついでに OS のジョブに紛れていた `?bench=1` のステップ（`matrix.size` が無いので決して走らない）を消した。デプロイの試験に `node tests/bench.mjs` と `node tests/summary-check.mjs` を足した。**確認できたこと**: 表の単体テスト（壊れた行は飛ばす、`|` を含むセル、`NaN` や `undefined` を出さない）、`node --check`、ワークフローの YAML。**まだ確かめていないこと**: 実際の CI での動き（この開発機ではブラウザを動かさない）。持ち主が `browsers.yml` を走らせたときに、表と artifact が出ること・T70 の WebKit が 600 秒で打ち切られて先へ進むことを見る。stock Firefox（`tests/stock-firefox.mjs`）は JSON を書かないので表に載らない（必要なら同じ 3 つの変数を足す）。 **レビュー（Fable、2026-09-25、サブエージェントに今日の 5 回の走行の実物を読ませて Fable が判定）: 1 つ直して OK。** 確かめたこと: 7 ジョブ全部に `results.jsonl` が残り表になる（失敗の行は理由つき、`|` も壊れない、T84 の `load`・`heapMB`、T93 の `isolated` も入る）。失敗したジョブの artifact に png・html・log の 3 点が揃い、log の先頭 2 行とコンソールで原因を絞れる（Firefox の「Error in input stream」が「Loaded numpy」の直後に切れたことまで読める）。投げられた例外も JSON と artifact に残る。再読み込み（T93 の段階 3）は最新の走行で 5 ブラウザ × 4 モデルとも通った。**直したもの**: 持ち時間（watchdog）がブラウザの起動（`launch`・`newPage`）の後に張られていて、起動で止まる実行（T70 の Windows の WebKit がこの形と見られる。止めた走行 36042479744 で webkit の後 40 分沈黙し、timed out の行が出なかった）には効かなかった → 起動の前に張り、ページが無いときは保存を飛ばす。あわせて、終了の直前の 1 行が `| tee` のパイプで失われうるので、書き終えてから終了する形に。表の下に「インストール済みの Firefox は数えていない」の 1 行を足した。あれば良い（やらない）: 同じ組が 2 回失敗すると artifact の名前が重なる（いまの並びでは起きない）。
- [x] **T87 [文書] 一覧の全モデルのライセンスと出典をページに出す。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-24）**。`src/models.js` に `LICENSES`（HF のリポジトリ → ライセンス名）と、サイトのモデルの `source`（Makefile の取得元と同じ）を足した。HF のモデルは `hf.repo` が出典。About の一覧は `sources()` から**ビルド時に生成**（出典ごとに 1 行、ライセンス名、そこから取ったモデルの名前）。ライセンスのリンクは HF のモデルカード、tiny-lm だけは同梱の `models/tiny-lm.LICENSE.txt`。**ライセンス名はすべて HF のモデルカードの `license:` から写した**（Apache-2.0: llm-jp の 5 つ・Qwen2.5 の 3 つ・Pythia の 5 つ・SmolLM2 の 2 つ・TinyLlama。MIT: tiny-lm・tinyllamas の 2 つ・rinna の 2 つ・sarashina2.2 の 2 つ・GPT-2・DeepSeek-R1 Distill）。`tests/models-check.mjs`（Node だけ、デプロイでも走る）が、ライセンスの無いモデルと、どのモデルも使わない `LICENSES` の項で落ちる。確認: ビルドした `dist/index.html` に 25 行が出ること。**まだ確かめていないこと**: 実ブラウザでの見た目（About の中の素のリスト。既存の一覧と同じ体裁）。 **レビュー（Fable、2026-09-24）: OK。** 25 件すべてを HF の API（`cardData.license` と `license:` タグ）と突き合わせて一致。出典を `hf.repo` と `source` の 2 本から `sourceOf()` で引く作りは、GGUF（T74）を足すときも `hf.repo` の側に乗るので耐える。About の中は既存の一覧と同じ素のスタイルで、新しい CSS は要らない。実ブラウザでの見た目は `browsers.yml` の回で見る。
- [x] **T85 [計測] 新しいアーキテクチャの int8 の品質。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-24）**。rinna GPT-2（日本語）・Pythia 160M・GPT-2 の 3 つを測った（表は AGENTS.md の T55 の隣）。原本比で int8 は +0.05% / +1.17% / +2.06%、7 ビットの活性値まで入れて +0.20% / +2.23% / **+17.2%**。**GPT-2 の位置の表は原因ではなかった**ので `layout()` は変えていない（形式もキャッシュ名もそのまま）。int8 の差は分類器から来る（float32 に戻すと GPT-2 は原本と同じ値）。**+1% を超えたもの**: Pythia（分類器が差の 6 割、残りは層）と GPT-2（7 ビットの活性値で大きく崩れる）。**対処はしていない**: GPT-2 だけ relaxed を使わない（8 ビットなら +4.7%）か、分類器を別の精度で持つかは、見た目の速度とメモリに効くので持ち主と Fable の判断を待つ。→ **T92** に積んだ。Qwen2.5 0.5B は float32 で約 2GB になるので、この開発機では測らなかった。**ついでに直したもの**: (1) `convert_hf.py` が GPT-2 と NeoX で落ちていた（`checkpoint_size()` に `arch` が無い）。`tests/test_convert_hf.py` を足した（修正前は 4 件とも失敗）。(2) `tests/perplexity.py` の 8 ビットの行が T52 の後に `load_kernels` の引数の変更で壊れていた → T52 の `disable=("relaxed",)` を使う形にした。計測のための `tests/perplexity_prepare.py`（ページと同じ変換で options を JSON に書く）と `tests/perplexity_native.py`（float32 をメモリマップで読む）を足し、`tests/perplexity.mjs` がその出力を読めるようにした。 **レビュー（Fable、2026-09-24）: OK。** 測り方は筋が通る: 原本の float32 だけネイティブの NumPy で出しているが、同じ int8 のファイルをネイティブと Pyodide の NumPy で測った値が一致している（GPT-2 で 36.068 と 36.068）ので、行をまたいで比べてよい。7 ビットで GPT-2 だけ崩れるのは、GPT-2 の残差に極端に大きいチャネルがあることと合うが、切り分けは T92。`tests/test_convert_hf.py` に Llama と Qwen2 も足して 4 アーキテクチャにした（Llama の経路は動いていたが、変えたときにそれが分かるように）。Qwen2.5 0.5B の未計測はこのままでよく、必要なら CI の Linux ランナー（17GB）で `tests/perplexity_native.py` を回す。
- [x] **T90 [運用] 端末のメモリに合わせて重いモデルを止める。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-24）**。(1) Worker は Python の `MemoryError` と JavaScript の `RangeError`（`ArrayBuffer` の確保や `WebAssembly.Memory.grow` の拒否）を見分け、`error` に `memory: true` とその時点の WebAssembly のメモリの大きさを付ける。ページはそれを「This device ran out of memory for …（必要 約 N MB、そのときの使用量 M MB）. A smaller model may fit, or closing other tabs may help.」と出す（Node の Pyodide で、`MemoryError` は `err.type`、2 つの `RangeError` は `err.name` で見分けられることを確かめた）。(2) `navigator.deviceMemory` が取れるときだけ、モデルの大きさ + 300MB が端末のメモリの半分を超えるモデルを選ぶ前に `confirm` で一言添える（大きな取得の確認と 1 つにまとめた）。取れないブラウザでは何もしない。モデルの大きさはサイトのモデルは `bytes`、HF のモデルは一覧の `note` の「int8 N MB」から読む（`tests/models-check.mjs` が全モデルで読めることを見る）。文面は既存のエラーと確認と同じく英語だけにしたので、日本語の下書きは要らなかった。(3) 落ちたときのメモリの大きさは (1) のメッセージに出る。`ready` の内訳には足していない。**まだ確かめていないこと**: 実ブラウザでの動き（この開発機ではブラウザを動かさない）。`deviceMemory` を小さく偽装したページで 1.5B を選ぶと一言出ることと、メモリ上限を絞ったページで理由つきのエラーが出ることは、CI か持ち主の手元で確かめる。 **レビュー（Fable、2026-09-24）: 2 つ直して OK。** (1) float16 の原本の見積もりが足りなかった: エンジンは float16 を読み込み時に float32 へ広げる（ファイルの隣に）ので、305MB のファイルがヒープ約 800MB になる。`modelBytes()` は `dtype: "float16"` ならファイルの 3 倍にした（float32 はそのまま、int8 は変換後の大きさのまま）。(2) `RangeError` は V8 ではスタック溢れ（Maximum call stack size exceeded）でも投げられるので、それは除外し、Firefox の `InternalError: out of memory` を足した。線の引き方（モデル + 300MB が端末の半分を超えたら聞く）は T90 の仕様どおりで、`deviceMemory` は 8 で頭打ちなので 8GB 以上の端末には何も言わない。`note` の「int8 N MB」を読む作りは、`tests/models-check.mjs` が全モデルで読めることを見ているので耐える（一覧の文言を変えるとテストで分かる）。実ブラウザでの出方は `browsers.yml` の回で見る。
- [x] **T92 [性能] GPT-2 の 7 ビットの活性値で崩れる品質を直す。**（Fable 5.1、2026-09-24。判断と実装を Fable が行った）崩れは**分類器の入力だけ**（模擬で分類器だけ 7 ビット +17.4%、それ以外を全部 7 ビットにしても +0.25%）。原因は最終 LayerNorm の重みが 4 チャネルを 12〜17 倍に増幅していること。分類器を 8 ビットにしても +6.2% で足りないので、**重みの大きい 8 チャネルを量子化の前に抜いて float32 の列で別に掛ける**（`add_columns` カーネル 1 つ、`simdkernel.so` 6307 → 6572 バイト）。門: norm の重みの最大/中央値が 4 以上のモデルだけ（GPT-2 13.9、ほかは 1.1〜1.9）で、ほかのモデルは 1 バイトも変わらず速度も同じ。**結果**: GPT-2 の 7 ビットの行 41.431 → 36.127（NumPy 比 +0.16%）。速度は GPT-2 で差が測れない。数値と選択肢の比較は AGENTS.md。テスト: `tests/test_outliers.py`（チャネルの選び方と列の取り出し、門）、`node tests/smoke.mjs`、`node tests/perplexity.mjs`。**残る +2% は int8 の重みの側（分類器）**で対処していない。
- [x] **T74 [追加] GGUF を取り込み元にする。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。Fable が決めた範囲（Llama と Qwen2、Q8_0 と F16、別の source として、`permute_heads` はかけない）で実装した。GGUF のテンソル表を HF の名前の safetensors 風ヘッダに見せて、既存の `Stream` にそのまま流す（Q8_0 の読み手と、Llama の q・k を HF の並びに戻す処理を足しただけ）。諸元と語彙はメタデータから。Worker は `.gguf` のモデルならヘッダを 2MB から 4 倍ずつ取り、`config.json` と tokenizer を取らない。**合格**: SmolLM2-135M-Instruct の Q8_0 で最尤トークン一致 99.7%、perplexity +0.13%（線は 99% と 0.2%）。語彙とオプションは safetensors の経路と完全に同じ。**取得は 269MB → 145MB（54%）**で、一覧の SmolLM2 135M Instruct を GGUF に切り替えた（About には GGUF の配布元と原本の両方が出る）。**公式の Qwen2.5 0.5B の GGUF は原本と別の重みだった**ので切り替えない（数字は AGENTS.md）。テスト: `tests/test_gguf.py`（合成の GGUF が safetensors と 1 バイトも違わない。Llama・分類器を別に持つ Llama・Qwen2 × int8・float32、Q8_0 が損失なしで戻る、短いヘッダは `Incomplete`、K-quant は断る）。`tests/gguf_check.py` は大きいテンソルを行のまとまりごとに比べるようにした（Qwen の埋め込みは float32 で 545MB）。**まだ確かめていないこと**: 実ブラウザ（`browsers.yml` の `huggingface` ジョブに SmolLM2 135M が入っているので、次の全体走行で GGUF の経路が通る）。フォルダのボタンから手元の GGUF を開く経路は作っていない（取得の経路だけ）。語彙は byte-level BPE だけで、sentencepiece 型（TinyLlama の GGUF など）は断る。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: 1 つ直して OK。** Q8_0 で最後の次元が 32 の倍数でないテンソルを断っていなかった（1 値あたり 34/32 バイトで数えるので、黙って位置がずれて「動くが文章が壊れる」形になる。ggml 自身が 32 の倍数を要求するので実物では起きない）→ `ValueError` で断り、試験を足した。確かめて問題なし: 語彙の数と `vocab_size` の差（Qwen2.5 の 151665 対 151936 も通る）、sentencepiece 型の語彙は断る、Llama の bias にも `turned` が掛かる（`unturned` は 1 次元でも `permute_heads` の逆）、Worker のヘッダの取り直しは 4 倍ずつで有界、生成の経路に分岐は増えていない、試験は並びの誤りとスケールの誤りを捕まえる。あれば良い（やらない）: bias 付き Llama の GGUF の試験（実物が無い）、取り直しを続きから、`rms_epsilon` を読む（エンジンが 1e-5 固定）、`?hf=` から GGUF を開く（T88 で）。

- [x] **T86 [運用] 実物のモデルの出力を固定値で守る。**（Opus 5.5、2026-09-24）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。`tests/fixed_outputs.py` と `tests/fixtures/fixed-outputs.json`。4 つ（Pythia 70M・GPT-2・SmolLM2 135M Instruct（T74 で GGUF になった）・llm-jp-3 150M instruct3）を、ページと同じ `Conversion` で float32 にして、`src/models.js` のプロンプトとテンプレートで greedy に 16 トークン。**やり方を仕様から 1 つ変えた**: ブラウザの中ではなくネイティブの Python で比べる。T72 の 2 つの壊れ方はどちらも変換器とオプションの誤りでブラウザなしで再現でき、float32 は NumPy とカーネルで一字一句同じなので、ブラウザを通す意味が薄い。代わりに Worker がオプションを重ねる順（変換の options に `src/models.js` の options を上書き）を真似ている。**壊すと落ちることを確かめた**（数字と文は AGENTS.md）。CI は `browsers.yml` の huggingface ジョブの小さい組の最後（`if: always()`）。**Fable に見てほしいこと**: 固定した 4 つの文がまともか（Pythia 70M の「, the "Battle of the First World War" was a series」は 70M としては普通と読んだ。llm-jp は流行を挙げずに断っているが、文としては壊れていない）。Qwen2 の bias の経路は入っていない（0.5B の float32 は約 2GB でこの開発機では作れない。CI のランナーなら作れる）。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: OK。** 4 つの文はまともと読んだ（llm-jp は断っているが日本語として崩れていない）。T72 の壊れ方（並べ替え、オプションの渡し忘れ）とトークナイザの変化を捕まえる。オプションの重ね方は Worker と同じ。float32 で比べるのは正しく、T93 の後は「NumPy の float32 = ページの出力」を `tests/forward-check.mjs`（最尤トークン全一致、logits の差 1e-3 以下）が支えている。仕様からの変更（ブラウザでなくネイティブ）は受け入れる。直した小さいもの: HF の取得に 60 秒の timeout と 3 回の再試行、コメントの「sentencepiece」→「Unigram の tokenizer.json」。あれば良い（やらない）: Qwen2（bias の経路）の 5 つ目は、CI のランナーなら float32 の 0.5B を作れるので、いつか。

- [x] **T93 [性能] `SharedArrayBuffer` を使えるようにして、複数コアで計算する。**（Opus 5.5 が実装、Fable が判定と仕様。2026-09-24〜25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。段階 1（forward を JS の `forward.js` へ。Python のエンジンとビット単位で同じ、int8 で 1.15〜1.26 倍）、段階 2（共有メモリとソフトウェアスレッド、刻む形、名指しで起こす、本数の検索。CI の Linux ARM で 4 本が段階 1 の 1.79〜1.94 倍）、段階 3（Service Worker `coi.js` で GitHub Pages のまま cross-origin isolated。既定でオン。実ブラウザで Qwen2.5 1.5B が 9.0 → 16.1 tok/s）、`kernel_forward` の退役。方針 2 と 6、見出しは持ち主が承認した文面に。詳しい経緯と数字は下の記録。**Fable に見てほしいこと**: forward.js の形（重みの置き場、段、検索）、`forward-check.mjs` の int8 の線、`coi.js` を全訪問者に入れたこと（`?coi=off` で解除）、検索が揺れの大きい機械でずれうること。 **レビュー（Fable、2026-09-25。`forward.js`・`helper.js`・`coi.js`・Worker とページの継ぎ目を通読）: 3 つ直して OK。** 形は仕様どおりで、判断した点はどれも筋が通る: 重みの置き場（Pyodide の外のメモリ、位置は Python から）、段（同じ入力の行列積をまとめ、行は 1 スレッドが丸ごと計算するので本数で数字が変わらない）、名指しで起こす同期、奇数の世代で段の書き換えを守る形、検索（塊で比べて切り替え直後を捨てる）。`forward-check.mjs` の int8 の線（85% / 5%、128 位置）は実測に余裕を持たせたもので受け入れる。float16 の `halfToFloat` は非正規化数と −0 も NumPy と同じ。**直したもの**: (1) Worker の `kernels === sharedKernels ? sharedKernels : jsKernels` は同語反復 → `kernels`。(2) `stopThreads()` が QUIT を立てて起こした直後に QUIT を 0 に戻していたので、遅れて起きたソフトウェアスレッドが QUIT を見逃す競合があった（`terminate()` で殺すので実害は無い）→ QUIT は次に起こすときまで立てたまま。(3) KV キャッシュの伸長（倍々に伸ばして古い場所へ詰め直す）をソフトウェアスレッドのある状態で試していなかった → `tests/threads-check.mjs` に `--kv-start`（既定 16）を足し、伸長をまたいでも 1・2・4 本で logits がビット単位で同じことを確かめた。**T97 への事前判断**: `coi.js` はクロスオリジンの取得（CDN、HF）まで本文を流し直しているが、COOP/COEP が要るのは同一オリジンの応答だけなので、同一オリジンだけ `respondWith` する形に変えてよい（T97 の項に書いた）。あれば良い（やらない）: `weightsMemory` の共有メモリの最大（int8 の 4 倍 + 1GB、上限 4GB）は `?without=int8` で広げる場合まで見込んでいて大きめだが、予約はアドレス空間だけなので害は小さい。
<details><summary>T93 の記録（判定・仕様・各段階の数字）</summary>

**当時の見出し**: T93 [性能] `SharedArrayBuffer` を使えるようにして、複数コアで計算する（COOP/COEP の付け方の再検討）— 状態: **段階 1〜3 は実装済み（段階 3 は本番での確認中）。残りは `kernel_forward` の退役**（2026-09-24 採用、持ち主の指示。**Fable が判定してから、実装は Opus**。規模 中〜大）
- 担当: 判定（どの経路で COOP/COEP を付けるか、何倍出るか）は **Fable**。方針 6 の変更は**持ち主の判断**。決まったら実装は Opus → Fable がレビュー。
- 根拠: 2026-09-21 の測り直しで、int8 の行列積は複数コアで**約 2 倍**まで伸びる（頭打ちはメモリ帯域）。T71 の往復の実測（0.10〜0.16ms）では、`postMessage` で分けられるのは分類器だけで 1.3〜1.5 倍止まり。**`SharedArrayBuffer` があれば**、Worker 間の同期が `Atomics` の数マイクロ秒になり、重みを Worker ごとに複製しなくてよく、層の行列積まで分けられるので、上限の 2 倍に近づける。使えない理由は 1 つだけ: ブラウザが `SharedArrayBuffer` を出すのは cross-origin isolated なページ（応答ヘッダ `Cross-Origin-Opener-Policy: same-origin` と `Cross-Origin-Embedder-Policy: require-corp` か `credentialless`）だけで、GitHub Pages はヘッダを変えられない。
- **COOP/COEP を付ける経路の候補**（判定で 1 つに絞る）:
  - **(A) GitHub Pages のまま `coi-serviceworker`**: Service Worker が応答にヘッダを足す（初回だけ自動で 1 回再読み込み）。サイトも URL も変えない。外部の資源は CORS が要る: Pyodide の CDN（jsdelivr）と Hugging Face は返すことを確認済み（`Access-Control-Allow-Origin: *`）。`credentialless` なら CORP のヘッダが無い資源も読める。**確かめること**: Service Worker の登録から `crossOriginIsolated` が真になるまでの流れが、Chromium・Firefox・Safari で通るか（Safari は `credentialless` 未対応の版がある）。方針 2 の「信頼性のための仕組みは足さない」との折り合い（Service Worker は 1 つの仕組みで、失敗しても Worker なしで動く形にする）。
  - **(B) ヘッダを付けられる静的ホスティングへ移す**: Cloudflare Pages と Netlify は `_headers` ファイル 1 つで応答ヘッダを付けられる（どちらも無料枠あり。Cloudflare Pages はファイル 1 つ 25MB までなので 8 MiB の部品はそのまま置ける。サイト全体 840MB を置けるか、無料枠の転送量で足りるかは**未確認**）。Vercel も `vercel.json` で付けられる。**代償**: URL が変わる（GitHub Pages は旧 URL を転送しないので、旧 URL には飛ばすページを置く。T62 と同じ手）、`deploy.yml` の配置先が変わる、方針 6 の書き換え。**利点**: Service Worker の再読み込みも `credentialless` の心配も無く、Range 要求と gzip の落とし穴（AGENTS.md）も配置先次第で消える。
  - **(C) 両方**: 本番は (B)、GitHub Pages は (A) 無しの単コア版として残す。2 か所の運用になるので、勧めない。
- **判定の手順（Fable、着手時）**:
  1. **速さの試作を先に、ヘッダより前に**: Node の `worker_threads` は `SharedArrayBuffer` を使えるので、この開発機で測れる。素の WASM カーネル（`kernels/kernel.ts` をそのまま。Pyodide は要らない）を N 個の Worker で共有メモリ上の重みに走らせ、分類器（語彙 × dim）と層の行列積を分けたときの 1 トークンの時間を、いまのエンジン（1 コア）と新旧交互に比べる。同期は `Atomics.wait` / `Atomics.notify`（メインスレッドは `Atomics.waitAsync` か、Worker の 1 つを取りまとめ役にする）。**2 Worker で 1.5 倍を超えなければ、ヘッダの話に進まず取りやめる**。
  2. 出るなら (A) を CI の `browsers.yml` で確かめる（`crossOriginIsolated` が真になるか、Pyodide と HF の読み込みが COEP の下で通るか、Safari でどうか）。
  3. (A) が通れば方針 6 の変更を持ち主に諮る（GitHub Pages のまま）。通らなければ (B) の候補を、サイズと転送量を確かめたうえで諮る。
- 設計の注意: Pyodide はスレッドなしのビルドなので、Python 側はいまのまま 1 本。共有するのは重みと活性値のバッファで、Worker は Pyodide を持たない素の WASM（`dylink.0` を付けずに普通にインスタンス化し、`WebAssembly.Memory({shared: true})` を渡す）。Pyodide のメモリは共有できないので、**重みを Pyodide の外（共有メモリ）に置き、エンジンの int8 の経路がそこを読む**形に変える。これは T71 より大きい変更で、エンジンの `kernel_forward` に手が入る。大小のコアの問題（AGENTS.md: 均等に割ると遅い）はブラウザからは選べないので、Worker 数は `hardwareConcurrency` の半分から試す。
- **Opus に渡す仕様（Fable、2026-09-24）。判定の材料 3 つを集める。判断はしない（数字と表を出して止める）。**
  - **仕様 1: 速さの試作（Node、`tests/threads-prototype/`）。** 目的は「共有メモリと素の WASM カーネルで、int8 の行列積を N コアに分けると 1 トークンが何倍速くなるか」を、いまのエンジンの tok/s と同じ条件で比べること。
    1. **カーネルの共有メモリ版のビルド**: `kernels/build.py` に 3 つ目の出力 `simdkernel_shared.wasm` を足す。同じ `kernel.ts` と `kernel_relaxed.ts` を、`--importMemory --sharedMemory --maximumMemory <MB>` と `--enable simd,relaxed-simd,threads` で 1 つの wasm にまとめる（`dylink.0` は付けない。普通に `WebAssembly.instantiate` する）。`--sharedMemory` は import するメモリの型を shared にするだけで、カーネルの中身は変えない。**pytest と smoke には影響しないこと**（`simdkernel.so` は 1 バイトも変えない。`git diff` で確かめる）。
    2. **試作の形**: メインスレッドは Pyodide を持たない（試作なので Python は要らない）。`WebAssembly.Memory({initial, maximum, shared: true})` を 1 つ作り、int8 のチェックポイント（`tiny-lm.bin` と `llm-jp-3-150m.bin`、`make models` の出力）をそこへ読み込む。テンソルの位置は `llama2_numpy.py` の `Llama.__init__` と同じ計算で出す（ヘッダ 7 int、グループ 32 の int8 値 + float32 スケール、順序は `layout()`）。活性値のバッファ（x・xb・q・k・v・hb・logits、KV キャッシュ）も同じ共有メモリに置く。**N 個の `worker_threads`** がそれぞれ同じ wasm を同じ共有メモリでインスタンス化する。
    3. **分け方は 2 つ実装して両方測る**: (a) 分類器だけ（`matmul_q8r(logits, xq, xs, wcls, ..., n, r0, r1)` の行 `r0..r1` を N 等分。メインが層を全部計算し、最後だけ配る）。(b) 層の行列積も（q・k・v・o・w1・w3・w2 と分類器の全部を行で N 等分。attention・norm・rope・swiglu はメインが 1 本で）。(b) では層ごとに同期が 2 回要る（attention の前に q・k・v が揃うこと、FFN の後に x が揃うこと）ので、往復の回数は 1 トークンに 2 × 層数 + 1。
    4. **同期**: 共有メモリの先頭に「世代」の i32 を 1 つと、Worker ごとの「済」の i32 を置く。メインは仕事（どの行列、どの行）を書いてから世代を `Atomics.add` して `Atomics.notify`、Worker は `Atomics.wait` で世代が変わるのを待ち、計算して自分の「済」に世代を書き `Atomics.notify`。メインは `Atomics.waitAsync` で全部の「済」を待つ（メインスレッドは `Atomics.wait` できない）。**スピン待ちは禁止**（数字が歪む）。
    5. **測り方**: 1 トークンの forward を、いまのエンジン（`node tests/compare-engines.mjs` と同じ Pyodide + `simdkernel.so`、Worker 0）と試作（N = 1・2・3・4）で、**同じプロセスの中で交互に** 64 トークンぶん × 3 回。ただし試作は Python を持たないので「試作の N=1」も測って、Python の費用（ctypes 呼び出し 6% など）を分けて読めるようにする。位置 0〜64 の greedy で、出力トークンの列が試作と本物で一致することも確かめる（**一致しなければ試作が間違っている**。速さより先に直す）。
    6. **コアの固定**: `taskset -c 4-7 node ...`（大コア 4 つ）と、固定なし（8 コア）の両方で取る。この開発機の大コアは 4〜7（AGENTS.md の 2026-09-21 の計測と同じ）。
    7. **出すもの**: 表 1 枚（モデル × 分け方 × N × 固定あり/なし → tok/s と、1 コア比の倍率）を T93 の項に書く。メモリの増えぶん（共有メモリの大きさと、Worker 1 つの RSS）も。**「2 Worker の (a) か (b) で 1.5 倍を超えるか」を数字で言い、判断は書かない。**
    - はまりそうな点: `--sharedMemory` は `--maximumMemory` が無いとエラー。Node 24 の relaxed SIMD は既定で有効のはず（`WebAssembly.validate` で確かめる。無ければ `matmul_q8` で測り、そう書く）。共有メモリの `initial` は llm-jp で 171MB + 活性値 + KV（4096 位置 × 12 層 × 2 × 512 × 4B = 200MB は大きすぎるので、試作は 256 位置でよい）。`Atomics.waitAsync` は Node 16 以降にある。試作は `tests/threads-prototype/` に置き、**リポジトリの他のファイルに触らない**（`build.py` の 3 つ目の出力だけ例外）。
  - **仕様 2: (A) `coi-serviceworker` の確認（CI）。** `public/` にヘッダを足す Service Worker（自作 30 行で足りる: `fetch` を捕まえて応答を複製し `Cross-Origin-Opener-Policy: same-origin` と `Cross-Origin-Embedder-Policy: credentialless` を付ける。`require-corp` も試す）と、それを登録して `crossOriginIsolated` と `typeof SharedArrayBuffer` を画面に出す**別ページ** `public/coi-test.html` を置く（本番のページには触らない）。`browsers.yml` に 1 ステップ足し、Chromium・Firefox・WebKit で「登録 → 再読み込み → `crossOriginIsolated === true`」が通るか、そのページから Pyodide の CDN と Hugging Face の `fetch` が COEP の下で通るかを、e2e の要領で記録する。結果は表（ブラウザ × `credentialless` / `require-corp` → 真か偽か、外部の読み込みが通るか）。
  - **仕様 3: (B) ホスティングの候補の調査（文書だけ）。** Cloudflare Pages・Netlify・Vercel について、公式の文書から: 応答ヘッダの付け方、ファイル 1 つの上限、サイト全体の上限、無料枠の転送量と月の上限、独自ドメイン無しの URL の形、GitHub Actions からの配置の方法。数字は**出典の URL つき**で、確認できないものは「未確認」。あわせて、いまのサイト（約 840MB、モデルの部品は 8 MiB）と 1 か月の転送量の見込み（CI の `browsers.yml` が週 1 回で 20GB 超を取ることも含める）を並べる。**勧めは書かない**（Fable が決める）。
  - 順番: 仕様 1 → 3 → 2（1 で 1.5 倍が出なければ 2 と 3 は要らないので、1 の表を出した時点で一度止まって報告する）。
- **仕様 1 の結果（2026-09-24、Opus 5.5、`node tests/threads-prototype/run.mjs`）。** 試作はエンジンが読み込んだ配列を共有メモリへ写し、エンジンのカーネルの経路と同じ順に同じカーネルを呼ぶ。**全部の実行で、出力トークン（greedy、59〜60 個）がエンジンと完全に一致した。** 同じプロセスの中で交互に 3 回、64 位置、tok/s は中央値。「試作 1 スレッド」はスレッドなしで Python を通らないだけの版。

  | モデル | 構成 | 大コア固定（4〜7） | 固定なし（8 コア） |
  |---|---|---:|---:|
  | tiny-lm | エンジン（Pyodide、1 スレッド） | 418 | 418 |
  | tiny-lm | 試作 1 スレッド | 592（1.42×） | 595（1.42×） |
  | tiny-lm | 試作 2 スレッド、分類器だけ | 722（1.73×） | 704（1.68×） |
  | tiny-lm | 試作 2 スレッド、全部の行列積 | 713（1.71×） | 671（1.60×） |
  | tiny-lm | 試作 3 スレッド、分類器だけ | 753（1.80×） | 745（1.78×） |
  | tiny-lm | 試作 4 スレッド、全部の行列積 | 656（1.57×） | 645（1.54×） |
  | llm-jp-3 150M | エンジン（Pyodide、1 スレッド） | 88.9 | 84.3 |
  | llm-jp-3 150M | 試作 1 スレッド | 103.0（1.16×） | 104.0（1.23×） |
  | llm-jp-3 150M | 試作 2 スレッド、分類器だけ | 114.6（1.29×） | 111.1（1.32×） |
  | llm-jp-3 150M | 試作 2 スレッド、全部の行列積 | 120.4（1.35×） | 125.5（1.49×） |
  | llm-jp-3 150M | 試作 3 スレッド、全部の行列積 | 121.7（1.37×） | 127.6（1.51×） |
  | llm-jp-3 150M | 試作 4 スレッド、全部の行列積 | 126.2（1.42×） | 121.1（1.44×） |

  括弧はエンジン比。**エンジン比の倍率は 2 つの効果の積**: (1) Python を通らないこと（試作 1 スレッドで tiny-lm 1.42×、llm-jp 1.16〜1.23×）と、(2) スレッド（試作 1 スレッド比で tiny-lm 1.22〜1.27×、llm-jp 1.16〜1.23×）。**スレッドだけの効きは 1.2〜1.3 倍**。
- **スレッドが 1.3 倍で止まる理由（切り分け済み）**: llm-jp の分類器（99584 × 512 の int8）の行列積だけを同じ同期の仕組みで分けると、1 スレッド 4.35ms、2 スレッド 3.41ms、3 スレッド 3.22ms、4 スレッド 3.41ms（大コア固定）。**各スレッドの分担を同時に走らせると、半分の仕事に 3.0ms かかる**（同期の費用ではなく、同時に読むと遅くなる）。1 回に読むのは int8 の重み 51MB と、グループごとの float32 のスケールと補正 13MB の計 64MB で、1 スレッドで既に約 15GB/s、頭打ちは約 20GB/s（2026-09-21 に C で測った上限 21〜24GB/s と合う）。**2026-09-21 の「約 2 倍」は int8 の値だけを読む C の計測で、1 スレッドが 12GB/s だったから**。いまのカーネルはスケールと補正も読み、1 スレッドの時点で上限の 7 割に居るので、余地が 1.4 倍しかない。`--no-liftoff`（最初から最適化コンパイラ）でも数字は同じだったので、V8 の段階的なコンパイルは関係ない。
- **仕様 1 の答え**: 「2 スレッドで 1.5 倍を超えるか」は、エンジン比なら tiny-lm は超える（1.68〜1.73×）、llm-jp は超えない（1.29〜1.49×）。スレッドだけなら両方とも超えない（1.2〜1.3×）。**この開発機はメモリ帯域が狭いスマホ級の ARM** で、帯域に余裕のあるデスクトップでは伸びしろが大きい見込み（未計測。CI の Linux ランナーで同じ試作を走らせれば分かる）。**別の発見**: Python の層のループを外すだけで 1.16〜1.42 倍（T47 の「層ごとに 1 回のカーネル呼び出し」に近い効き。T47 は保留中）。
- 試作のメモリ: 共有メモリは tiny-lm 36MB、llm-jp 189MB（重み + 活性値 + 65 位置の KV）。Worker 1 つごとの増えぶんはカーネルの wasm（7KB）と V8 の固定費だけで、重みの複製はない。RSS は未計測。
- 仕様からの変更: カーネルは 1 つの wasm にまとめず、`kernel.ts` と `kernel_relaxed.ts` を別々に共有メモリ版にした（`simdkernel_shared.wasm` と `simdkernel_relaxed_shared.wasm`。定数の名前が重なるため。同じ共有メモリを読むので計算は同じ）。取りまとめ役はメインスレッドではなく Worker の 1 つ（`Atomics.wait` が使え、ページのエンジンも Worker の中で動くので同じ形）。同期の回数は「全部の行列積」で 1 トークンに 4 × 層数 + 1 回（q・k・v、o、w1・w3、w2 のあとに 1 回ずつ）。
- **仕様 3 の結果（2026-09-24、Opus 5.5。公式の文書から。勧めは書かない）。** いまのサイトは `dist` で 839MB・127 ファイル、いちばん大きいファイルは 8 MiB の部品。CI の `browsers.yml` は 1 回の全体走行でサイトから約 6GB を取る（OS のジョブ 5 つで 23 通りのブラウザ + インストール済みの Firefox 5 つ、それぞれ stories260K・stories15M・tiny-lm・llm-jp の約 222MB。見積もりで未計測）。週 1 回で月に約 27GB。HF から取る 20GB 超は HF の転送で、ここには乗らない。訪問者の転送量は未計測。

  | | GitHub Pages（いま） | Cloudflare Pages | Netlify | Vercel |
  |---|---|---|---|---|
  | 応答ヘッダ（COOP/COEP） | 付けられない | `_headers` ファイル（ルール 100 個まで、1 行 2,000 文字）。静的ファイルに付く | `_headers` か `netlify.toml`。自前で配る静的ファイルに付く | `vercel.json` の `headers`（1 デプロイのルート 2048 個に数える） |
  | ファイル 1 つの上限 | 未確認（100MB 超は警告と聞くが、文書では未確認） | 25 MiB | 未確認 | 未確認 |
  | サイトの上限 | 1 GB | 無料で 20,000 ファイル（大きさの上限は未確認） | 未確認 | CLI で上げる元のファイルが Hobby で 100 MB（ビルド済みの出力を上げる場合に当たるかは未確認） |
  | 転送量（無料） | 月 100 GB の目安（soft） | 静的なら無制限（「requests to static assets are free and unlimited」） | 月 300 クレジットの上限で、1GB が 20 クレジット、本番デプロイ 1 回が 15 クレジット（使い切ると翌月まで止まる） | 月 100 GB（Fast Data Transfer）。Hobby は非商用に限る |
  | デプロイの回数 | 1 時間 10 回の目安（soft） | 無料で月 500 回（直接アップロードも数えるかは未確認） | 1 回 15 クレジット | 1 日 100 回 |
  | GitHub Actions から | いまの `deploy.yml` | `cloudflare/wrangler-action` で `pages deploy <dir> --project-name=<name>`（`CLOUDFLARE_API_TOKEN` と `CLOUDFLARE_ACCOUNT_ID` が要る） | 未確認 | 未確認 |

  **読み方の材料（勧めではない）**: Netlify の無料枠は、このリポジトリのデプロイの回数（1 日に十数回）だけでクレジットを使い切る。Vercel は元のファイル 100MB の上限がビルド済みの出力に当たるなら、839MB のサイトは載らない（未確認）。Cloudflare Pages はファイルの大きさ（8 MiB < 25 MiB）・ファイル数（127 < 20,000）・転送量のどれにも当たらない。どこへ移しても URL は変わる（独自ドメインを使わない限り）。出典: [GitHub Pages の上限](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits)、[Cloudflare Pages の上限](https://developers.cloudflare.com/pages/platform/limits/)、[Cloudflare Pages のヘッダ](https://developers.cloudflare.com/pages/configuration/headers/)、[Cloudflare Pages の CI からのデプロイ](https://developers.cloudflare.com/pages/how-to/use-direct-upload-with-continuous-integration/)、[Cloudflare の料金](https://www.cloudflare.com/plans/developer-platform/)、[Netlify のクレジット](https://docs.netlify.com/manage/accounts-and-billing/billing/billing-for-credit-based-plans/credit-based-pricing-plans/)、[Netlify のヘッダ](https://docs.netlify.com/manage/routing/headers/)、[Vercel の上限](https://vercel.com/docs/limits)、[Vercel の目安](https://vercel.com/docs/limits/fair-use-guidelines)（いずれも 2026-09-24 に参照）。
- **仕様 2 の結果（2026-09-24、Opus 5.5、`coi.yml` の実行 36008163233、本番の `https://takano32.github.io/pyodide-llm/coi-test/`）。** GitHub Pages のまま、`/coi-test/` にだけ効く Service Worker が COOP と COEP を足す。1 回の自動の再読み込みの後で:

  | ブラウザ（Linux・macOS・Windows の 3 つとも同じ結果） | `require-corp` | `credentialless` |
  |---|---|---|
  | Chromium 148・Chrome 152〜153・Edge 152〜153 | 真 | 真 |
  | Playwright の Firefox 150 | 真 | 真 |
  | Playwright の WebKit 26.4 | **真** | **偽**（WebKit は `credentialless` を知らない） |

  真になった組み合わせではすべて、**ページから Worker へ共有の `WebAssembly.Memory` を渡して `Atomics` で書けた**、**Worker の中で CDN から最新の Pyodide 314.0.7 と NumPy を読み込めた**、**Hugging Face の GGUF を Range 要求で読めた（206）**。再読み込みは 1 回。→ **`require-corp` なら、試したすべてのブラウザで GitHub Pages のまま `SharedArrayBuffer` が取れる。** ホスティングを移す（仕様 3 の (B)）必要は、ヘッダのためには無い。Safari そのものと iPhone は未確認（Playwright の WebKit まで）。インストール済みの Firefox（Selenium）も未確認。
- **8 スレッドまで（2026-09-25、Opus 5.5、持ち主の希望）。** 同じ試作で 1・4・6・8 スレッド。

  | モデル | 構成 | 大コア固定（4〜7） | 固定なし（8 コア） |
  |---|---|---:|---:|
  | tiny-lm | エンジン | 443 | 394 |
  | tiny-lm | 試作 4 スレッド、分類器だけ | 782（1.77×） | 725（1.84×） |
  | tiny-lm | 試作 8 スレッド、分類器だけ | 764（1.72×） | **130（0.33×）** |
  | tiny-lm | 試作 8 スレッド、全部の行列積 | 575（1.30×） | 256（0.65×） |
  | llm-jp-3 150M | エンジン | 86.0 | 85.2 |
  | llm-jp-3 150M | 試作 4 スレッド、全部の行列積 | 117.2（1.36×） | 122.0（1.43×） |
  | llm-jp-3 150M | 試作 6 スレッド、全部の行列積 | 103.5（1.20×） | **47.5（0.56×）** |
  | llm-jp-3 150M | 試作 8 スレッド、全部の行列積 | 116.3（1.35×） | **52.0（0.61×）** |

  分類器の行列積だけ（99584 × 512、1 回の時間）: 大コア固定で 1 スレッド 4.27ms、2 で 3.28、4 で 3.23、6 で 3.57、8 で 3.23ms。固定なしで 1 スレッド 4.33ms、2 で 3.40、4 で 4.16、6 で 5.94、8 で **7.36ms**。**8 スレッドは 4 スレッドより速くならない。** 大コアに収めれば帯域の頭打ち（約 3.2ms、約 20GB/s）で止まるだけだが、固定なし（ブラウザの実際）では 5 本目からが小コア（A55、大コアの約 3 分の 1 の速さ）に載り、均等に分けた分担を小コアが終えるまで全員が待つので、**1 スレッドより遅くなる**（2026-09-21 の C の計測と同じ形）。ブラウザからはコアを選べないので、ブラウザで使うならスレッドは 2〜4 本まで。分担を均等でなく速さに合わせる（仕事を小さく刻んで早く終えた者が次を取る）形にすれば小コアの害は消せるが、帯域の頭打ちは変わらない（未試作）。
- **判定の材料のまとめ（Fable と持ち主へ。Opus は判断しない）**: (1) **取れるか**: 取れる（`require-corp` の Service Worker、再読み込み 1 回）。(2) **速くなるか**: この開発機（スマホ級の ARM）では、スレッドだけの効きは 1.2〜1.3 倍で、頭打ちはメモリ帯域。エンジン比では Python を通らない効果と合わせて tiny-lm 1.7〜1.8 倍、llm-jp 1.3〜1.5 倍。帯域の広い機械では未計測（試作を CI の Linux ランナーで走らせれば分かる）。(3) **代わりに払うもの**: 初回の再読み込み 1 回、Service Worker という仕組みが 1 つ増える（方針 2 との折り合い）、エンジンの int8 の重みを Pyodide の外の共有メモリに置き直す作り替え（T71 より大きい）。(4) **別の発見**: Python の層のループを外すだけで 1.16〜1.42 倍（スレッドなしで取れる。T47）。
- **判定（Fable、2026-09-25）: エンジンの形は (a) forward を JS に移す、を勧める。持ち主の判断待ち。** (a) は試作がそのまま実測値（tiny-lm 1.7〜1.8 倍、llm-jp 1.3〜1.5 倍）で、スレッドの 1.2〜1.3 倍に「Python の層のループを外す 1.16〜1.42 倍」が掛かる。(b) Python のループを残して行列積ごとに JS へ橋渡しする形は、スレッドの分しか取れず、しかも橋渡し（Python → JS の呼び出しとコピー、llm-jp で約 100 回/トークン）の費用が乗る（未計測。tiny-lm の 3.3ms/トークンでは 3 割を食う恐れ）。(a) なら Python の `kernel_forward` を退役させて forward は NumPy 版（参照と予備）と JS 版の 2 つになり、JS 版は 1 スレッドでも共有メモリなしでも動く。(b) は forward が 3 つになる。(a) は T94（WebGPU）の「forward を丸ごと外に置く」形と同じ。**代償は見出しの言い直し**: 「モデルは素の Python で重い計算だけカーネル」→「Python が読み込み・トークナイザ・サンプリング・生成のループを持ち、1 トークンの forward がカーネル」。NumPy 版は残るので「WASM Python でどこまで」の答えは消えない。(a) の変更は大きい（llama・gpt2・neox と bias・rotary・並列残差を JS に書き、出力トークンの一致で確かめる。試作は llama の素の形だけ）。(b) を選ぶなら橋渡しの費用を先に測る。
- **持ち主の判断（2026-09-25）: (a) で進める。** あわせて持ち主の案: **スレッド数を推論ごとに変えて測り、最適な数に収束させる仕組み**。Fable の意見: 考えは正しい（ブラウザはコアを選べず、数を間違えると 1 本より遅い。最適な数は端末で違う）。設計の注意は 4 つ: (1) 推論ごとではなく 16 トークンほどの窓で測る（Worker は最初に全部起こし、使わないものは `Atomics.wait` で寝かせるので切り替えは無料）、(2) 1 本から増やして悪くなったら止める（8 本の窓を訪問者に見せない）、(3) 揺れ 5〜10% と位置の影響を分ける（隣り合う位置で交互に、中央値で）、(4) 端末ごとに `localStorage` に覚え、たまに測り直す。**ただし先に試すこと**: 行列積の行を塊に刻み（大きさは行数とスレッド数から導く）、原子的なカウンタで早く終えたスレッドが次を取る形（work stealing）。小コアは自然に少ない塊しか取らないので、8 本でも待たされない。費用は塊ごとに原子操作 1 回。これで固定なしの 8 本が「増やしても悪くならない」なら、動的な選択は要らない。山が残るなら持ち主の案を上に重ねる。**順番: 刻む形の試作 → 固定なしで 1〜8 本を測り直す → 要るなら動的な選択。** 実装の仕様は Fable が書いてから Opus へ。
- **スレッド数に上限の定数を置かない（持ち主の指示、2026-09-25。64 や 128 が最適な機械もありうる）。** 設計: (1) 止める条件は「倍々に増やして前より遅くなった」だけ。前の 2 点の間を絞って終わる。上限の数は持たない。(2) `hardwareConcurrency` も上限にしない（Safari は 8 に丸めて返すことがあり、2026-09-21 の計測ではコア数の 2 倍のスレッドのほうが速かった）。出発点の目安に使うだけ。(3) 残る制約はメモリだけで、Worker 1 つの費用（数 MB、未計測）× 本数を `deviceMemory` と比べて決める。定数ではない。(4) `Atomics.notify` に起こす数を渡し、塊の数を超えて起こさない。(5) **塊の大きさも導く**: 行列積ごとに「スレッド数の数倍の塊」になるよう行数から決める（llm-jp の k の射影は 512 行しかない）。塊が小さすぎて原子操作の費用が見える境目は測って決める。刻む形と合わせれば「増やしても悪くならない」はずで、そのとき動的な選択は「どこまで起こすか」を決めるだけになる。**測れる場所**: この開発機は 8 コア、CI のランナーは 4 vCPU なので、多数のスレッドは持ち主のデスクトップか訪問者の報告（T91）でしか分からない。試作の `run.mjs` はスレッド数を引数で受けるので、機械があれば `node tests/threads-prototype/run.mjs llm-jp-3-150m 1 4 16 64 128` のように測れる。
- **刻む形と動的な選択の試作（2026-09-25、Opus 5.5、`run.mjs --schedule equal,steal --chunks N --auto`）。** 刻む形（steal）: 行列積の行を「スレッド数 × `--chunks`」個の塊に分け、原子的なカウンタで取り合う。段を書き換える前に、世代を奇数にして起きているソフトウェアスレッドが全員寝るのを待つ（前の段の遅れたソフトウェアスレッドが次の段の塊を取る競合を防ぐ）。起こす数は塊の数まで。出力トークンは全部の実行でエンジンと一致。

  | 固定なし（8 コア）、全部の行列積 | tiny-lm 均等 | tiny-lm 刻む | llm-jp 均等 | llm-jp 刻む |
  |---|---:|---:|---:|---:|
  | エンジン | 356 | | 83.2 | |
  | 試作 1 スレッド | 474（1.33×） | | 102.3（1.23×） | |
  | 2 スレッド | 1.46× | 1.53× | 1.40× | 1.36× |
  | 4 スレッド | 1.05×（203〜704 と揺れる） | **2.02×** | 1.43× | 1.45× |
  | 8 スレッド | **0.66×** | 1.27× | **0.55×** | 1.12× |

  **刻む形で崩れは消えた**（8 本でもエンジンより遅くならない。揺れも小さくなった）。**ただし山は残る**: この機械では 4 本が 8 本より速い（帯域の頭打ちと、起こす数が増える費用）。塊の数（llm-jp、`--chunks`）: 1 スレッドあたり 1 つだと 8 本で 0.90×（取り合いが起きない）、2〜16 はどれも 4 本で 1.35〜1.45×、8 本で 1.15〜1.19× で差が無い。→ **「1 スレッドあたり 2 つ以上」で足りる。** 境目の細かさは費用にならなかった。
- **動的な選択（倍々の検索）は、仕組みは動くが、この機械では答えが安定しない。** 取りまとめ役が必要になった時点でソフトウェアスレッドを自分で起こし（上限の数は無い）、現在の最良と倍の数を比べ、5% 以上速いときだけ上げる。比べ方を 2 回直した: (1) 1 トークンごとに交互に切り替えると、多いほうが不利に出た（1 本の間にソフトウェアスレッドのコアが休んでクロックが下がるため、と見ている。未確認）→ 塊で「最良・候補・候補・最良」の順に並べ、切り替え直後の 1 トークンは捨てる形にした。(2) 起こしたばかりのソフトウェアスレッドは最適化前のカーネルで走る → 切り替え直後を捨てることで兼ねた。それでも tiny-lm では 1 トークンの時間が 1.6ms から 6.1ms まで跳ね（**このとき開発機では持ち主の別の作業が動いていた**。CPU の取り合いと見るのが自然）、同じ条件の検索が 1 本・2 本・4 本・8 本とばらばらに答えた。覚えた数での実行は 1.04〜1.40×、固定の 4 本（刻む形）は 1.71〜1.91×。**雑音の大きい機械で短い窓の検索は当てにならない。** 手当ての候補（未試作）: 窓を長くする、検索を何回かの生成にまたがって続けて多数決にする、覚えた数を時々だけ測り直す。どれにするかは Fable の判断。
- **Worker 1 本あたりのメモリ（Node の worker_threads、カーネルの wasm を共有メモリでインスタンス化して待たせた状態）**: 最初の 1 本で +12MB、2 本で +12、4 本で +13、8 本で +15、16 本で +15MB。**2 本目からは 1 本あたり 1MB 未満**。重みは共有なので増えない。ブラウザの Web Worker は未計測。
- **CI のランナーでの計測（2026-09-25、Opus 5.5、`threads.yml` の実行 36029077727、128 位置、中央値）。** どのランナーも出力トークンはエンジンと一致。倍率はエンジン比。

  | ランナー | モデル | 試作 1 本 | 2 本 均等 / 刻む | 4 本 均等 / 刻む | 8 本 均等 / 刻む | 16 本 均等 / 刻む | 検索が選んだ数 |
  |---|---|---:|---:|---:|---:|---:|---|
  | Linux ARM（4 vCPU、17GB） | tiny-lm（エンジン 690） | 1.27× | 1.45× / 1.63× | 1.47× / **2.06×** | 1.10× / 1.74× | 0.85× / 1.33× | **4（正しい）**、覚えた数で 2.04× |
  | Linux ARM | llm-jp（エンジン 111） | 1.11× | 1.64× / 1.59× | 1.94× / **2.16×** | 1.65× / 1.89× | 1.45× / 1.64× | **4（正しい）**、覚えた数で 1.96× |
  | Linux x64（AMD EPYC 7763、4 vCPU） | tiny-lm（エンジン 595） | 1.21× | 1.36× / **1.72×** | 1.12× / 1.68× | 0.90× / 1.52× | 0.71× / 1.23× | 2（ほぼ正しい）、覚えた数は 1 で 1.22× |
  | Linux x64 | llm-jp（エンジン 106） | 1.11× | 1.46× / 1.56× | 1.38× / **1.62×** | 1.22× / 1.47× | 1.10× / 1.28× | **1（誤り）**: 1 本 7.94ms 対 2 本 8.18ms と測った |
  | macOS（Apple M1 の仮想機、3 vCPU、揺れが大きい） | tiny-lm（エンジン 887） | 1.45× | 1.40× / 1.58× | 1.14× / **1.72×** | 0.61× / 1.10× | 0.49× / 0.96× | **1（誤り）** |
  | macOS | llm-jp（エンジン 124） | 1.12× | 1.03× / 1.27× | 1.10× / **1.47×** | 1.03× / 1.38× | 0.64× / 0.99× | **1（誤り）** |

  **読み取れること**: (1) **刻む形はどこでも均等より速いか同じで、コア数を超えても崩れない**（16 本 / 4 vCPU で均等は 0.49〜0.85×、刻むと 0.96〜1.64×）。(2) 帯域の広い機械では**エンジン比 2 倍を超えた**（Linux ARM で tiny-lm 2.06×、llm-jp 2.16×）。スレッドだけの効き（試作 1 本比）は 1.6〜1.9 倍で、この開発機の 1.2〜1.3 倍より大きい。(3) 最適な数はランナーの vCPU 数（3〜4）の近くで、それを超えて伸びた機械はない（多数のコアの機械は未計測のまま）。(4) **検索は、揺れの小さい機械（Linux ARM）では正しく 4 本を選んだが、ほかでは最初の「1 本 対 2 本」で誤った**。x64 の llm-jp は固定の計測で 1 本 117・2 本 166 tok/s とはっきり差があるのに、検索の中では 2 本が遅く出た。**起こしたばかりのソフトウェアスレッドが最適化前のカーネルで走る時間が、候補の最初の塊に乗っている**と見ている（未確認）。手当ての候補: ソフトウェアスレッドは読み込みのうちに起こして、ダミーの行列積で温めておく（検索の中で起こさない）。どうするかは Fable の判断。
- 未解決: llm-jp の検索（128 位置）は、試作に掛けた 800MB の上限（cgroup。このスコープのメモリだけを数えるので、同時に動いていた別の作業は数に入らない）で OOM になった（固定の 8 本・64 位置は上限の中で走る）。原因は未特定（Worker の数ではない。上の計測で軽い）。この開発機のメモリで追うのは危ないので止めた。
- **Fable の判定と実装の仕様（2026-09-25）。** 判定: (1) **検索は残す**（`hardwareConcurrency` は big.LITTLE で外れる: この開発機は 8 と答えるが最速は 4 で、刻む形でも 8 本は 4 本の 6〜7 割。対称なランナー 3 台では vCPU 数が最速）。手当ては**ソフトウェアスレッドを読み込みの間に起こしてダミーの行列積で温める**（検索の中では起こさない）。出発点は `hardwareConcurrency`、そこから半分と倍を比べて絞る（倍々の上限は無い。悪くなったら止める）。答えは端末ごとに `localStorage` に覚え、生成の何回かに 1 回だけ隣（半分と倍）を測り直す。共有メモリが無い（`crossOriginIsolated` が偽）ならソフトウェアスレッドは起こさず 1 本。(2) 見出しの言い直しと方針の変更は**持ち主が承認した（2026-09-25）**。文面: イントロは「ブラウザ上の WebAssembly 版 Python（Pyodide）で言語モデルを動かす実験です。読み込み・トークナイザ・サンプリング・生成のループは素の Python で、1 トークンぶんの計算は WASM SIMD カーネルが、使える端末では複数のコアで行います。実験なのでモデルは小さく、出力はそれなりです。」（英語も同じ内容）。方針 2 に「Service Worker は COOP/COEP を足すためだけに置く。登録に失敗しても 1 コアで動く」を足し、方針 6 の「スレッドには頼らない」を「スレッドは Service Worker で取れたときだけ使い、取れない端末でも同じ速さの 1 コア版で動く」に変える。書き換えるのは段階 3。
- **Opus に渡す実装の仕様。3 段階で、段階ごとに 1 コミット。各段階で pytest・smoke・出力トークンの一致を通してから次へ。**
  - **段階 1: forward を JS に移す（スレッドなし、共有メモリなし）。** 目的は「Python の層のループを外す」ぶんの 1.16〜1.42 倍を先に取り、JS 版 forward の正しさを固めること。
    1. `public/forward.js`（素の ES モジュール。Worker からも Node からも import できる）: `createForward({ memory, kernels, layout, config }) → { forward(token, pos, needLogits), grow(), destroy() }`。試作の `thread.mjs` の forward を元に、**llama・gpt2・neox の 3 つと bias（Qwen2）・rotary・並列残差・GPT-2 の位置の表・LayerNorm・GELU・外れ値チャネル（`add_columns`、T92）**をエンジンの `kernel_forward` と同じ順で。KV キャッシュの倍々の拡張（`KV_START`）も同じに。
    2. **重みの置き場は `WebAssembly.Memory`（この段階では共有しない）**。ダウンロード（`worker.js` の部品の書き込み）と変換（`Conversion.checkpoint`）の出力先を Pyodide のバッファからこのメモリへ変える。**テンソルの位置は Python から取る**（レイアウトの写しを JS に増やさない。落とし穴「3 か所」を 4 か所にしない）: `Llama` に `weights=None` の形を足し、`take()` がバイト列を読まずに offset と shape だけを記録して `Llama.tensors` として返す。JS はそれを読んで `layout` を組む。int8 の補正（`scales × 値の和`）は JS が読み込み時に共有メモリの中で計算して置く。
    3. **Python 側**: `Llama` は tokenizer・`generate()`・サンプリング（`penalize` / `sample` のカーネルは Pyodide のメモリの logits に対して今のまま）・stats を持つ。`forward` は JS の関数（`pyodide.ffi` で渡す）を呼び、JS が logits を Pyodide のメモリの配列（アドレスを渡す）へ書く（語彙 × 4 バイトのコピー、1 トークン 1 回）。`kernel_forward` は消す。NumPy の forward（`kernels=None`、`?without=kernels`）は残す: その経路では重みは今までどおり Pyodide 側に読む（2 つの置き場が要るのは NumPy のときだけ）。
    4. **確かめ方**: (a) `tests/smoke.mjs` に「JS 版と NumPy 版の greedy の出力トークンが、サイトの 4 モデルで一致する」を足す（float32 は一字一句、int8 は加算順の落とし穴があるので logits の最大差と一致率を出し、線は `gguf_check.py` と同じ 99% / 0.2%）。(b) `tests/fixed_outputs.py` の 4 つの固定値を JS 版でも出す Node のスクリプト（CI の huggingface ジョブに足す）。(c) 速度は `tests/compare-engines.mjs` を「Python の kernel_forward 対 JS 版」で新旧交互に（tiny-lm と llm-jp）。落ちていたら次に進まない。
    5. `worker.js` の `bench` と `?without=` の切り替えは JS 版に合わせて直す（`int8` / `relaxed` / `sampler` の意味はそのまま）。
  - **段階 2: 共有メモリとソフトウェアスレッド（刻む形と検索）。**
    1. `crossOriginIsolated` が真なら `WebAssembly.Memory({ shared: true })`、偽なら段階 1 のまま。真のときだけ `public/helper.js`（Worker。試作のソフトウェアスレッドの部分: 世代を待ち、塊を取る）を **読み込みの間に** `hardwareConcurrency` 本まで起こし、ダミーの行列積で温める（起こす本数の上限は定数ではなく `deviceMemory` から: 1 本の費用は段階 2 で測って書く）。
    2. 取りまとめ役は `forward.js` の中（今の試作の `phase()`）。**刻む形だけ**（均等は捨てる）。塊の大きさは行数 ÷（本数 × 2）以上。段の書き換えは奇数の世代で守る（試作どおり）。起こすのは塊の数まで。
    3. 検索: 出発点 `hardwareConcurrency`、比べるのは「最良・候補・候補・最良」の塊で切り替え直後の 1 トークンは捨てる、5% 以上速いときだけ動く、半分と倍を試して止まる。答えは `localStorage`（鍵は `hardwareConcurrency`・`deviceMemory`・UA）に覚え、生成 8 回に 1 回だけ隣を測り直す（回数は測って決めてよい）。**ステータス行に本数を出す**（「SIMD kernels, int8, relaxed SIMD, 4 threads」）。`?threads=N` で固定できるようにする（計測用）。
    4. 確かめ方: 出力トークンが 1 本のときと一致（int8 の加算順は塊の切れ目で変わらない: 各行は 1 スレッドが丸ごと計算する）。速度は `browsers.yml` の OS ジョブで、ステータス行の本数と tok/s を JSON に残す。
  - **段階 3: Service Worker と見出し。** `public/coi.js`（`coi-test/sw.js` を元に、範囲はサイト全体、`require-corp`）。ページは起動時に登録し、`navigator.serviceWorker.controller` が無ければ 1 回だけ再読み込み（`sessionStorage` で回数を守る）。**Pyodide の CDN と HF 以外の外部の読み込みが無いことを確かめる**（フォントなど。あれば CORS を確かめる）。`?v=` を付ける。登録に失敗しても 1 本で動く。方針 2 と 6 の書き換え、`index.astro` の見出し（持ち主が決めた文面）、AGENTS.md の構成の表と gist の `10-` の更新。`browsers.yml` に「isolation が真になったか」と本数を記録。
  - 各段階の完了条件: 段階 1 は tiny-lm と llm-jp の tok/s が Python 版より上がっていること（新旧交互）。段階 2 は CI の Linux ARM で 4 本が段階 1 の 1.5 倍以上。段階 3 は本番で `crossOriginIsolated` が真になり、23 通りのブラウザで動くこと。
- **段階 1a（2026-09-25、Opus 5.5）: JS の forward と、エンジンのテンソルの表。** 仕様からの変更: 段階 1 を 2 つのコミットに分けた（1a は forward と表と確認、1b は Worker の読み込みの経路と `kernel_forward` の退役。変更が大きく、Worker はこの開発機で試せないため）。Pyodide の `WebAssembly.Memory` を借りる案は、Pyodide がそれを公開していない（内部の名前に頼ることになり、最新版を使う方針と合わない）ので採らず、仕様どおり別のメモリに置く。
  - `Llama(None, tokenizer, external=…)`: 重みを読まず、`take()` が位置と形だけを `Tensor` として記録する（**レイアウトの写しを JS に作らない**）。計算で作る小さな配列（int8 の RoPE の表、GPT-2 のゼロの表）と外れ値チャネル（T92）の選択は Python が作って渡す。ファイルの大きさがヘッダと合わなければ断る。`tests/test_external.py` が 4 アーキテクチャ × 3 dtype で、表の指す中身が NumPy の読み込みと同じことを見る。
  - `public/forward.js`: エンジンの `kernel_forward` と同じ順に同じカーネルを呼ぶ。float16 と「int8 を外したとき」の int8 は読み込み時に float32 へ広げる（`Math.fround` で NumPy と同じ積）。relaxed の補正も同じ式。KV キャッシュは倍々に伸ばし、伸ばすたびに古い場所へ詰め直す（メモリを 2 倍にしない）。カーネルは `kernels/build.py` の素の版（`simdkernel_plain.wasm`、`simdkernel_relaxed_plain.wasm`）。
  - **確かめたこと（`node tests/forward-check.mjs`、Node 上の Pyodide、64 位置の greedy）: logits がエンジンと 1 ビットも違わない**。サイトの 6 モデル（stories260K・stories15M・tiny-lm・llm-jp-3 150M の int8 と float32、tiny-lm と stories15M の原本の float16 / float32）、T85 で変換した GPT-2（外れ値チャネルあり）・Pythia 160M（NeoX）・rinna GPT-2・GGUF の SmolLM2、合成の Qwen2（bias あり、int8 と float32）、tiny-lm の `without relaxed` / `without int8` / `without sampler`。デプロイの試験にも入れた（サイトの 4 モデル）。
  - **速さ（新旧交互、エンジン = Python の `kernel_forward`）**: tiny-lm 452 → 534（1.18×）、llm-jp 90 → 104（1.15×）、stories15M 414 → 523（1.26×）、stories260K 1869 → 3559（1.90×）、GPT-2 1.19×、Pythia 1.15×、rinna 1.19×、SmolLM2 1.24×。float32 の原本はほぼ同じ（0.99〜1.06×。帯域が律速で、Python の分が小さい）。**段階 1 の完了条件（tiny-lm と llm-jp が上がる）は Node で満たした**。ブラウザでの数字は 1b の後に CI で。
- **段階 1b-i（2026-09-25、Opus 5.5）: Worker を JS の forward に切り替えた。** 重みの置き場は `weightsBuffer(size)` の 1 か所: カーネルが使えれば `forward.js` のメモリ、`?without=kernels` か WebAssembly SIMD の無い環境なら今までどおり Python の bytearray（NumPy のエンジン）。サイトの部品・`?checkpoint=`・手元のファイル・変換のキャッシュの 4 つは書き先を差し替えただけ。**HF の変換は、変換器に書き先（`sink`: `open(size)` と `write(offset, bytes)`）を渡し、JS のメモリへ直接書かせる**（`Writer` と `Stream` と `Conversion` に `sink`）。Python のバッファを経由すると、Pyodide のメモリは縮まないので、モデル 2 つぶんを持ち続けることになる。確かめたこと: Node の Pyodide で Worker と同じ流れ（SmolLM2 の safetensors と GGUF → 書き先 → JS の forward）を通し、JS のメモリのチェックポイントがネイティブの変換とバイト単位で同じ、Python には何も残らない、答えが「The capital of France is Paris.」。`tests/test_convert.py` に書き先の試験（float32・float16・int8 で同じバイト）。ヒープの報告（T90・T84）は Pyodide のメモリ + 重みのメモリ。モデルを手放すときは `Llama.release()`（JS が握る Python の配列を返す）。**実ブラウザは未確認**（この開発機では動かさない）: デプロイの後に `browsers.yml` で確かめる。`kernel_forward` はまだ残っている（1b-ii で退役）。
  - **1b-i の実ブラウザでの確認（`browsers.yml` の実行 36032774754）: 全部動いた。** OS のジョブ 5 つ（23 通りのブラウザとインストール済みの Firefox、それぞれ 4 モデル）はすべて成功。HF の 14 モデルも全部動き、T86 の固定値も 4 つとも一致。前回の走行（切り替えの前、36003988306）と比べて、Windows x64 のランナーで tiny-lm が Chromium 326 → 406・Chrome 371 → 455・インストール済みの Firefox 357 → 452 tok/s、llm-jp-3 150M が 74.7 → 101.9・90.5 → 97.5・78.6 → 92.0 tok/s。HF のモデルは 7〜12% 速く、stories260K は 2338 → 3801 tok/s（ランナーは共有の仮想機なので 1 回ずつの比較は目安）。**失敗は `?bench=1` の 1 ステップだけで、「TypeError: network error」**。前回（切り替えの前）も同じ失敗だったので今回の変更とは別の既存の不具合（T45 のベンチの経路。未調査）。
- **段階 1b-ii の一部（2026-09-25、Opus 5.5）: Node の道具をページと同じ経路へ。** `tests/engine.mjs` の `pyodideWithEngine()` が、Python に `kernel_llama(checkpoint, tokenizer, **options)`（JS の forward のエンジン。`disable` に `kernels` があれば NumPy）を渡す。`smoke.mjs`（float32 は NumPy と一字一句、GPT-2 と NeoX、スイッチ、GQA、KV の伸長まで全部通った）・`perplexity.mjs`（tiny-lm の 7 ビットの行が NumPy 比 +0.36%、以前と同じ水準）・`compare-engines.mjs`（コミット済みの `llama2_numpy.py` と `forward.js` の組と、作業中の組を交互に。`forward.js` は data: URL で読み込むので一時ファイルは要らない）を移した。**`kernel_forward` の退役は段階 2 の終わりに回す（仕様からの変更）**: スレッドの試作（`tests/threads-prototype/run.mjs` と `threads.yml`）が、エンジンが Python の中に int8 のまま持つ重みを共有メモリへ写して動いているので、いま退役させると捨てる予定の試作を書き直すことになる。段階 2 でソフトウェアスレッドを `forward.js` に組み込めば試作は役目を終える。それまでは `forward-check.mjs` が「JS 版と Python のカーネル版がビット単位で同じ」を見続けられる（退役したら比べる相手を NumPy に替える）。プロファイラ（`profile.mjs` / `profile_token.py`）も Python のカーネル版の中身（ctypes の費用）を測る道具なので、退役と同時に JS 版に書き直す。
- **ほかに JS へ移して速くなる所の検討（2026-09-25、Opus 5.5。持ち主の判断: 1.3 倍に届かないものは見送る）。** 1 トークンを分けて測った（Node、ms/トークン）: tiny-lm は JS の層 0.49・分類器と logits のコピー込み 1.77・生成 greedy 1.87・サンプリング 2.03、llm-jp は 5.14・9.49・9.59・9.94、stories15M は 0.87・1.80・1.91・1.95。forward の外は Python の生成ループと復号が約 0.1ms（tiny-lm で 5%、llm-jp で 1%）、サンプリングが 0.04〜0.35ms（計算はもう WASM）、logits のコピーが 0.02〜0.03ms。**生成の途中を JS に移しても数 %** で、しかも承認した見出しの「素の Python」の部分なので**移さない**。生成の外の候補も**どれも 1.3 倍に届かない見込みなので見送る**: (1) 読み込み時の int8 の補正をカーネルに: 段階 1 で tiny-lm の準備が 163 → 351ms（JS の素のループ）になったが、準備完了約 4 秒の中の 0.2 秒で 1.05 倍未満（llm-jp は 460 対 440ms でほぼ差なし）。(2) prefill のまとめ処理（T49）: 1 スレッドの int8 の行列積は演算が律速（T53）なので、まとめても重みを読む回数しか減らない。prefill を 0 にしても応答全体で llm-jp 1.13 倍、Qwen2.5 0.5B で 1.2 倍程度が上限。スレッドで帯域が頭打ちになる段階 2 の後は効き始めるが、上限は同じ。(3) 変換の量子化をカーネルに（T89）: 変換が準備完了の 3 割を超えていれば 1.3 倍に届きうるので、T84 の数字で決める（T89 に書いてある条件のまま）。**生成の tok/s を大きく動かすのは段階 2 のスレッドだけ**（CI で 1.5〜2.2 倍）。
- **段階 2a（2026-09-25、Opus 5.5）: `forward.js` に共有メモリとソフトウェアスレッド。** `weightsMemory(size, { shared: true })` は先頭 4 KiB を制御の領域にした共有メモリ（最大は 4GB から断られたら小さく）。行列積は「同じ入力を読むものを 1 つの段」にまとめ（q・k・v、w1・w3）、入力の量子化は取りまとめ役が 1 回だけ、行の塊を取りまとめ役とソフトウェアスレッドが取り合う（試作の刻む形、1 スレッドあたり 4 塊）。`public/helper.js` がブラウザの Worker と Node の worker_threads の両方で動くソフトウェアスレッド。`setThreads(n)` で次のトークンから n 本（足りないソフトウェアスレッドを起こす）、`stopThreads()` で終わり。**見つけて直した不具合**: ソフトウェアスレッドが全員 1 つの番号で待ち、取りまとめ役が人数だけ起こす形だと、一度 8 本ぶん起こした後に 2 本で走らせると、段ごとに「いちばん長く寝ていた」別のソフトウェアスレッドが冷えた状態（ときに小コア）で起こされ、同じ 2 本が 1.5ms と 8ms を行き来した（試作は本数ぶんしか作らないので出なかった）→ **ソフトウェアスレッドごとに自分の待ち番号を持たせ、使う本数ぶんを名指しで起こす**。もう 1 つ: 取りまとめ役を Pyodide の居るメインスレッドに置くと待ち合わせが遅れる（tiny-lm 4 本で 1 本比 0.51〜0.66×、Worker に置くと 0.85×）。ページでは取りまとめ役はモデルの Worker なので、`tests/threads-check.mjs` も forward を Worker で走らせる形にした。**確かめたこと**: どの本数でも logits が 1 本とビット単位で同じ（tiny-lm・llm-jp、1〜8 本）。**速さ（この開発機、Worker の形、別の作業が動いていて揺れが大きい）**: tiny-lm 2 本 1.12〜1.14×・4 本 0.83〜0.85×、llm-jp 2 本 1.11×・4 本 1.19×・8 本 0.96×。同じ時の試作は tiny-lm 4 本で 1.21×。本当の効きは静かな CI のランナーで測った（`threads.yml` の実行 36039250118、128 位置、5 回の中央値。倍率は段階 1 の JS の forward 1 本が基準）。**どこでもどの本数でも logits が 1 本とビット単位で同じ。**

  | ランナー | モデル | 1 本 | 2 本 | 4 本 | 8 本 | 16 本 |
  |---|---|---:|---:|---:|---:|---:|
  | Linux ARM（4 vCPU） | tiny-lm | 890 tok/s | 1.34× | **1.79×** | 1.54× | 1.09× |
  | Linux ARM | llm-jp-3 150M | 128 tok/s | 1.43× | **1.87×** | 1.71× | 1.49× |
  | Linux x64（AMD EPYC、4 vCPU） | tiny-lm | 757 tok/s | **1.44×** | 1.43× | 1.32× | 1.14× |
  | Linux x64 | llm-jp-3 150M | 129 tok/s | **1.49×** | 1.46× | 1.32× | 1.16× |
  | macOS（M1 の仮想機、3 vCPU） | tiny-lm | 1656 tok/s | 1.08× | 1.09× | 0.99× | 0.80× |
  | macOS | llm-jp-3 150M | 252 tok/s | 1.11× | 1.12× | 1.02× | 0.93× |

  **段階 2 の完了条件（Linux ARM で 4 本が段階 1 の 1.5 倍以上）は満たした**（1.79× と 1.87×）。最速の本数は機械で違う（ARM は 4、x64 は 2、M1 は 2〜4 でほぼ伸びない）ので、決め打ちではなく検索が要る（段階 2b）。vCPU 数を超えると下がるが、刻む形なので崩れない（16 本でも ARM と x64 は 1 本より速い。M1 だけ 0.80〜0.93×）。M1 は 1 本がもう速く（tiny-lm 1656 tok/s）、帯域に余裕が無いと見ている（未確認）。ページでの有効化は段階 2b（本数の検索と Worker への組み込み）と段階 3（Service Worker）の後。
- **段階 2b の検索（2026-09-25、Opus 5.5）。** `forward.js` の `findThreads({ from, remembered, recheck, chose })` と `newGeneration()`: 生成のトークン（logits を作るもの）だけを測り、「最良・候補・候補・最良」を 4 トークンずつの塊で比べ（切り替え直後の 1 トークンは捨てる）、候補が 5% 以上速いときだけ移る。出発点から半分を試し、半分が速くなければ倍を試し、速くなくなったところで止まる（上限の定数は無い）。足りないソフトウェアスレッドは裏で起こし、それまでは今の最良で測らずに進む。覚えた本数があれば検索せず、生成 8 回に 1 回だけ隣と比べ直す。ソフトウェアスレッドは起動直後に制御の領域の空きで小さな行列積を 4000 回ずつ回して温める（起こしたばかりは最適化前のカーネルで走り、検索がそれを本数の遅さと取り違えるため）。**CI の結果（`threads.yml` の 2 回）**: 1 回目は Linux ARM で 2 回とも 8 本を選び、最速の 4 本の 83% と 93% だった。比較の記録を足した 2 回目（実行 36041465061）は 6 通りすべてで最速の 93〜100%（ARM は 2 モデルとも 4 本で 100%、x64 は 4 本 95% と 2 本 100%、M1 の仮想機は 1 本で 93% と 95%）。ARM の「4 本 対 2 本」は 0.57 対 0.83ms/トークンとはっきり差が出る一方、M1 の仮想機は 1 トークンが 10ms まで跳ねることがあり、揺れの大きい機械では 1 回の検索がずれうる（覚えた本数と時々の測り直しで後から直る）。
- **段階 2b の組み込み（2026-09-25、Opus 5.5）: Worker とページ。** Worker は `crossOriginIsolated` が真のときだけ共有メモリ用のカーネル（`simdkernel_shared.wasm`）を読み、重みを共有メモリに置き、ソフトウェアスレッドを `helper.js` の module Worker として起こす（入れ子の Worker）。共有メモリが取れなければ 1 本で動く。モデルができたら準備完了の前に本数を決める（`startThreads`）: `?threads=N` なら固定、ページが覚えている本数（`localStorage`、鍵はモデル・論理コアの数・`deviceMemory`・UA）があればそれで始めて生成 8 回に 1 回だけ隣と比べ直し、無ければ論理コアの数から検索して、答えをページに知らせて覚えさせる。生成ごとに `newGeneration()`。ステータス行と回答の下の行に、2 本以上のときだけ「, N threads」「· N threads」（英語。既存の表示と同じ体裁）。**まだ働かない**: ページが cross-origin isolated になるのは段階 3（Service Worker）から。それまでは今までどおり 1 本で、CI の実ブラウザでもそれを確かめる。
- **段階 3（2026-09-25、Opus 5.5。持ち主の判断: 効果が大きいので既定でオン）: Service Worker と見出し。** `public/coi.js` がサイト全体で応答に COOP と COEP（`require-corp`）を足す。ページは Worker を起こす前に、まだ isolated でなければ登録して 1 回だけ再読み込みする（`sessionStorage` で 1 回に抑える。登録に失敗したら、あるいは再読み込みしても isolated にならなければ 1 本で動く）。`?coi=off` で解除して 1 本。見出しは承認済みの文面（日英）。方針 2 と 6 を承認済みの文面に書き換えた。CI の 3 つのテスト（e2e・ベンチ・インストール済みの Firefox）は初回の再読み込みに耐える形にし、e2e は isolated になったかを記録する。gist の `10-` の更新は外向きの操作なので T83 に回す。**段階 2b の確認の走行（36042479744）**: OS のジョブ 4 つ（Linux x64・Linux ARM・macOS・Windows ARM）と HF の大きい組は成功（今までどおり 1 本で動く）。HF の小さい組は `?bench=1` の既存の「network error」で失敗（固定値は 4 つとも一致）。windows-latest は T70 の WebKit の止まりに当たって 43 分を超えたので止めた。
- **段階 3 の実ブラウザでの確認（`browsers.yml` の実行 36047994839）: ソフトウェアスレッドは本番で働いた。** Chromium・Firefox・WebKit のどれでも isolated になり、ステータス行に「4 threads」、回答ごとに検索が選んだ本数（2 か 4）が出た。**Qwen 系（Linux x64 のランナーの Chromium。持ち主の希望で取ったベンチ）**:

  | モデル | 段階 1 の前（36003988306） | 段階 1（36032774754） | 段階 3（36047994839） | 伸び |
  |---|---:|---:|---:|---:|
  | Qwen2.5 0.5B Instruct | 26.0 tok/s | 27.8 | **41.1** | 1.58× |
  | Qwen2.5 Coder 0.5B Instruct | 26.5 | 27.6 | **39.6** | 1.49× |
  | Qwen2.5 1.5B Instruct | 9.0 | 9.3 | **16.1** | 1.79× |
  | DeepSeek-R1 Distill Qwen 1.5B | 9.1 | 9.1 | **16.1** | 1.77× |

  ほかも同じ形: llm-jp-3 440M 36.9 → 57.5、llm-jp-3 980M instruct3 は 15 → 28.5、TinyLlama 1.1B は 12 → 24.0、Pythia 1.4B 18.9、llm-jp-3 150M は Linux x64 で 108 → 139・Linux ARM で 160 tok/s。**大きいモデルほど伸びる**（1B 級で約 2 倍）。準備完了までの時間は変わらない。**見つかった不具合 2 つ（直した）**: (1) `?bench=1` がメモリ不足で止まった: 共有メモリは最大を宣言して予約するので、4GB の最大でモデルを読み直すたびに予約が積み重なった → 最大を「チェックポイントを float32 に広げた量（int8 の 4 倍）+ 1GB」にし（4GB が上限）、モデルを手放すときに古いエンジンへの参照を全部切る。(2) Linux ARM の e2e が 1 件失敗: 再読み込みの途中でページにまだボタンが無く、待ちの条件が例外を投げた → 条件を例外を投げない形に。
- **`kernel_forward` の退役（2026-09-25、Opus 5.5）。** Python のカーネル版の forward を消し、forward は NumPy（`?without=kernels` と参照）と `forward.js`（ページ）の 2 つになった。`kernels=` はサンプリングのためだけ。int8 を int8 のまま持つのは `external` のときだけ（NumPy の forward は全部広げる。これを直さないと NumPy の forward が広げていない int8 を受け取る）。`tests/forward-check.mjs` の比べる相手を NumPy にした。**線は実測から決めた**（128 位置、NumPy の greedy の文を両方に読ませる）: float32 は最尤トークンが全位置で同じで logits の差 1e-3 以下（実測 1.7e-5〜3.7e-5。足し算の順が違うので最後のビットはずれる）、int8 は最尤トークンの一致 85% 以上・perplexity の差 5% 以内（128 位置での実測 stories15M 97.7%・+0.52%、tiny-lm 100%・−0.23%、llm-jp 93.8%・+2.00%。活性値を 7 ビットに丸めるぶんの差。64 位置では llm-jp が 87.5%・+3.42% と揺れたので、位置は 128 に固定し、線に余裕を持たせた。本当に壊れたときは一致がほぼ 0、perplexity が何倍にもなる）。`tests/profile.mjs` と `profile_token.py` は Python の forward の中身（ctypes の費用）を測る道具だったので、`forward.js` のカーネルの呼び出しを包んで時間を足し上げる形に書き直した（旧版の「空の関数に差し替えた差」の方式は、出力が NaN になって後の計算が遅くなり、マイナスの割合が出たので採らなかった）。`forward.js` に計測用の `wrap` を 1 つ足した。スレッドの試作（`tests/threads-prototype/`）と `threads.yml` の試作の部分を消した（数字はこの項に残っている）。 **誤り**: 旧プロファイラの 2 ファイル（`tests/profile.mjs` の旧版と `tests/profile_token.py`）の削除は、`git rm` で登録したまま T83 の台帳の変更をコミットしたので、T83 のコミット `d144c75` に入ってしまった。push 済みなので履歴は書き換えない。
  - （退役のときにやること）`kernel_forward` を消し、それに頼る道具（`smoke.mjs`・`perplexity.mjs`・`profile.mjs`・`compare-engines.mjs`・`threads-prototype/run.mjs`・`forward-check.mjs`）を JS の forward に移す。`forward-check.mjs` の比べる相手は NumPy に（float32 は出力の文が一字一句、int8 は線の内側）。
  - （もとの予定）1b でやること: Worker の読み込みの経路（サイトの部品・キャッシュ・HF の変換・手元のファイル・`?checkpoint=`）を JS のメモリへ向け、`kernel_forward` を消し、`forward-check.mjs` の比べる相手を NumPy に変える（float32 は出力の文が一字一句、int8 は線の内側）。
- 関係: **T71 はこの判定の後**（`SharedArrayBuffer` が取れるなら T71 の postMessage 方式は要らない。取れないなら T71 の分類器だけの分け方に戻る）。T47（層ごとに 1 回のカーネル呼び出し）は、共有メモリの設計と相性がよいので、そのとき一緒に見直す。
- 完了条件: 経路が 1 つに決まり（持ち主の判断つき）、tok/s が 1 コアより有意に上がることを新旧交互に測って示す。上がらなければ理由を AGENTS.md に書いて取りやめる。方針 6 を変えたなら AGENTS.md の方針も直す。

</details>

- [x] **T89 [性能] 変換の速さ。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。**律速の判定（T84 の仕組みで記録した CI の数字、実行 36047994839、Linux x64 の Chromium）**: ブラウザの中の変換（Python）は、0.5B 以上のモデルで準備完了の 32〜45%（Qwen2.5 1.5B は 54.2 秒のうち 24.1 秒、Qwen2.5 0.5B は 22.8 秒のうち 9.4 秒）、200M 以下で 13〜29%。変換の 65〜82% は `quantize()`（T89 の下調べ）。**やったこと**: カーネルの `quantize_x` を SIMD にした（32 個を 8 本の f32x4 で: abs・max・割り算・掛け算・偶数丸めはどれもスカラーと同じ IEEE の計算なので、結果は 1 ビットも変わらない）。これは変換器の `quantize()` とまったく同じ計算（グループ 32、スケールは最大値 / 127、1/スケールを掛けて偶数丸め）なので、**新しいカーネルは足さず**、変換器に `quantize_rows=`（`Writer`・`Stream`・`Conversion`・`convert_weights` に 1 つ通した。既定は NumPy のままでビルドは何も変わらない）を足して、Worker がカーネル版（`llama2_numpy.kernel_quantizer()`）を渡す。行の長さが 32 の倍数でないとき（グループが 32 未満）は NumPy 版。**確かめたこと**: Pyodide の中で、カーネル版と NumPy 版の int8 とスケールが 1 バイトも違わない（ゼロだけのグループ・外れ値・relaxed の bias 64 も元のスカラー版と同じ）。GPT-2 と NeoX の int8 の変換結果がカーネル版と NumPy 版で同じ（smoke）。pytest（32 の倍数の行だけがカーネル版に渡り、結果は同じ）。**速さ（この開発機、Node 上の Pyodide）**: `quantize` だけで 6.0〜6.3 倍（4864 × 896 の行列で 35 → 5.9ms）。変換全体（取得なし、同じプロセスで交互）で SmolLM2（bf16、269MB）1.67 → 0.70 秒（2.38×）、Pythia 160M（f16、375MB）1.87 → 0.75 秒（2.48×）、GPT-2（f32、548MB）2.08 → 1.03 秒（2.03×）。**実ブラウザでの結果（`browsers.yml` の実行 36051814069 と、T89 の直前の 36047994839。Linux x64 のランナーの Chromium）**: ブラウザの中の変換は **3.4〜4.3 倍** 速くなった（Qwen2.5 1.5B 24.1 → 7.1 秒、Qwen2.5 0.5B 9.4 → 2.2 秒、llm-jp-3 980M 15.3 → 4.3 秒）。準備完了は Qwen2.5 1.5B 54.2 → 39.3 秒（1.38×）、TinyLlama 1.1B 41.4 → 31.2（1.33×）、Qwen2.5 Coder 0.5B 24.6 → 19.4（1.27×）、Qwen2.5 0.5B 22.8 → 18.6（1.22×）、SmolLM2 360M 21.1 → 15.9（1.33×）。ただし llm-jp-3 980M は 1.11×、Pythia 1B は 1.04×、DeepSeek-R1 1.5B は 0.95×（63.5 → 66.8）と、**変換が縮んだぶん取得（huggingface.co から）が律速になり、取得の揺れがそのまま出る**。21 モデルで 0.94〜1.38 倍、多くは 1.1〜1.3 倍。生成の活性値の量子化も同じ関数なので、生成もわずかに速くなりうる（未計測）。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: OK。** ビット単位の一致の根拠は成り立つ: abs・max・割り算・掛け算・偶数丸めはどれも IEEE の同じ演算で、`trunc_sat` とスカラーの `<i32>` が違うのは範囲外と NaN だけ、`|x| ≤ amax` かつ `scale = amax / 127` なので範囲外は起きない（bias 64 でも最大 127）。差が出るのは入力に NaN があるときだけで、NumPy の `quantize()` も NaN を壊すので、どの経路でも壊れている（カーネルのコメントに一言足した）。`n` が 32 の倍数でない呼び出しは無い（`forward.js` は `plan.int8` のときだけで、それは `suitable` が条件。`Writer` は `shape[-1] % 32 == 0` のときだけ渡す）。ネイティブのビルドは触っていない。smoke の GPT-2 と NeoX の比較は全行列が 32 の倍数の行なので実質的。
<details><summary>T89 の着手前の記録</summary>

**着手前の見出し**: T89 [性能] 変換の速さ（1.5B 級で数分かかる）— 状態: **進行中（変換の内訳まで測った。律速の判定は T84 の CI の数字で）**（2026-09-24 採用、Fable の提案。**T84 の数字を見てから**。規模 中）
- 担当: Opus（計測と実装）→ Fable がレビュー（新旧交互の数字を見る）。
- 根拠: Qwen2.5 0.5B は準備完了まで 108 秒、1.5B 級はその 3 倍の見込み（未計測）。T82 の JSON で「取得」と「変換」の内訳が出るので、**どちらが律速か**がまず分かる。変換なら Python 側（bf16 の広げ方、`quantize()` の NumPy、`Writer.put` のコピー）に手が入る。取得なら並列数と部品の大きさ（いま 8 MiB × 6）。
- 手順: (1) T84 の内訳で律速を決める。(2) 変換が律速なら `tests/profile.mjs` の要領で `Stream.feed()` の時間を関数ごとに測る。候補: `quantize()` をカーネルにする（グループ 32 の最大値と丸め。AssemblyScript で 30 行）、bf16 → float32 の広げ方を `view` でやる、`Writer.put` の `ascontiguousarray` を減らす。(3) 新旧を交互に測る（落とし穴）。
- 完了条件: 1.5B 級の準備完了が測って記録され、律速が名指しされている。手を入れたなら前後の数字。
- **(2) を先に測った（2026-09-24、Opus 5.5、`node tests/profile-convert.mjs`、この開発機の Node 上の Pyodide、取得なし）**: SmolLM2 135M（bf16、269MB）は Python の変換 3.2 秒、Pythia 160M（f16、375MB）3.5 秒、GPT-2（f32、548MB）3.3 秒。**入力で毎秒 80〜170MB**。内訳は `quantize()` が 65〜82%（その大半は NumPy の `max` の reduce と `astype`）、bf16 の広げ方は 0.5 秒（SmolLM2）、`Writer` の書き込みはどれも 0.1 秒未満。ブラウザでは SmolLM2 の準備完了が 39.2 秒だったので（T63）、**この開発機では変換は 1 割未満で、律速は取得**と見るのが自然。ただしブラウザの中の変換時間は未計測（T84 の仕組みで `seconds.convert` が CI の JSON に出る）。**見立て**: `quantize()` をカーネルにしても縮むのは数秒で、手を入れる価値は T84 の数字で変換が 3 割を超えたときだけ。取得が律速なら、効くのは並列数と部品の大きさ（いま 8 MiB × 6）か、GGUF（T74、取得が半分）。

</details>

- [x] **T71 [性能] 重みを行で分けて複数の Worker で計算する。** **完了（やらない、2026-09-25、持ち主の判断）**: 「重みを行で分けて Worker ごとに複製し、postMessage で分ける」案は、T93 で GitHub Pages のまま `SharedArrayBuffer` が取れるようになり（Service Worker）、重みを共有したままソフトウェアスレッドで分けられたので要らなくなった。往復の実測（0.10〜0.16ms）は AGENTS.md に残す。
<details><summary>T71 の記録</summary>

**当時の見出し**: T71 [性能] 重みを行で分けて複数の Worker で計算する — 状態: **着手前の判定は済（2026-09-24、Fable）。ただし T93 の判定の後**（2026-09-21 採用。持ち主の発案。`SharedArrayBuffer` が取れるなら不要になる）
- 担当: **着手前の判定（往復の費用）は Fable** → 見合うなら実装は Opus → Fable がレビュー（2026-09-24 に「Fable が調べ直してから実装」を役割で分けた）。
- 背景（2026-09-21 の計測、AGENTS.md に詳しい）: バッチ 1 の int8 は 1 バイトあたり 1 積和なので、1 コアの演算（大コア 1 つで 11.9 G 積和/秒）とメモリの上限（21〜24GB/s）の比がそのまま上限になる。**この機械では約 2 倍まで伸びる**（大コア 2〜3 つ、または大コア 4 つにスレッド 8 本で 23 前後）。いまは 1 コアしか使っていないので、その 2 倍ぶんを取りにいく話。
- ブラウザでの制約: `SharedArrayBuffer` は使えない（GitHub Pages が COOP/COEP を送れない。方針 6）。だから**メモリを共有せず、重みを行で分けて Worker ごとに持たせる**（合計メモリは増えない。ただし Worker ごとに Pyodide と NumPy がもう 1 つ要るので、その固定費が乗る。未計測）。やり取りは `postMessage` のコピーで、送るのは活性値（dim ぶん、数 KB）、返るのは部分結果。
- 効きどころ: 1 トークンに行列積は約 73 回あり、全部を分けると往復が 73 回 × Worker 数になって費用負けする見込み。**いちばん大きいものだけ分ける**: llm-jp-3-150m では分類器が 1 トークンの 38%（語彙 99584 × dim 512、int8 で 51MB を毎トークン読む）。ここだけなら往復は 1 トークンに 1 回で済む。見積もりでは分類器が 4.3ms → 2.2ms になり、全体で 15% ほど（未計測）。層の行列積（52%）も分けるなら往復が増えるので、測ってから決める。
- **着手の前の判定（30 分）**: Worker 間の `postMessage` の往復に何 ms かかるかを、数 KB のペイロードで測る。1 回が 0.2ms を超えるなら分類器だけでも見合わない可能性が高いので、そこで打ち切って持ち主に相談する。この機械（8 コア）と、できれば CI の Linux ランナーでも測る。
- 注意: **大小のコアに均等に割ると 2 つの大コアより遅い**（AGENTS.md）。ブラウザからはコアを選べないので、Worker の数は 2〜3 から試す。速度はスマホ級の ARM とデスクトップで違う見込み（デスクトップは帯域に余裕があるので伸びしろが大きい）。
- 調べ直す範囲（Fable、着手時）: ここに書いた見積もりは 2026-09-21 の計測からの推定で、往復の費用も Worker ごとの固定費も未計測。数字は鵜呑みにせず測り直すこと。分け方（分類器だけ / 層の行列積も / 層で分ける）の比較もそこで決める。
- 完了条件: llm-jp-3-150m（または tiny-lm）の tok/s が、Worker なしと比べて有意に上がることを新旧交互に測って示す。上がらなければ理由を AGENTS.md に書いて取りやめる。メモリの増えぶんも測って書く。
- **着手前の判定（2026-09-24、Fable、この開発機の Node の worker_threads。SharedArrayBuffer なし、`postMessage` の転送）**: 活性値（dim 個の float32）を N 個の Worker に配り、logits の分担（語彙 / N 個の float32）を集める往復 1 回が、llm-jp の形（dim 512・語彙 99584）で 2 Worker 0.15ms・3 で 0.16ms・4 で 0.20ms・8 で 0.29ms、tiny-lm 級（dim 768・語彙 32000）で 2 Worker 0.10ms・3 で 0.13ms・8 で 0.24ms。**線（0.2ms）の内側なので進める。** ブラウザの `postMessage` は未計測（同じ仕組みなので同程度と見る）。
- **見積もりの更新（往復の実測を入れて）**: (a) **分類器だけ**分ける（往復は 1 トークンに 1 回）: llm-jp は分類器 4.5ms（1 トークン 12ms の 38%）が 3 スレッドで約 1.5ms + 往復 0.16ms になり、**1 トークン 12 → 9.2ms（約 1.3 倍）**。tiny-lm は分類器 1.8ms（54%）が 0.6 + 0.13ms で **3.3 → 2.2ms（約 1.5 倍）**。(b) **層の行列積も**分けると、層ごとに 2 回の同期（attention の後と FFN の後）が要り、llm-jp は 24 往復 × 0.15ms = 3.6ms が乗る。計算は帯域の上限で 2 倍までしか縮まないので 12 → 約 6 + 3.6 = 9.6ms で、(a) と変わらない。**8 Worker で 1.8 倍は、この仕組みでは届かない**: 帯域の上限（大コア 2〜3 つで頭打ち）と、Worker が増えるほど往復が伸びること（8 で 0.29ms）の 2 つが壁。**狙うのは (a) の 1.3〜1.5 倍**。(a) は上限 2 倍の分類器の部分だけに効くので、分類器の割合が大きい小さいモデル（tiny-lm、GPT-2）ほど伸びる。
- **代償（未計測、見立て）**: Worker ごとに Pyodide + NumPy + エンジンの固定費（llm-jp のヒープ 283MB のうち重み 171MB を除いた約 110MB）が乗り、2 Worker なら +220MB。準備完了も Worker ぶん遅れる（Pyodide の読み込みは並列でき、HTTP キャッシュは共有）。スマホでは Worker を 0 にする（`deviceMemory` か `hardwareConcurrency` で決める）。
- **次の一手（Fable の 2 回目）**: Node で試作して測る。メインの Pyodide がエンジンを持ち、N 個の worker_threads がそれぞれ Pyodide を持って分類器の行の分担（int8 のまま）を `matmul_q8r` で計算する。tiny-lm と llm-jp で N = 0・1・2・3 の tok/s を新旧交互に測り、**1.2 倍を超えなければ取りやめて理由を書く**。この開発機のメモリ（空き約 1.2GB）では、llm-jp は N = 2 まで。

</details>
- [x] **T47 [性能] Python の呼び出し回数を減らす（層ごとに 1 回のカーネル呼び出し）— 状態: 保留（2026-09-19、持ち主の判断: 却下に近い保留）。** **完了（T93 で果たした、2026-09-25、持ち主の判断）**: 狙いは「Python からのカーネルの呼び出しを減らす」ことだった。T93 で 1 トークンの forward を丸ごと JS（`forward.js`）に移し、Python から forward への呼び出しは 1 トークンに 1 回になった（Python の層のループを外した効きは int8 で 1.15〜1.26 倍、T93 の段階 1a）。層ごとに 1 回のカーネルにまとめる案そのものは実装していない。
<details><summary>T47 の記録</summary>

**当時の見出し**: T47 [性能] Python の呼び出し回数を減らす（層ごとに 1 回のカーネル呼び出し）— 状態: 保留（2026-09-19、持ち主の判断: 却下に近い保留）
- 保留の理由: 既定モデルで +7%、tiny-lm で +8% のために、層全体のカーネルを 3 通り（float32 / `matmul_q8` / `matmul_q8r`）持ち、forward の経路を 2 本維持することになる。費用に見合わない。再検討するのは、ctypes の呼び出しがもっと高い環境が見つかったとき（T45 のベンチで実機の数値が集まったら見る。Playwright の Firefox は 1 回 24µs だが全体の 4%）か、層の数が多くて 1 トークンが軽いモデルを既定にしたとき。
- 目的: 1 トークンで約 100 回カーネルを呼んでいるのを、層ごとに 1 回（1 トークン 12 回程度）にする。「インタプリタのコストはいくらか」を数値で出すのは、この実験の核心。T53 の実測: ctypes の呼び出し + Python は llm-jp-3-150m で 1 トークンの 7%、tiny-lm で 12%、stories260K で 53%。効くのは、誰も待たされていない小さいモデルだけ。
- 効果の見積もり（2026-09-19、T54 の後のカーネルで `tests/profile.mjs` を取り直した実測から。Chromium、位置 16〜79）:
  - llm-jp-3-150m: 1 トークン 11.29ms のうち ctypes の呼び出し 0.72ms（231 回 × 3.1µs）+ Python と NumPy 0.09ms。呼び出しを 231 → 約 15 回にすると約 0.72ms 減って **+7%**（ページの 256 トークンで 75〜79 → 80〜84 tok/s）。位置が進むほど attention が増えるので割合は下がり、位置 4000 付近では +2〜3%。
  - tiny-lm: 2.32ms のうち 0.24 + 0.05ms。約 0.25ms 減って forward だけなら +12%、ページでは `generate()` と Worker のぶんがあるので **+8%**（295 → 約 318 tok/s）。
  - stories260K: 0.43ms のうち 0.21 + 0.03ms。forward だけなら約 2 倍、ページでは **+28%**（1000 → 約 1280 tok/s）。ただし誰も待たされていないモデル。
  - Firefox: ctypes の呼び出しは 1 回 24µs と 8 倍高いが、1 トークン（149ms）の 4% でしかない。**+4%**。Firefox が遅いのはカーネルの中（T59）。
  - 1 トークンを丸ごと 1 回の呼び出しにしても、層ごとからさらに減るのは 12 回ぶん（0.04ms）だけ。
- 費用: 中〜大。層 1 つぶんのカーネルが float32 / `matmul_q8` / `matmul_q8r` の 3 通り要る。`matmul_q8r` は別モジュール（relaxed SIMD の無いブラウザがモジュールごと拒否するため）なので、relaxed 側のモジュールに rmsnorm・rope・attention・swiglu を含む層全体の複製が入る。forward の経路が 2 本になり、両方をテストで維持する。
- 手順:
  1. 層 1 つぶんの処理（rmsnorm → q/k/v → RoPE → attention → o → 残差 → FFN）を 1 回の呼び出しにまとめたカーネルを足す。ポインタは層ごとの構造体（NumPy の配列に並べたアドレス）で渡す。「Python が層を順に呼ぶ」設計は保つ。float32 と int8 の両方。既存の細かいカーネルは NumPy との比較と GQA のテストのために残す。
  2. あり / なしを切り替える引数を `Llama()` に持たせて、効果を測れるようにする。T52 が先に入っていればそのスイッチの 1 つとして足し、T47 が先なら T52 がこの引数を一般化する（どちらが先でもよい）。
- 完了条件: NumPy と同じ出力（float32 は一字一句、`smoke.mjs` の既存の検査）。tiny-lm と stories260K の tok/s の変化を Node と Chromium で計測して記録する。速くならなければ、その数値を書いて却下に回す。

</details>
- [x] **T70 [運用] windows-latest の WebKit が tiny-lm で止まる。** **完了（T97 に合わせた、2026-09-25、持ち主の判断）**: Windows の WebKit が止まる件は、T82 の持ち時間（1 回 600 秒）で打ち切られて先へ進むようになった。2026-09-25 の走行でも Windows のジョブが長引いたので、Windows のブラウザの不具合としてまとめて T97 で調べる。
<details><summary>T70 の記録</summary>

**当時の見出し**: T70 [運用] windows-latest の WebKit が tiny-lm で止まる — 状態: 未着手・優先度低（2026-09-21、Opus。2 回とも再現。**持ち主の判断: 実機では動いていそうなので急がない**。CI の中だけの問題として後回しにする。週 1 回の実行が 90 分ぶん無駄になることは承知のうえ）
- 担当: Opus（T82 の仕組みで切り分けてから）→ Fable がレビュー。
- 症状: `browsers.yml` の `windows-latest` だけが、WebKit で tiny-lm を動かすところから先に進まず、ジョブの `timeout-minutes: 90` で打ち切られる。**2 回とも同じところ**（run 35461537794、1 回目 18:32:50Z→20:03:02Z、再実行 14:24:14Z→15:54:27Z）。ほかの 5 ジョブは 4〜11 分で成功する。
- 分かっていること: 同じジョブで chromium 148 と firefox 150 は 4 モデルすべて通る（chromium は stories260K 1652.5・stories15M 431.3・tiny-lm 419.5・llm-jp-3-150m 97.1 tok/s）。webkit 26.4 は stories260K（694.2）と stories15M（182.5）まで通り、次の tiny-lm で止まる。**T67 より前のページでは同じ組み合わせが 5.2 秒で準備完了していた**（run 35455487317、commit 8f8319b）。T67 で変わったのは既定モデルと `localStorage` の記憶なので、tiny-lm を選ぶ操作と自動で始まるロードが競合している可能性がある（未確認）。ほかの OS の WebKit では起きない。
- 手順の案: (1) `tests/e2e.mjs` の待ち時間（いまは 30 分と 10 分）をモデル 1 個ぶんに縮めて、止まったときに何が出るか（ステータス行、コンソール、`.error`）を Summary に残す。(2) その情報で、選択の競合なら `?model=` で開く形に変える。(3) 直らなければ windows-latest の WebKit だけ外し、理由を `kernels/README.md` の表に書く。
- 完了条件: `browsers.yml` が 90 分で打ち切られずに終わる。原因と対処を AGENTS.md に書く。

</details>

<details><summary>T74 の着手前の記録</summary>

**着手前の見出し**: T74 [追加] GGUF を取り込み元にする（取得が半分になる）— 状態: **未着手（突き合わせの手順は Fable が確定。実装は Opus）**（T77 は 2026-09-24 に完了。着手の条件の「突き合わせの手順を先に作る」を 2026-09-24 に Opus 5.5 が作った。**Fable が見るまで GGUF の読み手は書かない**。規模 中）
- 担当: Opus → Fable がレビュー。**着手前の判定（突き合わせの手順を先に作る）が条件**で、その手順自体を Fable が見てから実装に入る。
- 目的: いまは bf16 や float32 の原本を取ってからブラウザで int8 にしている（Qwen2.5 0.5B は 0.9GB 取って 545MB にする）。**GGUF の Q8_0 は 32 個ごとに fp16 のスケールで、うちの int8（グループ 32、float32 のスケール）とほぼ同じ形**なので、そのまま読めれば取得が半分で済む。
- やること（2026-09-21 に書き出した）:
  1. **ヘッダを読む**（魔法の 4 バイト、版、テンソル数、メタデータの数）。メタデータは型つきの key-value で**型が 12 種類**あり配列もある。パーサのいちばん大きい部分。
  2. **諸元を引く**（`general.architecture`、`llama.block_count`、`llama.embedding_length`、`llama.attention.head_count` など）。**`config.json` は要らなくなる。**
  3. **テンソル表を読む**（名前・次元・型・ファイル内の位置）。名前が HF と別物（`blk.0.attn_q.weight`、`ffn_down.weight`、`output_norm.weight`）なので対応表が要る。
  4. **中身を変換する**。Q8_0 は 32 個ごとに fp16 のスケールで、うちの int8（32 個ごとに float32 のスケール）とほぼ同じなので、スケールを広げて並べるだけ。F16 は既存の経路。
  5. **語彙を組み立てる**（`tokenizer.ggml.tokens` と `scores` か `merges` から `tokenizer.bin`）。**`tokenizer.json` も要らなくなる。**
- **着手の順番についての Opus の意見（2026-09-21）**: T78・T79・T45 は続けて処理して問題ない。**T74 だけは「できる」と「やるべき」が別**。理由は (1) **間違えてもエラーにならず文章だけ壊れる**（T72 で同じ形を踏み、ブラウザで日本語が壊れて初めて気づいた）、(2) 今日の積み増しで `llama2_convert.py` は 1100 行を超え、分岐が `arch`・`bias`・`rotary`・`parallel_residual` の 4 つ、変換の transform が 5 種類あるところに、さらに別のファイル形式を足すことになる。→ **やるなら「突き合わせの手順を先に作る」ことを着手の条件にし、できれば T77（Fable の見直し）の後。** ただし今日と同じやり方（実物のモデルで必ずブラウザまで確認する）を続けるなら、Opus が続けて処理すること自体に無理はない。
- **着手時に必ず確かめる落とし穴**: llama.cpp は変換のときに q と k の並べ替えを HF と逆向きにかける。**GGUF の中の並びが T64・T72 でやった並べ替えの前か後か**を実物で確認すること。ここを間違えると「動くが文章が壊れる」形で出る（T72 で踏んだのと同じ形）。
- 範囲: **Q8_0 と F16 だけ**。K 量子化（Q4_K など）には手を出さない。GGUF はヘッダにアーキテクチャと諸元を持つので `config.json` は要らないが、テンソル名が別（`blk.0.attn_q.weight` など）なので対応表が要る。
- 注意: 小さいモデルの GGUF は、llama.cpp 向けの大物ほど揃っていない。**着手前に「1.5B 以下で Q8_0 か F16 の GGUF を持つモデル」を数える**こと（未計測）。数が少なければ取りやめる。
- **再調査（2026-09-21、Opus。持ち主の指示）**: 状況は変わらず、**llm-jp-3-150m と 440m には GGUF が無い**（980m にはある）。日本語で Q8_0 があるのは `sarashina2.2-0.5b` で、そちらは素の safetensors でも取れる（→ T79）。GGUF 全体では上位 100 リポジトリのうち 78 が Q8_0 を出しており形式としては標準だが、**中身は safetensors でも取れるモデルの再配布**なので、増えるのは取得の速さだけ。**T74 はこの順位のままで良い。** ついでに分かったこと: 断られていた日本語の指示モデルは GGUF ではなく **1 ファイルだけの分割形式**が理由だった（→ T78）。
- 着手前の調査（2026-09-21、Opus。HF を検索して確認）: **見返りが偏っている。** Q8_0 の GGUF があるのは Qwen2.5-0.5B-Instruct（Qwen 公式）・SmolLM2-135M-Instruct・GPT-2・Pythia の派生。**llm-jp-3-150m と rinna/japanese-gpt2-small には見つからなかった**ので、このサイトが前に出している日本語モデルには効かない。実装自体は素直で、GGUF はヘッダに諸元と語彙を持つので `config.json` も `tokenizer.json` も要らず、Q8_0 は 32 個ごとに fp16 のスケールでうちの int8 とほぼ同じ形。テンソルは連番で並ぶのでいまのファイル順の変換に乗る。ただし KV の型が多く、パーサは 3 つの中でいちばん大きい。**着手前に日本語のモデルにも効くか見直し、効かないままなら順位を下げるか取りやめる。**
- 完了条件: GGUF から取った int8 と、原本から変換した int8 が同じ出力を出す（同じモデルで比べる）。取得のバイト数が半分になることを実測で示す。

- **突き合わせの手順（2026-09-24、Opus 5.5）。** `tests/gguf_check.py`。GGUF の読み方は**検査のための参照として別に書いた**（将来 `llama2_convert.py` に入る読み手とコードを共有しない。共有すると同じ間違いを両方がする）。2 つのモード:
  - `tensors <gguf> <HF のディレクトリ>`: GGUF の全テンソルを HF の名前に対応させて原本と比べる（相対誤差、q と k の並び、Q8_0 なら int8 の値が `quantize()` と同じ割合）。メタデータは `config.json` と、語彙は `tokenizer.json` と比べる。
  - `logits <out A> <out B> <テキスト>`: 変換済みの 2 つのチェックポイントの logits を比べる（最大の差、最尤トークンの一致率、それぞれの perplexity）。**T74 の完了条件はこれを「GGUF から作った int8」と「safetensors から作った int8」の間で取ること。**
- **実物で分かったこと**（SmolLM2-135M-Instruct。GGUF は bartowski の Q8_0、リビジョン `09816acd`、Apache-2.0。**公式の HuggingFaceTB には GGUF のリポジトリが無かった**）:
  - **q と k は、すでに llama2.c の並びに回して入っている**（誤差は回した並びで 0.57%、HF のままの並びで 139%）。→ GGUF の読み手は `permute_heads` を**かけない**。着手時の落とし穴への答え。
  - **Q8_0 の int8 の値は、うちの `quantize()` と 99.93〜99.96% 同じ**。違いはスケールが float16 か float32 かの丸めだけ。相対誤差は全テンソルで 0.7% 以下。
  - メタデータ 8 項目と語彙 49152 語・merges 48900 個がすべて一致。F32 は norm だけ、ほかは全部 Q8_0。`output.weight` は無い（埋め込みと共有）。
  - 大きさ: GGUF 144.8MB、safetensors 269.1MB（**取得は 54%**）。safetensors から作る int8 は 151.4MB。
  - 基準の値: 同じ safetensors から作った float32 と int8 の間で、300 トークン（英語版 Wikipedia）の最大の logit の差 1.66、最尤トークンの一致 96.3%、perplexity 10.81 対 10.74。**GGUF の int8 と safetensors の int8 の差はこれよりずっと小さいはず**（int8 の値が 99.95% 同じなので）。合格の線の案: 一致率 99% 以上、perplexity の差 0.2% 以内。**線は Fable が決める。**
- **Fable に見てほしいこと**: (1) 参照の読み手が仕様どおりか（型 12 種、配列、アラインメント、Q8_0 のブロック 34 バイト）、(2) 合格の線、(3) 取得元を再配布（bartowski）にしてよいか。
- **レビュー（Fable、2026-09-24）: 手順は OK、3 つ直して確定。** (1) 参照の読み手は仕様どおり（値の型 13 種のコード、文字列と配列の長さは u64、テンソルの次元は内側から、データはヘッダの後を `general.alignment`（既定 32）に揃えた位置から、Q8_0 は float16 のスケール + int8 × 32 の 34 バイト）。**版 2 と 3 だけを受け付ける assert を足した**（版 1 は数を u32 で持つ）。**Qwen2 の bias（`attn_q.bias` など）も名前を対応させ、q・k の並びの判定にかけるようにした**（Qwen2.5 0.5B が T74 のいちばん大きい見返りなので、読み手を書く前にそれも確かめられる）。(2) **合格の線**: 最尤トークンの一致 99% 以上、perplexity の差 0.2% 以内（`logits` が判定して exit 1）。根拠は、同じモデルの float32 対 int8 で 96.3% と 0.6% なので、読み手の間違い（並び、スケール）はこの線の外に大きく出る。(3) **取得元は再配布（bartowski、Apache-2.0、リビジョン固定）でよい**: 公式の GGUF が無いモデルが多く、中身は同じ重み。About の一覧には原本と GGUF の両方を出す（`LICENSES` に両方入れる）。**実装は Opus に渡す**: `Safetensors` / `Arrays` / `Stream` と同じ interface の別の source として（T77 の判断）、Llama と Qwen2・Q8_0 と F16 だけ。`permute_heads` はかけない。完了条件は `tests/gguf_check.py logits` が線の内側に入ること。**落とし穴**: `logits` は int8 のモデルを float32 に広げるので、2 つ同時に載せると SmolLM2 でも約 1.1GB になり、この開発機が 2 回落ちた → 1 つずつ載せて logits を取り置く形にした（1 つで峰 874MB、40 トークンで 14 秒）。

</details>
- [x] **T105 [追加] 2 ファイル以上に分割された safetensors を読む。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。形: 各シャードの見出しを「テンソルの領域を順につないだ 1 本のファイル」の見出しにまとめ（`llama2_convert.joined_shards()`、base 0）、Worker は各シャードを自分の base から順に流す。**`Stream` は 1 行も変えていない**（T105 の手順にあった「シャードごとに `Stream` を作って `Writer` を共有する」より小さい）。ビルド側（`convert_hf.py`）は `Shards`（複数の `Safetensors` を 1 つの読み手に）。1 シャードの経路（T78）もこの形に入った。試験は `tests/test_shards.py`（Llama・Qwen2・GPT-2 × 2・3 シャード × 順と逆順 × float32・int8 で、1 ファイルとバイト単位で同じ。重複と欠けたシャードは断る）。e2e に `hf:<owner>/<repo>@<revision>` の ID を足し、名前を渡したモデルだけを走らせる手動のワークフロー `models.yml` を作った（一覧に入れる前のモデルを試すため。T106・T107 でも使う）。**本番の実測**（`models.yml`、Linux の Chromium）: 2 シャードの Qwen2.5-3B-Instruct（bf16 で 6.2GB）は準備完了 88.7 秒（取得 75.2 秒、変換 13.4 秒）、7.8 tok/s（4 本）、ヒープ 4016MB（32 ビットの上限の 4GiB = 4295MB の手前）。回帰: 1 シャードの sarashina2.2 0.5B Instruct 33.7 秒・31.9 tok/s、単一ファイルの llm-jp-3 150M instruct3 10.9 秒・140 tok/s、GGUF の SmolLM2 7.8 秒・89.5 tok/s、stories260K。**分かったこと**: 小さいモデルはほぼ 1 ファイルで配られていて、2 シャード以上は 3B 級（bf16 で 6GB 超）か Qwen3（1.7B が 2 シャード）だった。3B 級の int8 はヒープが 4GB にほぼ届くので、**T105 で読めるようになったモデルの多くは T101（Memory64）が無いと載らない**。完了条件の「一覧から動く」は `?hf=` で確かめた。一覧に足すかどうかは T81 で決める（Qwen2.5-3B のライセンスは qwen-research で、非商用）。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: OK。** 確かめたこと: 見出しをつなぐ順とデータを流す順が同じ（名前順）、`data_offsets` はデータ領域の先頭からの相対なので `length = max(end)` でよい、`__metadata__` だけのシャードと空テンソルも通る、中止は 1 シャードずつ `await` するので効く、1 シャードの経路は以前と同じ base と size で `Stream` に入る、`convert_hf.py` の閉包は既定値で束縛している、試験は逆順（`lm_head` が先頭のシャード）も見ている。直したのは AGENTS.md の worker.js の行（8 MiB × 6・Cache API のままだった）。あれば良い（やらない）: ローカルのシャードが非対応なことをページで言う（T88 で）。
- [x] **T108 [性能] プロンプトをまとめて計算する（T49 の復活）。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。形: `forward.js` の 1 トークンの forward を「最大 `BATCH`（16）トークンを層ごとにまとめて通す `run()`」にし、1 トークンはその 1 つの場合にした（経路は 1 本）。行列積の仕事に「トークン数と間隔」の欄を足し（`JOB` 12 → 16）、行の塊（`BLOCK_BYTES` 16KB）ごとに全トークンぶんのカーネルを呼ぶ。**新しいカーネルは 0**。各 (行, トークン) は 1 トークンのときと同じカーネルの呼び出しなので、**結果はビット単位で同じ**。活性値は「トークンごとの枠」に置く（枠の中は今までの並び、次のトークンの枠が後に続く。間隔は全部 `S` の 1 つ）。配列ごとに 16 トークンぶんの塊を並べた最初の形は、tiny-lm で 1 トークンずつの生成が 3% 遅くなった（先頭どうしが 32KB 離れてキャッシュの同じ行を取り合う）。Python は `Llama.forward_many`（forward.js が `forwardMany` を持つときだけ）で、`generate()` はロジットの要らないプロンプトのトークンを `PROMPT_BLOCK`（64）ずつ渡す。**確かめたこと**: `tests/forward-check.mjs` に「同じ文を 1 つずつとまとめて（37 ずつ、BATCH とずらして、KV キャッシュは 8 から伸ばす）読ませて最後の logits が 1 ビットも違わない」を足した。stories260K（float32）・tiny-lm・llm-jp-3 150M・GPT-2・Pythia（int8、relaxed）と、relaxed なし（`matmul_q8`）で同じ。`tests/threads-check.mjs` も 1・2・4 本で同じ。`tests/test_external.py` に `generate()` の数え方（文、位置、統計）の試験。**実測**（この開発機、`threads-check`、64 位置、プロンプトのトークンの tok/s、まとめる数 1 / 2 / 4 / 8 / 16）: llm-jp-3 150M は 1 本 198 / 180 / 204 / 221 / 232（1.17 倍）、2 本 204 / 252 / 319 / 371 / 396（1.94 倍）、**4 本 211 / 325 / 465 / 559 / 615（2.91 倍）**。tiny-lm は 1 本 1.30 倍、4 本 2.56 倍。塊の大きさ（4KB〜256KB）は 16 トークンで 586〜614 と差が揺れの範囲。指示の書式（llm-jp、47 トークン、1 本）のプロンプトは 236 → 204 ms、tiny-lm（60 トークン）は 32 → 21 ms。**1 トークンずつの生成は変わらない**（新旧交互 12 回: tiny-lm 1.03 倍、llm-jp 0.99 倍）。本番の確認（`models.yml`、Linux の Chromium、2026-09-25）: サイトのモデル 3 つと HF のモデル 5 つ（llm-jp instruct3・SmolLM2・Qwen2.5 0.5B・GPT-2・Pythia 160M）がどれも動き、tok/s は前と同じ水準（llm-jp-3 150M 150.7、Qwen2.5 0.5B 42.0）。 **レビュー（Fable、2026-09-25）: OK。** 各 (行, トークン) が 1 トークンのときと同じカーネル呼び出しであることは、外れ値の列（GPT-2）を含めてビット単位で確かめられた。**直したもの**: `PROMPT_BLOCK` を 64 → 16（forward.js の BATCH）に。Worker は `forward_many` の 1 呼び出しの間メッセージに答えられず、64 だと 1.5B 級で 1 回が 1〜2 秒になり停止ボタンが効かない（未計測、T100 の費用からの見積もり）。
- [x] **T107 [性能] Hugging Face からの取得を速くする。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。部品の大きさを 8 MiB → **16 MiB** にした（並列数は 6 のまま）。計測の仕組み: `?hfParts=<MiB>&hfConnections=<N>`（計測用、画面からは選べない）、`tests/e2e.mjs` の `E2E_QUERY`、手動のワークフロー `fetch.yml`（4 × 3 の組み合わせを 3 回、回ごとに順を逆に）と `tests/fetch-table.mjs`。**実測**（CI の Linux、本番のページ、Chromium、中央値、「取得」は準備完了の内訳の download から変換を引いた秒数）: Qwen2.5 0.5B は 8 MiB × 6 本が 11.3 秒、16 MiB × 6 本が 8.3 秒、16 MiB × 8 本が 7.8 秒、16 MiB × 12 本が 7.7 秒、4 MiB はどの本数でも 22.7〜23.7 秒。Qwen2.5 1.5B は 22.2 → 21.0 秒（16 MiB × 8 本は 20.4）、4 MiB × 4 本は 34.7 秒。準備完了は 0.5B で 20.7 → 17.2 秒。**効くのは部品の大きさ**（要求 1 回ごとの待ちが大きい）で、並列数は 6 から増やしても 6% まで。**並列数を上げなかった理由**: 待ち行列に溜まる量（最悪で並列数 × 2 × 部品）が 192MB から 256MB に増えるのに、伸びは揺れと同じくらい。32 MiB 以上は測っていない。本番の確認（2026-09-25、`models.yml`）: 16 MiB になった後の Qwen2.5 0.5B は準備完了 18.8 秒（取得と変換 10.7 秒）で、HF の 5 モデルとも動いた。 **その後（同日の夕方、持ち主の報告と求め）**: 遅い回線では最初の部品（16 MiB）が届くまで進捗が出ず、変換が遅くなった・始まらないように見えた。**最初の部品は 8 MiB にし、その速さ（4 MB/s 以上か）と `deviceMemory`（4 以下なら小さいまま）で残りを 16 か 8 に決める**形にした（Fable）。`?hfParts=` を付けたときは固定。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: OK、直すものなし。** `?hfParts` の解析はページの `location.search`（Worker の `?v=` ではない）、範囲 1〜64 MiB × 1〜32 本、待ち行列の見積もり 192MB は 16 × 2 × 6 で合う、保存の単位（8 MiB）とは独立、`fetch.yml` の `run.log` への書き出しでも `E2E_RESULTS` は残る、表の NaN の行は末尾に並ぶ。数字の読み方も妥当（効くのは部品の大きさで、並列数は 1 割前後）。
- [x] **T99 [運用] 変換済みのモデルを OPFS に置く。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。形: 保存・読み出し・一覧・削除を `public/kept.js` の 1 か所にまとめ、Worker とページの両方がそれを使う（Worker は最初の変換のときに読み込む。ページは動的に import）。OPFS があれば 1 モデル 1 フォルダ（`model.bin` を 1 ファイル、`tokenizer.bin`、最後に `manifest.json`）に Worker の同期アクセスで書き、無ければ今までの Cache API（`converted-v1`、8 MiB の部品）。読むときは OPFS → Cache API の順。**Cache API にあるものは移さない**（読めて、一覧に出て、消せる）。容量は `estimate()` で先に判断せず、書けなかったら理由を返す。**書いた後に読み直して大きさを確かめる**（合わなければ消して理由を返す）。Worker は保存を使わなかった理由を準備完了の報告に載せる（`keptMiss`）。試験: `tests/kept-check.mjs`（OPFS と Cache API の小さな偽物で、保存と読み出しが同じバイト、OPFS が先、Cache API への後戻り、T99 の前の Cache API の保存が読める、容量不足で何も残らない、書いたと言って残っていない書き込みを見つける、manifest の無いフォルダは数えない。デプロイの単体試験に入れた）。e2e に `E2E_TWICE`（答えの後に読み込み直して 2 回目の準備完了を測る。ディスクに残るプロファイルで動かす）と、`models.yml` に `twice`。**実測**（CI の Linux、本番、2 回目の準備完了）: Chromium は SmolLM2 135M が 9.3 → **3.4 秒**、llm-jp-3 440M instruct3 が 18.4 → **4.4 秒**（440M の 503MB は、ヘッドレスの Chromium の Cache API では容量不足で保存できなかった大きさ）。Firefox は 34.2 → 21.7 秒、40.5 → 23.1 秒（Playwright の Firefox は遅い。OPFS から読んでいる）。**WebKit（Playwright）は未確認**: 書いた直後の読み直しは通るのに、読み込み直すと OPFS にも Cache API にも何も残らない（一時的なプロファイルでも、ディスクのプロファイルでも同じ）。Playwright の WebKit の保存の扱いと見ているが、本物の Safari では確かめていない。 **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: 1 つ直して OK。** 直したもの: OPFS の `write()` の戻り値（書けたバイト数）を捨てていて、容量の切れ目で例外でなく短い書き込みで返ると `tokenizer.bin` の欠けを誰も拾わなかった → 長さが違えば失敗にし、試験を足した。確かめて問題なし: manifest を最後に書く不変条件が両方の置き場で成り立つ、失敗時はフォルダごと消す、大きさが合わないときに Cache API へ落ちるのは意図どおり、`createSyncAccessHandle` は Worker の経路だけ、T99 の前の Cache API の鍵と `keptName` は同じ符号化、2GB でも写しの峰は +8 MiB、ページの削除ボタンは一覧があるときだけ作られる。WebKit（Playwright）の件はコードに原因が見当たらず、Playwright の WebKit が永続のデータストアを持たない見方と整合する。本物の Safari は未確認のまま。あれば良い（やらない）: OPFS からの読み出しに同期ハンドルを使う（今は 0.7 秒台で不要）。
- [x] **T109 [性能] attention をソフトウェアスレッドで分ける。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。**先に測った**（`tests/profile.mjs` に `--from` を足した。llm-jp-3 150M、1 本）: 1 トークンのうち attention は位置 16 で 2%、2000 で 48%、4000 で 65%。形: attention のカーネルにヘッドの範囲（`h0`・`h1`）を足し、ヘッドを行列積の行と同じように段の仕事（種類 3）として分けた。各ヘッドは単独で計算されるので、分け方で数字は変わらない。仕事の並びは「種類、8 つの引数、行数、トークン数、3 つの間隔」の 14 個に整理した（forward.js と helper.js で同じ。制御領域にはそのまま写し、塊の大きさと最初の塊の番号を後ろに置く）。まとめ処理（T108）では全トークンの attention を 1 つの段にする（各トークンにスコアの置き場を持たせた）。**確かめたこと**: `threads-check` で 1・2・4 本のロジット、プロンプトのまとめ処理、下書きがビット単位で同じ。`forward-check`（stories260K・tiny-lm・GPT-2）も通る。**実測**（この開発機、llm-jp-3 150M、生成の tok/s、新旧をカーネルごと入れ替えて 2 回ずつ交互、`threads-check --from`）: **レビュー（Fable、2026-09-25、サブエージェントに読ませて Fable が判定）: OK。直したのは重複。** 仕事の並びは forward.js と helper.js で一致し（14 語、`ctl.subarray(at, at + SIZE)` で count と 3 つの間隔が正しく取れる）、attention のスクラッチはトークンごと、ヘッドの範囲は互いに素、KV の伸長は count > 1 でも最後の alloc の前提が保たれる。段の前に全トークンの k・v を書いてから attention に入るので因果性も保たれる。**直したもの**: 制御領域の配置と `call`・`runRows`・塊の式が 2 部あったのを `public/jobs.js` に寄せた（forward.js と helper.js が自分の `?v=` で読む）。慣らしのスクラッチが 16 番目の仕事の最後の語（byte 3072）と同じ番地だったのを、仕事の後（3328）に置き直し、収まらなければ読み込み時に止まる 1 行を入れた。attention は常に 1 トークン 1 仕事なので `blockRows` はそこでは使われない、と書いた。

  | 開始の位置 | 旧・1 本 | 旧・4 本 | 新・1 本 | 新・4 本 |
  |---|---:|---:|---:|---:|
  | 2000 | 56.6 / 56.9 | 35.0 / 38.3 | 55.9 / 55.5 | 67.2 / 64.6 |
  | 4000 | 38.3 / 38.3 | 20.5 / 23.0 | 37.4 / 37.4 | 45.0 / 43.8 |

  **旧版は長い文脈では 4 本が 1 本より遅かった**（0.54〜0.67 倍: 行列積だけを分けて、attention の間ソフトウェアスレッドが待っていた）。新版は 4 本どうしで 1.7〜2.2 倍、1 本は変わらない。
- [x] **T110 [性能] KV キャッシュを float16 で持つ。**（Opus 5.5、2026-09-25）— 状態: **完了（Fable のレビュー済み、2026-09-25）**。**使うのは int8 のモデルを共有メモリで動かすときだけ**（ソフトウェアスレッドがあるとき。2026-09-25、持ち主の提案「速くなる環境なら使って、遅くなる環境なら使わない」）。float32 のモデル（NumPy と数字で突き合わせる基準の経路）と、隔離されていないページ（1 本）は float32 のまま。形: attention のカーネルを総称の `attentionOf<T>` 1 つにして、`attention`（float32）と `attention_f16` を書き出した。float16 → float32 は、大きさのビットをずらして 2^112 を掛ける（厳密。非正規化数とゼロも）。書く側は新しいカーネル `to_f16`（最近接偶数丸め）。k・v はいったん各トークンの枠（float32）に書き、bias と RoPE の後でキャッシュへ入れる（float32 のときは写すだけで、経路は 1 本）。`?without=kv16` で外せる（T52 の仕組み。ベンチの `?bench=full` に 6 段目を足した）。**確かめたこと**: smoke で `to_f16` が NumPy の `astype(float16)` と全ビット一致（端の値・非正規化数・あふれ）、`attention_f16` が「float32 に広げたキャッシュでの attention」とビット単位で同じ（ヘッドを 2 つの範囲に分けても）。`threads-check` で 1・2・4 本が同じ。`forward-check` は共有メモリ（float16）と `--plain`（float32）の両方（デプロイでも両方走る。試験の道具 `tests/engine.mjs` は本番の隔離されたページと同じ共有メモリを既定にした）。**品質**（`perplexity.mjs`、日本語の記事 1500 トークン、7 ビットの活性値）: llm-jp-3 150M は float16 の KV で 29.957、float32 の KV で 30.094、tiny-lm は 88.438 対 88.628。**悪化は無い**（差は丸めの揺れ）。**速さ**（この開発機、llm-jp-3 150M、生成の tok/s、新旧をカーネルごと入れ替えて 2 回ずつ交互）: **レビュー（Fable、2026-09-25）: OK。** `to_f16` は境界値 326 個（最小の正規数、最大の非正規数、非正規 → 正規への繰り上がり、tie、65520 → inf）で NumPy と全一致、`attention_f16` は有限な half 63488 通り全部で float32 に広げた `attention` と全一致（レビュー担当が手元で確かめた。リポジトリには smoke の標本だけ）。**直したもの**: smoke の標本に「非正規 → 正規へ繰り上がる境界」を足した。inf は 65536 として読まれることをカーネルのコメントに書いた（key に inf が出るモデルは無い前提のまま）。

  | 開始の位置 | float32・1 本 | float32・4 本 | float16・1 本 | float16・4 本 |
  |---|---:|---:|---:|---:|
  | 16 | 108.7 / 106.0 | 124.8 / 122.3 | 106.1 / 106.7 | 128.2 / 127.8 |
  | 2000 | 56.9 / 57.0 | 66.6 / 65.8 | 34.7 / 34.8 | 74.9 / 71.8 |
  | 4000 | 38.4 / 38.6 | 44.3 / 44.4 | 20.8 / 20.8 | 55.0 / 51.9 |

  **4 本では速く（位置 4000 で 1.2 倍）、1 本では遅い（0.54〜0.61 倍）**: 1 本のときの attention は演算で律速されていて、読み出すたびの変換の命令が効く。4 本ではメモリの読み出しで律速されていて、読む量が半分になるぶんが効く。だから共有メモリのときだけ使う（隔離されたページでもスレッドの検索が 1 本を選ぶと遅くなるが、この開発機と CI では 2〜4 本を選んでいる）。**メモリ**（計算で出る値）: llm-jp-3 150M の 4096 位置の KV キャッシュは 12 層 × 4096 × 512 × 2（k と v）で、float32 の 201MB が float16 で 101MB。

- [x] **T106 [追加] Llama 3 系に対応する。**（Opus 5.5、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。Fable の仕様の 1〜6 をそのまま入れた。(1) **RoPE の llama3 方式**: 周波数を作る関数を `llama2_numpy.rope_frequencies(width, theta, scaling)` の 1 つにし、変換器の `rope_table()`（float32 / float16 の表）とエンジンの int8 / float16 の表（`llama_tensors()`・NeoX の `gpt2_tensors()`）が同じものを使う。`Llama(rope_scaling=)` は辞書でも Worker からの JS のオブジェクトでも受ける。`check_config()` は Llama の `rope_type == "llama3"` だけを通す。変換器が `options` に `rope_scaling` を載せる。forward.js は触っていない。**照合**: この開発機に PyTorch が無く transformers の関数が動かないので、**llama.cpp の変換が GGUF に書いた `rope_freqs`**（bartowski/Llama-3.2-1B-Instruct-GGUF の Q8_0、「元の周波数 / 縮めた周波数」の 32 個）と比べ、相対 2e-7 で一致（float32 の精度）。この 32 個を試験の固定値にした。(2) **前分割 `llama3`**: Qwen の枝に「数字を何個まで続けるか」（qwen 1、llama3 3）を持たせた。(3) **`ignore_merges`**: 前分割の 1 片が語彙にあればその 1 語。`tokenizer_json_options()` が常に `ignore_merges` を返す（SmolLM2・Qwen2.5 は偽）。(4) **書式の先頭の BOS を外す**（`one_turn_template()`。llm-jp の `?hf=` の書式の先頭の `<s>` も外れる）。(5) **書式の中の特殊トークンを `options["specials"]` に**（tokenizer.json の `special: true` と GGUF の control のうち書式に現れるもの、長い順）。(6) 一覧に **Llama 3.2 1B Instruct**（unsloth の写し、`original` は meta-llama、ライセンスは「Llama 3.2 Community License」をモデルカードから。書式は T73 で読み、`src/models.js` に `template` は書いていない）と、`browsers.yml` の大きい組。GGUF の `llama-bpe` は `llama3` と `ignore_merges` にしたが、**llama.cpp は Llama 3 の縮め方を `rope_freqs` の表で書くので、その表のある GGUF は断る**（読まずに素の RoPE で動かすと黙って壊れるため）。**確かめたこと**: pytest 385 件（新しい `tests/test_llama3.py` 60 件: 周波数の固定値、縮めない場合は元の式と同じ、ほかの縮め方は断る、float32 のファイルの表と int8 でエンジンが作る表が一致、前分割がパターンと一致、`ignore_merges` の真偽それぞれで本物の `tokenizers` と一致、書式の特殊トークンが 1 トークンになる、GGUF の `llama-bpe` と `rope_freqs` の拒否）。**わざと壊して確かめた**: `ignore_merges` を外す・`rope_scaling` を options に載せない・数字を 1 つずつにする・`specials` を載せない、のどれも落ちる。**実物の語彙**（unsloth/Llama-3.2-1B-Instruct の `tokenizer.json`、この開発機の `tokenizers` 0.23.1）で 10 種類の文（日本語、英語と長い数字、空白とタブ、絵文字と外字、コード、全角と半角カナ、ベトナム語、アラビア語とアラビア数字、URL、大文字の短縮形）が 10 / 10 で同じ ID の列。書式は `<|start_header_id|>system…{prompt}<|eot_id|><|start_header_id|>assistant<|end_header_id|>\n\n`（先頭の `<|begin_of_text|>` は外れた）。`node tests/smoke.mjs`・`forward-check.mjs`・stories15M の greedy の回帰も通る。**T86 の固定値は見送り**（仕様どおり: 1B の float32 は 5GB で作れない）。**本番の実測**（`models.yml`、Linux の Chromium、2026-09-25）: Llama 3.2 1B Instruct は準備完了 32.3 秒（取得 24.4 秒、変換 4.5 秒）、19.6 tok/s（4 本）、ヒープ 1770MB。答えは「Since my knowledge cutoff is December 2023, I can suggest three potential trends…」と書式のシステムの文を踏まえた英語で、240 トークンで `<|eot_id|>` により自分で止まった。ヘッドレスの Chromium の OPFS には 1.4GB が入りきらず保存されなかった（「took only part of a write」。T99 の書いた後の確かめが拾った）。回帰: SmolLM2 135M（GGUF）8.2 秒・88.1 tok/s、Qwen2.5 0.5B 15.3 秒・42.4 tok/s、`hf:llm-jp/llm-jp-3-150m-instruct3`（書式を自分で読み、先頭の `<s>` が外れた）9.7 秒・139.4 tok/s、stories260K。どれも文は壊れていない。 **レビュー（Opus xhigh、2026-09-25）: 2 つ直して OK。** must-fix: transformers 5 が保存した config.json（RoPE の設定が `rope_parameters` 1 つ）を読まず、theta 10000・縮めなし・head 全体を回す形で黙って変換していた（この開発機の transformers 5.12.1 で形を確かめた。`?hf=` と T81 で足すモデルが対象）→ `normalize()` が古い名前に直す。Llama 3・Qwen の theta・NeoX の部分回転の 3 つで、2 つの書き方から同じバイトになる試験（直す前は落ちる。3e8702e）。should: Qwen と Llama 3 の前分割が、改行の間の空白（空白だけの行）で本物と違った → 直して、40000 個の乱数の文字列で 3 つのパターンが `regex` と一致（de539ec）。確かめたこと: `rope_frequencies` は transformers の式と同じで、境界の波長ちょうどでは比較の向きで結果が変わらない、linear・dynamic・yarn・`type` の鍵・null は断るか素通し、プロンプトに書いた `<|eot_id|>` が制御トークンになるのは transformers の既定と同じ。本番: Llama 3.2 1B（25.7 tok/s）と Qwen2.5 0.5B（前分割を直した後、run 36133209383）。残りは T116（保存した変換が変換器の版を知らず、T106 の前に保存した `?hf=` のモデルは古い書式のまま）。

<details><summary>T106 の記録（第 1 段の報告と Fable の判定・仕様）</summary>

**当時の見出し**: T106 [追加] Llama 3 系に対応する（`rope_scaling` の llama3 方式と tiktoken 型の前分割） — 状態: **判定済み、実装は Opus（次の回）**（Fable、2026-09-25）（2026-09-25 採用、Fable の提案、持ち主の指示。**T81 の前**。規模 中）
- 担当（Fable の切り分け。判定は済み、下）: **実装は Opus → Opus xhigh がレビュー。** まず Opus が Llama-3.2-1B-Instruct の `config.json` と `tokenizer.json` を読んで、断られる理由（`rope_scaling`、前分割のパターン）と、エンジンに足す量を書いて止まる。
- 根拠: Llama-3.2-1B / 3B と、その日本語の派生（多数）は人気の小型モデルだが、いまは `rope_scaling` で断り、前分割のパターン（tiktoken 型。`(?i:'s|'t|'re|'ve|'m|'ll|'d)|[^\r\n\p{L}\p{N}]?\p{L}+|\p{N}{1,3}| ?[^\s\p{L}\p{N}]+[\r\n]*|\s*[\r\n]+|\s+(?!\S)|\s+`）も知らない。
- 手順: (1) `rope_scaling.type == "llama3"`（`factor`・`low_freq_factor`・`high_freq_factor`・`original_max_position_embeddings`）を RoPE の表を作るときに反映する（周波数ごとに波長で 3 つの帯に分けて縮める。int8 は表を持たないので、エンジンとページの `forward.js` が同じ式で作る）。(2) `pretokenize()` に `llama3` のパターンを足す（`\p{N}{1,3}` が qwen と違う）。`tokenizers` との突き合わせは T63 と同じやり方（この開発機の `transformers` で）。(3) 語彙 128256 の `tokenizer.bin`（byte-level BPE）。(4) 一覧に Llama-3.2-1B-Instruct（int8 約 1.3GB、desktop only）を足す。ライセンスは Llama 3.2 Community License なので `LICENSES` に正しく写す。
- 完了条件: Llama-3.2-1B-Instruct が `browsers.yml` で動き、`tokenizer.json` との一致（10 種の文）と T86 の固定値（float32）が通る。
- **第 1 段の報告（Opus、2026-09-25）。** 調べたのは unsloth/Llama-3.2-1B-Instruct（meta-llama の原本はゲート付きなので、同じ重みの写し。`config.json`・`tokenizer.json`・`tokenizer_config.json`・`generation_config.json` と safetensors の見出しだけを取った。重みは取っていない）。
  - **いまの変換器が断る場所は 1 つだけ**: `check_config()` の「it uses RoPE scaling」。テンソルの名前と形は素の Llama と同じ（146 本、bias なし、`tie_word_embeddings` が真、`head_dim` 64 = 2048 / 32 で今の計算と同じ）。bf16 で 2.47GB、int8 で約 1.4GB。
  - **足りないものは 4 つ。**
    1. **RoPE の llama3 方式**: `rope_scaling = {rope_type: llama3, factor: 32, low_freq_factor: 1, high_freq_factor: 4, original_max_position_embeddings: 8192}`、`rope_theta` 500000。周波数ごとに波長で 3 つの帯に分ける（波長が `8192 / 4` より短ければそのまま、`8192 / 1` より長ければ `factor` で割る、間は線形に混ぜる）。表を作る場所は **Python の 2 か所だけ**: 変換器の `rope_table()`（float32 / float16 のファイルに入る表）と、エンジンの `llama_tensors()`（int8 のとき。`forward.js` は `plan.derived` で受け取るので **JS は変わらない**）。どちらも `rope_theta` と同じようにオプション（`rope_scaling`）で渡す。T72 の落とし穴どおり、変換器が `options` に載せる経路も試験する。
    2. **前分割のパターン**: Qwen2 の `qwen` と違うのは数字だけ（`\p{N}` が `\p{N}{1,3}`、数字は 3 つずつ）。`pretokenize()` に `llama3` を足し、`PRETOKENIZERS` にこのパターンの文字列を登録する。
    3. **BPE の `ignore_merges: true`**: 前分割した 1 片が語彙にそのままあれば、merge をかけずにその 1 語を取る。本物の `tokenizers` で真と偽を切り替えて比べると、**AGENTS.md・TODO.md・エンジンのソースの 2583 行では差が 0**。ただし語彙 128000 語のうち **588 語は merge を順に当てても 1 語に戻らない**（ベトナム語・アラビア語などが中心）。なのでエンジンの BPE に「1 片が語彙にあれば 1 語」を足す（`tokenizer.json` の `ignore_merges` を `options` で渡す。SmolLM2 と Qwen2.5 では偽）。
    4. **BOS が 2 つになる**: `chat_template` は読めて（T73 の Jinja で、`strftime_now` も込み）、1 ターンの書式は `<|begin_of_text|><|start_header_id|>system<|end_header_id|>\n\nCutting Knowledge Date: December 2023\nToday Date: 25 Sep 2026\n\n<|eot_id|><|start_header_id|>user<|end_header_id|>\n\n{prompt}<|eot_id|><|start_header_id|>assistant<|end_header_id|>\n\n`。エンジンの `generate()` は必ず `self.bos` から始めるので、先頭の `<|begin_of_text|>` と合わせて BOS が 2 つになる。書式の先頭が BOS の文字なら外す（変換器の `one_turn_template()` の側で）のが小さい。**ついでに分かったこと**: 書式の「Today Date」は変換した日で固定される（変換結果は Cache API に残るので、日付は古くなる）。
  - **ほかに気づいたこと**: 停止トークンは `config.json` では `<|eot_id|>`（128009）1 つ、`generation_config.json` では 128001・128008・128009 の 3 つ。指示モデルが出すのは 128009 なので今の読み方で足りる。語彙は 128000 + 特殊 256 = 128256 で、今の `tokenizer_bin()` の範囲。日本語の派生は 3B（shisa-v2.1-llama3.2-3b）が多く、1B の日本語の派生はほとんど見当たらない。
  - **Fable に判定してほしいこと**: (a) RoPE の llama3 方式をオプション `rope_scaling`（辞書のまま）で渡してよいか、(b) `ignore_merges` をエンジンの BPE に入れる形（1 片ごとに語彙を引く）、(c) 先頭の BOS を書式から外す場所（変換器か、エンジンか）、(d) 一覧に足すモデル（Llama-3.2-1B-Instruct はライセンスが Llama 3.2 Community License。原本はゲート付きなので、unsloth の写しから取るか）。
- **判定と仕様（Fable、2026-09-25）。第 1 段の 4 つはどれも足す。形は次のとおり。**
  1. **RoPE の llama3 方式**: 周波数の列を作る関数を 1 つにする（`llama2_numpy.rope_frequencies(width, theta, scaling=None)`。`scaling` は config の `rope_scaling` の辞書そのまま: `factor`・`low_freq_factor`・`high_freq_factor`・`original_max_position_embeddings`）。波長 2π/f が `original / high` より短ければそのまま、`original / low` より長ければ `f / factor`、間は `smooth = (original / λ − low) / (high − low)` で `(1 − smooth) · f / factor + smooth · f`（transformers の `_compute_llama3_parameters` と同じ式）。変換器の `rope_table()` はこの関数を import して使い（変換器はエンジンと同じ場所に必ずあるので import してよい）、エンジンの `llama_tensors()` の int8 の経路も同じ関数で表を作る。オプションの名前は `rope_scaling`（辞書か None）。`check_config()` は `rope_type == "llama3"` だけ通し、ほかの `rope_scaling` は今までどおり断る。**forward.js は触らない**（表は `plan.derived` で受け取る）。試験: この開発機の transformers で head_size 64・theta 500000・Llama 3.2 の値の周波数を出して固定値にし、pytest で照らす。T72 の落とし穴どおり、変換器が `options` に `rope_scaling` を載せる経路も試験する。
  2. **前分割 `llama3`**: `pretokenize()` の qwen の枝との違いは数字だけ（1 つずつ → 最大 3 つずつ）なので、qwen の枝に「数字を何個まで続けるか」を持たせる（qwen は 1、llama3 は 3）。`PRETOKENIZERS` に第 1 段の正規表現の文字列を登録する。確認は T63 と同じ: pytest で小さな語彙を学習させて `tokenizers` と比べ（CI でも走る）、実物の語彙でも 10 種の文で一致を見る（この開発機の `tokenizers`）。
  3. **`ignore_merges`**: `encode_bytebpe()` で、前分割の 1 片（バイト文字の列）がそのまま語彙にあれば merge をせずその 1 語にする。`tokenizer.json` の `model.ignore_merges` を `tokenizer_json_options()` が `options` に載せる（既定 False。SmolLM2・Qwen2.5 は False のまま）。GGUF は `tokenizer.ggml.pre == "llama-bpe"` のとき `llama3` の前分割と `ignore_merges = True`。
  4. **BOS の重なり**: `one_turn_template()` が返す文字列の先頭が `bos_token` の文字なら外す（`generate()` が必ず BOS から始めるため）。エンジンは触らない。
  5. **T73 のこぼれ（ついでに直す）**: `?hf=` の経路では、`chat_template` から組んだ書式の特殊トークン（`<|start_header_id|>` など）を `specials` として渡していないので、文字として綴られてしまう（T73 の「4 → 63 トークン」は綴られた数と見る。ChatML なら 20 前後のはず）。変換器が `added_tokens` の `special: true` のうち書式に現れるものを `options["specials"]` に載せる。`src/models.js` に `specials` があるモデルはそちらが優先。試験: SmolLM2 の `tokenizer.json` の書式で `<|im_start|>` が 1 トークンになること。
  6. **一覧に足すモデル**: unsloth/Llama-3.2-1B-Instruct（bf16 2.47GB → int8 約 1.3GB、desktop only。ライセンスは「Llama 3.2 Community License」をモデルカードから写す。原本の meta-llama はゲート付きなので写しから。方針 8）。3B（2 シャード、int8 約 3.3GB）は T101 か T98 の後。書式の「Today Date」が変換した日で固定されるのは受け入れる（Cache に残る）。
  7. 完了条件は第 1 段のとおり: `browsers.yml`（大きい組）で動く、`tokenizer.json` との一致、T86 の固定値（float32）を 5 つ目として足す（CI のランナーなら 1B の float32 5GB は作れないので、**T86 は int8 では文が揺れる**: 固定値は 1B では見送り、`?hf=` の e2e と perplexity の表で確かめる）。

</details>

- [x] **T91 [運用] 訪問者のベンチの結果を Issue で受け取り、1 枚の表にする。**（Opus 5.5、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）。完了条件の「1 件でも `30-` に載る」は最初の報告を待つ**（T83 で写す）。形: `.github/ISSUE_TEMPLATE/benchmark.md`（題名「Benchmark: <model> on <device>」、ラベル `benchmark`、問いは機種・OS・ブラウザの 3 つだけ。論理コアの数・モデル・ソフトウェアスレッドの本数（backend の列）・Pyodide の版・user agent はページの Markdown が持つので聞かない）。ページの「report it as an issue」（T76 が置いたリンク）は `reportUrl()` で、雛形を指定し、本文に 3 つの問いとページの Markdown を入れて開く。`reportsTable()` が Issue の本文を読んで「機種 | OS | ブラウザ | モデル | 論理コア | tok/s | カーネルなし | backend | 報告」の 1 行にし、`node tests/reports.mjs` が `gh` でラベルの付いた Issue を全部読んで表を出す。問いに例（括弧の中）が残っていれば「?」にする（推測で埋めない）。T91 の前の形（Markdown だけ、「8 threads」）も読める。**ついでに直した**: Markdown とベンチの吹き出しの見出しの「8 threads」は `hardwareConcurrency`（論理コアの数）で、ソフトウェアスレッドの本数と紛れるので「8 logical cores」にした（本数は backend の列にある）。リポジトリにラベル `benchmark` を作った（無いと雛形の `labels:` が付かない）。**確かめたこと**: `tests/bench.mjs`（URL の雛形・題名・本文の末尾がページの Markdown そのまま、問いの文言がページと雛形で同じ、2 つだけ答えた報告と何も答えていない報告と T91 の前の報告と報告でない Issue からの表）、`npm run build`。**本番**（`models.yml` の `bench`、Linux の Chromium、2026-09-25）: tiny-lm の `?bench=1` が通り（381.0 対 60.0 tok/s）、吹き出しの下のリンクが雛形を指し本文の末尾がページの Markdown であることを `tests/bench-browser.mjs` で確かめた（この確かめも足した）。**未確認**: 本文を URL で渡したときに GitHub が雛形のラベルも付けるか（付かなければ `reports.mjs` が拾わないので、最初の報告で見る）。 **レビュー（Opus xhigh、2026-09-25）: OK。** 確かめたこと（サブエージェントが Node で）: 問いの文言がページと雛形で違えば `tests/bench.mjs` が落ちる、`reportUrl()` は 6 行の表と 190 字の user agent で 1775 字、`parseReport()` は CRLF・次の行の答え・箇条書き・コードの囲い・表 2 つ・T91 の前の「8 threads」を読む。ラベルが付くかは記録どおり最初の報告で見る（Issue を作るのは外向きなので、このレビューでは作っていない）。あれば良い（やらない）: 答えに `|` があると列がずれる、雛形に 4 つ目の問いを足しても試験は通る、ラベルが付かなかったときに題の「Benchmark:」で拾うフォールバック。

<details><summary>T91 の記録（採用のときの項）</summary>

**当時の見出し**: T91 [運用] 訪問者が自分の端末で測ったベンチの結果を送ってもらう方法と、集まった結果の表 — 状態: 未着手（2026-09-24 採用、Fable の提案。**T76 の後**。規模 小。2026-09-25 に題を分かる言葉に直した。もとの題は「ベンチの報告の受け皿」）
- 担当（Fable の切り分け、2026-09-25。レビューは Opus xhigh）: **Opus が最後まで**。Issue の雛形（`.github/ISSUE_TEMPLATE/benchmark.md`: 題名の形、貼る場所、機種・OS・ブラウザ・論理コアの数・ソフトウェアスレッドの本数の欄）と、gist の「訪問者の報告」の表の形。T76 の「コピー」の隣のリンクだけは T76（Fable）が置く。
- 何をするか: 訪問者が `?bench=1` で自分の端末の速さを測れるようになった（T45。画面に結果を出すのが T76）。その結果を**このリポジトリに送ってもらう手段**（GitHub の Issue を、決まった書式で書ける雛形）と、**送られてきた結果を 1 枚の表にまとめる場所**（gist の計測の表）を用意する。
- 根拠: 送り先が決まっていないと結果が集まらない。CI のランナーは 4 vCPU の共有の仮想機ばかりなので、実機（スマホ、コアの多いデスクトップ、本物の Apple シリコン）の数字は訪問者の報告でしか手に入らない。T93 のソフトウェアスレッドが多数のコアでどこまで伸びるか（16〜128 本）、T84 の実機の列、の判断材料になる。
- 手順: `.github/ISSUE_TEMPLATE/benchmark.md`（題名の形、貼る場所、機種と OS の欄）を置き、T76 の「コピー」の隣に Issue へのリンクを 1 つ足す。集まったものは T83 の `30-` に「訪問者の報告」の表として写す（機種・OS・ブラウザ・モデル・tok/s・backend）。
- 完了条件: ひな形から作った Issue が GitHub でそのまま表になる。1 件でも報告が `30-` に載る。

</details>

- [x] **T114 [運用] スレッド数の検索の判断をコンソールに出す。**（Opus 5.5、2026-09-25。持ち主の指示: 8 コアの Android で 4 本になった理由を後から追えるように）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。形: `forward.js` の `findThreads()` に `compared(verdict)` を足し、比べるたびに「今の本数・候補・それぞれの 1 トークンの時間の中央値・時間を取ったトークン数・候補が 5% 以上速かったか」を渡す。Worker が `threads-compared` としてページへ送り、ページはコンソールに 1 行ずつ書く（例: `threads: 8 at 1.81 ms against 4 at 1.20 ms per token (16 tokens timed): 4 is faster by more than 5%`）。決まったときは「何本に、どこから（論理コアの数か、前の訪問で覚えた本数か）」も 1 行。画面（ステータス行）には出していない（見た目を変えないため）。**確かめたこと**: `tests/threads-check.mjs` が、渡された判断が検索の記録（`searchLog`）と同じ順・同じ中身で、時間が正であることを見る。この開発機（Cortex-A78×4 + A55×4、Android の 8 コアと同じ組み）の tiny-lm で、検索は 8 本から 4 本を選び（8 本 1.7〜2.8ms 対 4 本 1.1〜1.2ms、次の 4 本対 2 本は 2 本が遅く 4 本のまま）、本数ごとに測ると 1 本 630、2 本 756、4 本 766、8 本 532 tok/s で、**4 本が最速、8 本は 1 本より遅い**。持ち主の Android の 4 本も同じ理由と見ている（大小のコアに均等に割ると小コアを待つ。AGENTS.md の T93）。**本番**（`models.yml` の `bench`、Linux の Chromium、4 vCPU）: コンソールに「threads: 4 at 2.36 ms against 2 at 2.01 ms per token (16 tokens timed): 2 is faster by more than 5%」「threads: 2 at 1.44 ms against 1 at 1.40 ms … 2 stays」「threads: 2 for tiny-lm, searched from 4 (the logical cores)」の 3 行が出た。**本数はどの機械でも測って決める**（この開発機の 4 本を既定などに書き込まない。2026-09-25、持ち主の注意）。 **レビュー（Opus xhigh、2026-09-25）: OK。** 確かめたこと: 判断の渡し方は検索の記録と同じ（`threads-check`）、コールバックは約 20 トークンに 1 回で、検索が終われば呼ばれない、「5% より速い」は `< best × 0.95` と同じ。本番: ベンチの走行のコンソールに `threads:` の 3 行が出た（run 36133264047）。あれば良い（やらない）: 前の訪問で覚えた本数から始めたときは「決まった」の行が出ない（記録は出ると書いている）、8 回目ごとの見直しの行が「searched from <論理コアの数>」と言う。

- [x] **T88 [描画] 任意の Hugging Face のリポジトリを画面から開けるようにする。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: 2 つ直して OK。** must-fix は T75 と同じ（URL、dc4ec4b）。あわせて、シートで開いたリポジトリが、ページを開いたリンクの `template` と `prompt` を引き継いでいたのも直した。should: `?revision=` を検査していなかった（`?hf=a/b&revision=../../../c/d/resolve/main` は a/b と表示したまま c/d のファイルを取った）→ シートと同じく名前の形だけ（1747026）。完了条件の「断られるモデルは理由が出る」を確かめた（記録に無かった）: ゲートの無い写し `unsloth/gemma-3-270m-it` は「it is a gemma3_text, and only Llama, Qwen2, GPT-2 and GPT-NeoX models are supported」、ゲート付きの `google/gemma-3-270m-it` は「Could not fetch …: 401」（run 36133299099）。確かめたこと: 利用者の文は `textContent` と `Option` だけで HTML に入らない、シートは `#composer` の外。残りは T119（誤りの表示が名前の欄にだけ付く、401・404 に言葉が無い）。

<details><summary>T88 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T88 [描画] 任意の Hugging Face のリポジトリを画面から開けるようにする — 状態: **持ち主が画面を確かめて master に入れた（2026-09-25）。レビュー待ち（Opus xhigh）**（Fable、2026-09-25）（2026-09-24 採用、Fable の提案。規模 小〜中）
- 担当: **Fable が 2026-09-25 に実装した（ブランチ `fable/drawing`、画面は持ち主に送付済み）。持ち主が画面を見て master に入れる。** → 2026-09-25 に持ち主が画面を見て「マージしていい」と言い、master の上に積み直して入れた（衝突なし）。
- 根拠: 先週で「素の Llama / Qwen2 / GPT-2 / NeoX なら何でも動く」（T80 の調査で 299 件）ようになったのに、入口は `?hf=owner/name` という**URL を知っている人にしか使えない**形。エンジンの広さが画面に出ていない。
- 手順: HF のグループの末尾に「ほかのリポジトリ…」を 1 つ足し、選ぶと `owner/name` を入れる小さな入力（既存のポップオーバーと同じ部品）が出る。入れた値は `?hf=` と同じ経路に流す。**断られたときの理由**（`check_config()` の文）をそのまま見せる（「なぜ動かないか」もこの実験の情報）。開いたものは `?hf=` 付きの URL として共有できる。
- 決まり: 常に見える要素は増やさない。コミットの前にスマホ幅と PC 幅の画面を撮って持ち主に見せる。訪問者に見える日本語は先に文面を出す。
- 完了条件: 一覧に無いモデル（例: `Qwen/Qwen2.5-0.5B`）を画面から開いて動く。断られるモデル（例: gemma）は理由が出る。390px でページ自体がスクロールしない。

</details>

- [x] **T101 [性能] Memory64 で 4GB を超えるモデルを載せる。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: OK（試験を 1 つ足した）。** 64 ビットの経路を自動で走らせるものが無かったので、deploy に `forward-check --wide` を足した（c612ff9）。確かめたこと（サブエージェント）: 32 ビットのビルドのコマンドは変わっていない（transform と memory64 は 64 ビットの出力だけ）、`ADDRESSES` はカーネルのソースの `usize` と一致、64 ビットの全 21 カーネルの番地を 2^32 + x にすると範囲外で止まる（黙って回り込まない）、forward.js・helper.js・jobs.js・worker.js に番地を切る `| 0`・Int32Array は残っていない、`--high` は重み・KV・活性値の全部を 4GiB より上に置く。本番: 記録に無かった `?wide=on&coi=off`（64 ビットの共有でないメモリ）は、最初 SharedArrayBuffer の不具合（T113 の項）で落ち（run 36130124346）、直して tiny-lm 424.8・SmolLM2 75.3 tok/s（run 36133252008）。CI の `threads-check --wide --high` で 1・2・4 本がビット単位で同じ（run 36133321814）。残りは T115（32 ビットに残す判定の余裕が 3B 級で足りない見込み、Firefox の上限）。

<details><summary>T101 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T101 [性能] Memory64 で 4GB を超えるモデルを載せる — 状態: **レビュー待ち（Opus xhigh）。4GB を超える Qwen2.5 7B が本番で答えた（Opus、2026-09-25）**（判定は Fable、2026-09-25）（Opus 5.5、2026-09-25）（2026-09-25 採用。同日に保留から出した、持ち主の判断: **T81 の前に、まず小さな判定だけ**。規模 判定は小、実装は中）
- 担当（Fable の切り分け、2026-09-25。判定は済み、下）: **実装は Opus、レビューは Opus xhigh。** 仕様: (1) `kernels/build.py` に `--memory-base 64`（AssemblyScript の 64 ビットのメモリ）で `simdkernel_plain64.wasm` と `simdkernel_shared64.wasm` を作る出力を足す（作れなければそこで止まって報告）。(2) `forward.js` の `weightsMemory` に `{ address: "i64" }` を選べる口を足し、`alloc` と各ビューの添字が `BigInt` になる範囲を最小にする（試作でよい。本番に入れるかは判定の後）。(3) `tests/forward-check.mjs` で 32 ビットと 64 ビットの logits が一致すること、`tests/compare-engines.mjs` の要領で 32 対 64 を新旧交互に測ること（tiny-lm と llm-jp、1 本と 4 本）。(4) CI の `threads.yml` でも取る。(5) ブラウザの対応（Chrome・Firefox・Safari）を文書で確かめて書く。報告して止まる。
- 中身: WebAssembly のメモリを 64 ビットにすると 4GB の上限が無くなる。`forward.js` のメモリだけを 64 ビットにすれば（Pyodide のメモリは 32 ビットのまま）、いまは載らないモデルが載る。
- **T81 の前にやる理由**: T81 は「そのときの実装で動くもの」を数えるので、4GB の壁があるうちに調べると、壁を越えるモデルは最初から候補に入らない。
- **判定で確かめること（実装の前に）**: (1) **作れるか**: カーネルは AssemblyScript で、64 ビットのメモリの出力は実験的な機能。`kernels/build.py` で 64 ビット版が作れるか。(2) **ソフトウェアスレッドと両立するか**: 共有メモリ（`shared: true`）と 64 ビットを同時に使えるか（T93 の段階 2 がそのまま動くか）。(3) **速さ**: 64 ビットのメモリはブラウザが境界の検査を省けないため遅くなると言われてきた（未計測）。同じモデルを 32 ビットと 64 ビットで新旧交互に測る（Node と CI の実ブラウザ）。遅くなるなら「4GB を超えるときだけ 64 ビット」に分ける。(4) **対応**: Chrome と Firefox は対応、Safari は未対応（要確認）。
- **分かっている重さ**: 7B 級の int8 は約 7GB（GGUF の Q8_0 でも取得が約 7GB、原本からの変換なら取得は 14GB 前後）で、端末には 8GB を大きく超えるメモリが要る。3B 級の int8 は約 3GB で、KV キャッシュしだいでは今の 4GB の中に収まる可能性がある（未計測）。
- **判定の結果で T81 の数え方を決める**: 作れて遅くならなければ、T81 で 4GB を超えるモデルも候補として数え、実装に進む。だめなら、T81 に「4GB を超えるので入らないもの」の件数だけを記録し、この項は保留に戻す。
- 完了条件（判定）: (1)〜(4) の答えと、32 ビット対 64 ビットの速さの表が、この項に書かれている。
- **報告（Opus、2026-09-25）。仕様の (1) で止まった: いまのツールでは 64 ビットのカーネルを作れない。**
  - **AssemblyScript 0.28.20 の `asc` は常に 32 ビットを指定する**（`dist/asc.js` の中で `setTarget(T, 0)` が固定。`--enable memory64` は通るが、出力は 32 ビットのメモリのまま）。コンパイラの内部には 64 ビットの設定（`Target.Wasm64 = 2`、`usize` が 64 ビットになる）があり、プログラムから API を呼べば選べる見込み。ただし `asc` がしている標準ライブラリの読み込みまで自前の小さなドライバで書くことになる（未着手、未確認）。
  - **ブラウザの対応**（MDN の browser-compat-data、`webassembly.memory64`）: Chrome 133・Firefox 134・Node 24 から。**Safari は preview のみ**（出荷版には無い）。なので入れるとしても「4GB を超えるモデルは Chrome と Firefox だけ」の扱いになる。
  - Node 24 で `new WebAssembly.Memory({ address: "i64" })` を作り、4GiB を超えて伸ばせることは確かめた（触らないページは実際には使われないので、この機械でも落ちなかった。**以後この機械で大きく伸ばす試しはしない**）。
  - Pyodide 自体は 32 ビットのままでよい（重みは T93 で forward.js のメモリに出ている）。64 ビットにするのは forward.js のメモリとカーネルだけ。
  - 4GB を超えて得られるもの: T105 で分かったとおり、3B 級の int8 はヒープが 4GB にほぼ届く（Qwen2.5-3B で 4016MB）。それより大きいモデル（Llama-3.2-3B、sarashina2.2-3b、llm-jp-3 3.7B）が候補になる。T98 の int4 が採れれば、同じモデルが 32 ビットのまま半分の大きさで載る。
  - **Fable に判定してほしいこと**: (a) 自前のドライバで AssemblyScript の 64 ビットの出力を試すか（規模は中）、(b) int4（T98）で 3B 級を 32 ビットに収める道と比べてどちらを先にするか、(c) Safari で動かない機能を足すか（持ち主の判断）。
- **判定と仕様（Fable、2026-09-25）。** 作り方は **AssemblyScript のコンパイラ API を直接呼ぶ小さなドライバ**（`asc` の CLI は `setTarget(…, 0)` を固定しているが、公開 API には `newOptions`・`setTarget`・`newProgram`・`parse`・`initializeProgram`・`compile` があり、`Target.Wasm64` を選べる）。カーネルの元は `kernel.ts` の 1 本のまま（`usize` が 64 ビットになるだけ）。順に:
  1. `kernels/asc64.mjs`（API のドライバ、60 行前後の見込み）で `simdkernel_plain64.wasm` と `simdkernel_shared64.wasm`（relaxed も）を作り、`(memory i64)` のモジュールになることを確かめる。**API で通らなければそこで止まって報告**（そのときは clang の wasm64 に C で書き直す道しか残らず、それは別の判断）。
  2. Node 24 で、4GiB を超える共有メモリの `buffer` に `Float32Array` などのビューを作れるか（V8 の上限）と `Atomics.wait` が効くかを小さく確かめる。**この開発機で 4GiB を実際に伸ばさない**（CI の Linux で）。
  3. `forward.js`: `weightsMemory(size, {shared, large})` で `large` のとき `address: "i64"`。**i64 のメモリではカーネルの `usize` 引数が JS から BigInt になる**。先に試作で `BigInt(...)` を呼び出しごとに作って `tests/compare-engines.mjs` の要領で 32 ビット版と交互に測る（tiny-lm と llm-jp、1 本と 4 本）。5% 以上遅ければ、カーネルの番地の引数を `f64` にして（32 ビット版も同じ署名にし、中で `<usize>` に戻す）JS からは Number のまま渡す形にして測り直す。`alloc()` と各ビューの添字は 2^53 まで Number でよい。
  4. **使うのは要るときだけ**: モデルの大きさ + 余裕が 4GiB を超えるときだけ 64 ビットのメモリと 64 ビットのカーネルを読み、それ以外は今の 32 ビットの経路を 1 バイトも変えない。Safari（Memory64 なし）では「このブラウザには大きすぎる」を出す（`memoryFailure` の文の隣）。
  5. 確認: `forward-check` を 64 ビットの経路でも（小さいモデルを大きいメモリに載せて logits が 32 ビットと同じ）、`threads.yml` で速さ、`models.yml` で Qwen2.5-3B（T105 で 4016MB まで来ていたもの）と、T98 の後なら Llama-3.2-3B。順番は **T98 の第 2 段の後**（5 ビットで 3B 級が 32 ビットに収まるなら、64 ビットが要るのは 7B 級からになり、急がない）。
- **仕様の 1 の報告（Opus、2026-09-25）。止まった。** ドライバを書く前に、`asc.js` の `setTarget(T,0)` を `setTarget(T,2)`（`Target.Wasm64`）に置き換えた使い捨ての写しで、`kernel.ts` を `--enable simd,memory64` でコンパイルした（写しは試しの後に消した。リポジトリには何も入れていない）。
  - **番地の計算は 64 ビットになる**（`usize` が i64 になり、`v128.load (i64.add …)` が出る）が、**メモリの宣言は 32 ビットのまま**で、binaryen の検証が全カーネルの全ロード・ストアで「load pointer type must match memory index type」を出して失敗する。`--importMemory`（forward.js の形）でも、自前のメモリでも同じ。つまり 0.28.20 のコンパイラは Wasm64 の対象でメモリを 64 ビットとして宣言しない（`asc` が 32 ビットを固定しているのは、この経路が通らないからと見る）。
  - コンパイラ API を直接呼ぶドライバでも、`compile()` が作るモジュールは同じなので、そのままでは通らない見込み。通すには、`compile()` の後、検証と最適化の前に binaryen のモジュールのメモリ（または取り込み）を 64 ビットとして宣言し直す手が要る（binaryen の API にはある。AssemblyScript の外側で型を書き換えることになり、未確認）。もう 1 つの道は clang の wasm64 で C に書き直すこと（仕様にある「別の判断」）。
  - **急がない理由**: T98 の第 2 段で 6 ビットが線の内側に入った。6 ビットなら 3B 級の int8 の約 3.3GB が約 2.4GB になり、32 ビットのメモリに収まる見込み（未計測）。64 ビットが要るのは 7B 級からになる。
  - **レビュー担当に決めてほしいこと**: (a) binaryen でメモリの宣言を 64 ビットに直すドライバを試すか、(b) T98 の第 3 段（6 ビット）を先にして T101 は保留に戻すか。Opus の見立ては (b)。

- **実装（Opus、2026-09-25、持ち主の「やる」）。** (1) **カーネル**: `kernels/wasm64.mjs`（AssemblyScript の transform、`asc` の `--transform`）が 2 か所を補う: `afterParse` で対象を Wasm64 にし、**プログラムが transform より前に作ったモジュールの `sizeType` も i64 にする**（ここが i32 のままだと、番地の足し算が全部 `i32.add` になって検証に落ちた。前回止まった原因）。`afterCompile` でメモリを 64 ビットとして宣言し直し、取り込みを付け直す（共有なら最大 16GiB）。カーネルの元の `.ts` は 1 本のまま。`build.py` が `simdkernel_{plain,shared}64.wasm` と relaxed 版を作る。(2) **JS**: 番地の引数は BigInt で渡す必要がある（Number だと TypeError）。`jobs.js` の `ADDRESSES`（カーネルごとの番地の引数の位置）と `addressed(exports, wide)` が、64 ビットのときだけ呼ぶ前に BigInt に変える。forward-check がこの表をカーネルのソースの `usize` の引数と照らす。メモリは `weightsMemory(size, {wide})`（大きさを BigInt で）、伸ばすのは `growMemory()`。(3) **選び方**: `needsWide(size)`（重み + 512MB が 4GiB を超える）のときだけ 64 ビット。64 ビットのメモリを作れないブラウザ（出荷版の Safari）では「このブラウザには大きすぎる」と言って止まる。`?wide=on` で小さいモデルでも 64 ビットを強制できる（試験用）。**確かめたこと**: forward-check の `--wide`（stories260K・tiny-lm・stories15M の 6 ビット）で 32 ビットと同じ数字、threads-check の `--wide`（tiny-lm・llm-jp-3 150M）で 1 本と 4 本のロジットがビット単位で同じ。
- **速さ（32 対 64、交互に 2 回ずつ、この開発機、threads-check）**: tiny-lm 1 本 636・627 対 559・547 tok/s（0.87 倍）、llm-jp-3 150M 1 本 106.2・106.0 対 94.2・94.5（0.89 倍）、4 本 125.1・122.2 対 113.2・115.9（0.90〜0.95 倍）。**原因は BigInt ではなくカーネル**: 4096×4096 の行列積を直接測ると `matmul_q8` が 2.51〜2.54 対 2.75〜2.97ms、BigInt の変換は 1 呼び出し数十 ns（1 トークン約 200 回で 0.1% 未満）。64 ビットのメモリはブラウザが境界の検査をガード領域で省けないためと見ている。だから**既定は 32 ビット**（2026-09-25、持ち主の「64 ビットを既定にした方が速そう」に対して数字で答えた）。仕様にあった「5% 以上遅ければ番地を f64 で渡す」は、遅さが変換ではないので効かない。
- **本番（2026-09-25）**: Chromium の `?wide=on`（run 36122003124）で tiny-lm 208.3 tok/s・SmolLM2 135M（HF から変換）73.3 tok/s、ステータス行は「SIMD kernels, int8, relaxed SIMD, 64-bit memory, 4 threads」（ソフトウェアスレッドと共有の 64 ビットのメモリが両立）。Firefox の `?wide=on`（run 36118534642）も tiny-lm・SmolLM2 が動いた（表示を足す前の版）。**踏んだこと**: 64 ビットのカーネルの準備を 32 ビットのものと同じ `try` に入れていたため、Playwright の WebKit（64 ビットのメモリを作れると答え、そのカーネルはコンパイルできなかった）で全部のカーネルを失い、`?wide=on` に関係なくすべてのモデルが NumPy で動いた（本番で約 1 時間。tiny-lm 45 tok/s）。別の `try` にして直し、WebKit は tiny-lm 294.8 tok/s に戻り、`?wide=on` では「このブラウザには大きすぎる」で止まる（run 36121174006・36121177763）。
- **4GiB を超えるモデルは、まだ動いていない（2026-09-25、run 36118544504）**: `hf:Qwen/Qwen2.5-7B-Instruct@a09a3545…`（int8 で約 7.6GB、自動で 64 ビット）を本番の Chromium（Linux、17GB）で。**準備完了までは行った**（ステータス行が出て、プロンプトを送った）が、**答えの吹き出しが「…」のまま 1 時間 1 トークンも出ず**に打ち切られた。遅いだけなら 1 トークン数秒の見込みなので、4GiB を超える番地のどこかで止まっていると見ている。`?wide=on` の小さいモデルは 4GiB より下しか使わないので、4GiB を超える番地は一度も試せていない。**次の手**: CI（Linux）で `threads-check --wide` を、チェックポイントを 4GiB より上に置いて走らせる（重み・KV・活性値の番地が 2^32 を超える。小さいモデルで済み、ロジットが 32 ビットと同じかを見る）。疑っているのは、2^32 を超える番地を扱う JS（`Uint8Array` などのビューの長さ、`alloc()`、ソフトウェアスレッドへの受け渡し）とカーネル（`<usize>` の計算で 32 ビットに切れる所）。この開発機では 4GiB を実際に確保しない（AGENTS.md）。
- **4GiB を超えても動いた（2026-09-25）。** 原因はソフトウェアスレッドへの仕事の表: 制御領域の Int32Array に書いていたので、2^32 を超える番地の上の桁が切れ、スレッドだけが違うバイトを計算していた（`threads-check --wide --high` で 1 本は正しく 4 本だけ食い違って分かった）。表を float64（`jobs.js` の `JOB_TABLE`、制御領域は 4 → 8KiB）にして直し、CI（run 36124701055、チェックポイントを 4GiB より上に置く）で 1・2・4 本がビット単位で同じ。**本番（run 36125281468、Linux の Chromium、17GB）**: `hf:Qwen/Qwen2.5-7B-Instruct@a09a3545…` が準備完了 455.9 秒（取得 182.3・変換 33.2・**construct 266.6 秒**）、3.5 tok/s、ステータス行「SIMD kernels, int8, relaxed SIMD, 64-bit memory, 4 threads」、ヒープ 9716MB。回帰（run 36125273980）: tiny-lm 606.1・llm-jp-3 150M 176.9・Qwen2.5 0.5B 55.6 tok/s。
- **残り（レビューの後）**: construct の 266.6 秒は、int8 の relaxed の補正（グループの和）を forward.js が JS で 1 要素ずつ足しているため（7B で 76 億回）。T98 の `six_sums` と同じくカーネルにすれば秒の桁になる見込み。OPFS には 7.6GB が入りきらず保存されなかった（「took only part of a write」）ので、2 回目も取得と変換からになる。

</details>

- [x] **T75 [描画] 最適化のスイッチを画面から切り替えられるようにする。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: 1 つ直して OK。** must-fix: URL と画面が食い違った。一覧でモデルを替えても URL を書かず、`?hf=` を開いた後で一覧のモデルを選んでからスイッチを切り替える（読み込み直す）と、HF のモデルに戻った（完了条件の「URL と画面が食い違わない」に反する）→ モデルを替えるたびに `nameInUrl()` が URL を書く（dc4ec4b、T88 と共通）。e2e に `E2E_THEN`（同じページでモデルを替える、6f5b170）を足し、替えた後の URL も見る。確かめたこと: `?without=` の重なり・空白・末尾のカンマ、`?kernel=off`、`?bits=4`・`06`・`6&bits=8` はページと Worker で同じに読まれる（サブエージェントが Node で）。本番: `?hf=HuggingFaceTB/SmolLM2-135M-Instruct` から tiny-lm に替えると URL が `model=tiny-lm` だけになり、答えた（run 36133231341）。残りは T119（カーネルを外した後のベンチ、設定のシートの高さ）。

<details><summary>T75 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T75 [描画] 最適化のスイッチを画面から切り替えられるようにする — 状態: **持ち主が画面を確かめて master に入れた（2026-09-25）。レビュー待ち（Opus xhigh）**（Fable、2026-09-25）（2026-09-21 採用。持ち主の希望。規模 小〜中）
- 担当: **Fable が 2026-09-25 に実装した（ブランチ `fable/drawing`、画面は持ち主に送付済み）。持ち主が画面を見て master に入れる。** → 2026-09-25 に持ち主が画面を見て「マージしていい」と言い、master の上に積み直して入れた（衝突なし）。
- 目的: T52 がクエリパラメータで作るスイッチを、訪問者が画面からも切り替えられるようにする。URL を知らない人にも「どれがどれだけ効いているか」を触って確かめてもらう。
- **見た目を新しく作らない**: ページには T35 の「生成設定」のポップオーバーがあるので、そこに 1 区画を足す（同じ余白・同じ文字の大きさ・同じ部品）。常に見える要素は増やさない。
- 出すスイッチは 4 つくらいまでに絞る（NumPy に戻す / int8 のまま計算する / relaxed SIMD / カーネルでサンプリング）。スイッチが増えるほど保守する経路も増えるので、T52 で作ったもの全部を出すとは限らない。
- 画面の状態と URL は同じものを指す（切り替えたら URL にも出る、URL で開いたら画面にも出る）。切り替えるとモデルを読み直すことになるので、その待ち時間の見せ方も決める。
- 進め方（持ち主の判断、2026-09-21）: Fable が担当する（見た目を新しく起こす部分があるため。2026-09-25 に実装した）。仕組みは T52 で Opus が用意しておく。**コミットの前に、スマホ幅と PC 幅の画面を撮って持ち主に見せる。** 訪問者に見える日本語（スイッチの名前と説明）は先に文面を出して確認する。
- 完了条件: 4 つのスイッチが画面から効き、ステータス行の backend の表示と tok/s が変わることを実ブラウザで確かめる。390px の幅でページ自体がスクロールしない。URL と画面が食い違わない。

</details>

- [x] **T76 [描画] ベンチマークの結果を見せる。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: 1 つ直して OK。** must-fix: ベンチのラウンドが失敗するか、途中でモデルを替えると、Worker の `disabled`（そのラウンドのスイッチ）と `benching` が戻らず、次のモデルはパネルがオンのままカーネルなしで動き、ページの Run は押せないままだった → Worker は `finally` で戻し、ページはエラーとモデルの切り替えで Run を戻し、ベンチは 1 ページに 1 回だけ頼む（a40a75d）。本番: `?bench=1` の tiny-lm（370.4 対 57.4 tok/s）と、記録に無かった llm-jp-3 150M（121.0 対 9.0）が通った（run 36133264047）。あれば良い（やらない）: 回答に `|` があると表がずれる、About の `?bench=1` は `hf=`・`bits=` を落とす、`?bench=0` でも走る。

<details><summary>T76 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T76 [描画] ベンチマークの結果を見せる — 状態: **持ち主が画面を確かめて master に入れた（2026-09-25）。レビュー待ち（Opus xhigh）**（Fable、2026-09-25）（2026-09-21 採用。持ち主の判断で T45 から切り出した。規模 小）
- 担当: **Fable が 2026-09-25 に実装した（ブランチ `fable/drawing`）。結果の吹き出しの画面は T96 の直しの後に撮る。持ち主が画面を見て master に入れる。** → 2026-09-25 に持ち主が画面を見て「マージしていい」と言い、master の上に積み直して入れた（衝突なし）。
- 目的: T45 が測って組み立てた結果を、訪問者が読んで貼れるようにする。
- 形: **結果の吹き出し 1 つだけ。常に見える要素は増やさない。** 中に表と「Markdown をコピー」のボタンを置く。入口は `?bench=1` と、About の中のリンク 1 行（リンクを置くかどうかも含めて持ち主に確認する）。
- 進め方: **コミットの前に、スマホ幅と PC 幅の画面を撮って持ち主に見せる。** 訪問者に見える日本語は先に文面を出して確認する。
- 完了条件: Chromium で表が出て、コピーした Markdown が GitHub でそのまま表になる。390px の幅でページ自体がスクロールしない。

</details>

- [x] **T111 [運用] Pyodide と NumPy を Service Worker で保持して、2 回目以降とオフラインを速くする。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: OK（Service Worker の直しは持ち主の端末で見てから、T117）。** 確かめたこと（サブエージェントが coi.js を偽物の Cache と fetch の上で動かした）: 写しを取るのは 200 だけ（206・Range・HF・モデルの部品・HEAD・POST・opaque は取らない）、clone を先に取ってから本文を使う、写しは `waitUntil` の中で取る、容量切れでもページへの応答は無傷、写しにも COOP/COEP が付く。本番: tiny-lm のオフライン（準備完了 1.7 秒、run 36133309976）、記録に無かった HF のモデルのオフライン（SmolLM2、OPFS から 2.1 秒、run 36130138451）。残りは T117（`ignoreSearch` が同じサイトの全ファイルに効いてビルドが混ざりうる、`caches.open()` の失敗、古い wheel と `/_astro/` が溜まる）。

<details><summary>T111 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T111 [運用] Pyodide と NumPy を Service Worker で保持して、2 回目以降とオフラインを速くする — 状態: **本番に出した（2026-09-25、持ち主の判断「master にマージして本番でやって、問題あったら差し戻そう」）。`?offline=on` を付けた人にだけ効く形。**持ち主の端末で機内モードのまま使えた（2026-09-25）**。**既定にした（2026-09-25、持ち主「既定の URL でもオフラインを検知してほしい」）: どの訪問でも写しを取り、ネットに失敗したら写しで答える。`?offline=off` で外す（覚える）、`?offline=on` で戻す**（本番: run 36116277692 で Chromium がフラグなしの URL のまま、ネットを切って読み込み直し準備完了 2.8 秒で答えた。run 36116282233 で WebKit の tiny-lm・SmolLM2 135M・stories260K も普段どおり）（Opus 5.5 が計測、2026-09-25）（2026-09-25 採用、Fable の提案、持ち主の指示。規模 小）
- 担当（Fable の切り分け。持ち主が採った）: **実装は Opus → Opus xhigh がレビュー。**
- 根拠: 準備完了のうち Pyodide と NumPy の読み込みが約 6 秒（T68・T69）。T93 で Service Worker ができたので、CDN の応答を Cache API に置けば 2 回目以降は CDN を待たず、オフラインでも開ける。ただしブラウザの HTTP キャッシュがすでに効いている可能性があり、効きは未計測。
- 判断の材料: (1) 効き: CI の実ブラウザで 2 回目の準備完了を、保持あり・なしで比べる（先に測ってから持ち主に諮る）。(2) 方針 2 との折り合い: 版は今までどおり実行時に最新を解決し、保持するのはその版の応答だけにすれば「手で上げるバージョン定数」は増えない。古い版の掃除が要る。
- 完了条件: 持ち主が採れば、2 回目の準備完了の前後の数字と、掃除の仕組み。
- **効きの計測（Opus、2026-09-25、`models.yml` の `twice`、本番、ディスクに残るプロファイル）。** 答えの後に読み込み直し、Worker 自身の内訳で Pyodide（NumPy を含む）の秒数を 1 回目と 2 回目で比べた。**2 回目はすでにブラウザの HTTP キャッシュが効いている。**

  | ブラウザ | モデル | 1 回目の Pyodide | 2 回目の Pyodide | 2 回目の準備完了 |
  |---|---|---:|---:|---:|
  | Chromium | stories260K | 4.14 秒 | 2.67 秒 | 2.7 秒 |
  | Chromium | tiny-lm | 3.98 秒 | 2.81 秒 | 3.1 秒 |
  | Chromium | llm-jp-3 150M | 4.62 秒 | 2.75 秒 | 3.2 秒 |
  | Chromium | Qwen2.5 0.5B（OPFS から） | 3.22 秒 | 2.61 秒 | 4.4 秒 |
  | Firefox（Playwright） | 4 つとも | 19.8〜20.8 秒 | 19.2〜19.5 秒 | 19.7〜24.7 秒 |

  - 2 回目に残る約 2.7 秒（Chromium）は、取得ではなく wasm のコンパイルと Python・NumPy の初期化と見ている（HTTP キャッシュから読んでいるので、Service Worker で保持しても取得の時間はもう縮まない）。Firefox の約 19.5 秒は Playwright の Firefox が遅いためで（AGENTS.md の落とし穴）、実際の Firefox の値ではない。
  - **速さの面での効きはほぼ無い見込み**。残る効きは**オフラインでも開けること**と、HTTP キャッシュが消された後（ブラウザが 10 分の GitHub Pages のキャッシュとは別に、jsDelivr の応答をいつまで持つかは未計測）。
  - **持ち主に決めてほしいこと**: オフラインで開けることのために、方針 2 に触る仕組み（CDN の応答を Service Worker で保持し、版が上がったら古いものを消す）を足すか。Opus の見立ては「速さのためなら不要」。

- **実装（Opus、2026-09-25、ブランチ `opus/offline`。Service Worker に触るので、持ち主の端末で見るまで master に入れない）。** `coi.js` を `offline=1` 付きで登録したときだけ、通した応答の写しを Cache API（`offline-v1`）に取る。対象はこのサイトのファイル（モデルの部品を除く。モデルは Worker が自分で保持している）と jsDelivr（Pyodide、NumPy、版の解決）。**オンラインでは今までどおり毎回ネットから取る**（最新の Pyodide を毎回解決する方針 2 はそのまま。T111 の計測どおり速さは変わらない）。ネットに失敗したときだけ写しを返す。写しは 1 ファイルにつき 1 つ（`?v=` の違うビルドや別の版の Pyodide は、新しいものが入ると消える）。フラグなしで登録されると写しを捨てる。ページは `?offline=on` を `localStorage` に覚え、以後の訪問も同じ Service Worker を登録する（`?offline=off` で戻す）。**フラグの無い訪問者の経路**: 応答にヘッダを足すところは同じで、違いは「ネットの失敗を捕まえて投げ直す」と「activate で空の `offline-v1` を消す」だけ。e2e に `E2E_OFFLINE`（答えの後にネットを切って読み込み直し、もう一度答える）、`preview.yml` にブランチをランナーで配って 3 つのブラウザで回す入力 `offline`、`models.yml` にも `offline` を足した。**CI（`preview.yml`、ブランチ、stories260K）**: Chromium はオフラインで準備完了 2.7 秒・答えた、Firefox（Playwright）は 19.3 秒・答えた、**WebKit（Playwright）はネットを切った読み込み直しで「WebKit encountered an internal error」**（ページの失敗か Playwright の制約かは未確認。本物の Safari で見る）。
- **本番（2026-09-25、`models.yml` の `offline`、Linux の Chromium）**: tiny-lm を `?offline=on` で答えさせ、ネットを切って読み込み直すと準備完了 2.0 秒で答えた。フラグなしの tiny-lm・SmolLM2 135M・stories260K も普段どおり。
- **モバイル回線ではなるべく通信しない**（2026-09-25、持ち主の求め）: 版が URL に入っているファイル（`/pyodide/v…/`、`?v=<ビルド>`、`/_astro/`）は中身が変わらないので、写しがあればネットに聞かずに返す。2 回目からの通信は Pyodide の版の確認（数百バイト）とページ本体だけ。回線の種類で分けないのは、Safari が Network Information API を持たないため（Chromium 系だけで分けても iPhone に効かない）。モデルのトークナイザも写しに入れた（部品だけは Worker が持つ）。
- **持ち主にしてもらうこと**: master に入れた後（フラグの無い訪問者には効かない形で）、(1) iPhone の Safari、Android の Chrome、PC の Chrome で `https://takano32.github.io/pyodide-llm/?offline=on` を開き、tiny-lm で 1 回答えさせる。(2) 機内モードにして（PC はネットを切って）ページを読み込み直し、開くか・もう一度答えるか。(3) ネットを戻し、`?offline=off` を開いて普段どおり動くか。Safari はホーム画面に追加したアプリでも見てもらえると確か。どれかが駄目ならその端末と画面を知らせてもらう。

</details>

- [x] **T98 [性能] int4 の重みで、読む量と取得を半分にする。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: OK（コードの直しは無し、文書を直した）。** 確かめたこと（サブエージェント）: `quantize6_x` と NumPy を 45 グループ（x.5 の偶数丸め、負の最大、ゼロ、非正規数のスケール、inf）で比べてバイト単位で一致（NaN のグループだけ違い、int8 と同じ既知のもの）、`matmul_q6`・`q6r` と `q8`・`q8r` を −124 と 124 の行と 9 グループ（4 つずつの後の端数）でビット単位で一致、GPT-2 の int6（分類器の共有あり・なし）を NumPy で動かして丸めた float32 とビット単位で一致、int6 の大きさはどのヘッダでもほかの 3 形式と重ならない。本番（記録に無い組み合わせ）: `?bits=6&without=kernels`（6 ビットを NumPy で）で SmolLM2・GPT-2 が動いた（7.3・8.2 tok/s、run 36130117663）、`?wide=on&bits=6`（6 ビットのカーネルを 64 ビットのメモリで）も動いた（28.9・40.5 tok/s、run 36130131483）。文書: 方針 5 の「int4」と、速さの「本数を増やすと差は縮む」（CI の 4 本では縮まなかった）を直した（d6c4464）。残りは T116（保存済みの判定がビット数を見ず、int8 で保存したモデルを 6 ビットにすると 500MB 超を確認なしで取り直す）と T119（フォルダの設定の JSON の dtype を `weightsFor()` が上書きする）。

<details><summary>T98 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T98 [性能] int4 の重みで、読む量と取得を半分にする — 状態: **第 3 段（6 ビットの実装）を入れた（Opus、2026-09-25、持ち主の「T98 進めてよい」）。実ブラウザの確認は下。レビュー待ち（Opus xhigh）**（判定は Fable、2026-09-25）（Opus 5.5、2026-09-25）（2026-09-25 採用、持ち主の指示。**調査から**。規模 中〜大）
- 担当（Fable の切り分け、2026-09-25。第 1 段と判定は済み、下）: **第 2 段の計測と第 3 段の実装は Opus → Opus xhigh がレビュー。第 2 段の数字で形を選ぶのもレビュー担当（決め方は下に書いてある）。** 第 1 段の仕様: `tests/gguf_check.py` と `tests/perplexity_prepare.py` に、GGUF の Q4_0・Q4_1・Q4_K（スーパーブロック 256、サブブロック 32 の 2 段のスケール。llama.cpp の `ggml-quants` の定義を独立に書く）を **float32 に広げて読む**読み手を足す（計測用。ページには入れない。形式は変えない）。SmolLM2-135M-Instruct（bartowski に Q4_0・Q4_K_M がある）と、もう 1 つ 0.5B 級（Qwen2.5 の公式は原本と別の重みなので避け、llm-jp か sarashina の Q4 があればそれ。無ければ SmolLM2-360M）で、原本・Q8_0・Q4_0・Q4_K_M の perplexity（英語 Wikipedia 1500 トークン、T85 と同じ）と最尤トークンの一致率を表にする。あわせて、自前の量子化の候補（グループ 16、ゼロ点あり、T92 の外れ値の分離）を NumPy で試して同じ表に足す。**報告して止まる**（+1% 以内に入る形があるか、無いか）。第 2 段（Fable）: 採る形と、legacy 形式への足し方（3 か所 + Cache API のキー）と、カーネル（int4 を int8 に広げて掛ける SIMD）を決めて仕様を書く。
- 根拠: 生成の律速はメモリ帯域（T93: 1 トークンごとに重みを全部読み、この開発機で約 20GB/s で頭打ち）。int4 なら読む量が int8 の半分なので、帯域律速の範囲では tok/s が最大 2 倍近くになりうる（未計測）。取得と Cache API の容量も半分。
- 過去の失敗: 2026-09-19 の素朴な int4（グループ 32、float32 のスケール 1 つ）は perplexity が原本比 +16.8% で不可だった（AGENTS.md）。**そのままでは入らない**。
- 調べること（Fable の判定の材料）: (1) GGUF の Q4_K（スーパーブロックとサブブロックの 2 段のスケール）や Q4_0・Q4_1 を読み込んで int8 に展開せず計算した場合の perplexity（T74 で GGUF は読める。`tests/perplexity.mjs` と `tests/gguf_check.py` で測れる）。(2) 自前で量子化するなら、グループを小さくする（16）・ゼロ点を持つ・外れ値を別に持つ（T92 の考え方）のどれで +1% 以内に入るか。(3) カーネル: int4 を int8 に広げながら掛ける SIMD（relaxed の積和はそのまま使える見込み）。(4) 形式: legacy 形式に int4 を足すと 3 か所（`layout()`・`checkpoint_dtype()`・エンジン）と Cache API のキーを直すことになる（落とし穴）。
- 完了条件: 品質（原本比の perplexity）と速さ（新旧交互）の表を出し、採るか取りやめるかを決める。採るなら 1 モデルで実装し、CI で確かめる。
- **第 1 段の報告（Opus、2026-09-25、`tests/int4_check.py`・`tests/gguf_check.py`・`int4.yml`）。** 各形式を原本の重みにかけて戻し（重みは float32 に戻るが形式の誤差を持つ）、ページと同じ変換をして NumPy の float32 で読ませた。行どうしの違いは重みの形式だけ。文は T85 の Wikipedia の冒頭 1500 トークン（llm-jp は日本語、ほかは英語）。「一致」は最尤トークンが原本と同じ割合。GGUF は llama.cpp の Q4 系を独立の読み手で float32 に広げた（原本との相対誤差は Q4_0 で約 9%、Q5_0 で約 4.5% で、式の誤りなら 100% 前後になる）。**+1% 以内に入る 4 ビットの形は無かった。**

  | 重みの形式 | ビット / 重み | SmolLM2 135M | SmolLM2 360M | Qwen2.5 0.5B | Pythia 410M | llm-jp-3 440M |
  |---|---:|---:|---:|---:|---:|---:|
  | int8、32 個ずつ（今のページ） | 9.0 | +0.06% | +0.24% | −0.07% | +0.04% | +0.05% |
  | int4、32 個ずつ、対称 | 4.5 | +47.6% | +35.3% | +21.8% | +38.6% | +24.5% |
  | int4、16 個ずつ、対称 | 5.0 | +39.6% | +24.8% | +14.3% | +27.0% | +21.7% |
  | int4、32 個ずつ、最小値つき | 5.0 | +32.4% | +27.6% | +17.3% | +29.9% | +16.1% |
  | int4、16 個ずつ、最小値つき | 6.0 | +25.5% | +14.6% | +10.4% | +17.1% | +12.7% |
  | int4 対称 + 埋め込みは int8 | 5.1〜6.6 | +26.5% | +19.2% | +15.4% | +35.8% | +21.4% |
  | int4 最小値つき + 埋め込みは int8 | 5.5〜6.8 | +23.7% | +17.2% | +12.9% | +27.0% | +13.7% |
  | GGUF Q4_0（bartowski） | 5.0〜5.4 | +14.9% | +12.5% | — | — | — |
  | GGUF Q4_K_M（bartowski） | 5.9〜6.2 | +4.7% | +2.9% | — | — | — |

  - 一致は int8 が 97〜98%、自前の int4 が 61〜80%、Q4_0 が 79〜82%、Q4_K_M が 86〜91%。
  - **llama.cpp の「4 ビット」は 4 ビットだけではない**: SmolLM2 の Q4_0 のファイルは、埋め込み（分類器と共有）を Q8_0 に、ffn_down の一部を Q4_1 に置いている。Q4_K_M は、次元 576 が 256 の倍数でないので大半が 5 ビットの Q5_0 で、ffn_down は Q4_K と Q6_K。実質のビット数は 5〜6 で、それでも +3〜5%。
  - 自前の形では、グループを 16 にする・最小値を持つ・埋め込みを int8 に残す、のどれも効くが、足しても +10〜17% までしか下がらない。T92 の外れ値の分離（活性値の側）は重みの誤差には効かないので試していない。
  - Pythia 70M（100 トークンの試し）は、分類器を兼ねない埋め込みまで int4 にすると壊れる（+68000%）。埋め込みを int8 に残すと +25%。
  - 小さいモデルほど崩れる（SmolLM2 は 135M で +47.6%、360M で +35.3%）。1B 以上は未計測。
- **Fable に判定してほしいこと**（第 2 段の前）: (a) +3〜5% を許すかどうか（方針 5 の「int8 の品質は原本と区別できない」を緩めることになるので、**持ち主の判断**でもある）、(b) 許すなら llama.cpp の K 系（Q4_K と Q6_K を混ぜる形）を自前で持つか、GGUF の Q4_K_M をそのまま読むか（読むだけなら変換は要らないが、カーネルは 4・5・6 ビットの 3 種類が要る）、(c) 1B 以上で測ってから決めるか（大きいモデルほど 4 ビットに強いと言われるが、ここでは未計測）。
- **判定と仕様（Fable、2026-09-25）。** 第 1 段の表の読み方: **4 ビットはどの形でも届かない**（自前で +10〜48%、llama.cpp の Q4_0 でも +12〜15%）。「+3〜5%」の Q4_K_M は名前に反して**大半が 5 ビット（Q5_0）で、ffn_down は 6 ビット**（dim が 256 の倍数でないため）。つまり持ち主が許した線に届いているのは 5〜6 ビットで、得られる縮みは int8（9 ビット/重み）比で 5.5 ビットなら 39%、6.5 ビットなら 28%。「半分」ではないが、0.5B が 500 → 300MB、3B 級が 4GB → 2.4GB になるので、**やる価値はある**。
  - **形は 1 つの一様な形式にする**（K 系のように行列ごとに違う形式を混ぜない。カーネルが 3 種類になり、コードのきれいさを優先する持ち主の判断に反する）。候補は Q5_0 と同じ並び: **32 個ずつ、5 ビット、float16 のスケール、対称**（22 バイト/32 個 = 5.5 ビット/重み）。6 ビット版（26 バイト、6.5 ビット）も同じ作り。埋め込み（分類器）は int8 のまま（第 1 段で効いた）。
  - **第 2 段（Opus、実装の前）**: `tests/int4_check.py` に「5 ビット・32・float16 スケール」「6 ビット・同」と、それぞれ「+ 埋め込み int8」の 4 行を足し、`int4.yml` で同じ 5 モデルを測る（SmolLM2 135M の Q5_0 は GGUF の行が実質それなので照合になる）。**決め方**: 5 モデルの最悪値が +5% 以内の形のうち、いちばん小さいもの（5 ビットが通れば 5 ビット。通らず 6 ビットが通れば 6 ビット。どちらも通らなければ T98 は表を残して閉じる）。ここで止まって報告。
- **第 2 段の報告（Opus、2026-09-25、`int4.yml` を `rows=int8,int5,int6` で、run 36103029683）。** 第 1 段と同じ文・同じやり方（形式をかけて戻し、float32 の NumPy で読む）。「5 ビット」「6 ビット」は 32 個ずつ・float16 のスケール・対称（5.5 と 6.5 ビット / 重み）。SmolLM2 135M は一覧が GGUF から取るので、原本（HuggingFaceTB@12fd25f7）の safetensors を `hf_fetch.py` の `hf:` で取った。

  | 重みの形式 | SmolLM2 135M | SmolLM2 360M | Qwen2.5 0.5B | Pythia 410M | llm-jp-3 440M | **最悪** |
  |---|---:|---:|---:|---:|---:|---:|
  | int8、32 個ずつ（今のページ） | +0.06% | +0.24% | −0.07% | +0.04% | +0.05% | +0.24% |
  | 5 ビット | +9.94% | +6.99% | +4.76% | +6.78% | +4.90% | **+9.94%** |
  | 5 ビット + 埋め込みは int8 | +6.28% | +4.82% | +3.36% | +6.10% | +4.30% | **+6.28%** |
  | 6 ビット | +3.37% | +2.10% | +1.01% | +1.66% | +1.55% | **+3.37%** |
  | 6 ビット + 埋め込みは int8 | +2.32% | +1.98% | +0.73% | +1.68% | +1.19% | **+2.32%** |
  | GGUF Q4_K_M（bartowski、照合） | +4.72% | +2.92% | — | — | — | |

  - 最尤トークンの一致: 5 ビット 79〜86%、6 ビット 89.5〜93.8%、6 ビット + 埋め込み int8 92.4〜94.0%（int8 は 97.4〜98.3%）。
  - ビット数（尺度込み、行列全体の平均）: 5 ビット 5.50、+ 埋め込み int8 は 5.96〜7.10、6 ビット 6.50、+ 埋め込み int8 は 6.83〜7.64。
  - **決め方（Fable）を当てはめると 6 ビット**: 5 ビットは埋め込みを int8 に残しても最悪 +6.28%（SmolLM2 135M。Pythia 410M も +6.10%）で線の外。6 ビットは埋め込みも 6 ビットのままで最悪 +3.37% で線の内側、しかも「+ 埋め込み int8」（6.83 ビット以上）より小さい。**int8（9 ビット）比で 28% 小さくなる**（0.5B の 545MB が約 390MB、3B 級の約 3.3GB が約 2.4GB）。
  - **照合**: GGUF の Q4_K_M（実質 5.9〜6.2 ビット、行列の大半が Q5_0）は 135M で +4.72%、360M で +2.92%。第 1 段の数字（+4.7%・+2.9%）とそろっていて、同じくらいのビット数の自前の 6 ビット（+3.37%・+2.10%）はそれより良い。
  - 小さいモデルほど崩れるのは第 1 段と同じ（135M がどの行でも最悪）。1B 以上は未計測。
  - **ここで止まる**（仕様どおり）。レビュー担当に確かめてほしいこと: (a) 決め方どおり 6 ビット（埋め込みも 6 ビット）で第 3 段に進んでよいか、(b) 第 3 段の仕様は 5 ビットで書かれている（`matmul_q5r`、22 バイト / 32 個）ので、6 ビット（26 バイト / 32 個、`matmul_q6r`）に読み替えてよいか。6 ビットは 4 ビット + 2 ビットに分けて詰めると 1 グループが 2 + 16 + 8 = 26 バイトで、展開は 5 ビットと同じ手間の見込み（未計測）。
- **6 ビットの確認（Opus、2026-09-25、`int4.yml` の `size=large` と `small`、`rows=int8,int6`、run 36104692355・36104696011）。** 持ち主の求め（「6 ビットで問題ないか確認して」）で、1B 級 3 つを足し、行ごとに greedy で 32 トークン書かせた文も並べた（`int4_check.py` が表の下に出す）。

  | モデル | int8 | 6 ビット | 6 ビット + 埋め込み int8 | 6 ビットの最尤トークンの一致 |
  |---|---:|---:|---:|---:|
  | llm-jp-3 980M instruct3（日本語） | +0.25% | +1.02% | +1.07% | 93.9% |
  | Llama 3.2 1B Instruct | +0.02% | +1.37% | +1.25% | 94.5% |
  | Qwen2.5 1.5B Instruct | −0.02% | +1.68% | +1.11% | 93.5% |

  - **1B 級は小さいモデルより崩れない**（+1.0〜1.7%。小さい 5 つは +1.0〜3.4% で、最悪は SmolLM2 135M の +3.37%）。どれも持ち主の線（+3〜5%）の内側。
  - **書いた文**（Wikipedia の冒頭 40 文字の続き、greedy）: Qwen2.5 0.5B・1.5B、Llama 3.2 1B、SmolLM2 360M は 6 ビットでも原本と一字一句同じ（例: Llama「ama) is a mountain in Japan, located in the Fuji-Hak」）。llm-jp-3 980M は 6 ビットが原本と同じ「は約3,776メートルに達」で、int8 のほうが違った（「は3,776.4 mで、」）。llm-jp-3 440M・Pythia 410M・SmolLM2 135M は数トークン目から分かれるが、int8 でも同じように分かれ、壊れた文は無い（例: 440M の 6 ビット「で、富士山の最高峰である。」）。
  - **結論: 6 ビット（埋め込みも 6 ビット）で問題ない。** 第 3 段の仕様の「5 ビット」は 6 ビット（26 バイト / 32 個、`matmul_q6r`）に読み替える。
- **設定のパネルでビット数を選べるようにした（2026-09-25、持ち主「設定から選択できても良いかも」）。** エンジンの欄（T75 の切り替えの下）に「automatic / int8 / 6 bits」の 3 択。T75 と同じく URL（`?bits=`）が状態で、選ぶと読み込み直す。サイトのモデル（ビルド時に作った int8）には効かない（`weightsFor()` が変換するモデルだけに効く）。画面は `preview.yml` で撮って持ち主に見てもらう。
- **動的に切り替える形の提案（Opus、2026-09-25。持ち主の「できれば動的に変更できたほうがよいのでは？」への答え。第 3 段の前にレビュー担当が形を決める）。** HF から取るモデルはページの中で変換するので、**何ビットで持つかは読み込むときに選べる**（ファイルの形式の問題ではない）。案: (1) 変換器の `dtype` に `"int6"` を足し、`Conversion(dtype=)` に Worker が渡す。(2) 選び方は 3 つ: 設定のパネルに「重み: int8 / 6 ビット / 自動」（既定は自動）、URL の `?bits=6`、自動は「int8 だと端末のメモリ（`deviceMemory` のある Chromium 系）か 32 ビットのメモリの上限（4GiB）に入らないときだけ 6 ビット」。持ち主の判断（+3〜5% は「選べること」まで許す）に合わせ、入るなら int8 のまま。(3) OPFS の保存（T99）はビット数ごとに別の鍵（同じモデルの int8 と 6 ビットを両方持てる）。(4) サイトに置くモデル（tiny-lm など）は int8 のまま（ビルド時に作るファイルで、切り替えるには両方置くことになり、サイトの 1GB の目安に響く）。(5) 一覧の「int8 1.4 GB」の表示は選んだビット数で変える。
- **第 3 段の実装（Opus、2026-09-25）。** 仕様（5 ビット、float16 のスケール）から 2 点変えた: (1) **スケールは float32**（int8 と同じ配列・同じ補正・同じ広げ方がそのまま使え、コードが増えない。代わりに 1 重み 7 ビットで、int8 比 22% 小さい。float16 なら 28%）。(2) **値は「下 2 ビットが 0 の int8」として詰め、スケールを 4 分の 1 にする**（積はビット単位で同じ。広げたものがそのまま int8 なので、カーネルは 32 を引かずに済み、forward.js・補正・外れ値の列・埋め込みは int8 と同じ式）。詰め方は `llama2_numpy.pack6`（32 個 24 バイト: 下位 4 ビットを 2 つずつ 16 バイト、上位 2 ビットを 4 つずつ 8 バイト）、カーネルは `kernels/six.ts`（マスクと定数のシフトだけ）と `matmul_q6`・`matmul_q6r`（jobs.js の種類 5・6）。形式の 3 か所（`layout()` の大きさ = `tensor_bytes()`、`checkpoint_dtype()`、エンジンの `take()`）と、`quantize.py` の `int6`、`?bits=` と自動の選択（`weightsFor()`: int8 が 32 ビットのメモリか `deviceMemory` の半分に入らないときだけ 6 ビット）。保存の鍵（`keptName()`）はもとから dtype を含む。**確かめたこと**: pytest（`tests/test_int6.py`: 詰めて戻すと同じ、量子化の範囲、6 ビットのファイルのロジットが「同じ値に丸めた float32 のファイル」とビット単位で同じ（Llama・分類器が別・Qwen2）、ファイル順の変換が同じ、32 の倍数でない行は断る。`test_checkpoint_dtype.py` の全アーキテクチャに int6）、`forward-check`（stories15M の 6 ビット: relaxed で一致 99.2%・perplexity −0.00%、relaxed なしで 97.7%・+0.10%、広げた経路で 100%）、`threads-check`（1・2・4 本でロジットがビット単位で同じ）、models-check（選び方）。**速さ**（この開発機、stories15M、`threads-check`）: 6 ビット 1 本 270・2 本 417・4 本 371 tok/s、int8 は 586・624・442。1 本は 0.45 倍で、広げる手間が効く（上位ビットを 1 回読みにする・4 グループずつにする、はどちらも効かなかった）。だから自動では int8 が入らないときだけ。**本番（`models.yml`、Linux の Chromium、`?bits=6`、run 36110317910・36110321337・36110325895）**:

  | モデル | 6 ビット: 準備完了 / tok/s / ヒープ | int8（同じ日の本番） |
  |---|---|---|
  | SmolLM2 135M Instruct（GGUF から） | 12.9 秒 / 46.5 / 332MB | 8.2 秒 / 88.1 |
  | Qwen2.5 0.5B Instruct | 27.4 秒 / 17.0 / 739MB | 22.8 秒 / 40.2 / 823MB |
  | llm-jp-3 980M instruct3 | 48.9 秒 / 10.5 / 1250MB | （28.5 tok/s、T93 の記録） |
  | Llama 3.2 1B Instruct | 63.7 秒 / 7.3 / 1507MB | 32.3 秒 / 19.6 / 1770MB |

  - **どれも動き、文は壊れていない**（例: Qwen「もちろんです、それぞれの流行りについて説明いたします。」、Llama「It's difficult to predict with certainty what will be popular next…」、llm-jp 980M は丁寧に断る文）。WebKit（relaxed なし、`matmul_q6`）の SmolLM2 も 24.9 tok/s で動いた。フラグなしの tiny-lm・stories260K・Qwen2.5 0.5B（int8）は普段どおり。
  - **速さは 4 本でも int8 の 0.37〜0.53 倍**（Llama 7.3 対 19.6、Qwen 17.0 対 40.2）。この開発機の stories15M で見た「本数を増やすと差が縮む」は、CI のランナーでは起きなかった。準備完了も遅い（Llama 63.7 対 32.3 秒: 6 ビットの量子化が NumPy のため）。ヒープの減りは 10〜15%（重みの 22% 減のうち、KV と活性値と Pyodide は変わらない）。
  - **結論**: いまの 6 ビットは「int8 では載らないモデルを載せる」ためのもので、速くはならない。自動の選び方（int8 が入らないときだけ）はこのまま。速くするなら (a) 広げる命令の少ない詰め方を探す（未検討）、(b) 変換の量子化を SIMD に、の 2 つ。レビュー担当に、この代償で採るか（自動で int8 が入らないときだけ）を確かめてほしい。
- **変換の量子化の内訳（Opus、2026-09-25、`node tests/profile-convert.mjs llm-jp-3-150m int8|int6`。道具はページと同じくエンジンとカーネルを読み、int8 は SIMD の `quantize_rows` で量子化するように直した）**: llm-jp-3 150M（bf16 305MB）の変換は int8 が 2.1 秒（147MB/s）、int6 が 4.0 秒（77MB/s）。cProfile で int6 の 3.8 秒のうち `pack6` 1.56 秒と `quantize6` 1.72 秒（`max`・`clip`・`astype` 込み）、int8 の SIMD の量子化は 0.33 秒。**案**: `quantize_x` と同じ作りの SIMD のカーネル `quantize6_rows`（グループの最大値 / 31 でスケール、丸め、4 倍して 24 バイトに詰める）を足し、NumPy の `quantize6` + `pack6` とバイト単位で同じことを試験で見る（T89 と同じやり方）。見込みは int6 の変換が int8 と同じくらい（約 2 倍速い）。**→ 入れた（2026-09-25、持ち主の「オススメですすめて」）**: `quantize6_x`（kernel.ts。グループの最大値は `quantize_x` と共有の `groupMax`）が量子化と詰めを 1 回で行い、`kernel_quantizer` の `quantize_rows(values, six=True)` として変換器に渡る。smoke で NumPy の `quantize6` + `pack6` とバイト単位で同じ（乱数、ゼロのグループ、x.5 が並ぶ偶数丸めの行、GPT-2 と NeoX の変換まるごと）。llm-jp-3 150M の変換は int6 が 4.0 → **1.1〜1.2 秒**（int8 と交互に 2 回ずつ: int8 1.0・2.1 秒、int6 1.2・1.1 秒）。本番（run 36113625273、`?bits=6`）: Llama 3.2 1B の準備完了 63.7 → **41.6 秒**（変換 6.8 秒）、Qwen2.5 0.5B 27.4 → 23.3 秒。残りの差は Worker の内訳の `construct` が 12.8 秒（int8 は 1.4 秒）で、forward.js が relaxed の補正（グループの和）を 1 要素ずつ `weightAt()` で足していたため。**カーネル `six_sums` にした**（forward-check が、JS でバイトから組み立てた和と 2000 グループで一致することを見る）。本番（run 36114563112、`?bits=6`）: `construct` は 12.8 → **0.52 秒**、準備完了は Llama 3.2 1B が **28.7 秒**（取得 21.8・変換 5.8 秒。int8 の同じ日の 32.3 秒と並ぶ）、Qwen2.5 0.5B が **15.9 秒**（int8 は 22.8 秒の回があった。取得の揺れが大きい）。生成は 8.3・18.9 tok/s で、int8（19.6・40.2）の約 0.45 倍のまま。
  - **第 3 段（Opus、Fable の仕様）**: legacy 形式に dtype を 1 つ足す（`checkpoint_dtype()`・`layout()`・`Llama.__init__` の 3 か所 + Cache の鍵、T77 の落とし穴どおり）。カーネルは **1 つ**: グループを int8 に展開して既存の `matmul_q8r` の内積（inline に切り出す）へ流す `matmul_q5r`（relaxed なしの `matmul_q5` も同じ形）。展開の費用は演算律速の 1 本では効くはずなので、新旧交互で測る（1 本と 4 本）。変換は `quantize()` の隣に `quantize5()`（NumPy）と `quantize_x` の隣の SIMD 版。`?without=` に足す名前は不要（int8 は別の dtype として残る）。配布は「大きいモデルは int8」の方針 5 のまま、5 ビットは選べる 1 つとして一覧に足す（既定は変えない: 品質の線を持ち主が許したのは「選べること」まで）。

</details>

- [x] **T96 [運用] `?bench=1` が CI で失敗し続ける原因を調べる。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: 2 つ直して OK。** must-fix: (1) 既定の tiny-lm の後に同じページで Pythia 1B を選ぶと「Maximum memory size exceeded」で読み込めなかった（直接開けば動く）。記録に無かった「同じページで小さいモデルから大きいモデルへ」を e2e の `E2E_THEN` で試して再現した（run 36130590977）。使い回す判定がチェックポイントしか見ず、1.2GB のメモリに 1.1GB が入ってから補正が入らなかった → 補正と 512MB まで見て、足りなければ作り直し、作り直す前に古いメモリへの参照（`weightsNow`）を切る。共有メモリの取れない端末でモデルごとにメモリを作っていたのも直した（d03b215）。(2) ベンチの後始末（T76 と共通、a40a75d）。ほかに、効いていなかった「`stopThreads()` の後は 1 秒ごとにエラー」の確かめを外し、記述を直した（14a179b）。本番: tiny-lm → Pythia 1B（2048 トークンまで書いた、15.6 tok/s）→ llm-jp-3 150M を同じページで（run 36133220937）。残りは T120（ブラウザが止めたソフトウェアスレッドを今も無期限に待つ）と T115（使い回しの判定と 64 ビットの判定の見積もりを 1 つにする）。

<details><summary>T96 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T96 [運用] `?bench=1` が CI で失敗し続ける原因を調べる — 状態: **直した。CI の `?bench=1` が通った（Fable、2026-09-25）。レビュー待ち**（2026-09-25 採用、持ち主の指示。**調査から**。規模 小〜中）
- 担当（Fable の切り分け、2026-09-25。原因と対処は Fable が入れた、下）: **残る確認は Opus → Opus xhigh がレビュー**。手順: (1) `tests/bench-browser.mjs` にページのコンソールと Worker のエラーの `message` と `stack` を全部出させ、`public/worker.js` のエラー報告に、メモリ不足と判定したときも元の例外の名前と message を添える（画面の文は変えない）。(2) CI で `?bench=1` を走らせて、どのラウンドで何の例外かを取る。(3) 原因を直す。T90 の判定（`RangeError` を全部メモリ不足とする）が広すぎたなら、判定を「message に memory / buffer / allocation を含むもの」に絞ってよい（Fable の事前判断）。ベンチの各ラウンドが共有メモリを作り直す（最大を予約する）ことが原因なら、ラウンドの間に前のメモリへの参照を全部切ってから次を作る、または 1 つの共有メモリを使い回す、のどちらでもよい。(4) `browsers.yml` でベンチのステップが通ることを確かめ、AGENTS.md の落とし穴に書く。 **T82 のレビューで分かったこと**: `bench-browser.mjs` は assert で落ちるので、失敗が JSON にも artifact にも残らない。(1) で `E2E_TIMEOUT`・`E2E_RESULTS`・`E2E_ARTIFACTS` の 3 つを `bench-browser.mjs` にも通す（`e2e.mjs` の `record` と `keepArtifacts` と同じ形）。
- 事実（`browsers.yml` の huggingface の小さい組の「The benchmark of ?bench=1」のステップ、`tests/bench-browser.mjs tiny-lm`）: 2026-09-24 の実行 36003988306（T93 の前）と 36032774754（段階 1b-i の後）は **「TypeError: network error」**、36047994839（段階 3 の後）と 36049665597（共有メモリの最大を直した後）は **「This device ran out of memory for tiny-lm 29M (it needs about 333 MB); the page was using 78 MB when it happened」**。モデルの e2e（同じジョブの 14 モデル）はどれも通っている。
- わかっていないこと: (1) どのラウンドで落ちるか（`ROUNDS` は「カーネルあり」と「なし」の 2 つ）。(2) メモリ不足の表示の元の例外。T90 の作りは `RangeError` を全部メモリ不足として表示するが、`RangeError` は範囲外の型付き配列（`new Uint8Array(buffer, offset, length)`）や上限を超えた `memory.grow()` でも出る。「78 MB を使っていた」はメモリ不足らしくない。(3) T93 の前の「network error」とつながっているか（ベンチはラウンドごとにモデルを読み直すので、中止した読み込みの後始末と次の取得が重なっている可能性。未確認）。
- 手順: この開発機ではブラウザを動かさないので CI で。`bench-browser.mjs` にページのコンソールと Worker のエラーの `message` と `stack` を全部出させる（T82 の artifact に残る）。`public/worker.js` のエラー報告に、メモリ不足と判定したときも元の例外の名前と message を添える（画面に出す文は変えない）。ラウンドを 1 つずつ走らせて、どこで落ちるかを切り分ける。
- 完了条件: 原因を 1 文で言え、直したなら `browsers.yml` でベンチのステップが通る。T90 の判定が広すぎたなら、判定を直して AGENTS.md の落とし穴に書く。
- **手がかり（Fable、2026-09-25、`preview.yml` の画面撮りで再現）**: 同じタブで 2 つ目の文書を開くと、その `WebAssembly.Memory()` が「could not allocate memory」で失敗する。撮った順は「stories260K を読む（共有メモリ）→ 同じタブで `?bench=1` へ移動 → 読み込みの時点で失敗（部品 1.1MB は届いている）→ 同じタブで `?hf=microsoft/phi-1_5` へ移動 → 同じ失敗」。**ベンチのラウンドの前に、文書を変えただけで落ちている**ので、原因は「前の文書の共有メモリの予約（最大 1GB 超）が、同じレンダラの次の文書でまだ解放されていない」側と見る（ヘッドレスの Chromium）。切り分け（2 回目、新しいコンテキストで撮り直し）: **新しいコンテキストでも `?bench=1` は落ちる**（同じ走行で、新しいコンテキストの `?model=stories260K` は 2 回とも通った）。落ちたときの画面は「TinyStories 260K: 1.1 / 1.1 MB」（ベンチの 1 ラウンド目が Cache API から読み直した進捗）と「ヒープ 45 MB」（Pyodide と NumPy だけ。重みのメモリは手放した後）。つまり**落ちるのはベンチの 1 ラウンド目、モデルの 2 回目の読み込みで作る 2 つ目の共有メモリ**で、`weightsMemory()` の共有の 3 段の予約も、その後の共有でない `new WebAssembly.Memory({initial})` も「could not allocate memory」になる。1 つ目のメモリ（最大 1GB 超の予約）がまだ生きているだけでは説明が付かない（予約は 2 つで 2GB）。V8 の予約の上限に当たっているなら、Pyodide の 4GB と手放したはずのものが数えられている可能性がある。**まず `error.stack` と、そのときの `performance.memory` と `weightsNow`・古い `memory` への参照（`outsideNow`・Python 側の `external`）を出す**（T96 の手順 (1)）。T96 では (1) 新しいコンテキストなら `?bench=1` が通るか（通れば「同じタブの 2 つ目」が原因で、ベンチ自体ではない）、(2) `weightsMemory()` の最大を予約ではなく必要量に近づける（`initial + 4096` から始めて足りなければ伸ばす順）か、モデルを手放すときに `memory` への参照を全部切る、のどちらで直るかを見る。
- **原因と対処（Fable、2026-09-25）。** `err.stack` を Worker から page の console に流し（e2e と `bench-browser.mjs` の記録に残る）、CI で `?bench=1` を走らせて取った stack: 落ちているのは `forward.js` の **共有でない** `new WebAssembly.Memory({ initial })`（共有の 3 段の予約が全部断られた後のフォールバック）で、呼び出し元はベンチの 1 ラウンド目の `load()`。つまり **2 回目のモデルの読み込みで、Worker は 18 ページのメモリすら作れない**。「共有メモリの 1GB の予約が積み重なる」という見立ては違った: WebAssembly のメモリは最大の宣言に依らず 1 つごとに大きなアドレス空間（V8 のガード領域込み）を予約し、Chromium のレンダラは Pyodide の 1 つ + 前のモデルの 1 つの後の 3 つ目を断る。**訪問者がモデルを 2 回切り替えても同じ**で、ベンチだけの不具合ではない。対処: Worker は重みのメモリを使い回す（`pooledWeights()`。前の engine は手放すが memory は残し、次のモデルが最大に収まる限り `grow` で伸ばす。**最初は「最大 4GB で 1 つ」にしたが、スマホで Pyodide のメモリが入らず「Loading Pyodide」で固まった（持ち主の報告、同日夕方）ので、最大はモデルから決め（4 × ファイル + 1GB）、収まらないモデルのときだけ作り直す形にした**）。`weightsMemory()` は `maximum` を受けるようにした（試験は今までどおり 1 回ずつ作る）。
- **2 つ目の原因（Fable、2026-09-25）。** メモリを使い回すようにしたら、今度はベンチが止まった。CI で状態行を 2 秒ごとに記録すると「Downloading → ready → Downloading → ready …」の繰り返し: ベンチの各ラウンドは Worker の `load()` を通り、それが ready をページに送る。**ページは ready を受けるたびに「ベンチをまだしていなければ始める」ので、ラウンド 1 の ready で 2 本目のベンチを頼み**、2 本目がラウンド 1 のスレッドを段の途中で止め、取りまとめ役が `Atomics.wait` で永遠に待った。メモリ不足で落ちていた間はここまで進まなかった。対処: ページは 1 回だけ頼む（`benchmarking` の旗）、Worker も走っている間の 2 度目は断る（`benching`）、そして取りまとめ役の待ちは `stopThreads()` の後なら 1 秒ごとに確かめてエラーにする（永遠に待つ作りを残さない。普通の読み込みは生成の終わりを待ってからスレッドを手放すので、この経路はベンチだけだった）。**結果**: `models.yml` の `bench` で stories260K（1469 tok/s）と tiny-lm（884 tok/s）の 2 ラウンドが通った。`browsers.yml` のベンチの段は次の週次の走行で見る。

</details>

- [x] **T113 [運用] iOS Safari では Service Worker（隔離）の下で Pyodide の NumPy の読み込みが終わらない。** — 状態: **完了（Opus xhigh のレビュー済み、2026-09-25）**。**レビュー（Opus xhigh、2026-09-25）: 1 つ直して OK。** must-fix（T110 から）: 隔離されていないページには `SharedArrayBuffer` という名前が無く、forward.js の `instanceof SharedArrayBuffer` が ReferenceError を投げて、int8 のモデルが全部読み込めなかった。この項のフォールバック（Service Worker を外して読み直す）は隔離なしなので、フォールバックに入った端末では既定の tiny-lm も動かなかった（朝 09:42 から。`?coi=off` でも同じ、run 36130698578）。記録に無かった `?wide=on&coi=off` を試して見つけた → 名前を使う前に `typeof` を見る。`tests/engine.mjs` は `--plain` のときこの名前を消し、直す前の状態で落ちることを確かめた（6cceee2）。本番: `?coi=off` で tiny-lm 454.9・SmolLM2 83.7 tok/s（1 本、run 36133241483）。確かめたこと: フォールバックはループしない、`sessionStorage` が例外を投げても止まる、7ae2aae の coi.js の処理は T97 の前と同じ（違いはコメントだけ）。残りは T118（期限が経過時間なので遅い回線では正常でも期限切れ、`await numpy` はどの期限にも入っていない）。

<details><summary>T113 の記録（実装とレビュー待ちまで）</summary>

**当時の見出し**: T113 [運用] iOS Safari では Service Worker（隔離）の下で Pyodide の NumPy の読み込みが終わらない — 状態: **T97 を戻して直った（持ち主の iPhone で確認、2026-09-25 夕方）。フォールバック（期限切れで Service Worker を外して読み直す）は残す。レビュー待ち**（2026-09-25 採用、持ち主の報告。**調査から**。規模 小〜中）
- 担当: Opus（調査）→ Opus xhigh がレビュー。
- **結果（2026-09-25 夕方）**: T97 を戻したら、持ち主の iPhone の Safari は通り、スマホの Chrome の「fetching」も通った（古い Service Worker が残っていた間は止まり、入れ替わってから進んだ）。原因は T97 で、Pyodide にも iOS にも問題は無かった。フォールバックは残す（別の理由で固まっても 1 コア版で動く）。
- 分かっていること: 持ち主の iPhone の Safari で、普通の URL では「Loading Pyodide 314.0.7: NumPy...」（`pyodide.loadPackage("numpy")`）で止まり、`?coi=off`（Service Worker なし、隔離なし）なら通る。CI の WebKit（Linux・macOS、Playwright）では隔離ありで通るので、iOS の WebKit だけ。同日の夕方に Fable がフォールバックを入れた: Pyodide の各段（loader / runtime / NumPy）に 60〜90 秒の期限を付け、期限切れなら**その 1 回だけ Service Worker を外して読み直す**（`sessionStorage` の `coi-fallback`。そのタブでは以後隔離なし）。だから iOS でも動くはずだが、1 コア版で、最初の 60 秒を無駄にする。
- 疑うところ: 隔離の下で `SharedArrayBuffer` が使えるとき、Pyodide が NumPy の展開に別の経路（スレッド）を使う可能性。`loadPyodide()` のオプションで止められるか、Pyodide の版の changelog を読む。iOS の実機でしか出ないので、持ち主に `?threads=1` や Pyodide の版の固定（`?pyodide=`、前の版）で試してもらって絞る。
- 完了条件: iOS Safari で隔離ありのまま NumPy の読み込みが終わる。無理なら、iOS だけ最初から隔離なしで開く判断を持ち主に諮る（60 秒の無駄を無くす）。

</details>

- [x] **T115 [性能] 32 ビットのメモリに残すか、使い回すかを、forward が実際に置くものから決める。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: OK（直しは T116・T120 の項）。** `footprint()` は上限になっている: サブエージェントが `createForward()` と Python が置くものを全部なぞる再生を作り、手元の実測と一致させた上で（tiny-lm 15.4 / 27.3 / 86.9 / 13.4 MiB など）、実物の形で比べた（Llama-3.2-3B int8 共有 1065.7 対 1070.6 MiB、Qwen2.5-3B 592.6 対 598.4、Qwen2.5 7B 1192.2 対 1198.0、GPT-2 の外れ値つき 75.2 対 76.0、llm-jp-3 3.7B int6 2441.3 対 2445.4。上回りは 0.75〜5.8 MiB、下回りは無し）。KV は bump で、伸びる瞬間は古い塊と新しい塊の和（見積もりの式どおり）。callable の dtype はどの経路（safetensors・GGUF・シャード・トークナイザの取り直し）でも `Stream` の中で解かれ、PyProxy も壊している。ヘッダの取り方: サイトの部品（途中で切れて取り直す、部品 0 が後から届く、の場合も。Node の試験台）、`url`・`local`・`hf`（Chromium・Firefox・macOS の WebKit。フォルダの経路はどのワークフローも走らせていなかったので models.yml で走るようにした、30d83a2・cf086ad）。本番: `?coi=off` で tiny-lm → Pythia 1B（2048 トークンを最後まで）→ llm-jp-3 150M の原本 → SmolLM2、2 シャードの Qwen2.5-3B（69.3 秒、8.4 tok/s）。**(3) Firefox の 64 ビットのメモリは 9GB を超えて取れた**: Playwright の Firefox 150（Linux）で Qwen2.5 7B の `?bits=8` が準備完了 239 秒・ヒープ 9716MB で答えた（「Chrome and Firefox can」の文はそのまま。本物の Firefox の上限は未確認）。**残り**: 共有のメモリを頼んで断られたとき KV が float32 になり、Qwen2.5-3B の int8・Llama-3.2-3B の int6 は位置 2048 でメモリ不足になる（見積もり 4.029・4.320 GiB）→ T130。**(1) の自動の規則は持ち主の判断**（6 ビットでも 64 ビットが要るモデル（7B、llm-jp-3 3.7B）で 6 ビットを選ぶ理由が無い、を足して出した）。あれば良い（やらない）: GPT-2 に使わない RoPE の表を 2 つ渡している（256KiB）、KV をその場で伸ばせば峰が下がる（Qwen2.5-3B の共有なし 4.029 → 3.888 GiB）。**形**: `forward.js` の `footprint(header, size, options)` が、ヘッダの 7 個の int と読み込み方（dtype・arch・int8 のカーネルか・relaxed か・KV を float16 で持つか）から、forward がチェックポイントの後ろに置くものの上限を出す（中身は AGENTS.md の「forward がチェックポイントの後ろに置くもの」）。`needsWide(size, after)`・Worker の `pooledWeights()`・`weightsMemory()` の最大（必要な分 + 1GB）がそれを使う。ヘッダは経路ごとに取る: サイトのモデルは部品 0 が届いたとき（`download()` の `header`）、フォルダと URL のファイルは先頭 28 バイト、HF の変換は変換器が `sink.open(size, header, dtype, arch)` で渡す、保存からの読み出しは最初の部品。**自動のビット数は Worker へ移した**: ページは HF の config を持たないので、`weightsFor()` は `?bits=` と `deviceMemory` だけを見て、それ以外は `undefined`（Worker が決める）。Worker の `automaticBits()` を変換器に dtype の代わりに渡し、変換器はヘッダが分かった時点で呼ぶ（`Stream` が `callable(dtype)` なら `dtype(header, arch, {int8: 大きさ, int6: 大きさ})`）。規則は T98 のまま「int8 が 32 ビットに入らなければ 6 ビット」。保存は選んだビット数の名前で置き、ビット数を指定しない読み込みは int8 → int6 の順に探す。**試験**: `forward-check.mjs` が各モデルで KV をコンテキストの最後まで倍々に伸ばし、メモリの大きさが見積もりを超えないこと・差が 5% + 6MiB 以内を見る（この開発機: stories260K・stories15M・tiny-lm・llm-jp-3 150M、共有・`--plain`・`--wide`・`--without int8`・`--without relaxed`、float16 の原本。差は 0.9〜4 MiB。deploy でも走る）。`test_convert.py` に dtype を選ぶ関数の 1 件（ヘッダと 2 つの大きさを受け、選んだ dtype のバイトになる）。e2e に `E2E_LONG`（数字を並べた長いプロンプト。答えはコンテキストいっぱい）、`models.yml` に `long`。**本番**（`models.yml`、Linux の Chromium、17GB）: Llama-3.2-3B-Instruct は `?bits=8` で **64 ビットのメモリ**に載り、2634 トークンのプロンプト（KV が 4096 への最後の倍化を通る）に答えた（準備完了 64.2 秒、ヒープ 4266MB、4.1 tok/s）。自動では **6 ビット・32 ビット**（70.5 秒、3459MB、1.9 tok/s）。Qwen2.5-3B-Instruct は自動で int8 のまま 32 ビット、2396 トークン（64.6 秒、4072MB、4.9 tok/s）。同じページで tiny-lm → llm-jp-3 150M の float16 の原本（見積もり 873MB の後ろ。旧規則なら入ると判定して足りなかった大きさ）→ SmolLM2 と替えて、どれも答えた（原本は 1774 トークン）。stories260K の greedy の文は同じ。**残り・レビューで見てほしいこと**: (1) 自動の規則は T98 のまま「32 ビットに入らなければ 6 ビット」にした。3B 級は Chrome でも 6 ビット（int8 の 64 ビットの約半分の速さ）になる。「このブラウザに入るなら int8（Memory64 のあるブラウザでは 64 ビットで）」に変えるかは方針の判断なので持ち主に（Firefox の上限が未確認なのも理由）。(2) 共有のメモリを頼んで断られた端末では、KV が float32 になり見積もりの 2 倍になる（コメントに書いた。コンテキストの終わり近くでメモリ不足になりうる）。(3) **Firefox の 64 ビットのメモリの上限は確かめられなかった**: Mozilla の出荷の告知（2025-01）は「今は 8GiB、いずれ 16GiB」。Qwen2.5 7B の int8（見積もり 9.15GiB）は 8GiB なら入らない。`PAGES_64`（16GiB）と「Chrome and Firefox can」の文はそのまま。

<details><summary>T115 の採用時の記録</summary>

- 根拠: `needsWide()` は「チェックポイント + 512MB が 4GiB を超えるか」で決めるが、forward はチェックポイントの後ろに relaxed の補正（int8 のファイルの 1/9、int6 は 1/7）と KV（伸びるときは古い塊と新しい塊が同時にあるので、峰は古い塊の 3 倍）を置く。レビューのサブエージェントが forward.js の置き方を Python に写して見積もった（記録のヒープと合う: Qwen2.5 7B は 8.83GiB 対 9716MB、Qwen2.5 3B は 3.61GiB 対 4016MB。差は Pyodide のメモリ）: **Llama-3.2-3B の int8（3.615GB）は 32 ビットのまま位置 1024（共有）/ 512（共有でない）で KV が伸びられずに止まる**、Qwen2.5-3B は共有でないとき 2048 で止まる、llm-jp-3 3.7B は自動で 6 ビットになって 1024 / 512 で止まる（見積もり、未計測）。止まると「メモリ不足」と出て、原因と違う。`alloc()` は伸ばしに失敗しても `top` を戻さない。
- あわせて: `weightsFor()`（src/models.js）の余裕 300e6 と `needsWide()` の 2^29 が食い違う（3.76〜4.0GB の int8 は、ページが int8 を選び Worker が 64 ビットにする。Safari では 6 ビットなら入るのに断られる）。T96 の使い回しの判定（レビューの d03b215 の「補正と 512MB」）も同じ見積もりにする。**Firefox の 64 ビットのメモリの上限**（2021 年の試作の告知では 8GB。今は未確認）を確かめ、7B の「Chrome and Firefox can」の文を合わせる。
- 手順: 見積もりの関数を forward.js に 1 つ置き（補正、seq_len いっぱいの KV と伸びるときの峰、活性値、分類器。形はヘッダの 7 個の int と options で分かる）、`needsWide()`・`pooledWeights()`・`weightsFor()` がそれを使う。
- 完了条件: 3B 級（Llama-3.2-3B か Qwen2.5-3B）が `models.yml` で長い生成を最後まで書く。32 ビットで足りるモデルは 32 ビットのまま（64 ビットは約 1 割遅い）。

</details>

- [x] **T123 [性能] 変換と読み込みの重いところをカーネルに（マルチコアは取りやめ）。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: OK。完了条件も満たした。** `int8_sums` は JS のループとビット単位で同じ（整数の和は厳密で、float32 への丸めは 1 回）、`widen_bf16` は smoke が 65536 通りをビットで比べる（NaN も）、4 つのビルド（plain・shared・64 ビットの 2 つ）が両方を持ち `ADDRESSES` にも載っている、Makefile の依存も足りている（サブエージェント）。**7B の construct**: 本番の Chromium で `hf:Qwen/Qwen2.5-7B-Instruct` の `?bits=8` が **1.2 秒**（T101 の 266.6 秒）、準備完了 190.8 秒（455.9 秒）、3.6 tok/s。Firefox では 8.8 秒。だから 266 秒は補正の JS のループだった（「約 10 秒ぶん」は 4GiB より下の番地の速さからの見積もりで、4GiB を超える番地の型付き配列が Chrome で数十倍遅かったと見るのが合う。見立て。AGENTS.md を直した）。入れたのは 1 コアのカーネル 2 つ: `int8_sums`（int8 の relaxed の補正。forward.js の JS のループを置き換え、9.2 倍）と `widen_bf16`（変換の bf16 → float32。`llama2_numpy.kernel_widener()` を変換器の `bfloat16=` に。3 倍、変換全体 0.78 → 0.60 秒）。本番で Llama-3.2-3B の construct 3.17 → 0.66 秒、変換 11.6 → 10.4 秒。**スレッドに配る第 3 段は取りやめ**（2026-09-25、持ち主の判断「変換そんなに速いなら並列化しなくていいや」）。理由の計測: この開発機（持ち主と同じ日本の回線）から huggingface.co へ、ページと同じ取り方（16 MiB × 6 本）で **8.3 MB/s**（Llama-3.2-1B の先頭 537MB に 64.3 秒、変換なし、Node の fetch）。変換は約 500MB/s なので、1B 級（bf16 2.5GB）は取得に約 300 秒、変換は約 5 秒の見積もり。CI（米国）でも 3B は取得と変換の実時間 54〜62 秒のうち変換 10〜14 秒。**大きいモデルで「変換が遅い」と見えたのは、変換が始まると進捗が「converting N%」だけになり、その % が届いたバイトの速さで進むから**（持ち主の Xiaomi 13T Pro で大きいモデル全般が遅かった、の見立て。端末の内訳は未計測）。表示の直しは T119 の (6) に積んだ。**レビューで見てほしいこと**: T101 の 7B の construct 266 秒の原因は、補正のループなら約 10 秒ぶんなので別にある（未計測）。

<details><summary>T123 の採用時の記録</summary>

- 根拠: HF のモデルを選んでから準備完了までのうち、Python の変換（bf16 を float32 に広げる、並べ替え、量子化）と、forward.js がエンジンを作るときの int8 の relaxed の補正（グループの和。JS で 1 要素ずつ足している）は、どちらも 1 コアで走る。Pyodide はスレッドなしのビルドで、ソフトウェアスレッド（helper.js）は forward の行列積と attention にしか使っていない。記録: Qwen2.5-3B は準備完了 88.7 秒のうち取得と変換で 75.2 秒・変換そのもの 13.4 秒（T105、CI）、Qwen2.5 7B の construct は 266.6 秒（T101。int8 の補正の JS のループと見ていたが、下の計測では 7B でも約 10 秒ぶんなので、残りの原因は未計測）。int6 の補正は T98 でカーネル（`six_sums`）にしてある。
- 手順: (1) **先に測る**: 準備完了の内訳（pyodide・download・convert・construct）と、変換の中の割合（`tests/profile-convert.mjs`）を、1B・3B で CI とこの開発機で取る。取得が律速の部分は、コアを増やしても縮まないので分けて書く。(2) int8 の補正をカーネルにする（`six_sums` と同じ形。1 コアのままで効く）。(3) 補正と変換の量子化を、段の仕事（`jobs.js`）としてソフトウェアスレッドに配る。量子化の出力はすでに forward.js の共有メモリに直接書いているので、入力（float32 か bf16 の行）を共有メモリの作業場に置けば、行の塊ごとに取り合える。ソフトウェアスレッドはいまエンジン（`createForward`）が起こすので、変換のあいだにも使えるように、エンジンから独立した起こし方にする（`?threads=` と検索した本数に従う。**開発機の本数で固定しない**）。(4) 隔離されていないページ（共有メモリなし）は 1 コアのまま、同じバイト。
- 決まり: 変換の結果は 1 本と N 本でバイト単位で同じ（`tests/test_convert.py` と、共有メモリの上での比較を `threads-check` か新しい試験で）。補正も同じ数（カーネルの丸めは forward.js の `Math.fround` と同じ）。
- 完了条件: CI（`models.yml`）で 1B 級と 3B 級の準備完了の内訳が前後で並び、convert と construct が縮んでいる。7B の construct が秒の桁になる。
- **第 1 段（2026-09-25、1 コアのカーネル）**: 測った内訳（この開発機、Node 上の Pyodide、`tests/profile-convert.mjs llm-jp-3-150m int8`、bf16 305MB）: 変換の Python 0.78 秒のうち bf16 → float32 の広げ（NumPy の 2 回の走査）0.28 秒、量子化（T89 のカーネル）約 0.25 秒、残りは切り出しと書き込み。**本番の内訳**（T115 の走行、CI の Linux の Chromium）: Llama-3.2-3B int8 は準備完了 64.2 秒のうち取得と変換の実時間 54.0 秒・変換 11.6 秒・construct 3.2 秒、Qwen2.5-3B は 64.6・53.9・13.5・3.7 秒。**取得と変換は重なっているので、CI では変換を縮めても準備完了は取得の 50 秒前後より縮まない。** 入れたもの: (1) `int8_sums`（int8 の補正。`six_sums` と同じ形。150M ぶんの重みで JS 193ms → 21ms、9.2 倍。forward-check が JS の和とビット単位で比べる）、(2) `widen_bf16`（bf16 の広げ。`llama2_numpy.kernel_widener()` を変換器の `bfloat16=` に渡す。0.284 → 0.090 秒、変換全体 0.78 → 0.60 秒。smoke が 65536 通りのビット列で NumPy と比べる）。
  **本番**（`models.yml`、CI の Linux の Chromium、0fca426）: Llama-3.2-3B int8（64 ビット）は construct 3.17 → **0.66 秒**、変換 11.6 → 10.4 秒、準備完了 64.2 → 69.9 秒（取得の揺れ: 取得と変換の実時間 54.0 → 61.8 秒）。tiny-lm・llm-jp-3 150M・llm-jp-3 150M instruct3（変換 0.50 秒）・SmolLM2（GGUF、0.78 秒）も通った。
- **次（第 3 段、スレッド）の前に持ち主に確かめること**: CI の回線では取得が律速で、変換をスレッドに配っても準備完了はほとんど縮まない見込み。重いと感じた端末・モデル・準備完了の内訳（ページの ready の行の pyodide・download・convert・construct）が分かれば、スレッドに配る値打ちがその端末で出るかを先に見積もる。

</details>

- [x] **T116 [運用] 保存した変換の鍵に、ビット数と変換器の版を入れる。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: 1 つ直して OK。** 直したもの（0e75f4e）: 覚えている 500MB 超のモデルが保存にあるかを確かめる間（`stillKept()` の await）に訪問者が別のモデルを選ぶと、Worker は `load` を先に受けて URL のスイッチなしで Pyodide を始め（`init(undefined)`: `?without=`・`?wide=`・`?hfParts=` が消える）、ページはその後で一覧の最初のモデルに戻しうる（Worker は選んだほうを読む）→ `load` にも `search` を載せ、`init` は `load` がまだ出ていないときだけ送る。確かめて問題なし: kept-check、`serves()` の名前（ビット数と文脈の長さ）と版、古い版を消して新しい版は残すこと、本番の 2 回目（Chromium で SmolLM2・llm-jp-3 150M instruct3・llm-jp-3.1 1.8B・CAT-Translate 0.8B・2 シャードの Qwen2.5-3B、Firefox、macOS の WebKit。OPFS から 2.2〜8.3 秒）。**版を 2 にした費用の判定**: 変わったのは T106 の options（BOS と `specials`）で、chat_template から書式を読むモデルは古い保存のままだと書式の特殊トークンが綴りのまま入る（基底モデルの options は変わっていない）。取り直しは 1B 級で 2.5GB、日本の回線（8.3 MB/s）で約 5 分。黙っては取らない（`stillKept()` と「kept」の表示が消えるので 500MB 超の確認が出る）。判定: 受け入れる。覆す条件: 保存のうちチェックポイントのバイトは同じなので、小さいファイル（config・tokenizer・見出し）だけを取り直して options を作り直す道がある。保存を持つ訪問者が多いと分かったら、次に版を上げる前にそれを作る。あれば良い（やらない）: 版の違う 2 つのタブを同時に開くと、同じ名前のフォルダを上書きし合う。**形**: 保存の名前（`keptName`）にはもうビット数が入っていたので、足したのは (1) **変換器の版** `CONVERTER`（`public/kept.js`、いま 2。manifest に書く。1 は版の無い manifest）と、(2) **どの保存がそのモデルに使えるかの判定を 1 つに**（`serves(kept, model)`: 頼んだビット数の名前か、頼まないときは Worker が選びうる int8・int6 の名前で、今の版のもの）。Worker の `openKept()` も名前を順に見て、古い版の保存は使わずに消す（新しい版の保存は消さない: 古いタブが新しいページの保存を消さないように）。ページの「kept in this browser」は `serves(one, weighed(entry))`、表示のときに古い版の保存を消す。**起動**: 覚えている 500MB 超のモデルは、今の読み方（ビット数と版）で保存にあるときだけ確認なしで始め、無ければ一覧の最初から（`stillKept()`。`?bits=6` で開いた人に int8 の保存を理由に 2.5GB を黙って取らせない）。**試験**: `kept-check.mjs` に 2 件（int8 の保存は int8 と「頼まない」に使え、int6 には使えない・Worker が 6 ビットを選んだ保存も「頼まない」で見つかる／古い版は使わず消し、新しい版は使わず残す）。`openKept` の版の判定を外すと落ちることを確かめた。**本番**（`models.yml` の `twice`、1573a8a）: 2 回目は保存から、llm-jp-3 150M instruct3 が 12.4 → 2.5 秒、SmolLM2 が 6.5 → 2.2 秒、`?bits=6` の llm-jp-3 150M instruct3 は 11.7 → 3.2 秒（int6 の名前で）。tiny-lm・stories260K も通った。**変換器の版を上げる決まり**を AGENTS.md の落とし穴に書いた（チェックポイントのバイトか options を変えたら上げる）。**レビューで見てほしいこと**: 版を 2 にしたので、訪問者がこれまで保存した変換は全部、次の読み込みで取り直しになる（T106 の options の直しを効かせるため。1B 級で 2.5GB）。

<details><summary>T116 の採用時の記録</summary>

- 根拠: (1) ページの「kept in this browser」の判定と `remember()` は、リポジトリとリビジョンしか見ない（manifest に dtype が無い）。int8 で保存したモデルを `?bits=6` や設定のパネルで 6 ビットにすると、表示は「kept」のまま、500MB 超の確認なしに取り直しが始まる（Llama 3.2 1B なら 2.5GB。方針 4 に反する）。(2) `keptName()` に変換器の版が入っていないので、変換器が options を変えても（T106 の BOS を外す・`specials`）、前に保存したモデルは古い options のまま使われる。落とし穴の「Cache の鍵」と同じ種類。
- 手順: manifest に dtype と変換器の版（変換器の中の定数。options や形式を変えたら上げる）を書き、ページの判定と Worker の読み出しがそれを見る。版の違う保存は使わずに消す。
- 完了条件: `tests/kept-check.mjs` に、ビット数の違う保存と版の違う保存の 2 つを足す。`models.yml` の `twice` で 2 回目が保存から読まれる。

</details>

- [x] **T120 [運用] ソフトウェアスレッドが止められても、取りまとめ役が永遠に待たない。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: must-fix 1 つと should 2 つを直して OK。** must-fix（96782ed）: スレッド数の検索が裏で起こしているソフトウェアスレッドが、エンジンが手放した後（訪問者が別のモデルを選んだ、`stopThreads()`、諦めた後）に立ち上がると、一覧に入って終わらなかった。使い回すメモリ（T96）の上で次のエンジンがそれを起こし、古いエンジンのカーネルで段の仕事をした: float32 のモデル（や `?without=relaxed`）から int8 のモデルへ替えると 1 本が塊を持ったまま例外で止まり、10 秒止まって 1 本に落ちた（サブエージェントが Node で再現）→ 手放した回数を数え、それをまたいで立ち上がったものはすぐ終わらせる。threads-check に 1 件（直す前の forward.js で落ちる）。should（d6209b7）: (a) 待ちの 10 秒を時計で数えていたので、端末がページごと止めて（アプリの切り替え、凍ったタブ）戻ると、全員止まっていたのに諦めることがあった（プロセスごと 10.8 秒止める試験で 28 回に 1 回）→ 頼んだより大きく待った間は数えない（8 回止めて 8 回とも 8 本のまま。本当に止まったソフトウェアスレッドは今までどおり諦める）。(b) 諦めた後の検索がページに「1 本」を伝え、ページは次の訪問のためにそれを覚えていた → 諦めた後は伝えない。確かめて問題なし: 10 秒が遅い端末の本当の仕事で切れないこと（いちばん大きい塊は Qwen2.5 7B の分類器を 2 本で分けたときの 68M 積和、3.3 G 積和/秒の小コアで約 21ms）、諦めた後のやり直しが同じ数を書くこと（プロンプトのまとめ・attention・分類器）、2 度目の諦めが起きないこと、`terminate()` が効く前に遅れて書いても同じ数であること（1500 位置で logits は同じ）。本番（d6209b7）: Chromium で tiny-lm → TinyStories 15M の原本（float32）→ llm-jp-3 150M（int8。直す前に 10 秒止まった組み合わせ）→ SmolLM2 → Qwen2.5 0.5B と替えて、どれも止まらずに答えた（llm-jp-3 150M は準備完了 2.4 秒）。`?bench=1` の全ラウンド、macOS の WebKit の tiny-lm → llm-jp-3 150M も通った。**形**: forward.js の `waitUntil()` は、見ている数（ACTIVE か FINISHED）が `STALLED_MS`（10 秒）動かなければ諦めて false を返す。段はそのとき `giveUp()`（全部のソフトウェアスレッドを止め、検索をやめ、以後そのエンジンは 1 本: `ensureHelpers`・`setThreads`・`findThreads`・検索の候補がどれも 1 に戻る）をして、その段を自分でやり直す。**やり直してよい理由**: 段のどの塊も自分の行だけを、段の間は変わらない入力から書くので、止まる前に書かれた分は同じ数で上書きされる。**期限の根拠**: いちばん長い 1 トークンは CI の Qwen2.5 7B の 286ms（T101）で、1 段はその一部。10 秒はその 30 倍以上。止まったことは console に 1 行（`forward.js: a software thread stopped …`）と、エンジンの `lostThreads`。**試験**: `threads-check` に、起こされると塊を 1 つ取って（COUNTER と ACTIVE を足して）数えずに終わるソフトウェアスレッドを足した（期限は試験用に 500ms）。2 本で始めて、ロジットが 1 本とビット単位で同じまま最後まで行き、`lostThreads` が真、`setThreads(4)` が 1 を返す。**諦める処理を外すと試験は止まったまま**（90 秒で打ち切られた）ことを確かめた。forward-check・smoke も通る。**本番**（dbd23a0、`models.yml`、Linux の Chromium）: tiny-lm 424・llm-jp-3 150M 116・Qwen2.5 0.5B 37 tok/s、どれも 4 本。**T112 の案（ページから Worker への ping）は入れていない**: Worker ごと止まる場合は別の話で、持ち主の端末で見る日に（T117・T118 と同じ扱い）。**レビューで見てほしいこと**: 10 秒のあいだ画面は止まって見える（その後は 1 本で続く）。

<details><summary>T120 の採用時の記録</summary>

- 根拠: T96 の「`stopThreads()` の後は 1 秒ごとにエラー」は効いていなかった（レビューで外した、14a179b）。ブラウザがソフトウェアスレッドの Worker を止めると（iOS はメモリで Worker を黙って止めることがある、T112 の案）、取りまとめ役は段の途中で無期限に待ち、ページは「…」のまま。
- 手順: 待ちが一定時間進まなければエラーにして 1 本で続ける（期限は遅い 1 段より十分長く: 7B の 1 段が何 ms かを先に測る）。T112 の案（ページから Worker への ping）と合わせて決める。
- 完了条件: `threads-check` で、ソフトウェアスレッドを 1 本 terminate したときにエラーか 1 本での続行になり、止まらない。

</details>

- [x] **T97 [運用] Windows の Firefox の「Error in input stream」を調べる。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: 1 つ直して OK。** 偽の fetch と仕様どおりの偽の Cache API の上で worker.js の `download()` をそのまま動かす試験台（サブエージェント、36 件）で取り直しの経路を確かめた: 0 バイト目・ヘッダの途中・28 バイト目の直後・部品の途中・最後の短い部品のどこで切れても、書いたバイトはファイルと同じでヘッダも正しい、3 度目で読み込みが止まる、404 は 1 回で終わる、中止の後は取り直さない、切れた本文は Cache API に入らず取り直した全体が 1 回入る、T118 の包みの下でも同じ、8 MiB の実寸でも同じ。**直したもの（fb27af1）**: (a) エラーにならずに短く終わった本文を部品の全体と見なし、Cache API にも短いまま保存していた（読み込みは最後に「got N bytes instead of M」で失敗し、次からの訪問は毎回その短い部品をネットに聞かずに読んで失敗する。消すにはサイトのデータの削除）→ 部品の長さを確かめて短ければ取り直し、保存する写しは長さが合わなければエラーにする。(b) 保存した部品の読み出しが切れると、取り直しも同じ保存から読んでいた → 取り直しの前にその部品を保存から消す。(c) Cache API の `match()`・`keys()` が失敗すると、ネットに聞かずにすべてのモデルが読めなかった → ネットに任せる（T117 の coi.js と同じ）。本番: Windows の Firefox・macOS の WebKit・Chromium で tiny-lm と llm-jp-3 150M を 2 回ずつ、Chromium と Firefox のオフライン（部品は Cache API からしか来ない）で通った。**取り直しが本物のブラウザで働いた場面は今回も見ていない**（Windows の Firefox は 4 回だけ走らせた）。あれば良い（T129 に入れた）: 1 つの部品が最終的に失敗した後もほかの接続がモデルの残りを取り続ける、サイトの部品の 5xx を取り直さない。**原因の切り分け**（`models.yml`、Playwright の Firefox 150、llm-jp-3 150M を 6 回ずつ）: windows-latest の既定 0/6・`?coi=off` 1/6、windows-11-arm の既定 1/6・`?coi=off` 0/6。**Service Worker の有無で変わらない**（どちらも 12 回に 1 回）ので、Service Worker の作り（Fable が素通しにして戻した件）とは関係が無い。Windows の Firefox が、GitHub Pages が gzip で送る部品の本文を途中で途切れさせる。HF の取得（`fetchRange`）は本文の途中の失敗も 3 回まで取り直していたが、サイトのモデルの部品（`download()`）は取り直さず、1 回の途切れで読み込みが失敗していた。**手当て**（9b6d774）: 部品を 2 回まで取り直す（途中まで数えたバイトを引き、同じ位置に同じバイトを書き直す。ヘッダの 28 バイトの拾い方も、取り直しで 2 度来ても正しくなるようにした）。**本番の確認**: 同じ 4 通りを 8 回ずつ、**32 回すべて通った**。ただし取り直しの記録（console の 1 行）は失敗した回にしか残らないので、**取り直しが実際に働いた場面は見ていない**（直す前の率なら 32 回に 1 度も出ない確率は約 6%）。T70（Windows の WebKit が止まる）は今回の走行の範囲外で、再現は未確認。**レビューで見てほしいこと**: 取り直しの経路は偽の fetch の試験台で確かめる価値がある（この開発機では worker.js を単体で動かす道具が無い）。

<details><summary>T97 の採用時の記録</summary>

- **T70（Windows の WebKit が tiny-lm で止まる）もここで見る**（2026-09-25 に T70 をここへ合わせた）。
- 担当（Fable の切り分け、2026-09-25。対処は Fable が入れた）: **残る確認は Opus → Opus xhigh がレビュー**。**Fable の事前判断**: COOP/COEP のヘッダが要るのは同一オリジンの応答（文書、`worker.js`・`helper.js` などのスクリプト）だけで、クロスオリジンの取得（Pyodide の CDN、huggingface.co の Range 要求）は CORS で通るので Service Worker が触る必要は無い。だから `public/coi.js` は**同一オリジンの要求だけ `respondWith` し、クロスオリジンの要求はブラウザにそのまま任せる**形に変えてよい（本文を流す量が減り、T97 の疑いも消える）。手順: (1) 先に `coi.js` をその形にして、`coi.yml` で isolated が保たれること（`worker.js` と `helper.js` の読み込み、ソフトウェアスレッドが動くこと）を確かめる。(2) `browsers.yml` を 2〜3 回走らせ、Windows の Firefox で「Error in input stream」が出る回数を、変更の前（今日の走行の記録）と比べる。(3) それでも出るなら、どの取得で出るかを T82 の記録から絞る。T70（Windows の WebKit が止まる）は同じ走行で見て、再現したら別の項として切り出す。
- 事実: `browsers.yml` の OS のジョブで、ページが「TypeError: Error in input stream」を報告して失敗することがある。2026-09-24 の実行 36003988306（T93 の前）は windows-latest の Playwright の Firefox の llm-jp-3 150M で 1 回。2026-09-25 の 36049665597（段階 3 の後）は windows-latest と windows-11-arm で 1 回ずつ。Linux と macOS では出ていない。Firefox が取得の本文のストリームを読み切れなかったときの例外（モデルの部品の取得か HF の範囲取得）と見ているが、どれかは未確認。
- 疑い（未確認）: 段階 3 から、すべての取得が Service Worker（`public/coi.js`）を通り、本文を `new Response(response.body, …)` でそのまま流している。Firefox の Service Worker で大きな本文（8 MiB の部品）を流すときに途中で切れるなら、段階 3 から増えたことになる。ただし段階 3 の前にも 1 回出ている。
- 手順: 走行ごとに、どのブラウザ・どのモデル・どの取得で出たかを数える（T82 の JSON とコンソールの記録）。`?coi=off`（Service Worker なし）と既定を、Windows の Firefox で同じ回数ずつ走らせて比べる。Service Worker が原因なら、`coi.js` で本文を流さずに COOP/COEP を付けずに済む取得（Worker からの部品の取得など）は `respondWith` しない、などの手当てを検討する（ヘッダが要るのは文書と Worker のスクリプトだけのはず。要確認）。
- 完了条件: 出る条件（ブラウザ・取得・Service Worker の有無）が分かり、Service Worker が原因なら手当てを入れて、同じ回数の走行で出なくなる。
- **戻した理由（Fable、2026-09-25 夕方）**: Service Worker がクロスオリジンの応答を素通しにする形にしたら、持ち主の iPhone の Safari で Pyodide の NumPy の段が終わらず（T113）、持ち主のスマホと PC の Chrome で HF の取得が始まらなくなった。CI の Linux（米国）のブラウザでは通るので気づけなかった。朝までの Service Worker は応答を作り直してページに渡していて、作り直した応答は隔離の検査を必ず通る。素通しにすると、検査が CDN の本物のヘッダ（地域の edge で違う）に落ちる。**Windows の Firefox の件は、素通し以外の手で**（例: 大きな本文だけ `respondWith` せずに流す前に、Firefox でどの要求が壊れるかを T82 の記録から絞る）。Playwright の Firefox では素通しで llm-jp-3 150M が通っていた（23.7 tok/s）ので、方向は合っている。

</details>

- [x] **T119 [描画] レビューで出た画面の小さな直し。**（Opus medium、2026-09-25）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: 2 つ直して OK。** 直したもの（2f9f054）: (6) の進捗が、分割されたモデル（Qwen2.5 3B、Llama 3.2 3B、7B）ではシャードごとに 0 MB に戻っていた（全体は合計なので「1.7 / 26.0 MB」に落ちる）→ シャードをまたいで数える。途中で切れた範囲の本文を取り直しで 2 度数えていた（「15.6 / 12.6 MB」）→ 切れた分を引く。存在しないコミットのハッシュには HF が EntryNotFound で答える（RevisionNotFound は枝やタグの名前だけ）→ 文に「, or has no commit …」を足した。確かめて問題なし: X-Error-Code は CORS で見える（Origin を付けた curl で Access-Control-Expose-Headers に載っている）、本番で無いリポジトリ・無いコミット・ゲート付きがそれぞれの文で止まる（run 36160946064）、4xx は 1 回・5xx とネットの失敗は 3 回、MB/s は 1 秒たつまで出さない。(3)(4)(5) は読んで確かめた。429（HF の回数制限）の文は T129 に入れた。(1) リポジトリのシートは、間違っている欄（名前かリビジョン）に印を付け、その欄を直せば消える。(2) HF に断られた取得を訪問者の言葉で: 状態だけでは分からない（ゲート付きも無いリポジトリも 401）ので、CORS が見せる `X-Error-Code`（`GatedRepo`・`RevisionNotFound`・`EntryNotFound`）で分ける（`refused()`）。4xx は取り直さない。(3) スイッチとビット数の切り替えの読み込み直しは `bench` を URL から外す。Worker は `?kernel=off` でもサンプリングのカーネルを読む（使うかはスイッチが決める）。(4) 訪問者のファイルの設定の JSON の `conversion.dtype` を `weightsFor()` が上書きしない（`models-check` に 1 件）。(5) 設定とリポジトリのシートは開く場所の余白より高くならず、中でスクロールする。(6) HF の変換中の進捗は「届いた量 / 全体 MB · 速さ MB/s · 変換した割合」（T123 で分かった「取得待ちが変換の遅さに見える」件）。**画面**: `preview.yml`（ブランチ t119-screens、run 36146035951）で電話幅と PC 幅を撮り、**持ち主が見て「よさそう」**（2026-09-25）。master に入れてブランチを消した。**本番**（152e9bd、`models.yml`）: tiny-lm・SmolLM2 は答え、`hf:meta-llama/Llama-3.2-1B` は「… is gated on huggingface.co: …」の文で止まる。(3)(4) は先に b5b7437 で入れた。

<details><summary>T119 の採用時の記録</summary>

- (1) T88: シートの誤りの表示が名前の欄にしか付かない（リビジョンだけ間違えて直すと、名前の欄に誤りが残って Open できない）。(2) T88: ゲート付き（401）と無いリポジトリ（404）が「Could not fetch …: 401」のまま。訪問者の言葉にし、4xx は取り直さない（今は 3 回試す）。(3) T75: スイッチでカーネルを外して読み込み直すと、`bench=1` が URL に残っていればベンチがまた走り、Worker の init が `simdkernel.so` を読まないので「everything」の行もサンプリングが NumPy になる（backend の表示に出ない）。(4) T98: フォルダで開いた HF のファイルの設定の JSON の `conversion.dtype` を `weightsFor()` が上書きする。(5) T75・T98: 設定のシートに max-height と overflow が無く、Engine と More を両方開くと電話で上端が画面の外に出る見込み（未計測。390×844 で両方開いた画面を撮る）。 (6) T123: HF の変換が始まると進捗が「converting N%」だけになり、その % は届いたバイトの速さで進むので、取得待ち（日本の回線で約 8MB/s）が変換の遅さに見える。届いた量と速さ（MB/s）を出す（持ち主の Xiaomi 13T Pro で「大きいモデルの変換が遅い」と見えた、2026-09-25）。
- 完了条件: 6 つそれぞれの直しと、(5)・(6) の画面。

</details>

- [x] **T112 [運用] Safari で Hugging Face の取得と変換が失敗することがある。** — 状態: **完了（2026-09-26、持ち主の iPhone の Safari で確認）**。残っていた「持ち主の Safari での確認」は、持ち主の iPhone の Safari で HF から取得して変換するモデル（rinna の japanese-gpt2-small）が答えて済んだ（2026-09-26）。直したもの（WebKit が `Content-Range` を読めない → HEAD の `Content-Length`、Range 要求に全体が返る → 流れてくるうちに範囲だけ切り出す、短い 206 はエラー）と CI の WebKit の小さい組は、下の記録と Opus xhigh のレビュー（2026-09-25）のとおり。

<details><summary>T112 の記録</summary>

- 担当: Opus（調査と対処）→ Opus xhigh がレビュー。
- 分かっていること: CI は HF のモデルを WebKit で試していない（`browsers.yml` の huggingface ジョブは Chromium だけ、macOS のジョブはサイトのモデルだけ）。2026-09-25 に Fable が `models.yml` で WebKit（Linux と macOS）× HF の 3 モデル × 2 回読み込みを走らせた（結果は走行の Summary）。持ち主には、出た文・モデル・1 回目か切り替え後か・Safari の版と機種を聞いている。
- 疑うところ（順に）: (1) T107 の 16 MiB × 6 本で待ち行列が最悪 192MB になり、iPhone の 1 タブの上限に近い（8 MiB のときは 96MB）。`?hfParts=8` で再現しなければこれ。(2) T96 の使い回す共有メモリを最大 4GB で作ること（WebKit が断れば 1GB → 256MB の順に落ちる。全部断られれば共有でない 1 本）。(3) T99 の OPFS の同期の書き込み（WebKit は Worker の `createSyncAccessHandle` を持つが、容量で失敗すると理由が「not kept」に出るだけで、失敗にはならないはず）。(4) HF の Range 要求が WebKit で 206 でなく 200 を返す・`Content-Range` が無い場合の扱い（`fetchRange` は 200 も受けるが、全体の大きさが NaN になると「ファイルが途中で終わった」になる。AGENTS.md の落とし穴に似た形）。
- 手順: 持ち主の文を見て (1)〜(4) を絞り、`models.yml` の WebKit で再現させ、直す。`browsers.yml` の huggingface ジョブに WebKit の小さい組（3 モデル）を足して、以後 CI で見る。
- 完了条件: 持ち主の Safari で同じモデルが通る。CI の WebKit で HF の 3 モデルが通る。
- **原因と対処（Fable、2026-09-25、`models.yml` の WebKit で再現させた）。** 2 つとも HF の CDN と WebKit の取得の相性で、コードの読み方の問題ではなかった。
  1. **`Content-Range` が読めない**: 3 モデルとも「The file ended before all of its tensors were read」（ヘッダを読んだ直後）。全体の大きさを Range 応答の `Content-Range` から取っていたが、CORS ではこのヘッダは既定で隠され、huggingface.co は名前で見せる一方、CDN（xet-bridge）は `Access-Control-Expose-Headers: *` で見せる。**WebKit は `*` を効かせない**ので大きさが NaN になり、取得のループが 1 回も回らなかった。対処: 大きさが読めないときは HEAD の `Content-Length`（CORS が常に見せる）を取る（`fileSize()`・`sized()`）。これで llm-jp-3 150M instruct3 が WebKit で通った（68 tok/s）。
  2. **Range 要求にファイル全体（200）が返る**: SmolLM2（GGUF、bartowski）は読み込めても壊れた文（語彙の外のトークン）を出した。スレッド無し・float16 のキャッシュ無しでも同じ。`fetchRange` は 200 も受けて本文をそのまま流していたので、WebKit が（キャッシュか CDN の都合で）Range を無視してファイル全体を返すと、変換器が位置のずれたバイトを読んで重みが壊れた。対処: 200 なら要求した範囲を本文から切り出し、console に記録する。これで SmolLM2 も WebKit で通った（47.4 tok/s）。
  - 持ち主のスマホの Chrome と PC の「始まらない」は、この 2 つではなく T97（Service Worker の素通し）だった。T97 を戻して通った（2026-09-25 夕方）。
- 残り（Opus）: `browsers.yml` の huggingface ジョブに WebKit の小さい組（3 モデル）を足す。持ち主の報告が続くなら、状態行の文と進捗の有無を聞いて絞る。
- **案（Fable、2026-09-25 夕方、未実装）**: iPhone で Qwen2.5 0.5B が「fetching」で一度止まった（再試行で通った。一時的なもの）。iOS はメモリで Worker を黙って止めることがあり、ページは永遠に待つ。**ページが 5 秒ごとに Worker へ ping を送り、30 秒答えが無ければ「Worker が止まった。この端末には大きすぎるかもしれない」と出す**仕組みを書きかけて、出さなかった（持ち主の端末で試さずに出さない決まり）。注意: Worker は OPFS の同期の書き込み（T99、500MB で数秒〜数十秒）や `Llama()` の間は答えられないので、期限は保存の時間を見てから決める。
- **レビュー（Opus xhigh、2026-09-25）: 1 つ直して、CI の WebKit の分は済み。持ち主の Safari の確認が残る。** should: Range 要求に 200 が返ると、部品ごとにファイル全体を取って丸ごと持っていた（SmolLM2 の 145MB で 10 倍前後、スマホでは落ちる見込み）→ 流れてくるうちに範囲だけ切り出し、残りは取り消す。頼んだより短い 206 は黙って穴の開いたファイルを変換していた → `inOrder` がエラーにする（165794d。試験台で、全体を返すサーバの取得が 9.0 → 4.8 倍、部品ごとの持ち分がファイル全体 → 部品の大きさ、短い 206 はエラー）。残りの「`browsers.yml` の huggingface ジョブに WebKit の小さい組」は入れた（3c0846d）。本番: WebKit で tiny-lm・SmolLM2・llm-jp-3 150M instruct3・Qwen2.5 0.5B（run 36133274189）。

</details>

- [x] **T117 [運用] Service Worker の写しの 3 つの直し。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26。持ち主の判断「CI で試して大丈夫なら完了でいいよ」）**。**レビュー（Opus xhigh、2026-09-26）: OK。** 確かめて問題なし: `family()` を実際のビルドの名前で（`/_astro/` は 2 つで衝突なし、`index.astro_astro_type_script_index_0_lang.<hash>.js` も 1 つの家族にまとまる、wheel の正規表現）、問い合わせを無視するのがナビゲーションだけであること、Cache API の 3 か所がすべて `quietly`、coi-js-check。本番のオフライン（記録に無いモデルで。Chromium と Firefox で llm-jp-3 150M と llm-jp-3 150M instruct3 が、ネットを切った読み込み直しで答えた。Chromium で 2.7・3.1 秒）。あれば良い（やらない）: Vite が同じ名前（`index.<hash>.js`）のチャンクを 2 つ出すようになると、`family()` が片方を追い出す（今のビルドは 1 つ）。(1) オフラインで問い合わせの違いを無視して答えるのはページ（ナビゲーション）だけ（別のビルドの `worker.js?v=` を混ぜない）、(2) Cache API が開けなければネットに任せる、(3) wheel の版と `/_astro/` のハッシュを同じ家族にまとめ、新しいものが古い写しを追い出す。試験は `tests/coi-js-check.mjs`（直す前の coi.js は落ちる、デプロイでも走る）。**CI**: `preview.yml` の `offline`（Chromium・Firefox は通る、Playwright の WebKit は T111 からの既知の失敗）と、**本番に対する `models.yml` の `offline`**（2026-09-26、Chromium と Firefox で、tiny-lm と SmolLM2 135M Instruct がオンラインで答えた後、ネットを切って読み込み直しても答えた。Chromium のオフラインの準備完了 2.3 / 2.5 秒）。直す前の本番では持ち主の iPhone の Safari がオフラインで動いた。

<details><summary>T117 の採用時の記録</summary>

- (1) オフラインのとき `copyOf()` の `ignoreSearch` が同じサイトの全ファイルに効き、`?v=` の違うビルドのファイル（worker.js・forward.js・helper.js・jobs.js）が混ざりうる（新しいビルドの取得が途中で切れた後など。レビューのサブエージェントの試験台で再現）。ページ（ナビゲーション）だけにする。(2) 版が URL に入ったファイルの経路で `caches.open()` の失敗を捕まえていない（試験台で、ネットが生きていても worker.js と pyodide.asm.wasm が network error になった。T111 の前は Cache API に頼っていなかった）。捕まえてネットに回す。(3) NumPy の wheel（名前に版が入る）と `/_astro/` の古いファイルは `family()` で消えずに溜まる。
- 完了条件: `preview.yml` の `offline` で 3 つのブラウザ、持ち主の iPhone・Android・PC で機内モードの読み込み直し。

- **基準（2026-09-26、持ち主）**: T117 の直しを入れる前の本番（T111 のオフライン）で、持ち主の iPhone の Safari が機内モードの読み込み直しでも動いた。
- **実装（2026-09-26、8b57616）**: (1) オフラインで問い合わせの違いを無視して答えるのはページ（ナビゲーション）だけ、(2) Cache API が開けなければネットに任せる（`quietly()`）、(3) `family()` が wheel の版と `/_astro/` のハッシュを同じ家族にまとめる。試験は `tests/coi-js-check.mjs`（Node の vm で coi.js をそのまま動かす、偽の Cache API と fetch。直す前の coi.js は (1) で落ちる）。デプロイでも走る。**CI**（`preview.yml` の `offline`、master、run 36153206636）: Chromium（オフラインで準備完了 1.8 秒、答えた）と Firefox（6.2 秒、答えた）は通り、Playwright の WebKit は読み込み直しで「WebKit encountered an internal error」。**これは直す前（T111、run 36106652756）から同じ**で、T117 で起きたものではない（本物の iPhone の Safari はオフラインで動く）。**残り: 持ち主の iPhone・Android・PC で、直した版の機内モードの読み込み直し。**

</details>

- [x] **T118 [運用] Pyodide の読み込みの期限を「進まない時間」で決める。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: OK（記録を 1 つ直し、残りは T129）。** 確かめて問題なし（worker.js を Node で動かす試験台、25 件）: 包んだ Response が保つもの（206 も含む状態、ヘッダ、`clone()`、`instantiateStreaming` が見る Content-Type）と失うもの（`url`・`redirected`・`type`）、それを読むものが worker.js にも Pyodide 314.0.7 にも無いこと、204・304・状態 0 は包まずに返すこと、成功・3 つの段の止まり・フォールバックのどれでも包みと計時が外れること。**Safari・Firefox**: 本番で Playwright の Firefox 150（Linux と Windows）と macOS の WebKit 26.4 が Pyodide を読んで答えた。Firefox の Pyodide の段は T118 の前後で同じ（tiny-lm の準備完了 24.4 → 25.3 秒）。本物の Safari は未確認（持ち主の iPhone で）。**記録を直した**: 「モデルの取得が続く間は進んでいると数える」は半分だけで、包むのは版を解決した後なので、最初の 8 部品と tokenizer は包まれず、終わったときに 1 つ数えるだけ（AGENTS.md に書いた）。**残り（T129）**: `integrity` を付けた fetch（`loadPackage` の wheel）は本文を読み終えるまで解決しないので数えられず、NumPy の段が数えられるのは先読みのおかげ。版の解決（data.jsdelivr.com）には期限が無い。どちらも読み込みの経路なので、持ち主の端末で試す回に。AGENTS.md の構成の表に slow-check と slow.yml を足した。**形**: 読み込みの間だけ Worker の `fetch` を包み、本文が届いたバイトを数える（`watchArrivals()`。import の 2 ファイルは PerformanceObserver の完了で数える）。各段（loader・runtime・NumPy）は **30 秒何も届かなければ**諦めてフォールバック（T113）に入る。NumPy の先読みも 30 秒何も届かなければ待たずに進む（そのときは `loadPackage` が自分で取る）。**確かめ方**: `slow.yml`（`tests/slow-check.mjs`）。CI の中に CONNECT のプロキシを立て、サーバからの流れを 1 本の回線のように共有の速さで流す（`slow`）か、cdn.jsdelivr.net を 200kB で止める（`stall`）。Chromium をそこに通して本番を開く。**直す前の本番**（8bbb48d の時点）: `slow` 0.4 Mbps で 9.0MB が届いていたのに 202 秒後に「did not finish "the runtime" in 90 seconds」、フォールバックして失敗（**不具合を再現**）。`stall` は 173 秒でエラー。**直した後**（308ce01）: `slow` は **209 秒で準備完了、フォールバックなし、隔離あり・4 本**、`stall` は「"the runtime" got nothing from the network for 30 seconds」→ フォールバック → 117 秒でエラー（止まったままにならない）。普通の回線（`models.yml`: tiny-lm・llm-jp-3 150M・SmolLM2）は 5〜8 秒で準備完了。**レビューで見てほしいこと**: 読み込みの間は `fetch` が本文を数える包みの Response を返す（`url` が空になる。Pyodide と NumPy の読み込みは CI の Chromium で通った。Safari・Firefox では未確認）。モデルの部品の取得も同じ時間に走るので、Pyodide だけが止まってもモデルの取得が続く間は「進んでいる」と数える（止まりを見つけるのが遅れるだけで、見逃しはしない）。

<details><summary>T118 の採用時の記録</summary>

- 根拠: T113 の期限は経過時間（the loader 60 秒、the runtime 90 秒、NumPy 60 秒）。Pyodide の約 8.9MB（pyodide.asm.wasm 3.44MB、標準ライブラリ 2.51MB、NumPy の wheel 2.93MB。レビューのサブエージェントが jsDelivr で測った）を 90 秒で取れない回線（約 0.8Mbps 未満。日本の速度制限は 128k〜1Mbps）では、正常でも期限切れ → Service Worker を外して読み直し → 2 回目も期限切れでエラーになる（2a7d34a の前は遅くても最後まで読めた）。`await numpy`（NumPy の先読み）はどの期限にも入っていない。
- 手順: 期限を「N 秒、取得が進まない」にする（進みの見方は未検討: PerformanceObserver の resource か、Service Worker が数える）。先読みにも期限。
- 完了条件: 帯域を絞った CI（Playwright の CDP）で、遅いが正常な読み込みは期限切れにならず、止まった読み込みは期限切れになる。

</details>

- [x] **T81 [追加] 足せるモデルを調べ直して、一覧に足す（調査と追加）。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**レビュー（Opus xhigh、2026-09-26）: must-fix 1 つを含め 4 つ直して OK。** 確かめ方（サブエージェント）: 7 つのモデルの固定したリビジョン・ゲートなし・大きさを HF の API で、書式を本物の Jinja（transformers の `apply_chat_template`）と、ページと同じ `Conversion` と `Tokenizer` で作った ID の列で、4 つのプロンプトずつ比べた。**sarashina の系統（0.5B・1B・CAT-Translate）の書式は本物と文字まで同じ**。`<|user|>`（9）・`<|assistant|>`（8）・`</s>`（2）は sentencepiece の CONTROL の語片なので `specials` に 3 つとも要る（足りている）。止まりの 1・2・7〜9 は答えの中に出ない。TinySwallow と SmolLM2 1.7B は変換器が読む書式が本物と同じ（既定のシステム文も）。**直したもの**: must-fix（ba0c82e）: TinySwallow のライセンスの文「… for what it learned from Gemma's data」はモデルカードに無い読み込みで、カードが挙げる Prohibited Use Policy が抜けていた（方針 8）→ カードのとおりに。should（fc778fe）: llm-jp-3.1 1.8B instruct4 はモデルカードの使い方が必ず渡すシステム文を落としていた → 系統の書式（`LLM_JP_INSTRUCT`、止まりの 7 も）。CAT-Translate は generation_config が無く、カードは transformers の既定（greedy、繰り返しの罰なし）で訳す → greedy に。AGENTS.md の sarashina の書式の行も。should（4105957、97d0b44）: note の int8 の大きさが低すぎた: sarashina2.2 0.5B の 2 つ 0.6 → 0.9 GB（分類器が別、語彙 102400）、ほかに Qwen2.5 1.5B 1.6 → 1.7、DeepSeek-R1 1.5B 1.6 → 2.0、Pythia 1.4B 1.5 → 1.6（`modelBytes()` がメモリの警告と 6 ビットの判断に使う）。本番（97d0b44）: llm-jp-3.1 1.8B が指示に答え（13.6 tok/s）、CAT-Translate 0.8B が greedy で「Fuji Mountain is the highest mountain in Japan, and many people climb it in summer.」と訳して止まった。**残り（候補 T131）**: sarashina の系統は本物の書式に BOS が無い（`add_bos_token` が偽）のにページは `<s>` から始め、基底の sarashina2.2 0.5B にはダミーの空白（語片 271）が付く（sentencepiece の `add_dummy_prefix` が 0）。文への影響は未計測。**調査**は下の記録と `docs/t81-survey-2026-09-26.md`（変換器が受け付ける 276 件、9 月 21 日の条件なら 116 件、断られる最大は qwen3）。**足したもの**（a99b78f、4a4bd51。どれもゲートなし、リビジョン固定、ライセンスはモデルカードから）: sarashina2.2 1B Instruct（MIT）、CAT-Translate 0.8B と 1.4B（日英の翻訳、MIT）、TinySwallow 1.5B Instruct（Apache 2.0 と Gemma Terms）、llm-jp-3.1 1.8B instruct4（Apache 2.0）、SmolLM2 1.7B Instruct（Apache 2.0）。`browsers.yml` の huggingface ジョブに 6 つの組（`added`）を足した。**一覧の誤りを直した**: sarashina2.2 0.5B Instruct の書式は ChatML ではなく `<|user|>{prompt}</s><|assistant|>` で、`<|user|>`（9）・`<|assistant|>`（8）・`</s>`（2）はトークン、7〜9（ターンの印）で止める（この系統の chat_template は `selectattr` を使うので T73 の読み手では読めない。本物の Jinja で 1 ターンを描いて写した）。**本番**（`models.yml`、Linux の Chromium）: sarashina2.2 0.5B Instruct 25.2 秒・30.9 tok/s（指示に答える）、CAT-Translate 0.8B 24.8 秒・31.5 tok/s（「Fuji‑san is Japan's tallest mountain, and many people climb it during summer.」で止まる）、sarashina2.2 1B 35.7 秒・18.7 tok/s、CAT-Translate 1.4B 35.4 秒・19.6 tok/s、TinySwallow 1.5B 56.7 秒・14.7 tok/s、llm-jp-3.1 1.8B 49.6 秒・13.9 tok/s、SmolLM2 1.7B 43.3 秒・13.5 tok/s（どれも 4 本）。**足さなかったもの**: 64 ビットのメモリの 7〜8B（llm-jp-4-8b-instruct は harmony の書式、Qwen2.5-7B、Llama-3.1-Swallow-8B）と 3B 級は、取得が 6〜17GB で CI の 1 回が長いので、足すかは持ち主に（T115 で動くことは Llama-3.2-3B で確かめてある）。**実装の要るものは候補に積んだ**（T124〜T127）。**レビューで見てほしいこと**: 書式を手で書いた sarashina の系統（`SARASHINA`）の特殊トークンと止まり、TinySwallow のライセンスの書き方。

<details><summary>T81 の採用時の記録と調査の要約</summary>

- **T101（Memory64）の判定の結果で、4GB を超えるモデルを候補に数えるかを決める**（2026-09-25、持ち主の判断）。数えないことになっても、「4GB を超えるので入らないもの」の件数は記録する。
- 担当（Fable の切り分け、2026-09-25）: **Opus が数えて、そのまま動くものは Opus が足す。新しいアーキテクチャやトークナイザが要るものは、件数と「何が足りないか」を書いて止まり、レビュー担当（Opus xhigh）が判断する。** 足すときの手順は T79・T80 と同じ（`src/models.js`・`LICENSES`・`browsers.yml` の一覧。ライセンスはモデルカードから写す。`browsers.yml` で動くことを確かめる）。1GB を超えるものは「desktop only」の組へ。
- **実装はほとんど無い見込み（2026-09-25）**: そのまま動くモデルの追加は、`src/models.js` の項目（リポジトリ・リビジョン・大きさ・生成の設定・プロンプト）、`LICENSES`（`tests/models-check.mjs` が見る）、`browsers.yml` の huggingface ジョブの一覧を書き足すだけ。T80・T79 と同じ作業。新しいアーキテクチャやトークナイザが要るものが見つかったら、実装は別のタスクとして積む（この項の中ではやらない）。
- 目的: モデルの世界は動くので、**時間が経ってから調べ直す**。T80（2026-09-21 の調査でそのまま足せるもの）とは別に、「いまの実装では足りないが、少し足せば入るもの」まで広げて見る。
- **調べる条件は「そのときの実装」で決めること。** この項目に書いてある条件をそのまま使わない。着手時に AGENTS.md の「現在の構成」と `public/llama2_convert.py` の `check_config()`・`architecture()`・`layout()` を読み、**そのとき動くアーキテクチャ・トークナイザ・ファイルの形を洗い出してから数える**（2026-09-21 の時点では llama / qwen2 / gpt2 / gpt_neox、sentencepiece と Unigram と byte-level BPE、safetensors 1 ファイルか 1 つだけの分割、`rope_scaling` と sliding window と mlp_bias は不可、カーネルは dim・kv_dim・hidden が 32 の倍数。**これは当時の条件であって、そのときの条件ではない**）。条件が広がっていれば候補も変わる。
- 見る範囲（T80 より広く）: (1) 新しく出た小さいモデル（特に日本語）、(2) いまは断っているアーキテクチャ（gemma2、phi3、mistral、stablelm、olmo など）の件数と、それぞれ何を足せば通るか、(3) 分割が 2 ファイル以上のモデルがどれだけあるか（T78 は 1 ファイルだけ）、(4) `rope_scaling` を使うモデルの件数（2026-09-21 時点では 1.5B 以下に 0 件だった）、(5) GGUF しか無いモデルが出てきたか（T74 の価値が変わる）。
- やり方: HF の API で件数を数えてから中身を見る。**数えずに「増えそう」と書かない**（T74 で見返りが偏っていることに気づけたのは数えたから）。調べた数字と日付を TODO に残す。
- 完了条件: そのまま動くものは一覧に足して `browsers.yml` で動くことを確かめる。実装の要るものは新しいタスクとして積む（または「いまは無い」と書く）。前回（2026-09-21）からの差分が分かる形で書く。

- **調査（2026-09-26、Opus medium のサブエージェント。全文は `docs/t81-survey-2026-09-26.md`、問い合わせと件数つき）**: 合否は変換器の `normalize()`・`check_config()`・`pretokenizer_name()` をそのまま import して判定。ダウンロード上位 2000 件（text-generation、transformers）のうち 8.5B 以下は 739 件、受け付ける 4 系統（llama・qwen2・gpt2・gpt_neox）はそのうち 325 件（ダウンロードの 51.7%）。4 系統の config を 438 件判定して **276 件が通る**（一覧にあるもの 9 件を含む。落ちた 162 件のうち 99 件は量子化済みの再配布、13 件はゲート付きの meta-llama）。通った 276 件の内訳（メモリは見積もり）: int8 で 32 ビット 140、6 ビットで 32 ビット 6、64 ビットが要る 130、2 シャード以上 140、RoPE が llama3 41。**2026-09-21 の条件なら 116 件**で、差の 160 件は T105・T106・T101・T98 で開いた（前回の 299 件は問い合わせが残っていないので直接は比べられない）。**断られるもの**（739 件の中）: qwen3 90（ダウンロードの 29.3%、最大。head ごとの q・k の norm と、dim / heads と違う head_dim が要り、legacy のヘッダの変更になる）、mistral 22（日本の組織では 34。v0.2 以降は llama と同じ形で、model_type の別名で開く見込み）、lfm2 22、granite 12、gemma3 11、phi3 11、gemma2 9、phi 7、stablelm 5（日本の組織で 16）。`rope_scaling` は llama3 66・linear 3・yarn 1、sliding window が真の qwen2 は 0。GGUF は上位 500 件のうち Q8_0 を持つ 372 件、読める llama / qwen2 で Q8_0 の 8.5B 以下は 36 件。GGUF にしか無い、今の読み手で読める新しい小さいモデルは見つからなかった。**そのまま足せる候補**: SakanaAI/TinySwallow-1.5B-Instruct、llm-jp/llm-jp-3.1-1.8b-instruct4、sbintuitions/sarashina2.2-1b-instruct-v0.1（書式は手書き）、cyberagent/CAT-Translate-1.4b と 0.8b、HuggingFaceTB/SmolLM2-1.7B-Instruct。64 ビットで llm-jp-4-8b-instruct（harmony の書式を手書き）、Qwen2.5-7B-Instruct、Llama-3.1-Swallow-8B-Instruct-v0.5。**気づいたこと**: 一覧の sarashina2.2 0.5B Instruct の書式が ChatML になっているが本来は `<|user|>{prompt}</s><|assistant|>`（影響は未確認）、rinna/japanese-gpt-1b は gpt2 の `gelu_fast` だけで断られている（式は gelu_new と同じ、4 件開く）、`chat_template.jinja` を読まないので 16 件以上で書式が自動で取れない、CMSManhattan/JiRackUltra_1b は出どころが確かめられないので勧めない。

</details>

- [x] **T133 [性能] 自動のビット数: 64 ビットのメモリのあるブラウザでは int8 のまま。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26。様子見の覆す条件は持ち主の端末で）**。**形**: 規則を `forward.js` の `automaticDtype(int8, after, wide)` に 1 つ置き（int8 が 32 ビットに入るか、64 ビットのカーネルがあれば int8、どちらも無ければ 6 ビット）、Worker の `automaticBits()` はそれを呼ぶだけ。ページの `weightsFor()` は `deviceMemory` が 8（Chromium の上限）なら 6 ビットを頼まない（Worker に任せる）。`memoryWarning()` は 8 と言う端末を「8GB 以上」と見て、8GB を超えるモデルにだけ「…, and this device has 8 GB or more: it may run out of memory.」と出す（文面は持ち主が選んだ、2026-09-26）。それまで 8 と言う端末には 3.7GB を超えるモデル（3B 級）に必ず警告が出ていた。**試験**: forward-check の頭に Llama-3.2-3B の見出しと int8 の大きさ（3614847004 バイト）で「64 ビットあり → int8、なし → int6」と Qwen2.5 1.5B は常に int8、models-check に 8 と 4 の端末の `weightsFor()` と警告の文。**本番（`models.yml`、`hf:unsloth/Llama-3.2-3B-Instruct@006f5dcd`、0e6955e）**: Linux の Chromium 148 は **int8・64 ビット・4 本で 7.5 tok/s**、準備完了 64.4 秒（取得 57.0 秒、うち変換 11.1）、ヒープ 4266MB（T115 のレビューの 6 ビット・32 ビットは 1.9 tok/s・3459MB）。Playwright の Firefox 150 も int8・64 ビット（1.8 tok/s は Playwright の Firefox なので速さではない、AGENTS.md の落とし穴）、ヒープ 4266MB。macOS の WebKit 26.4 は **int6・32 ビットのまま**（1.6 tok/s、準備完了 128.0 秒、ヒープ 3061MB）で答えた。WebKit の走行はコンソールの 404 1 つで「失敗」と記録された: 10.4 秒の、取得の始まり（2 シャードのモデルを `?hf=` で開くと、まず `model.safetensors` を問うて 404、次に index を読む）。一覧に入れるモデルは index を名指しするので起きない（T132 で確かめる）。**様子見（覆す条件）**: 持ち主の端末で 3B 級を動かし、メモリ不足やタブの落ちが出たら 6 ビットに戻す。**レビューで見てほしいこと**: 8 と言う端末で 6 ビットを頼まなくしたので、本当に 8GB ちょうどの端末で 7B（ヒープ約 9.7GB）を選ぶと、警告は出るが int8 で読みにいく（それまでは 6 ビットを頼んでいた。どちらでも 8GB には入らない）。**レビュー（Opus xhigh、2026-09-26）: must-fix 1 つを直して OK。** 確かめ方: 手元の試験（pytest 436・smoke・forward-check の共有・`--plain`・`--wide`・6 つの検査）、新しいモデル 13 個の config.json から峰を `footprint()` で出した（下）。`models.yml` で本番に対して記録に無い境界: 同じページで tiny-lm → Llama 3.2 3B（64 ビットの int8、準備完了 63.2 秒・7.7 tok/s）→ Qwen2.5 3B（32 ビットの int8、54.8 秒・8.3 tok/s）と、32 → 64 → 32 ビットのメモリに替える（Chromium）。WebKit（macOS）で一覧の Llama 3.2 3B（6 ビット・32 ビット、142.5 秒・4.5 tok/s）、sarashina2.2 3B（6 ビット、138.4 秒・4.7 tok/s）、Qwen2.5 7B（断り）。Chromium で `?hf=` の Qwen2.5-32B。**must-fix（1f37a98）**: Python から呼ばれた JavaScript の中で投げた例外は `PythonError`（`type` が `JsException`）で届き、Worker の T90 の判定を素通りしていた。HF の変換は重みのメモリを変換器の `sink.open()` 越しに作り、forward.js は KV を Python の forward の呼び出しの中で伸ばすので、**Safari の「4 GB を超える」断り（T132・T125 で 11 個）と、HF のモデルのメモリ不足が、すべて Python のトレースバックで訪問者に出ていた**（CI の WebKit の Qwen2.5 7B で見つけた。T90 から）。いまは最後の行から元の名前と文を取り出して同じように扱う: 本番で WebKit の Qwen2.5 7B は断りの文だけ、Chromium の `?hf=` の Qwen2.5-32B は「This device ran out of memory for …」の文で止まる。Error はその文、RangeError はメモリ不足、スタック溢れと TypeError はトレースバックのまま（Node の Pyodide で確かめた）。**峰（GiB、チェックポイント + forward が置くもの、4096 位置、relaxed の補正あり、共有 / 共有なし）**: Llama 3.2 3B の int8 4.41 / 5.07、sarashina2.2 3B 4.83 / 5.76、Qwen2.5 3B 3.82 / 4.03、Qwen2.5 7B 9.15 / 9.48、Swallow 8B 10.06 / 10.81、llm-jp-4 8B 10.68 / 11.43、Mistral 系の 7B 9.18〜9.33 / 9.93〜10.08。**64 ビットのメモリでは、共有を断られても上限（16GiB）に当たらない**ので、T130 の形が残るのは 32 ビットに置く Qwen2.5 3B（3.82 / 4.03）だけ。T133 で、T130 の案 (a)「断られたら 64 ビットに」は Chrome と Firefox でそのまま取れる。**sarashina2.2 3B は WebKit でも入る**: relaxed SIMD が無いので補正が無く、6 ビットで 4GiB の内側（補正ありの見積もりでは 4.05 で外、と一度見誤った。CI で確かめた）。**8 と言う端末で 6 ビットを頼まないこと（見てほしいこと）**: 本当に 8GB の端末では 7B は int8（9.2GiB）でも 6 ビット（7.4GiB）でも入らず、16GB 以上なら int8 が 2 倍速い。頼まないほうが良い（OK）。**台帳の直し**: 上の「一覧に入れるモデルは index を名指しするので起きない（T132 で確かめる）」は誤り。一覧の分割されたモデルも `model.safetensors` を先に問うので、WebKit では 404 がコンソールのエラーに出る（今回の WebKit の 4 回すべて。AGENTS.md の T132 の行は正しい）。**残り**: `?hf=` の 16GiB を超えるモデル（Qwen2.5-32B、int8 で約 37GB）は、見出しの時点で断られずに取得を始め、7.8GB を取ったところで書き込みが失敗した（RangeError: Invalid typed array length。原因は未調査）→ T129 の (7) に。

<details><summary>T133 の採用時の記録</summary>

- 判断（2026-09-26、T115 のレビューで持ち主に出した (c)）: 今の規則「int8 が 32 ビットのメモリに入らなければ 6 ビット」（T98・T115）を、64 ビットのメモリ（Memory64）が使えるブラウザ（Chrome・Firefox）では「int8 のまま 64 ビットのメモリに」に変える。Safari（Memory64 なし）は今のまま 6 ビット。
- 数字（CI の Linux の Chromium）: Llama-3.2-3B は 6 ビット・32 ビットで 1.9 tok/s・ヒープ 3459MB、int8・64 ビットで 4.1 tok/s・4266MB（2.2 倍速く、メモリは 23% 多い）。Qwen2.5 7B は int8・64 ビットで 3.6 tok/s・9716MB、6 ビットは int8 の 0.37〜0.53 倍（T98）。6 ビットの品質の代償は 1B 級で +1.4〜1.7%（T98）。64 ビットのメモリは 32 ビットより約 1 割遅い（T101）。
- 一緒に直すこと: ページの `weightsFor()` は `deviceMemory` の半分を超えると 6 ビットを頼むが、Chromium の `deviceMemory` は 8 が上限なので、どの機械でも int8 が約 3.7GB を超えると 6 ビットになる（大きなデスクトップでも）。`memoryWarning()` も同じ理由で 7B には必ず出る。上限の 8 と答える端末では、この 2 つをどうするか（当てない、文を「8 GB 以上」に）を決める。
- 様子見（覆す条件）: 持ち主の端末で 3B 級を動かし、メモリ不足やタブの落ちが出たら 6 ビットに戻す。
- 完了条件: `models.yml` で Llama-3.2-3B が自動で int8・64 ビットになり 4 tok/s 前後。macOS の WebKit では 6 ビットのまま。forward-check・models-check が新しい規則を見る。

</details>

- [x] **T132 [追加] 大きいモデル（3B 級と 7〜8B）を一覧に足す。**（Opus medium、2026-09-26）— 状態: **レビュー済み（Opus xhigh、2026-09-26）。完了条件の「持ち主のデスクトップで 1 つ以上」が残る**。**足したもの**（c279e6b。どれもゲートなし、リビジョン固定、ライセンスはモデルカードから）: Qwen2.5 3B Instruct（Qwen Research License Agreement、非商用）、sarashina2.2 3B Instruct（MIT）、Qwen2.5 7B Instruct（Apache 2.0）、Llama 3.1 Swallow 8B Instruct v0.5（カードの License の節の「Meta Llama 3.1 Community License and Gemma Terms of Use」。メタデータは llama3.3 と gemma）、llm-jp-4 8B instruct（Apache 2.0）、Llama 3.2 3B Instruct（unsloth の写し、原本は meta-llama、Llama 3.2 Community License）。**動かないもので外したものは無い。** 7〜8B の note に「Chrome and Firefox」（新しい文言）。`browsers.yml` の huggingface ジョブに 4 つ目の組 `huge`（6 つ、取得 68GB、1 回 3000 秒、ジョブは 120 分）。**本番（Linux の Chromium 148、4 本）**: 一覧の項目で（c279e6b）Qwen2.5 3B 58.6 秒・8.1 tok/s・4072MB（32 ビットの int8）、sarashina2.2 3B 67.4 秒・8.1 tok/s・4409MB、Qwen2.5 7B 162.4 秒・3.8 tok/s・9716MB、Swallow 8B 215.4 秒・3.7 tok/s・10322MB、llm-jp-4 8B 208.8 秒・2.5 tok/s・10973MB（`?hf=` の書式なしでは 3.6 tok/s。一覧の書式はシステム文のぶんプロンプトが長い）。Llama 3.2 3B は `?hf=` で 64.4 秒・7.5 tok/s・4266MB（T133 の確認）。どれも日本語か英語で指示に答えた（sarashina・Swallow は箇条書き、llm-jp-4 は表。llm-jp-4 は final のチャンネルで直接答えた）。**書式**: llm-jp-4 は harmony を手で書いた（`HARMONY`。`{date}` をページが今日で埋める `filled()`、`<|channel|>final<|message|>` まで書く、特殊トークンの後に空白。本物の Jinja と `tokenizers` で 4 つのプロンプトが同じ ID）。Swallow は読み手が自分で読める（本物と同じ ID、プロンプトの `trim` だけ違う）。sarashina2.2 3B は 1B と同じ語彙と特殊トークンなので `SARASHINA`。Qwen の 2 つは一覧の Qwen と同じ `CHATML`。**レビューで見てほしいこと**: (1) llm-jp-4 の書式の空白の工夫（エンジンに正規化を足さずに書式で合わせた。T131 で直すならそこ）と、本物は BOS なしで始まるのにページは BOS から始めること、(2) 答えの頭に空白が 1 つ付く（llm-jp-4 は ▁ から書く）、(3) 出典の一覧で unsloth の写しが「Llama 3.2 3B Instruct (GGUF)」と出ていた（`sources()` が `original` のある項目の取得元に必ず「(GGUF)」を付けていた。1B の頃からの誤り）→ **持ち主が文面「(copy)」を選び、直した**（2026-09-26）、(4) 週 1 回の `browsers.yml` に 68GB の取得を足してよいか → **持ち主「大きいモデルも入れっぱなしでよい」**（2026-09-26）。**レビュー（Opus xhigh、2026-09-26）: must-fix なし。直したもの 2 つ（1 つは T133 の must-fix と同じ直し）。** 確かめ方（新規のサブエージェント）: 6 つの固定したリビジョンがあり、ゲートが無く、`download` がシャードの合計と同じで、トークナイザのファイルがあること、ライセンスの文がモデルカードのとおり（Qwen2.5-3B の LICENSE の題は「Qwen RESEARCH LICENSE AGREEMENT」、Swallow 8B はカードの本文）を HF の API で。書式は、ページと同じ `Conversion`（シャードの見出しは Range で取り、重みは取らない）と `Tokenizer` で作った ID の列を、transformers 5.12.1 の `apply_chat_template` と 8 つのプロンプトで比べた: llm-jp-4 の HARMONY は間の ID が 8 / 8 で同じで、違いは先頭の BOS（T131）と、足した `<|channel|>final<|message|>`（本物のトークナイザが assistant の final のターンを符号化するのと同じ ID）だけ。Swallow 8B と Llama 3.2 3B は前後に空白のあるプロンプト（テンプレートの `trim`）の 1 つだけ違う。Qwen2.5 の 2 つは BOS と、本物の書式の既定のシステム文を落としている分（T138 へ）。止まりのトークンは 6 つとも generation_config の eos を含む（llm-jp-4 の 2・10・11・13 は `<|return|>`・`<|start|>`・`<|end|>`・`<|call|>`、1 は BOS）。本番（`models.yml`）: WebKit で一覧の Llama 3.2 3B と sarashina2.2 3B が 6 ビットで答え、Qwen2.5 7B は断りの文で止まる（直した後）。Chromium で tiny-lm → Llama 3.2 3B → Qwen2.5 3B と同じページで替えて答える（T133 の項）。**直したもの**: (1) 1f37a98（T133 の must-fix と同じ）: Safari で 7〜8B を選ぶと、断りの文が Python のトレースバックで出ていた。(2) 82b5d22: `filled()` が打った文を置き換えの文字列として渡していたので、`$$`・`$&`・`` $` ``・`$'` がパターンとして読まれた（`$'` は HARMONY の後ろ半分を特殊トークンごと書き込む。T60 からの形）→ 関数で渡す（models-check に 4 つ）。**見てほしいこと**: (1) llm-jp-4 の空白の工夫は本物と同じ ID なので OK、BOS は T131。(2) 答えの頭の空白は T131。(3)(4) は持ち主が決めた。**あれば良い（やらない）**: 一覧の分割されたモデルは `model.safetensors` を先に問うので、WebKit では 404 がコンソールに 1 つ出る（一覧の項目が index を名指しできれば消える）。Llama 3.2 の止まりは config.json の 128000・128009 で、generation_config の 128001・128008 が無い（128009 の `<|eot_id|>` で止まるので実害なし）。

<details><summary>T132 の採用時の記録</summary>

- 足すもの（T81 の調査の値。どれもゲートなし、リビジョン固定）:
  - 3B 級: Qwen/Qwen2.5-3B-Instruct（`aa8e72537993ba99e69dfaafa59ed015b17504d1`、6171926992 バイト、qwen-research＝非商用、書式は自動）、unsloth/Llama-3.2-3B-Instruct（`006f5dcd1393c3add266de40994ba96225e9689d`、6425529048、llama3.2。自動の書式には今日の日付が入る）、sbintuitions/sarashina2.2-3b-instruct-v0.1（`4f3626fb1b64b3e97c908e67f27b2d627ba2a999`、6711252896、MIT、書式は `SARASHINA`）。
  - 7〜8B（64 ビットのメモリ、Chrome と Firefox だけ）: Qwen/Qwen2.5-7B-Instruct（`a09a35458c702b33eeacc393d103063234e8bc28`、15231271888、Apache 2.0、書式は自動）、llm-jp/llm-jp-4-8b-instruct（`098f2b2cf33021eba19a6d3582aa3d071ccc0aff`、17180435544、Apache 2.0、harmony の書式を手で書く、語彙 196608）、tokyotech-llm/Llama-3.1-Swallow-8B-Instruct-v0.5（`b1f8317099a97e790ec872c1225ca155979b4816`、16060556376、ライセンスは llama3.3 と gemma の 2 つ、書式を手で書く）。
- 分かっていること: Qwen2.5-3B は int8 のまま 32 ビットに入る（準備完了 65〜69 秒、4.9〜8.4 tok/s、ヒープ 4072MB）。Qwen2.5 7B の int8 は Chromium で準備完了 191 秒・3.6 tok/s・ヒープ 9716MB、Firefox でも 9.7GB を取れた（2026-09-26 のレビュー）。日本の回線（8.3 MB/s）では 3B 級の取得に約 12〜13 分、7〜8B に約 31〜35 分。
- 手順: T81 と同じ（`src/models.js`・`LICENSES`・`browsers.yml`）。note に「desktop only」と、64 ビットが要るものは Chrome と Firefox だけであること。`browsers.yml` の huggingface ジョブには大きい組をもう 1 つ（1 回の持ち時間 3000 秒）か、手動だけにするかを決める。書式を手で書くものは本物の Jinja と `tokenizers` で突き合わせる（T81 のレビューのやり方）。
- 完了条件: `models.yml` で 6 つを試し、準備完了・tok/s・ヒープを記録する。動かないものは理由を書いて一覧に入れない（持ち主: 試さないと分からない）。持ち主のデスクトップで 1 つ以上。

</details>

- [x] **T94 WebGPU で forward を GPU に置く — 第 0 段（判定の計測）で完了。** — 状態: **完了（2026-09-26、持ち主の判断「T94 を完了にして新しく採番してもいいよ」）**。第 0 段の結論: **CPU の forward をそのまま移した形（バッチ 1 の生成、層ごとに 7 つの行列積と 7 つの小さなディスパッチ、毎トークン logits を全部読み戻す）では、持ち主の 3 台とも CPU に勝たない**（Llama 3.2 1B の 1 トークンの GPU ÷ CPU（スレッドの上限の見積もり）: Android 0.97、iPhone 0.69、ARM の Chromebook 0.53）。スマホの GPU は大きい行列で CPU 1 本の約 3 倍の帯域を引くが、1 トークンに約 18〜20ms の固定費が乗る。その内訳（ディスパッチの回数と読み戻しの大きさ）は分けて測っていない。**GPU を生かす形（プロンプトの行列 × 行列、融合、GPU でのサンプリング）は測っていない**（持ち主の指摘、2026-09-26）ので、それは T134（総合的なベンチマークに GPU の計測を含める）と T135（WebGPU で速くする）に新しく採番した。`/gpu-test/` のページと `gpu.yml` は T134 に引き継ぐ。

<details><summary>T94 の記録（採用・設計・第 0 段の計測）</summary>

- 担当: 第 0 段（判定の計測）と第 1〜3 段の実装は Opus、各段の判定とレビューは Opus xhigh。Worker とページの契約を変えるので、第 1 段の設計は docs/review-by-opus.md の「Fable に回すもの」に当たるかをレビュー担当が決める。速さの計測は持ち主の端末（下）。
- **目的との折り合い（持ち主が採用した）**: WGSL のシェーダは Python でも WASM でもないが、AssemblyScript の SIMD カーネルと同じく「重い計算だけ外に出す」の延長として扱う。Python は今までどおりトークナイザ・サンプリング・生成ループを持つ。ステータス行は何で動いたかを必ず言う（「WebGPU」）。
- **根拠**: バッチ 1 の生成は重みを 1 回読むだけのメモリ帯域律速で、CPU は実際の int8 カーネルで約 15〜20GB/s が頭打ち（T93、この開発機）。1B 級の int8 は 1 トークンに約 1.3GB を読むので、ソフトウェアスレッドを使っても十数 tok/s で止まる（CI の Linux で Llama 3.2 1B 19.6、3B 4〜7 tok/s）。**デスクトップの GPU は数百 GB/s**、Apple の M 系は 100GB/s 以上（未計測）。スマホは GPU と CPU が同じ LPDDR を共有するが、CPU の 1〜4 コアが出せる帯域より GPU のほうが多く引けることが多い（未計測。第 0 段で測る）。プロンプトの処理（T108）は行列 × 行列なので GPU で大きく効く見込み。
- **2026-09 の対応（調べた範囲、未確認のものはそう書く）**: Chrome と Edge は 113 から既定で有効（Android の Chrome も）。Safari は 26（macOS・iOS・iPadOS）で既定で有効。Firefox は Windows で 141、Apple Silicon の macOS で 145 から、Android はまだ（2026 年の後半の予定）。int8 の内積は WGSL の言語拡張 `packed_4x8_integer_dot_product`（`dot4I8Packed`、Chrome 123 から。Safari と Firefox は未確認、`navigator.gpu.wgslLanguageFeatures` で見る）。半精度は `shader-f16`（機能、アダプタによる）。**Worker の中の `navigator.gpu`** は仕様上使えるが、各ブラウザでの実際は第 0 段で確かめる。
- **T94 を保留にした理由（計測の場所）への答え**: GitHub の GPU のランナー（T4、Linux で 1 分 $0.052）は Team か Enterprise の組織だけで、個人のこのリポジトリでは使えない。だから**速さは持ち主の端末で測る**（PC の Chrome、Android の Chrome、iPhone の Safari）。第 0 段の計測のページを本番に置き、持ち主が開いて、結果を T91 の Issue の形（`benchmark` のラベル）で送る。**CI は正しさだけ**: Chromium のソフトウェアのアダプタ（SwiftShader）で WebGPU が取れれば、NumPy との一致を CI で見られる（取れるかは未確認。第 0 段の最初に `browsers.yml` の Linux で見る）。
- **作り（T93 の後の形に合わせた）**:
  - いまの forward は JavaScript（`forward.js`）で、Python は `Llama(external=)` 越しに `start(plan)` → `forward(token, pos)` / `forwardMany()` / `bind(logits)` を呼ぶ。**GPU 版は同じ契約の `gpu.js` を 1 つ足す**（Python と変換器は変えない。legacy 形式も変えない）。
  - **同期と非同期の橋渡し（第一案、2026-09-25 に書き足した）**: 生成ループは Worker の JavaScript が Python のジェネレータを `next()` で 1 つずつ回していて、すでに 50ms ごとに `await` でイベントループに戻っている（`generate()` の `breathe()`）。だから **ジェネレータが 1 トークンごとに GPU の結果待ち（`mapAsync` の Promise）を印として `yield` し、JS がそれを `await` してから次の `next()` で Python が logits を読む**形にすれば、GPU の Worker も共有メモリも要らず、**隔離されていないページでも GPU が使える**。Python の変更は生成ループの待ちの `yield` 1 つ（プロンプトは `forwardMany` を 1 回の送信にして、その後に 1 回待つ）。CPU の経路は今のまま。持ち主が見せた「Pyodide → JS の薄いラッパー → WebGPU、`await` で待つ」の案から（汎用の GPU Tensor は採らない: 1 演算ごとのディスパッチと往復の固定費が 1 トークンに数百回乗る）。**代わりの案（以下）**: 第 1 段のレビューで 2 つを比べる。
  - **同期と非同期の橋渡し（代わりの案）**: Python の生成ループは同期で `forward()` を呼ぶが、GPU の結果の読み戻し（`mapAsync`）は非同期で、同じスレッドでは待てない（イベントループに戻れない）。だから **GPU を動かすのは別の Worker（GPU の Worker）**にし、モデルの Worker は共有メモリの制御語に仕事を書いて `Atomics.wait` で待つ。GPU の Worker は `queue.submit` → logits を `mapAsync` で読み、共有メモリに書いて `Atomics.notify`。ソフトウェアスレッド（helper.js）と同じ仕組みで、**cross-origin isolated のページだけ**（T93 の段階 3 から既定）。隔離されていないページは CPU のまま。別の道の JSPI（T102、保留）はここでは使わない。
  - **重み**: 読み込みの後、WebAssembly のメモリのチェックポイントから GPU のバッファへ 1 回だけ送る。int8 は値とスケールを**チェックポイントと同じ並び**のまま（形式を変えない）。1 つのバッファの上限（`maxStorageBufferBindingSize`。既定 128MiB、アダプタによって数 GB）を超えるテンソル（Llama 3.2 3B の分類器 128256 × 3072 の int8 は 394MB）は行の塊に分ける。int6 は第 2 段（シェーダで広げる）。
  - **1 トークンの forward を丸ごと GPU に**: 1 回のコマンドで全層（1 層に 10 前後のディスパッチ）、KV キャッシュも GPU に置き、読み戻すのは logits だけ（語彙 × 4 バイト。128256 語で 513KB）。層ごとの往復はしない。シェーダは forward.js の段と同じ順・同じ単位（rmsnorm・行列 × ベクトル・rope・attention・swiglu・add、GPT-2 と NeoX の layernorm・gelu）にして、`forward-check` の許容で NumPy と比べられるようにする。
  - **int8 の行列 × ベクトル**: (a) 重みをシェーダで float32 に広げて掛ける（帯域律速なら十分、どのブラウザでも動く）、(b) 活性値を int8 に量子化して `dot4I8Packed`（CPU の `matmul_q8` と同じ計算、出せるブラウザだけ）。第 0 段で両方測る。
  - **メモリ**: 重みは WebAssembly のメモリと GPU の両方に残る（Apple とスマホは同じ物理メモリなので 2 倍）。第 1 段はそれを受け入れ、変換の `sink` から GPU へ直接書く形（WebAssembly のメモリを通さない）は第 3 段で決める。GPU を失ったとき（`device.lost`）は CPU に戻る。
- **段階と止まる点**:
  - **第 0 段（判定の計測、小〜中）**: 計測のページ `public/gpu-test/`（本番のページには触らない。T93 の `coi-test` と同じ扱い）。出すもの: アダプタの情報・上限・機能（`shader-f16`、packed int8）、Worker の中で取れるか、int8 の行列 × ベクトルの GB/s（(a) と (b)、llm-jp 150M・Llama 1B・3B の層と分類器の形）、ディスパッチ 200 回と logits の読み戻し（0.5MB）を含む 1 トークン相当の往復の時間、共有メモリと `Atomics.wait` を挟んだ橋渡しの時間。それらから **1B と 3B の見込みの tok/s = 1 ÷（読む量 ÷ 帯域 + ディスパッチの固定費 + 往復）** を出し、同じ端末の CPU（`?bench=1` の数字）と並べる。**止まる点**: 持ち主の 3 台の結果を並べて報告。判定の線: **1B 級の見込みが持ち主の PC で CPU の 1.5 倍以上**なら第 1 段へ。覆す条件: スマホで 1.5 倍を超えれば PC が下回っても進む（訪問者の多くはスマホ）／往復が 1 トークンの時間の半分を超える端末ばかりなら取りやめ。
  - **第 1 段（Llama 系・int8・float32 の KV・`?gpu=on` のときだけ）**: `gpu.js` と GPU の Worker、橋渡し、正しさの試験（CI のソフトウェアのアダプタか、取れなければ計測のページに比べる機能を足して持ち主の端末で）。止まる点: `?gpu=on` で本番に出し、持ち主の端末で tok/s を測って報告。
  - **第 2 段**: GPT-2・NeoX、int6、プロンプトのまとめ処理（行列 × 行列）、`shader-f16` があれば float16 の KV。
  - **第 3 段（既定にする）**: 最初の数トークンで CPU のスレッドと GPU の両方を測って速いほうを選ぶ（スレッド数の検索と同じ考え方。**開発機の結果で固定しない**）。変換の結果を GPU へ直接書くか。止まる点: 既定にするかは持ち主が決める（方針 6 の「どの端末でも動く」との関係も書く）。
- **第 0 段の実装（2026-09-25、Opus medium、fbdbead）**: 計測のページ `https://takano32.github.io/pyodide-llm/gpu-test/`（先にモデルのページを 1 回開くと隔離される）。「also Llama 3.2 3B」にチェックすると 3.2GB を GPU に置く。結果は Markdown とコピーのボタン、Issue のリンク（ラベル `gpu`）。**CI（`gpu.yml`、Linux・macOS・Windows）**: Chromium・Chrome・Edge はソフトウェアのアダプタ（SwiftShader、上限 1GiB、packed int8 あり、shader-f16 なし）が取れ、**2 つのシェーダは JS の参照と一致**（相対誤差の最大 1e-5〜2e-4）。速さはソフトウェアなので意味が無い（0.0〜0.1 GB/s）。ヘッドレスの Firefox は Linux で `navigator.gpu` なし、macOS であってもアダプタなし、WebKit は無し。橋渡しの往復は 12〜45 µs（1 トークンの時間に比べて無視できる）。同じページの CPU（`matmul_q8`、1 本）は CI で 8.8〜18.7 GB/s。**止まる点: 持ち主の 3 台（PC の Chrome、Xiaomi 13T Pro の Chrome、iPhone の Safari）で開いた結果を待つ。**
- **持ち主の Android（Xiaomi 13T Pro、Chrome 153、GPU は Arm Valhall、2026-09-25）**: シェーダは JS と一致（1e-5 級）、packed int8 と shader-f16 あり、1 つのバインディングは 256MiB。int8 の行列 × ベクトル（GPU の shader 広げ / packed / CPU 1 本）: 150M の w1 1.7 / 3.2 / 8.8 GB/s、1B の w1 11.4 / 23.0 / 10.7、1B の分類器 26.6 / 31.3 / 10.5。**1 トークン**: llm-jp 150M は 25.3ms（packed 21.7ms、46 tok/s）で CPU の見積もり（1 本 94、スレッドの上限 188 tok/s）の 0.25 倍、**Llama 1B は 76.8ms（packed 67.0ms、14.9 tok/s）で CPU のスレッドの上限の見積もり 15.4 tok/s の 0.97 倍**。橋渡しの往復 94.8µs。**読み**: 大きい行列の帯域は GPU が CPU 1 本の約 3 倍だが、1 トークンに約 18〜20ms の固定費（約 240 回のディスパッチ、小さい行列でのシェーダの効率、logits の読み戻し）が乗る（150M は帯域だけなら 3.5ms のところ 21.7ms、1B は 1.39GB ÷ 31GB/s = 45ms に約 20ms）。だからモデルが大きいほど有利で、3B は約 1.2 倍の見込み（未計測）。**この端末では線（スマホで 1.5 倍超）に届かない**。CPU の「上限 2 倍」は見積もりで、この端末のスレッドの実測ではない。判定は PC（と iPhone）の結果を待つ。第 1 段に進むなら、固定費を下げる工夫（小さい行列を 1 つのディスパッチにまとめる、行列ごとのワークグループの形）が要る。
- **持ち主の iPhone（Safari 27、iOS 18.7 の表示、Apple GPU、2026-09-25）**: シェーダは JS と一致、packed int8 と shader-f16 あり、バインディング 683MiB。**このページは隔離されていなかった**（Worker の中の WebGPU は取れた）。行列 × ベクトル（shader 広げ / packed / CPU 1 本）: 150M の w1 3.9 / 2.4 / 9.8 GB/s、1B の w1 8.8 / 8.2 / 20.6、1B の分類器 31.8 / 20.6 / 26.9。**CPU 1 本が Android の約 2 倍速い。** 1 トークン: 150M 18.0ms（packed 13.0ms）、**1B 49.0ms（20.4 tok/s）、packed は 61.0ms で遅い**。CPU のスレッドの上限の見積もり 29.6 tok/s の 0.69 倍。ただしモデルのページも隔離されていないなら iPhone の CPU は 1 本で、そのときは **1 本の見積もり 14.8 tok/s の 1.38 倍**（第一案の `await` の形なら隔離なしでも GPU を使える）。**スマホ 2 台はどちらも線（1.5 倍超）に届かない。判定は PC の結果で。** 端末によって広げる形と packed のどちらが速いかが逆になる（Android は packed、iPhone は広げる形）ので、第 1 段は両方を持って端末ごとに測って選ぶ形になる見込み。
- **持ち主の PC（Chromebook、Chrome 152、GPU は Arm Valhall、2026-09-26）**: シェーダは JS と一致、packed int8 と shader-f16 あり、バインディング 2048MiB。行列 × ベクトル（shader 広げ / packed / CPU 1 本）: 150M の w1 1.2 / 1.9 / 6.4 GB/s、1B の w1 4.5 / 9.1 / 8.4、1B の分類器 6.6 / 9.3 / 9.6。**GPU の帯域が CPU 1 本と同じくらいの端末**（デスクトップの GPU ではなく、スマホと同じ系統）。1 トークン: 150M 30.7ms（packed 25.1ms）、**1B 212.6ms（packed 155.2ms、6.4 tok/s）で CPU のスレッドの上限の見積もり 12.1 tok/s の 0.53 倍**。橋渡し 14.8µs。
- **第 0 段の判定の材料（2026-09-26）**: 1B の GPU ÷ CPU（スレッドの上限の見積もり）は Android 0.97、iPhone 0.69（隔離なしの 1 本となら 1.38）、PC（ARM の Chromebook）0.53。**3 台とも線（PC で 1.5 倍以上、またはスマホで 1.5 倍超）に届かない。** 覆す条件: デスクトップ級の GPU（独立した GPU か Apple の M 系）で 1.5 倍を超えること（持ち主の手元に無い）。取りやめか保留かは持ち主に聞いている。
- 危ないところ（第 0 段で見る）: スマホの GPU のメモリの上限と、大きいバッファを作ったときのタブの強制終了（iOS）、GPU のウォッチドッグ（1 回の submit が長いと打ち切られる。3B の分類器を 1 ディスパッチにしない）、ブラウザごとの `maxBufferSize`。
- 関係: T93（CPU の複数コア）と両立（GPU の無い・遅い端末は CPU）。T123（変換のカーネル）とは独立。T101（Memory64）は CPU 側だけの話で、GPU のバッファは 4GB の制約を受けないが、アダプタの上限を受ける。
- 完了条件: 第 3 段まで行けば、1B 級の tok/s が CPU より有意に上がることを持ち主の端末の実ブラウザで示し、既定の選び方が決まっている。第 0 段で線に届かなければ、数字と理由を AGENTS.md に書いて取りやめる。

</details>

- [x] **T126 [追加] GPT-2 の gelu_fast と、RoPE の linear を受け付ける。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**形**（2f8be56）: GPT-2 の許す活性化に `gelu_fast`（`gelu_new` と同じ tanh の近似）。`rope_frequencies()` に linear（角速度を factor で割る）、`check_config()` が llama 系の llama3 と linear を通す。GGUF は `rope.scaling.factor` も読む。**試験**: `test_llama3.py` の「変換器の float32 の表とエンジンの int8 の表が同じ」を linear でも（`rope_scaling` を options で渡す経路ごと）、linear の角速度が素の 1/4、GPT-2 の 4 つの GELU の名前が通り relu は断られる。pytest 426 件。**足したもの**: rinna/japanese-gpt-1b（1.3B、MIT、リビジョン `33fc2e4b`、`browsers.yml` の `added` の組）。**本番（Linux の Chromium、4 本）**: 準備完了 52.6 秒（取得と変換 44.9 秒、うち変換 30.5 秒）・17.4 tok/s・ヒープ 1958MB、「これからの流行りは「脱落者」だろう。 今の時代は、やりたいことだけをやれる時代ではない。」。回帰の確認に japanese-gpt2 small（9.6 秒・119.9 tok/s）と Llama 3.2 1B（llama3 の RoPE、30.7 秒・19.0 tok/s、どちらも答えた）。**足さなかったもの**: deepseek-coder 1.3b（linear で開く見込みだった 3 件）は**前分割で断られる**（4 つの Split と 1 桁ずつの Digits と正規表現なしの ByteLevel。T81 の調査は config だけで数えていた）。base は `pytorch_model.bin` だけ。だから **linear は実物のモデルで確かめていない**（合成モデルの表の一致だけ）。deepseek-coder を入れるなら、その前分割を `pretokenize()` に足す別のタスクが要る（持ち主に）。**レビューで見てほしいこと**: rinna 1B の変換が 30.5 秒と、同じ 1B 級の Llama 3.2 1B（3.9 秒）より 8 倍長い（GPT-2 の Conv1D の転置と `c_attn` を溜める分と見ているが、未計測）。**レビュー（Opus xhigh、2026-09-26）: must-fix 1 つを直して OK。** 確かめ方: 新規のサブエージェントが、rinna 1B の語彙でページと同じ `Conversion` と `Tokenizer` の ID の列を本物の sentencepiece と比べて見つけ、この開発機で一覧の sentencepiece のモデル 16 個を本物の sentencepiece 0.2.2 と比べた（数字は AGENTS.md の落とし穴「トークナイザの確かめていない前提」）。**must-fix（この項のコミット）**: rinna の 3 つ（T126 の gpt-1b と、T65・T72 からの gpt2 small・gpt-neox small）はバイトの語彙片が無いので、改行とタブが `byte + 3` の語片で綴られていた（gpt-1b では「改行\nを含む\n\n文」が「▁ 改 行った を含む った った 文」、本物は「▁ 改 行 ▁ を含む ▁文」。タブは「する」）。AGENTS.md の「新しい語彙を足すときは確かめる」が T126 で確かめられていなかった。直し: 変換器が sentencepiece のモデルの正規化（`nmt_` の名前と `remove_extra_whitespaces`）と、バイトの語彙片が無いときの `[UNK]` を options（`nmt`・`collapse`・`unknown`）に載せ、エンジンが従う（`Tokenizer.normalized()`、`[UNK]` は続けば 1 つ。gpt-1b は sentencepiece の BPE、ほかの 2 つは unigram で、両方に入れた）。`CONVERTER` を 5 に（保存した変換は作り直される）。pytest に 4 件（合成の sentencepiece のモデルで、変換器の options の経路ごと。直す前の形では 13 が出ることも見る）。rinna の 3 つは 11 の文で本物と 9〜10 / 11 違っていたのが 0 / 11、BMP の全文字で約 57,000 → 5・5・35（残りは NFKC の Unicode の版の違い、T131 へ）。ほかの 13 個は前と同じ。**変換が 8 倍長い（見てほしいこと）**: 原因は Conv1D の転置と見てよい。Pyodide の NumPy の転置は 192〜304 MB/s（2048×8192 の float32 で 349ms、同じ大きさのコピーは 50ms）で、rinna 1B は層ごとに float32 で 201MB（c_attn・attn の c_proj・c_fc・mlp の c_proj）を転置するので 24 層で約 4.8GB、約 24 秒（見積もり。30.5 秒の大半）。64 行ずつに分けた転置なら 3 倍速い（111ms）。取得と重なるので、日本の回線（2.7GB の取得に約 5 分）では準備完了に出ない → やらない。**linear の RoPE を実物で確かめていない**のは記録のとおり（一覧に開く実物が無い。合成モデルの表の一致だけ）。**あれば良い（やらない）**: `?hf=` は `tokenizer.json` と `tokenizer.model` しか試さないので、`spiece.model` だけのリポジトリ（rinna）は `?hf=` では開かない（一覧は名指しするので開く）。

<details><summary>T126 の採用時の記録</summary>

- 根拠: rinna/japanese-gpt-1b と派生の 4 件は gpt2 の `gelu_fast` だけで断られる（式は gelu_new と同じで、neox ではすでに許している）。RoPE の linear（deepseek-coder の 3 件）は位置を割るだけ。
- 足すモデル: rinna/japanese-gpt-1b、deepseek-coder 1.3b。

</details>

- [x] **T125 [追加] mistral を llama の別名として読む。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**(3) は持ち主が決めた（2026-09-26、レビュー担当の勧めの文面）**: 訪問者に見える 3 か所（シートの説明、一覧の「Other repository…」、変換器の断りの文）に同じ並びで Mistral を足した（「Llama, Mistral, Qwen2, GPT-2 or GPT-NeoX」）。T124 で Qwen3 を足すときも同じ 3 か所。**形**（0f43609）: `normalize()` が mistral を llama に読み替え、`sliding_window` があれば文脈をその長さまでに切る（窓の中では窓なしと同じ位置を見る。4096 の窓はページの文脈 4096 と同じ）。試験は「mistral の名前で llama と 1 バイトも違わない」「窓が短ければ文脈がそれに、長ければそのまま」。**トークナイザ**: Mistral の系統の `tokenizer.json` は sentencepiece 流の BPE でエンジンは読まないので、どれも配っている `tokenizer.model` を読む。**書式**: 6 つとも本物の Jinja と `tokenizers` で 4 つのプロンプトの ID を突き合わせた（v0.2 は先頭の空白を書かない、zephyr は `</s>` の後に空白、Rakuten 2.0 mini は最初の 1 トークンだけ違う（T131）、前後に空白のあるプロンプトは `trim` のぶん違う）。**足したもの**（どれもゲートなし、リビジョン固定、ライセンスはモデルカードから）: RakutenAI 2.0 mini instruct（1.5B、Apache 2.0）、RakutenAI 7B chat（Apache 2.0）、Swallow-MS 7B instruct v0.1（Apache 2.0）、Mistral 7B Instruct v0.2 と v0.3（Apache 2.0）、zephyr 7B beta（MIT）。`browsers.yml` に組 `mistral`（6 つ、76GB）。**本番（Linux の Chromium、4 本）**: Rakuten 2.0 mini 37.8 秒・16.2 tok/s・2092MB、Rakuten 7B chat 181.9 秒・3.6 tok/s・9475MB、Swallow-MS 7B 266.4 秒・3.8 tok/s・9456MB、Mistral v0.2 156.2 秒・3.6 tok/s・9316MB、v0.3 163.8 秒・4.0 tok/s・9324MB、zephyr 146.5 秒・3.8 tok/s・9358MB（7B はどれも 64 ビット）。日本語の 3 つは日本語で、英語の 3 つは英語で、3 つ挙げる形で答えた。**断ったもの**: cyberagent/CAT-Translate-7b（`head_dim` が dim / heads と違う、T124 と同じ変更が要る）、Rakuten 7B instruct（書式が無い）、openchat と OpenHermes（`added_tokens.json` の特殊トークンが `tokenizer.model` に無い）。**レビューで見てほしいこと**: (1) 窓で文脈を切る判断（窓より長い文脈をページは使わない）、(2) `?hf=` の Mistral は `tokenizer.json` を断られてから `tokenizer.model` に移る（実物では未確認）、(3) 訪問者に見える「Other repository… — any Llama, Qwen2, GPT-2 or GPT-NeoX」とシートの説明に Mistral を足すか（文面は持ち主に）。**レビュー（Opus xhigh、2026-09-26）: must-fix なしで OK。** 確かめ方: 新規のサブエージェントが 6 つのリビジョン・ゲート・大きさ・ライセンス（どれもカードのとおり）と、ページと同じ `Conversion` と `Tokenizer` の ID の列を transformers の `apply_chat_template` と 8 つのプロンプトで比べた: Mistral v0.2・v0.3 は打った `</s>` の 1 つだけ違う（本物は 2 にする）、zephyr と Rakuten 7B chat は BOS のぶんだけ（カードの使い方は BOS を足すので同じ）、Swallow-MS は `trim` と打った `</s>` と全角の NFKC（`tokenizer.model` の正規化は nfkc で、ページはそれに従う。`tokenizer.json` は従わない。どちらで学習したかは未確認）、Rakuten 2.0 mini は BOS と最初の「▁A」（T131）。**6 つの `tokenizer.json` はどれも `ValueError` で断られる**（前分割が無い、または Metaspace）ので、`?hf=` は `tokenizer.model` に移る。**(2) を本番で確かめた**（`models.yml`、Chromium）: `?hf=` の Rakuten 2.0 mini は 39.0 秒・15.4 tok/s で書式を読んでアシスタントとして答え、`?hf=` の Mistral v0.3 は 168.9 秒・3.6 tok/s（64 ビット）。v0.3 の読んだ書式は `[INST] {prompt}[/INST]` で、`[INST]`・`[/INST]` は `tokenizer.model` の CONTROL の語片として特殊トークンになる（手で書いた `MISTRAL_V3` と同じ）。**(1) 窓で文脈を切る判断は正しい**: transformers の窓の mask は「問いの位置 − 鍵の位置 < 窓」を見るので、文脈が窓以下なら全位置を見るのと同じ。**(3) の文面は持ち主に出す**（案「A Llama (or Mistral), Qwen2, GPT-2 or GPT-NeoX model …」）。**台帳の直し**: AGENTS.md の `browsers.yml` の組は 5 つ（`mistral` を足した。`added` は rinna 1B を入れて 7 つ）。**残り**: `?hf=` の Mistral v0.2 と zephyr は、変換器が読んだ書式ではダミーの空白と特殊トークンの後の空白で ID が本物と違う（一覧は手で書いて合わせてある）→ T131。フォルダで開くとき、`tokenizer.json` と `tokenizer.model` の両方があると前者だけを渡して断られる（分割されていない Rakuten 2.0 mini だけ）→ T138 の (4)。Swallow-MS のシステム文 → T138 の (2)。

<details><summary>T125 の採用時の記録</summary>

- 根拠: 22 件、日本の組織で 34 件。v0.2 以降は llama と同じ形（sliding window が null）なので、model_type の別名で開く見込み。sliding window のあるもの（v0.1）は断ったまま。
- 足すモデル: 調査の 22 件と日本の組織の 34 件から、有名でゲートの無いものを全部（RakutenAI-2.0-mini 1.5B など。トークナイザが断られないかは 1 つずつ確かめる）。

</details>

- [x] **T127 [追加] chat_template.jinja を読む、と selectattr。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**。**形**（e594a31、75fd65c）: 読み手に `selectattr` / `rejectattr`・`length`・`list`・`first`・`last`、`namespace()` と属性への `set`、整数の算術、`is` の判定、`not in`、大小の比較、スライス、引数のある文字列のメソッド、マクロの定義（読み飛ばし）、transformers と同じ `trim_blocks` と `lstrip_blocks`、エスケープした引用符、引用符を含むコメント。`chat_template.jinja` は `tokenizer_config.json` に書式が無いときだけ取る（404 を増やさない）。フォルダの経路も `tokenizer_config.json` と `chat_template.jinja` を読む。`tokenizer.model` のときは CONTROL の語片を特殊トークンとして渡す（`sentencepiece_specials()`）。`CONVERTER` 4。**確かめ方**: 一覧の HF のモデルで書式のある 28 件を本物の jinja2 と比べて 28 件とも同じ（前は 18 件）。一覧の外の Qwen3 0.6B・1.7B・4B-Instruct-2507、Qwen3-Swallow 8B、MiniCPM5 1B、Llama 3.1 8B、Supra2 100M も同じ。読めないのは llm-jp-4（マクロを呼ぶ）だけ。本物のテンプレート 7 つと 1 ターンを `tests/fixtures/chat-templates.json` に（`trim_blocks` を外すと 2 件、`selectattr` を外すと 3 件落ちる）。**本番（Linux の Chromium）**: `?hf=` の sarashina2.2 1B Instruct は書式を読み、アシスタントとして答えた（「I'm here to help you with any questions…」。e2e の `?hf=` はプロンプトが空）。**CI で見つけて直した**: 最初の版では `tokenizer.model` の特殊トークンを渡しておらず、同じ `?hf=` の sarashina で `<|user|>` と `</s>` が文字に綴られて指示に答えなかった（75fd65c）。回帰: 一覧の sarashina2.2 1B（17.3 tok/s、箇条書きで答えた）・TinyLlama（20.9）・Qwen2.5 0.5B（37.0）。**分かったこと**: MiniCPM5 1B は `chat_template.jinja` を読めたが、`head_dim` で断られる（T124 と同じ変更）。**レビューで見てほしいこと**: (1) None を「定義されていない」と見る扱い（transformers は `tools=None` を渡す。結果は同じになる理由は AGENTS.md）、(2) `+` は数どうしなら足し算・それ以外は文字列の連結、(3) `chat_template.jinja` と `tokenizer_config.json` の両方にあるときは `tokenizer_config.json` を使う（transformers は `.jinja` を先に読む。両方ある unsloth の Llama 3.2 では同じだった）。**レビュー（Opus xhigh、2026-09-26）: must-fix 1 つと、直したほうがよいもの 3 つを直して OK。** 確かめ方（新規のサブエージェント）: 本物のテンプレートを transformers 5.12.1 の `apply_chat_template`（読めないものは transformers の `render_jinja_template`）と 1 ターンで、2 つのプロンプトずつ比べた: 一覧の 28 件、一覧の外から選んだ 81 件（Qwen3・Phi・granite・OLMo・Mistral・日本の組織のものなど）、ダウンロード上位 500 件のうちゲートの無い 302 件。**黙って違う文を書くものは 0 件**（直した後で同じ 237・諦める 116・書式なし 70）。ほかに合成のテンプレートで境界（None と未定義、`+` の数と文字列、エスケープ、`trim_blocks`・`lstrip_blocks` とコメント、優先順位、スライス、`namespace`、`selectattr` など）を突き、空白の制御は 3,000 個の乱数のテンプレートで突いた。**must-fix（この項のコミット）**: `strftime_now()` を変換の日の日付で書いていたので、保存した変換（manifest の `options.template`）では次の日から日付が古い（一覧の Llama 3.2 1B・3B の「Today Date」）。いまは `{date:書式}` と書き、ページの `filled()` が送るときに訪問者の今日で埋める（T132 の `{date}` を広げた。strftime の %d %m %Y %y %b %B %a %A %H %M %S、ほかの書式は諦める）。日付で計算するテンプレート（Unsloth の Mistral Small の写しは日の数字が「01」かで「昨日」を出す）はこの形で書けないので、2 つの日（2025-01-01 と 2026-12-31）で本物の日付の描画と埋めた形を比べ、違えば諦める（直した直後の再比較で、その 1 件が「違う」に出て見つけた）。**直したもの**: `or`・`and` が True / False を返していた（`(system_message or 'You are a helpful assistant.')` が「True」。karakuri の `|string or '4'` は `|string` で諦めていて無事だった）→ Jinja と同じく決めた側の値で、右は読まない。コメントの後の `{%-` がコメントの前の文まで削っていた（乱数の 53 件の違いはすべてこれ）→ コメントを空の文として残す。エスケープの順（`'\\\\n'` が改行になる、`\\x41`・`\\u2581` を読まない）→ Jinja の字句解析と同じ unicode-escape。変換器の書式が変わるので `CONVERTER` 6。pytest に 1 件（どれも本物の Jinja と同じ出力を、この開発機で確かめた値）。**見てほしいこと**: (1) None を未定義と見る扱いは、本物のテンプレートでは結果が変わらない（sarashina と CAT のマクロ、Mistral、EXAONE、Llama、Olmo-3、Nemotron を確かめた）ので OK。合成なら `{% if tools is defined %}` で違う（あれば良い）。(2) `+` は本物のテンプレートでは違いが出ない。`'a' + 1` のように Jinja が例外を出すところで文を書くが、そのテンプレートは transformers でも使えないので OK。(3) `.jinja` と `tokenizer_config.json` の両方があるのは 39 件、中身が違うのは 13 件で、transformers はどちらでも同じ 1 ターンを書く（1 件は `{% include %}` で読み手が諦める）ので OK。**あれば良い（やらない、T138 へ）**: 読めるテンプレートの 42 / 237 がプロンプトに `trim` をかける（一覧の Llama 3.2・Swallow 8B・Swallow-MS・Rakuten の 2 つも）。ページはかけないので、前後に空白や改行のあるプロンプトだけ違う。空白で囲んだ目印で 1 回描けば見分けられる。ほかは本物に無い形（浮動小数の文字列、`loop.revindex`、文字列の添字、リストどうしの `+` など）。

<details><summary>T127 の採用時の記録</summary>

- 根拠: 通った 276 件のうち少なくとも 16 件は書式が `chat_template.jinja` にしかなく、自動で取れない。sarashina2.2 の系統は `selectattr` で読めず、手で書いた（T81）。
- 効くところ: 一覧のモデルは手で書けば足りるので、主に `?hf=` で開く一覧に無いモデル。T124 の Qwen3 の書式が読めるかもここで決まる。

</details>

- [x] **T128 [描画] モデルの一覧を、グループごとに「日本語が使える小 → 大、英語だけの小 → 大」に並べる。**（Opus medium、2026-09-26）— 状態: **完了（Opus xhigh のレビュー済み、2026-09-26）**（持ち主が画面を見て OK、2026-09-26「T128 スクリーンショット確認済み、よし」）。**形**: `src/models.js` の `MODELS` を、ファイルに書いた順（`LISTED`）から規則で並べ替えて作る（グループは `GROUPS` の順、その中で `writesJapanese()`（note に「日本語」）が先、それぞれ `modelBytes()` の小さい順。安定な並べ替え）。足したモデルはどこに書いても自分の位置に入る。`tests/models-check.mjs` が順と既定が先頭であることを見る。`tests/screenshots.mjs` に一覧の画面（select を全部の項目の list box にして撮る。開いた select は画面撮りに写らないため）。ブランチ `t128-list-order` で `preview.yml` を撮り、持ち主が見てからマージした（e160caf、ブランチは消した）。本番: 既定の tiny-lm・stories260K・japanese-gpt2 small が答えた。**レビューで見てほしいこと**: ファイルの中の順と画面の順が違うこと（ファイルの先頭のコメントに書いた）、同じ大きさのモデルはファイルの順。**レビュー（Opus xhigh、2026-09-26）: must-fix なしで OK。** ページは `MODELS` の順番の位置を使わない（既定は `MODELS[0]`、ほかは id で引く。optgroup はグループごとに `filter`）ので、並べ替えで壊れるところは無い。`Array.prototype.sort` は安定なので同じ大きさはファイルの順。`modelBytes()` が大きさを出せない項目（note に「int8 N GB」が無い）があると比べ方が壊れるが、models-check の順の試験が落ちるので足すときに気づける。ファイルの順と画面の順が違うことは先頭のコメントに書いてあり、`LISTED` の名前で分かる（OK）。本番: 今回のレビューの `models.yml` の走行（一覧の項目を id で開く 20 回あまり）はどれも順と無関係に開いた。

<details><summary>T128 の採用時の記録</summary>

- 持ち主の指示（2026-09-26）: コンボボックスの 3 つのグループ（サイトのモデル・原本・HF から取得して変換）のそれぞれの中を、**日本語か英語（日本語が使えるもの）の小 → 日本語か英語の大 → 英語だけの小 → 英語だけの大**の順にする。
- レビュー担当の見立て（2026-09-26）: 今の方針 4（「日本語の軽い → 重い、そのあと英語の軽い → 重い」）とほぼ同じで、それを厳密にする形。英語だけのモデルが末尾に固まるので、あとで減らすときはその塊を外すか隠すだけで済む。今の一覧は次のところでずれている: Qwen2.5 1.5B Instruct（日本語 / English）が英語だけの塊の中にある。塊の中の大きさの順が厳密でない（japanese-gpt2 small の 130MB が llm-jp-3 150M instruct3 の 171MB の後、Qwen2.5 0.5B の 545MB が llm-jp-3 440M の 503MB の前）。
- 決めておくこと: 「日本語が使える」は note に「日本語」があるもの（日英の両方と翻訳を含む）。大きさは `modelBytes()`（int8 の大きさ、原本は読み込んだときの大きさ）。小と大の境は「desktop only」の印（int8 がおよそ 1GB を超える）で、並びは大きさの順なので境は並びを変えない（境で見出しや区切りを入れるかは画面を見て決める）。既定の tiny-lm は先頭のまま（サイトのモデルの日本語でいちばん小さい）。
- 英語だけのモデル（40 のうち 19。サイト 4: TinyStories 260K・3.5M・15M・42M、原本 2: TinyStories 15M・42M、HF 13: Pythia の 5 段・SmolLM2 の 3 つ・GPT-2・Qwen2.5 Coder 0.5B・TinyLlama・Llama 3.2 1B・DeepSeek-R1 1.5B）のうち、試験と計測に使っているもの: stories260K（決定的な出力の確認: e2e・smoke・deploy・フォルダの経路）、stories15M（smoke・forward-check・pytest）、Pythia の 5 段（T84 の梯子）、SmolLM2 135M の GGUF（T95 の wllama との比較、GGUF の経路）、GPT-2（外れ値の列の経路、T92 の唯一の例）、Llama 3.2 1B（RoPE の llama3 の唯一の例）。減らすときの案: (b) 外す（試験と計測は `hf:` で開けるが、`browsers.yml` の一覧と e2e の期待値を書き直す）、(c) コンボボックスからは外し `?model=` では開ける（CI はそのまま）。**判断（2026-09-26、持ち主）: 「はじめはぜんぶ表示しよう、あとで少なくする」**。
- 手順: `src/models.js` の並べ替え、方針 4 の文（AGENTS.md）を持ち主の並びに、models-check に並びの試験（グループごとに、日本語が使えるものが先・その中で大きさの順・英語だけのものも大きさの順）を足して、この先に足すモデルもその位置に入るようにする。`preview.yml` で電話幅と PC 幅の画面を撮って持ち主が見る。
- 完了条件: 持ち主が画面を見て OK。

</details>

- [x] **T124 [追加] qwen3 を読む**（Opus medium、2026-09-26）— 状態: **完了**（must-fix の候補 1 つは T143 に分けた、持ち主の判断待ち）
- **レビュー（Opus xhigh、2026-09-26）: 実装は OK。must-fix の候補 1 つ（`?hf=` の書式の特殊トークン）は T124 の不具合ではなく変換器の元からの形で、直すと `CONVERTER` が上がるので T143 に分けた。残りの should は T144。** 確かめ方（新規のサブエージェント）: 渡し漏れを layout から worker まで全部なぞって漏れなし。合成モデルで記録に無い組み合わせ（bias と qk_norm が同時、head_dim ≠ dim/heads と GQA と int6、eps の境界値）を 8 モデル × float32・int8・int6 × 共有・`--plain`・`--wide` の forward-check と threads-check で全部合格。わざと壊して pytest（E4・E7・E9・E10）と smoke（3 つ）が落ちることを確かめた。実物の Qwen3 0.6B の固定値も一致。CI（このレビューの回）: 同じページで tiny-lm → Qwen3 0.6B → Qwen3 4B（32 ビットから 64 ビットのメモリへ）が答え、Qwen3 4B の 6 ビット（64 ビット）1.8 tok/s、Firefox の Qwen3 0.6B も答えた。**記録の直し**: (2) の eps は **Qwen2.5 では良くならなかった**（Qwen2.5 0.5B Instruct の原本、float32、NumPy、1500 トークン: 日本語 1e-6 が 23.7700・1e-5 が 23.7455（1e-6 が +0.10%）、英語 19.5181・19.5110（+0.04%））。変更は残す（原本と同じ計算になるので）。覆す条件: 一覧の 1e-6 のモデルで 1e-6 が 0.5% 以上悪いものが出たとき。「1e-6 の 9 モデル」は config で数えると 8（Qwen2.5 の 5 つ、TinySwallow、DeepSeek-R1、llm-jp-4）。「pytest 471」は tokenizers のある `.venv` での数。(5) 考える形（Qwen3 0.6B、int8、一覧の設定、seed 1・2）は考えが 208・157 トークンで、合計 332・226 トークン（4096 には遠い）。ただし答えの末尾は 2 回とも英語だった（すぐ答える形は日本語）。note に書かない判断はそのまま。(4) Qwen3 Swallow 8B のすぐ答える形を本番で 1 回回した（models.yml、Linux の Chromium、64 ビットのメモリ、4 本）: 準備完了 171.4 秒・3.4 tok/s、日本語で筋の通った答え。残してよい。(1) 設定の増え方: いまの 3 つなら耐えるが、ファイルから分からない設定が options・form・checkpoint_dtype の引数・footprint の headDim と 4 つの経路を別の名前で通り、1 つ足すたびに約 14 か所を触る。同じ名前の 1 つの辞書を通す形を勧める（T144）。
- **結果（2026-09-26、Opus medium）**
  - **コミット**: (0) afaf133 `"head_dim": null` を無いと読む、(1) ea872ce head ごとの q・k の norm（options の `qk_norm`）、(2) a6befe1 dim / heads と違う head の大きさ（options の `head_dim`）、c764c17 RMSNorm の eps をモデルのものに（options の `rms_norm_eps`、`CONVERTER` 7）、c8c77dd 一覧の 12 項目と文面。見出しは 7 個の int のまま（持ち主の判断 (a)）。`qk_norm`・`head_dim`・`rms_norm_eps` はどれもそのモデルのときだけ options に載る。
  - **レビューが挙げた「黙って壊れる所」はすべて直した**: 並べ替えの head の大きさ（`permute_heads` は行数が heads × head の大きさでなければ断る。試験あり）、`footprint()`（`sink.open` と自動のビット数が `{arch, head_dim}` を渡す）、forward.js の枠（q・xb・xq を heads × head_dim の幅に）、convert_hf.py（読む）。GGUF の読み手は `attention.key_length` と `attention.layer_norm_rms_epsilon` を読む（Qwen3 の GGUF は今の読み手の arch の外なので、使えるのは Llama の GGUF の head_dim だけ）。
  - **eps（覆す条件に当たった）**: Qwen3 0.6B（float32、NumPy、511 トークン、T85 の文章）の perplexity は、エンジンの 1e-5 で日本語 34.707・英語 17.156、本来の 1e-6 で 34.678・17.137（+0.08%・**+0.12%**）。+0.1% を越えたので、カーネルの `rmsnorm` に eps の引数を足した（`make kernels` は Makefile の依存のまま、kernel.ts の変更だけ）。一覧の 1e-6 のモデル（Qwen2.5 の 5 つ、TinySwallow、DeepSeek-R1 Distill Qwen 1.5B、llm-jp-4 8B）もこれで本来の eps になる。1500 トークンで測らなかったのは、float32 の 2.4GB がこの開発機のページキャッシュに入らず 1 回が 1 時間を超える見込みだったため（同じトークンで比べるので差の向きと大きさは 511 でも読める）。
  - **試験**: pytest 471（`tests/test_qwen3.py`: 合成モデル → 変換 → 参照と一致を head の大きさ 3 通りと GQA で、ストリーム変換を float32・int8・int6 で、変換器の options からエンジンへの経路（`qk_norm`・`head_dim`・`rms_norm_eps`）、norm が効いていること、違う head の大きさの並べ替えが落ちること、GPT-2 / NeoX は違う head_dim を断ること）。smoke に合成の Qwen3（head の大きさ 16・32・dim の半分、eps 0.25）で forward.js と NumPy の一致（forward.js の norm を止める・eps を無視させると落ちることを確かめた）。forward-check の共有・`--plain`・`--wide` は前と同じ値。**実物の Qwen3 0.6B（int8、forward.js）で `footprint()` と実際の確保**: 共有で 755.3 対 756.3 MiB、共有なしで 1427.3 対 1428.3 MiB（head_dim を渡さないと 419.1・755.1 で 45% 少ない）。`tests/fixed_outputs.py` に Qwen3 0.6B（すぐ答える形、float32、greedy 16 トークン:「以下のような3つの今流行りのトレンドを挙げます：」）。ほかの 4 つは変わらない。
  - **書式**: 12 項目とも、9 つのプロンプト（空白で囲んだもの、改行、`<|im_end|>` や `<think>` を打ったもの、全角）で本物の `apply_chat_template`（すぐ答える形は `enable_thinking=False`）と ID の列が同じ（先頭の BOS を除く、T131）。道具は `~/tmp/t124/formats/compare.py`（レビューの道具の形）。`<think>`・`</think>` は `tokenizer.json` が special と呼ばない追加のトークンなので、一覧の Qwen3 は全部 `specials` に書いた。
  - **一覧（持ち主の判断: 名前と note は勧めの文面、足せるものは全部）**: Qwen3 0.6B・1.7B・4B・8B と Qwen3 Swallow 8B RL を thinking と no thinking の 2 項目ずつ、Qwen3 4B Instruct 2507、**Qwen3 4B Thinking 2507（持ち主の選択肢に無かったのを Opus が足した。Instruct 2507 の対で、同じ Apache 2.0・ゲートなし）**。生成の設定はカードの勧めのうちページのサンプラーにあるもの（考える形 0.6・0.95、すぐ答える形 0.7・0.8、繰り返しのペナルティなし。Swallow はカードが 0.6・0.95 しか書かないので両方それ）。3 か所の文は「Llama, Mistral, Qwen2, Qwen3, GPT-2 or GPT-NeoX」。
  - **本番（models.yml、Linux の Chromium、4 本）**: Qwen3 0.6B thinking 準備完了 20.3 秒・32.9 tok/s、no thinking 19.2 秒・34.2 tok/s、1.7B 36.0〜37.3 秒・14.3 tok/s、4B 81.6 秒・6.2 tok/s、4B Instruct 2507 77.2 秒・4.4 tok/s、4B Thinking 2507 80.3 秒・4.4 tok/s（4B の 3 つは 64 ビットのメモリ。4B と 2507 の差は別々のランナー）、8B 190.1 秒・3.6 tok/s、Swallow 8B thinking 196.4 秒・3.3 tok/s。WebKit の 0.6B 16.7 秒・21.1 tok/s。0.6B に約 3000 トークンのプロンプト（`long=850`）で文脈 4096 の最後まで書き切った（KV が head_dim 128 の大きさで伸びきる）。どれも筋の通った日本語で答え、考える形は英語で考え始める（Qwen3 の常）。browsers.yml に qwen3 の組（8 個、取得 64GB）。
  - **レビューで見てほしいこと**: (1) options を「そのモデルのときだけ載せる」形で 3 つ足した（`qk_norm`・`head_dim`・`rms_norm_eps`）のと、`sink.open` と自動のビット数の 4 番目の引数を arch から `{arch, head_dim}` の辞書に変えた形が、この先の設定の増え方に耐えるか。(2) eps の変更で一覧の 1e-6 の 9 モデルの数値が変わる（良くなる向きのはずだが、Qwen2.5 などで測っていない。`CONVERTER` 7 で保存した変換は作り直し）。(3) 変換器は special でない追加のトークンを `specials` に渡さない（本物の tokenizers は読む）。一覧は手で書いて合わせたが、`?hf=` の Qwen3 では訪問者が `<think>` と打つと綴られる。変換器で渡すかは T138 と一緒に決めたい。(4) Qwen3 Swallow 8B の no thinking はカードに勧めが無い（持ち主が「足せるものは全部」）。(5) 考える形の 4096 トークンの使い切り（note に書かない、持ち主の判断）。

<details><summary>T124 の採用時の記録と設計</summary>

- 根拠: T81 の調査で、ダウンロード上位 2000 件の 8.5B 以下のうち断られる系統で最大（90 件、ダウンロードの 29.3%、日本の組織で 14 件。Qwen3-Swallow 8B など日本語の新しいモデルもここ）。足りないのは head ごとの q・k の RMSNorm と、`dim / heads` と違う `head_dim`。legacy のヘッダ（7 個の int）が head_dim を持たないので、形式を変える設計（docs/review-by-opus.md の「Fable に回すもの」に当たる）。**レビュー（Opus xhigh、2026-09-26）の見立て**: bias や arch と同じく、ファイルから分からない設定として options で渡せば見出しは変えずに済みそう（`checkpoint_dtype()`・`layout()`・エンジンの 3 か所と `footprint()` が head_dim を受け取る）。どちらにするかは実装の前に Fable か持ち主が決める。
- 足すモデル（採用時の方針: 有名なものは全部）: Qwen3 の 0.6B・1.7B・4B・8B、日本語の Qwen3-Swallow 8B など（ゲートなし・リビジョン固定・ライセンスはモデルカードから、T81 と同じ手順）。書式は chat_template が読めなければ手で書く。

- **設計（2026-09-26、Opus medium）**
  - **足りないものは 2 つで、別々に効く**（config.json を読んだ、2026-09-26）: (1) **head ごとの q・k の RMSNorm**（`self_attn.q_norm.weight` / `k_norm.weight`、各層に head_dim 個ずつ。qwen3 のすべて）、(2) **head_dim が dim / heads と違う**（Qwen3 0.6B は 128 対 64、4B は 128 対 80、MiniCPM5 1B（llama）は 128 対 96）。Qwen3 1.7B・8B・Qwen3-Swallow 8B は head_dim = dim / heads なので (1) だけで開く。ほかに (0) **`"head_dim": null` を「無い」と読まない小さな不具合**: cyberagent/CAT-Translate-7b（mistral、dim / heads = 128）が「heads do not divide」で断られている（`config.get("head_dim", dim // n_heads)` が null を返す）。
  - **(1) q・k の norm**: T64 の bias と同じ形にする。各層の 2 本のベクトル（head_dim の float32）をファイルの末尾に足し、見出しは変えない。ファイルから分からないので options（`qk_norm=True`、変換器が `q_norm.weight` の有無で決める）でエンジンに渡す。計算は RoPE の前に head ごとに `rmsnorm`（既存のカーネル、n = head_dim、heads + kv_heads 回。新しいカーネルは 0）。並べ替え: q・k の重みは `permute_heads` で回すので、norm の重みも同じ並びに（bias と同じく 1 次元の `permute_heads`）。
  - **(2) head_dim を渡す方法の選択肢**:
    - (a) **options で渡す**（`head_dim=`。見出しは 7 個の int のまま）。変えるのは `layout()`・`checkpoint_dtype()`・`Llama.__init__` の 3 か所と `footprint()`、forward.js の枠（q と attention の出力は heads × head_dim で、dim と違う）、RoPE の表（幅 head_dim）、GGUF の読み手（`attention.key_length`）。bias・arch・rotary と同じやり方で、形式の変更が要らない。**弱み**: フォルダで開く legacy のファイルは設定の JSON で head_dim を渡す必要がある（bias や arch と同じ。大きさの判定 `checkpoint_dtype()` は head_dim が無いと合わずに断る）。
    - (b) **見出しを変える**（8 個目の int など）。legacy 形式（llama2.c）との互換が切れ、保存した変換（`CONVERTER`）、`convert_hf.py`・`quantize.py`・サイトのモデルのファイルがすべて関わる。docs/review-by-opus.md の「形式を変える設計 → Fable」に当たる。
  - **勧め: (a)**。数字: 変えるファイルは (a) が 6 つ（llama2_convert・llama2_numpy・forward.js・jobs.js の段の大きさ・worker の footprint の呼び出し・pytest）、(b) はそれに加えて見出しを読むすべて（エンジン・変換器・convert_hf・quantize・tests の固定値、サイトの `make models` の出力）。見返りは同じ（開くモデルは同じ）。**覆す条件**: head_dim ≠ dim / heads のモデルを「フォルダで開く legacy のファイル」として配る必要が出たとき（今は無い）は (b)。
  - **順番の提案**: (0) の不具合 → (1)（Qwen3 1.7B・8B・Qwen3-Swallow 8B が開く）→ (2)（Qwen3 0.6B・4B、MiniCPM5 1B、CAT-Translate-7b は (0) だけで開く）。それぞれ別のコミット。確かめ方は T64・T72 と同じ 3 本（合成モデル → 変換 → 参照と一致、ストリーム変換、options の経路）と実物の固定値（`tests/fixed_outputs.py` に Qwen3 0.6B を足す）。
  - **書式**: Qwen3 の chat_template は T127 で読めるようになった（本物と同じ 1 ターン）。**Qwen3 は既定で考えてから答える**（`<think>…</think>`。`enable_thinking=false` を渡すと空の think を書く）。ページは今は渡さないので考える形になる（DeepSeek-R1 と同じ扱い）。考えない形にするかは持ち主に。
  - ライセンス: Qwen3 は Apache 2.0（Qwen/Qwen3-0.6B のカード、ゲートなし）。
  - **持ち主の判断（2026-09-26）**: head_dim は **(a) options で渡す**。考えるかは「**両方を別々に用意できないのか？**」→ 一覧に 2 つの項目で並べる（例「Qwen3 1.7B（考えてから答える）」と「（すぐ答える）」）。重みは同じリポジトリの同じ版なので、保存した変換は共有され（`keptName` はリポジトリ・版・ビット数・文脈）、違うのは書式だけ（すぐ答えるほうは Qwen3 の書式の `enable_thinking=false` と同じ、空の `<think>\n\n</think>\n\n` を先に書く形を手で）。**実装に進める。**
- **レビュー（Opus xhigh、2026-09-26、設計）: 進めてよい。ただし変える場所の一覧が足りない（直す場所を下に足した）。** 確かめ方: 新規のサブエージェントが、head_dim = dim / heads を前提にしている所をファイルと行で全部洗い、合成のモデルで試した。(a) 数字: 前提にしている所は約 40 か所（llama2_convert の `layout`・`checkpoint_size`・`Stream`・`convert_pieces`・`rope_table`・`check_config`・`gguf_model`、llama2_numpy の `checkpoint_dtype`・`Llama.__init__`・`llama_tensors`・NumPy の forward、forward.js の `frameArrays`・`footprint`・attention の段、worker.js の `sink.open`・`afterCheckpoint`・`automaticBits`、convert_hf.py、試験の `conftest.py`・`test_convert.py` の `Sink`・forward-check）。jobs.js と helper.js は head の大きさを仕事で受け取るので変えなくてよい。**抜けていたもの（直さないと黙って壊れる）**: 並べ替えの head の大きさ（`Stream` と `convert_pieces` の `head_size`）。`permute_heads` は違う大きさでも例外を出さず、Qwen3 0.6B の形の `wq` で行の 50% が入れ替わった。**直さないと足りなくなるもの**: q・k の norm の重みは `("permute", 1)` で head_dim を渡す（heads の数で回すと例外、古い大きさだと黙って違う）。`footprint()` に head_dim が届かない（`sink.open` も自動のビット数の呼び出しも渡していない）と 0.6B の KV を半分に数え、長い文脈の終わりでメモリが足りなくなる。forward.js の枠 `XQ` も q の幅を入れる（Qwen3 の大きさでは今でも足りるが、たまたま）。convert_hf.py は qwen3 を断るか読むか、一覧・`LICENSES`・models-check。(b) eps: カーネルの RMSNorm の eps は 1e-5 固定、Qwen3 は 1e-6。head ごとの norm では、norm の前の平均二乗 m のベクトルが sqrt((m + 1e-6) / (m + 1e-5)) 倍されるだけ。合成のデータで attention の確率の変化の最大は m ≥ 1e-2 で 7.5e-4 以下、m = 1e-3 で 7.7e-3、m = 1e-4 で 1.6e-2〜6.9e-2。(c) 判定: 持ち主の決めた (a) options で進める。変える場所は上の一覧で。試験は T64・T72 の 3 本に加えて **head_dim と qk_norm が変換器の options からエンジンに届く経路**（T72 の教え）と、並べ替えを違う head の大きさで回すと落ちる試験。実物の Qwen3 0.6B で、ネイティブの float32（eps 1e-6）と perplexity を比べる。生成の設定はモデルカードの勧め（考える形は temperature 0.6・top-p 0.95、すぐ答える形は 0.7・0.8。考える形で greedy は使うなと書いてある）を、ページのサンプラーにあるものだけ。考える形は文脈 4096 を考えだけで使い切りうる（1.7B の約 13 tok/s で 4096 トークンに約 5 分、未計測）ことを note に書くかは持ち主に。(d) 覆す条件: 実物の Qwen3 で eps の違いが perplexity で +0.1% を超えたら、RMSNorm のカーネルに eps の引数を足してから一覧に入れる（カーネルの変更）。

</details>

- [x] **T138 [追加] 書式を本物に寄せる残り**（Opus medium、2026-09-26）— 状態: **完了**
- **レビュー（Opus xhigh、2026-09-26）: T138 の変更は OK。範囲外の DeepSeek-R1 の書式は must-fix と判定して直した（b5352fd）。** 確かめ方（新規のサブエージェント）: `{prompt:trim}` を、一覧の 39 リポジトリと HF の人気の 1000 件から取れた 646 件（読めるのは 135 種類、trim が 28）のテンプレートで、19 通りのプロンプト（前後の空白・タブ・改行・CRLF・全角空白・NBSP・空白だけ・空）について、変換器の書式を本物の JS の `filled()` で埋めた文と transformers の `render_jinja_template` を比べた。2,565 通りのうち 2,479 が同じで、残り 86 は空白の文字の集合の違い（JS の `trim()` は U+FEFF を削り、Python の `strip()` は U+0085・U+001C〜001F を削る）84、falcon-7b-instruct（読めない構造）1、DialoGPT の空のプロンプト 1。T138 の規則で読めなくなったテンプレートは 0。`filled()` に打った `{prompt}`・`{date}`・`$'` は置き換わらずに残る。**DeepSeek-R1 Distill Qwen 1.5B（判定: 直す）**: (a) config.json の BOS は 151643（文の終わり）、tokenizer と generation_config は 151646。float32 の原本で perplexity は本物の BOS の 2.55 倍（日本語 599.2 対 235.2）・2.74 倍（英語 152.4 対 55.6）、greedy の 5 つのうち 4 つが壊れた（本物の形なら 5/5 がまとも）。(b) 損 = ppl(ページ) ÷ ppl(本物)。T131 の ±3% とは桁が違う。(c) 一覧の項目の options で `bos: 151646` と `<think>` を specials に、書式を `<｜User｜>{prompt}<｜Assistant｜><think>\n` に（`format_check.py` で 0/9 → 9/9。`CONVERTER` は上げない）。本番（models.yml、Linux の Chromium、直した後）: 準備完了 31.4 秒・19.5 tok/s で、既定のプロンプト（17 × 24）に「17 × 20 = 340、17 × 4 = 68、足して…」と順に考えた（直す前の float32 の greedy では問いを失っていた）。(d) 覆す条件: ページの int8 と既定の生成設定で、今の書式が 5 つのうち 4 つ以上にまともに答えるなら逆（未計測、推測では差は残る）。`<think>\n` の要否は、`</think>` が対にならずに見える画面を持ち主が嫌うかで決めてよい。**記録の直し**: (3) SmolLM2 135M（GGUF）が手書きの ChatML のままなのは「固定値が変わるから」ではなく「測って得が無かった」（float32 の greedy で、本物の書式は一覧のプロンプトで繰り返しに入り悪い、ほかの 5 つでは同じくらい）。(4) llm-jp と sarashina も道具で比べられる（llm-jp の 4 つはカードのシステム文を `SYSTEM` に足すと 9/9、sarashina と CAT-Translate の 5 つは本物の Jinja の文を特殊トークンで区切って sentencepiece で符号化すると 9/9）。**残り**: `?hf=` の special でない追加のトークンは T143、道具とエラーの文と空白の集合は T144。
- **結果（2026-09-26、Opus medium）**
  - **コミット**: e4a224a (3) trim、54b1fa3 (1)(2) 一覧の書式、639d941 (4) トークナイザの候補。
  - **(3) trim**: 新しい option は作らず、変換器が書式に `{prompt:trim}` と書く（ページの `filled()` が `trim()` する。手で書く書式も同じ印）。見分け方は目印を空白で囲んでもう 1 回描き、両方消える → `{prompt:trim}`、両方残る → `{prompt}`、片方だけ → 読めない。**最初は「目印の周りに空白があるか」で見分けて RakutenAI 7B chat を誤った**（`USER: ` と ` ASSISTANT:` で囲む書式）。テストの固定値（`tests/fixtures/chat-templates.json`）に本物の jinja2 で「trim するか」を足した（7 つのうち Llama 3.1 と Rakuten 7B が trim）。変換器が読むもので当たるのは一覧の Llama 3.2 の 2 つと Swallow 8B → `CONVERTER` 8。
  - **(1)(2)**: Qwen2.5 の 5 つは手で書いた ChatML をやめて変換器が読む書式に（既定のシステム文が入る）。**SmolLM2 360M も同じにした**（レビューの一覧に無かったが同じ形: 既定のシステム文が落ち、BOS が `<|im_start|>` そのものなので先頭に 2 つ並んでいた）。Swallow-MS はカードのシステム文を入れ、`{prompt:trim}`（本物はターン全体の `strip()` なので頭の空白は残る。頭に空白のあるプロンプトだけ違う）。RakutenAI の 2 つも `{prompt:trim}`。
  - **(4)**: `?hf=` の候補に `spiece.model`、フォルダの経路は `tokenizer.json`・`tokenizer.model`・`spiece.model` のあるものを全部その順に渡す。
  - **確かめ方**: 一覧の HF のモデルで書式のある 41 項目を、本物の `apply_chat_template` と 8 つのプロンプト（前後の空白、末尾だけの空白と改行、改行、全角など）で ID まで比べた（`~/tmp/t138/compare.py`）。変えた 9 項目はどれも先頭の BOS を除いて同じ（Swallow-MS は頭の空白と全角の NFKC の 2 つを除いて同じ）。pytest 471、models-check に `filled()` の `{prompt:trim}`、smoke、forward-check、build。**本番（models.yml、Chromium）**: Qwen2.5 0.5B 16.7 秒・33.6 tok/s（システム文のとおり「…アシスタントであり…」と答える）、SmolLM2 360M 11.1 秒・41.7 tok/s、RakutenAI 2.0 mini 31.2 秒・12.9 tok/s、Llama 3.2 1B 26.5 秒・15.1 tok/s、Swallow-MS 7B 160.5 秒・3.8 tok/s（64 ビット）、フォルダの経路 `local` 4.3 秒・`hf` 3.6 秒（stories260K の決まった文）、`?hf=rinna/japanese-gpt2-small`（`spiece.model` だけ）9.3 秒。
  - **レビューで見てほしいこと**: (1) `{prompt:trim}` を書式の中の印にした形（option を増やさない）。(2) 突き合わせで見つけた範囲外のもの: **DeepSeek-R1 Distill Qwen 1.5B の本物の書式は `<｜Assistant｜>` の後に `<think>\n` を書き、ページの先頭の BOS も本物と違う**（ページ `<｜end▁of▁sentence｜>`、本物 `<｜begin▁of▁sentence｜>`）。手で書いた書式のまま。直すかはレビューで。(3) SmolLM2 135M（GGUF）は手書きの ChatML のまま（固定値の T86 の文が変わるので触らなかった）。(4) llm-jp の系統と sarashina の系統は突き合わせの道具（transformers の遅いトークナイザ）では比べられない（前のレビューで確かめ済み）。

<details><summary>T138 の採番時の記録</summary>

- 根拠: レビューで本物の Jinja と `tokenizers` と突き合わせて、黙って違うが軽いものが残った（どれも文への影響は未計測）。
- (1) T132: 一覧の Qwen2.5（0.5B・1.5B・3B・7B・Coder 0.5B）は手で書いた `CHATML` で、本物の書式とモデルカードが必ず入れる既定のシステム文（「You are Qwen, created by Alibaba Cloud. You are a helpful assistant.」）を落としている。`template` を外せば変換器が読む本物の書式になる（特殊トークンも読む）。
- (2) T125: Swallow-MS 7B はモデルカードが必ず渡すシステム文（`<<SYS>>\nあなたは誠実で優秀な日本人のアシスタントです。\n<</SYS>>\n\n`）を落としている（T81 の llm-jp-3.1 と同じ直し）。
- (3) T127: 読めるテンプレートの 42 / 237 がプロンプトに `trim` をかける（一覧の Llama 3.2・Swallow 8B・Swallow-MS・Rakuten の 2 つ）。空白で囲んだ目印で 1 回描けば見分けられるので、変換器が options に載せ、ページが送る前に削る。
- (4) T126: `?hf=` はトークナイザに `tokenizer.json` と `tokenizer.model` しか試さないので、`spiece.model` だけのリポジトリ（rinna）は `?hf=` で開かない。フォルダの経路は 1 つしか渡さないので、両方あるフォルダ（Rakuten 2.0 mini）は `tokenizer.json` で断られて止まる（T125 のレビュー）。
- 手順: (1)(2) は `src/models.js`、(3) は変換器と `filled()`（`CONVERTER` を上げる）、(4) はページの候補の並び。本物の `apply_chat_template` と ID の列で突き合わせる（レビューのやり方、`~/tmp/review-2026-09-26/formats/`）。
- 完了条件: (1)〜(3) の対象が本物と同じ ID の列（先頭の BOS は違ってよい: T131 は却下）。`models.yml` で答える。

</details>

- [x] **T140 [運用] 既定のブランチを master から main に**（Opus medium、2026-09-26、持ち主の指示「そのあとブランチ名変更しちゃおう」）— 状態: **完了**
- **やったこと（2026-09-26）**: 名前を変える前に、残っていた作業のブランチ（`/benchmark/` の GPU の計測）を画面の確認の後に本線に入れて消し、リモートを master の 1 本にした（持ち主の指摘「残すブランチはひとつにしないとブラ名の変更が事故る」）。5bb474e で `deploy.yml` を `main` に → 環境 `github-pages` のデプロイしてよいブランチに `main` を足す → GitHub で master を main に（既定のブランチも main に）→ 手元の checkout を main に（`origin/HEAD` も）→ 4403301 の main への push でデプロイが通り、本番の `models.yml`（main から起動、stories260K 5.7 秒・tiny-lm 5.4 秒）で答えた → 環境の決まりから `master` を消した。AGENTS.md の方針 7 を「main に push するとデプロイされる」に。
- **残したもの**: Pages の設定の `source.branch` は `master` のまま（デプロイは Actions の `build_type: workflow` なので使われない）。TODO.md の過去の記録の「master」はそのまま。リポジトリの中の `…/master/…` は他人のリポジトリ（llama2.c、nvm）。

<details><summary>T140 の採番時の記録</summary>

- **`master` を指しているところ（2026-09-26 に洗った）**: (1) `.github/workflows/deploy.yml` の `on: push: branches: [master]`、(2) **環境 `github-pages` の「デプロイしてよいブランチ」の決まり（custom branch policy が `master` だけ）**: GitHub のブランチ名の変更がこれを書き換えるかは未確認で、書き換えなければ main からのデプロイが断られる、(3) AGENTS.md の方針 7「master に push するとデプロイされる」の 1 行（TODO.md の 11 か所は過去の記録なのでそのまま）、(4) 手元の checkout と記憶（`feedback-*` の「master」）。Pages の設定の `source.branch` も `master` だが、`build_type` が `workflow`（Actions でデプロイ）なので効かない（未確認）。ブランチの保護は無い。リポジトリの中の `…/master/…` のリンクは他人のリポジトリ（llama2.c、nvm）だけ。
- **GitHub が面倒を見るもの**: 既定のブランチの切り替え、古い URL（`tree/master` など）の転送。週 1 回の `browsers.yml` と `workflow_dispatch` は既定のブランチで動く。
- **手順**: ① 走っているワークフローが終わるのを待つ → ② `deploy.yml` を `main` にするコミット（master に push してもデプロイは起きない）→ ③ 環境の決まりに `main` を足す（`gh api -X POST repos/takano32/pyodide-llm/environments/github-pages/deployment-branch-policies -f name=main`）→ ④ 名前を変える（`gh api -X POST repos/takano32/pyodide-llm/branches/master/rename -f new_name=main`）→ ⑤ 手元: `git branch -m master main && git fetch origin && git branch -u origin/main main && git remote set-head origin -a` → ⑥ main への push でデプロイされ本番が動くのを確かめる → ⑦ 環境の決まりから `master` を消す → ⑧ AGENTS.md と記憶の「master」を直す。
- **戻し方**: 名前を main から master に戻し、`deploy.yml` を戻す。
- 完了条件: main への push でデプロイされ、本番が動き、`master` を指すものが（過去の記録のほかに）残っていない。

</details>

- [x] **T139 [運用] 開発を OCI Ampere に移行**（Opus、2026-09-26）— 状態: **完了**（段 7 の切り替えは、2026-09-26 のレビューの回を a1-free で行って済んだ。古い機械の `~/tmp` を消すかは T139 と切り離して持ち主が決める）
- **レビュー（Opus xhigh、2026-09-26）: must-fix 2 つを直して OK。完了条件も満たした。** この会話は段 6 を実装したので（docs/review-by-opus.md の決まり 1）、判定は新規のサブエージェントのもの。確かめ方: GitHub から写しを clone して docs/dev-setup.md の手順を書いてあるとおりに（apt + 小さな `.venv` の形と、全部を `.venv` に入れる形の両方）、pytest は 2 つの形とも 471、6 節の 12 のコマンドが全部通り、写しの dist で `e2e.mjs stories260K chromium` も通った。記録の主張（transformers の METADATA、protobuf、Playwright の断り、`systemd-run`、`/tmp`）も確かめた。**直したもの**: (1) docs/review-by-opus.md の手順 2 が system の `python3 -m pytest` のままだった（tokenizers が無いと `test_bytebpe.py` とそれを import する `test_llama3.py` の 185 件が skip。うち 13 件は tokenizers と関係が無い）→ `.venv/bin/python`。(2) dev-setup.md の「Firefox・WebKit も同じ形」: **WebKit は Ubuntu 26.04 の arm64 で動かない**（24.04 の版が求める ICU 74・libxml2.so.2 が 26.04 に無い）→ CI で。ほかに文書だけの直し: skip の書き方、全部を `.venv` に入れる形の順と `source .venv/bin/activate`（Makefile は PATH の python3）、Playwright の入れ方を AGENTS.md から案内、`bunx` の出どころ、sentencepiece 0.2.1 と 0.2.2 は 7 モデルで同じ ID、`format_check.py` は GGUF の 9 項目を比べない、台帳の頭に「いまの状態」の案内、数え方（12 のコマンド）と「上の」。**やらない（あれば良い）**: `test_llama3.py` を `test_bytebpe.py` から切り離す、`format_check.py` が GGUF の項目を原本と比べる、T126 の sentencepiece の突き合わせの道具をリポジトリに（`.venv` を使えば試験は減らないので急がない）。
- **結果（2026-09-26、a1-free の最初の会話、Opus xhigh）**
  - **段 4（在庫の片付け）**: 済。T140 の前に残りのブランチを本線に入れて消していた（リモートもローカルも main だけ、ワークツリーは 1 つ）。
  - **段 6**: `docs/dev-setup.md` のとおりに用意して、deploy.yml と同じ 12 のコマンド（pytest・6 つの検査・smoke・forward-check の 3 つ・build）が全部通った。手元の時間は AGENTS.md の「開発機の移行（T139）」。
  - **手順のとおりに入らなかったもの（dev-setup.md と `tests/requirements-reference.txt` を直した）**: (1) **transformers 5.12.1 と tokenizers 0.23.1 は PyPI では一緒に入らない**（5.12.1〜5.15.1 は `tokenizers<=0.23.0` を求め、import のときにも止まる。0.23.0 は PyPI に無い。前の開発機の 5.12.1 は git から作ったもので縛りが無かった）→ 5.16.1。書式の突き合わせは記録のある 4 項目（Qwen3 0.6B・Rakuten 7B・Llama 3.2 1B が 9/9、Swallow-MS は既知の 2 つ）で記録と同じ。(2) **protobuf が要る**（apt の python3-protobuf）。無いと `format_check.py` は sarashina で止まる。(3) **Playwright 1.60 は Ubuntu 26.04 の arm64 を断る** → `PLAYWRIGHT_HOST_PLATFORM_OVERRIDE=ubuntu24.04-arm64` で 24.04 の版を入れる（足りない共有ライブラリは無かった）。
  - **手元のブラウザ**（Playwright の Chromium 148、`tests/e2e.mjs`）: stories260K（準備完了 7.3 秒）、tiny-lm（7.2 秒・312 tok/s）、llm-jp-3 980M instruct3（HF から取得と変換、359.8 秒・13.4 tok/s・ヒープ 1398MB・機械全体の使用の峰 5.3GB）、Qwen2.5 3B Instruct（GGUF 3.3GB を取得して変換、586.6 秒・3.1 tok/s・ヒープ 4050MB・機械全体の使用の峰 8.1GB で、available は 3.8GB まで下がった）。どれも 2 スレッド（隔離されている）。AGENTS.md の「この開発機ではブラウザを動かさない」は「7B 級は動かさない、峰の見込みを先に見る」に書き直した。
  - **見込みと違ったこと**: (a) **回線は速くない**: huggingface.co から 5.4 MB/s（`fetch-node.mjs`、16 MiB × 6 本、256MB。OCI の東京）。前の開発機の家の回線（7.1〜9.4 MB/s）より遅い。1B 級の準備完了のほとんどは取得（348 秒）。(b) **1 コアの速さ**: `node tests/profile.mjs`（JS の forward、1 本、2 回とも同じ）で llm-jp-3 150M が 12.0 ms / トークン（前の開発機の記録は 9.2）、tiny-lm 1.62（1.6）、stories15M 1.67（1.6）。小さいモデルは A78 と同じくらいで、llm-jp-3 150M は 1.3 倍遅い（「同程度」の見込みは小さいモデルでだけ当たった）。
  - **書式の全件**（`format_check.py`、33 項目。書式のある 41 項目のうち、GGUF の 9 項目（T74 の SmolLM2 135M と T136 の段 ① の 8 つ）は道具が外す）: 違いは 13 で、どれも既知の種類（AGENTS.md の `format_check.py` の行）。ただし **transformers 5.16.1 の参照は `tokenizer.model` のある一部のモデル（sarashina の 3 つ、CAT-Translate の 2 つ）で本物と違う**（CAT-Translate は `tokenizer.json` があるのに `tokenizer.model` から作り直して 1 文字ずつ）。違いの出たプロンプトで、ページの ID は本物の sentencepiece と同じだった。
  - **持ち主の指示（AGENTS.md の「持ち主の指示」と落とし穴に書いた）**: 「メモリたくさんあるから活用せよ」（重いものも峰の合計が available に収まれば並行に）、「Chrome DevTools MCP も活用してね」。持ち主の入れた Chrome DevTools MCP は X サーバーが無くて起動しなかったので、`--headless --isolated` で登録し直した（`claude mcp add chrome-devtools -s local -- bunx -y chrome-devtools-mcp@latest --headless --isolated`、持ち主が再接続）。本物の Chrome 154 で本番のページの tiny-lm が準備完了になった（隔離、2 スレッド、コンソールにエラーなし）。
  - **venv はリポジトリの中の `.venv`**（2026-09-26、持ち主の指示「移動させてください」。`.gitignore` に足した。最初は dev-setup.md の書いていた `~/venvs/reference` に作った）。
  - **範囲外で気づいたこと（レビューで見る）**: (1) `tests/e2e.mjs` の 1B と 3B の実行で、Playwright の一時プロファイルの OPFS が変換の保存を途中で断った（`notKept`: the origin private file system took only part of a write）。T99 の落とし穴（ヘッドレスの Cache API の割り当て）と同じ種類か未確認。(2) T134 の見出しの「GPU の項目に足す計測はブランチで実装済み」は、T140 で本線に入った後も古いまま。

<details><summary>T139 の採番時の記録</summary>

**採番時の見出し**: T139 [運用] 開発を OCI Ampere に移行 — 状態: **進行中（準備）**（2026-09-26、持ち主の指示「移行計画をタスクにして進めようよ」「マシンが変わるということはエージェントの記憶がなくなるので準備が必要でしょ？」。規模 小〜中）

- **根拠**: 今の開発機（ARM の big.LITTLE、約 6.6GB で空きは約 2.3GB、スワップなし）はメモリで詰まっている: float32 の原本の計測（T124 の eps は 1500 トークンをあきらめて 511 に縮めた）、1B 以上の突き合わせ（CI に回す）、ブラウザ（動かさない決まり）、重い計測は同時に 1 つ。**移る先**: OCI の Ampere A1（Neoverse N1、ARM）、**2 コア・12GB**（Always Free の今の枠いっぱい: 月 1,500 OCPU 時間・9,000 GB 時間、Oracle の文書で確かめた、2026-09-26）、回線は速い（データセンター）。1 コアの速さは今の A78 と同程度の見込み（未計測）。
- **移って手元でできるようになるもの**（見込み、未計測）: 3B 級までの float32 の計測と GGUF の突き合わせ、ヘッドレスの Chromium で 1〜3B 級の確認（デプロイ前に `e2e.mjs`）、重い計測を 2 つ同時に。**CI に残るもの**: 7〜8B（ヒープ 9.7〜11GB）、64 ビットのメモリ、スレッドの伸び（2 コア）、ブラウザごとの速さ。**持ち主の家の回線に近い速さは測れなくなる**（データセンターの回線）: それは `/benchmark/` の回線の節（T134）を持ち主の端末で。
- **エージェントの記憶が無くなる問題**: エージェントの記憶は `~/.claude/projects/<リポジトリのパスから作った名前>/memory/` の 18 個のファイルで、この機械にしか無い。会話の履歴も持っていけない。新しい機械でユーザー名やリポジトリのパスが変われば、同じファイルを写しても別の名前のフォルダになる。だから**記憶に頼らず、リポジトリに書いて持っていく**のを基本にする。
- **段と止まる点**
  1. **記憶をリポジトリへ**（Opus、今の機械で）— **済（2026-09-26）**: 作業の決まり 8 つを AGENTS.md の「持ち主の指示（エージェントの記憶から移した）」に。持ち主の指示「記憶を写さなくていいようにレポジトリに書き下ろしてください」で残りも移した（持ち主の好み 4 つ、Pyodide を選んだ理由の調べ、`gh repo rename` の落とし穴。日ごとの状態の記憶は台帳にすでにあった）。**記憶のフォルダは写さない**。 記憶のうち作業の決まり（持ち主の指示: T 番号ごとのコミット、統合したブランチを消す、出してから戻す、開発機の値を既定にしない、「ソフトウェアスレッド」「フォールバック」の言葉、前提は採番する、速さよりきれいなコード）を AGENTS.md の「作業の決まり」に移す。すでに AGENTS.md か TODO.md にあるものは書かない。持ち主のこと（日本語で話す、など）は AGENTS.md の冒頭に 1 行。計測の履歴（`project-*`）は台帳にすでにあるので移さない。
  2. **手元にしか無い道具をリポジトリへ**（Opus）— **済（2026-09-26）**: `tests/format_check.py` と `tests/requirements-reference.txt`（Qwen2.5 0.5B・Qwen3 0.6B・Rakuten 7B・Llama 3.2 1B が 9/9、Swallow-MS は既知の 2 つを除いて同じ、で前の道具と同じ結果）。 書式を本物の `apply_chat_template` と ID で突き合わせる道具（T124・T127・T138 の `compare.py`、`~/tmp` にある）を `tests/format_check.py` に。参照の道具の入れ方（transformers 5.12.1・tokenizers 0.23.1・jinja2・sentencepiece 0.2.2 を venv に）を `tests/requirements-reference.txt` に。`~/tmp` の計測の途中のファイル（30GB）は持っていかない（数値は台帳にある）。
  3. **開発機の用意の手順を書く**（Opus）— **済（2026-09-26）**: `docs/dev-setup.md`（deploy.yml と同じ手順、参照の venv、ブラウザ、機械しだいの落とし穴の確かめ方、用意できたかの確かめ方）。持ち主の知らせ（新しい機械の OS は **Ubuntu 26.04 LTS**）を受けて、Ubuntu の apt のパッケージと venv と nvm の手順を足した（Ubuntu 26.04 では未確認、段 6 で直す）。 `docs/dev-setup.md`（Node 24 は `.nvmrc`、Python と NumPy と pytest、`npm ci`、`make kernels`、`make models`、参照の venv、`gh auth login`、`systemd-run --user` が使えるか、`/tmp` が tmpfs か、Playwright のブラウザの入れ方）。最後に「用意できたか」の確かめ方（pytest・smoke・forward-check の 3 つ・6 つの検査・build が通る）。
  - **新しい機械を ssh で見た（2026-09-26、持ち主の許し「ssh a1-free で接続できるので調査していいよ」、読むだけ）**: Ubuntu 26.04.1 LTS、aarch64、**Neoverse-N1 × 2**（flags に `asimddp`（内積）あり、`i8mm` なし）、メモリ 11.9GB（ほかの常駐: 持ち主の別の Claude Code のセッションと searxng の docker で約 1GB、空き約 9.7GB）、**スワップ 4GB あり**（今の機械は無し）、`/` の空き 169GB、**`/tmp` は tmpfs（5.9GB）**（今と同じ注意が要る）。**ユーザー名は同じ `takano32`**: リポジトリを `~/GitHub/pyodide-llm` に置けば記憶のフォルダの名前（`~/.claude/projects/-home-takano32-GitHub-pyodide-llm/`）も同じになり、写せばそのまま効く。入っているもの: git、gh 2.46.0（**未ログイン**）、Node v24.20.0（nvm）、Python 3.14.4、apt の python3-numpy 2.3.5・python3-pytest 9.0.2・python3-venv、wget、curl、gcc、make、docker。**無いもの**: tokenizers（pytest の一部と deploy.yml が入れる）、transformers と sentencepiece（参照の venv）、Playwright のブラウザ。`systemd-run --user --scope -p MemoryMax=…` は使える（linger は無し）。`~/GitHub` は空。
  4. **在庫の片付け**（Opus、今の機械で）: 走っている並行の作業（T136 の突き合わせ、T134 の `/benchmark/`）を終わらせ、ワークツリーとブランチを本線に入れるか消す。push し忘れが無いか確かめる。
  5. **新しい機械の用意**（**持ち主**）: インスタンス、ユーザー、`gh auth login`、Claude Code。リポジトリを clone。記憶のフォルダは写さない（1 でリポジトリに移した）。
  6. **新しい機械で確かめる**（Opus、新しい会話で）: `docs/dev-setup.md` のとおりに用意し、試験が全部通ることと、手元の時間（pytest・smoke・forward-check の tok/s・`make kernels`）を測って AGENTS.md に。**AGENTS.md の「この開発機では」の決まりを書き直す**（ブラウザの禁止 → 「7B 級は動かさない、メモリの見込みを先に見る」、`/tmp` の注意は新しい機械で確かめてから、スレッドの計測は CI と持ち主の端末、計測環境の行）。
  7. **切り替え**: 新しい機械で最初のタスクを 1 つ終えたら完了。古い機械の `~/tmp` は持ち主が消してよい。
- 完了条件: 新しい会話が AGENTS.md と TODO.md と `docs/dev-setup.md` だけで作業を始められ、試験が全部通り、手元の時間が記録されている。

</details>

- [x] **T142 [運用] 監視の修正**（Opus xhigh、2026-09-26、持ち主の問い「CI を待機するの、きちんとスクリプトにしたほうがよくないか？不安定過ぎる。あと、バックグラウンドでやろうよ」）— 状態: **レビュー待ち**
- **結果（2026-09-26）**: `tests/ci.mjs`（`run` / `wait` / `deploy`、期限つき、状態が変わるたびに 1 行、`gh` の失敗は取り直す、`--grep`）と、AGENTS.md の落とし穴「待ちは ID で、期限つきで、裏で」と構成の表の行。**dispatch が run の ID を返すのは REST API の版 `2026-03-10` を頼んだときだけ**（`gh` 2.46 の既定の版では 204 で本文なし。最初はそれで「Unexpected end of JSON input」になった）。確かめたこと: `run models.yml models=stories260K` で dispatch から ID、状態の行、終わり、`--grep` の行、exit 0 まで 49 秒。`wait` を終わった run に。`deploy` は、待つ間に新しい push で取り消されたデプロイ（deploy.yml の concurrency で、順番待ちのものは新しいものに替わる）を「cancelled」で終えていたので、後のデプロイ（そのコミットも含む）を追うようにした（e8d9468 のデプロイが取り消され、ac7513c のデプロイの成功まで待てた）。期限切れ（exit 2）も確かめた。**持ち主の問い「さらなる改善はできないかね？何回も回すんだよ？」で足したもの**: 1 回の呼び出しで何本でも待って最後に表（models.yml を 3 本まとめて出して待てた）、失敗したジョブの最初のエラーまでの行、同じ run の二重の待ちを断る（`.tmp/ci/` の lock とログ、確かめた: 2 つ目は exit 4 で先の待ちのログを示す）、回数制限で落ちない、長い run では問い合わせを 30 秒ごとに、workflow ごとの既定の `--grep`、models.yml の `run-name`（Actions の一覧で見分けられる）。持ち主の問いの前後に、終わらない待ちがもう 1 つ出た: 止めようとした `pkill -f <名前>` が、それを打ったシェル自身のコマンド行に一致して自分を止めた（害は無し）。

<details><summary>T142 の採番時の記録</summary>

**採番時の見出し**: T142 [運用] 監視の修正 — 状態: 未着手（2026-09-26、持ち主の指摘「監視が終わらんでしょ、それでしか観測してないよ？」と指示「監視の直し方を『監視の修正』というタスク名で積んでおこうぜ」。規模 小）

- 根拠: 持ち主には「CI がとてつもなく遅い」と見えていたが、GitHub の記録では CI は数分で終わっている（デプロイ約 3 分、models.yml 3〜4 分、bench.yml 8〜13 分。止まっていたのは T141 の Windows のジョブだけ）。遅く見えたのは、エージェントの待ち（監視）が終わらないため。2026-09-26 のレビューの回で、サブエージェントの `until ! pgrep -f fixed_qwen3.py; do sleep 5; done` が、`pgrep -f` がループ自身のコマンド行に一致して、待っていた計算が終わった後も 52 分回っていた。同じ計算の終わりを待つループが 3 本重なってもいた。
- 直し方（案）: (1) CI は run の ID で待つ（`timeout 1800 gh run watch <id> --exit-status` のように、終わりの状態なら何でも抜け、期限もある）。「最近の N 件」で探さない（新しい run が入ると ID が外れる）。(2) 手元の計算は PID（`while kill -0 $pid`）か、最後に必ず書く印のファイルで待つ（失敗しても `trap` で書く）。`pgrep -f` で名前を探さない。(3) どの待ちにも期限を付け、期限で「まだ動いている」と報告する。同じものを 2 回待たない。(4) サブエージェントに渡す頼みにも同じ決まりを書く。
- 置き場: AGENTS.md の落とし穴（と、要るなら待ちの小さな道具を tests/ に）。
- 完了条件: AGENTS.md に決まりがあり、次のレビューの回で待ちが期限なしに残らない。

</details>

- [x] **T141 [運用] CI の実行が終わらず、Windows のジョブが 90 分止まる**（Opus xhigh、2026-09-26、持ち主の問い「案のうち Windows にきくやつはやらんか？」）— 状態: **レビュー待ち**
- **結果（2026-09-26）**: 止まっていたのは Windows の WebKit の `browser.close()`（返らない）。見張りのタイマーはその前に止めていたので、何も終わらせなかった。`tests/e2e.mjs`・`tests/bench-browser.mjs` は close を 15 秒で見切り、`tests/stock-firefox.mjs` も含めて「ok」を書いたら `process.exit(0)` で終わる（37f3b23、ac7513c）。**確かめたこと**: 直す前の main の models.yml（windows-latest、WebKit、4 モデル）は 4 モデル目の close で 13 分止まった（取り消した）。直した後の同じ組は 2 分 14 秒で終わった。**browsers.yml を 1 回（run 36241545614）: 全体で 20 分**（前の 2 回は Windows x64 が 90 分の上限で取り消され、全体も約 90 分）。Windows x64 のジョブは 19.6 分で最後まで回った。ほかの OS のジョブは 5〜11 分、HF の 6 つの組は 5.8〜18.9 分。**残った 1 件（別の件）**: そのジョブで Playwright の Firefox の stories15M が 600 秒の持ち時間を使い切り、見張りで「timed out」として止まって次へ進んだ（ジョブは failure）。前の 2 回の Windows では同じ組が 26 秒で準備完了していたので、たまに起きる止まりと見る（原因は未確認）。

<details><summary>T141 の採番時の記録</summary>

**採番時の見出し**: T141 [運用] CI の実行が「ok」の後で終わらず、Windows のジョブが 90 分止まる — 状態: **進行中**（2026-09-26、持ち主の問い「案のうち Windows にきくやつはやらんか？」。規模 小）

- 根拠: browsers.yml の windows-latest のジョブが 2 回続けて（run 36088172033・36133345856）90 分の上限で取り消された。「Run the models in every browser」が 5122 秒。2 回とも最後の行は WebKit の実行の答えの文で、その実行の「ok」は出なかった（その前の「ok」は前のモデルのもの）。`tests/e2e.mjs` は答えを書いた後、見張りのタイマー（`E2E_TIMEOUT`、600 秒）を止めてから `browser.close()` を待ち、そのあと「ok」を書いて、`process.exit` を呼ばずに終わるのを待つ作り。**止まったのは `browser.close()`**（Windows の WebKit で返らない）: 直す前の main で models.yml（windows-latest、WebKit、4 モデル、run 36240760614）を回すと、4 モデル目の llm-jp-3-150m が答えの文を書いた後、「ok」を出さずに 13 分止まった（取り消した）。ほかの OS のジョブは 3〜10 分。
- 直し: `tests/e2e.mjs`・`tests/stock-firefox.mjs`・`tests/bench-browser.mjs` は「ok」を書いたら `process.exit(0)`（stdout を流しきってから）。`browser.close()` は 15 秒で見切る。
- 確かめ方: 直す前の main で models.yml（windows-latest、WebKit、stories260K・stories15M・tiny-lm・llm-jp-3-150m、run 36240760614）は llm-jp-3-150m の close で止まった（上）。**直した後の同じ組（run 36240871082）は 4 モデルとも「ok」まで行き、ジョブは 2 分 14 秒で終わった**。そのあと browsers.yml を 1 回。
- 完了条件: browsers.yml の windows-latest のジョブが上限の前に終わる。

</details>

## やらないと決めたこと

- T131 BOS と先頭の空白を、モデルの tokenizer の設定どおりにする。**2026-09-26 に却下（持ち主の判断、レビュー担当の勧め）**: モデルが学習で見たのと同じトークンの列を渡すためのもので、効くかを先に測った。先頭に BOS を置くか置かないかで perplexity の差は ±3% 以内、良くなる向きは文章で入れ替わる（下の記録）。直すと保存した変換が全部作り直しになる（7B は日本の回線で約 30 分の取り直し）のに、得るものがはっきりしない。**見直す条件**: BOS かダミーの空白のせいで答えが目に見えて壊れるモデルが出たとき。一覧の書式は手で本物に合わせてあり、効くものは T138 にある。
  <details><summary>T131 の記録</summary>

  - 根拠: sarashina2.2 の系統（0.5B・1B の Instruct、CAT-Translate）は本物の書式に BOS が無い（`tokenizer_config.json` の `add_bos_token` が偽、書式も `bos_token` を書かない）のに、`generate()` は必ず BOS から始める。基底の sarashina2.2 0.5B は sentencepiece の `add_dummy_prefix` が 0（tokenizer.json の Metaspace も `prepend_scheme: "never"`）なのに、エンジンは最初の片にダミーの空白を付ける（「これからの流行りは」が本物の [14537, 52195, 302] に対して [271, 14537, 52195, 302]）。文への影響は未計測（CI の答えはまとも）。
  - 手順: 先に測る（perplexity か greedy の文を、BOS のあり・なし、空白のあり・なしで）。効くなら、変換器が `add_bos_token` と `add_dummy_prefix` を options に載せてエンジンが従う（`CONVERTER` を上げる）。
  - **先に測った（2026-09-26、判断はまだ）**: 同じ文章の同じトークンを、先頭に BOS を置いた場合と置かない場合で予測させた perplexity（窓 512、1,497 トークン、Wikipedia の 3 記事、int8 と 7 ビットの活性値、Node の Pyodide。道具はリポジトリに入れていない）。日本語: Qwen2.5 0.5B Instruct は BOS あり 23.30・なし 23.43、sarashina2.2 0.5B Instruct は 20.97・20.41、rinna japanese-gpt2 small は 30.29・30.69。英語: 19.25・19.29、13.42・13.61、42.38・41.69。**差は ±3% 以内で、向きは文章で入れ替わる**（sarashina は日本語で BOS なしが 2.7% 良く、英語で BOS ありが 1.4% 良い。rinna はその逆）。本物の書式が BOS を書かないことが、文を目に見えて悪くしている証拠はこの計測には無い。指示への答えの質は測っていない。
  - **2026-09-26 の Opus xhigh のレビューで足したもの**: (a) 本物の書式が BOS を書かないのにページが BOS から始めるのは、sarashina の系統のほかに Qwen2.5（`<|endoftext|>`）、llm-jp-4（`<|startoftext|>`）、rinna、Rakuten 2.0 mini。(b) Rakuten 2.0 mini は最初の 1 語片（本物は「A」、ページは「▁A」）。`?hf=` の Mistral v0.2 は読んだ書式の先頭の空白とダミーの空白が重なり「▁」が 1 つ多く、zephyr は `</s>` の後の空白が無い（一覧は手で書いて合わせてある）。(c) llm-jp-4 の答えの頭に空白が 1 つ付く（▁ から書くのを、特殊トークンの後なので落とさない）。(d) Python と sentencepiece の NFKC の Unicode の版の違い（本物は U+FF5E「～」を残し、エンジンは「~」にする。rinna で BMP の 5〜35 文字）。

  </details>
- JS + WASM 版のエンジン（別プロジェクト）。
- ~~WebGL / WebGPU（このモデル規模では CPU SIMD に勝てない。モデルを 0.5B 以上にするなら再検討）~~ **再検討の条件を満たした（一覧に 1.5B まで入った）ので T94 に積んだ**（2026-09-25）。WebGL は計算に向かない（シェーダでの整数の扱いと読み戻しが不便）ので、見るのは WebGPU だけ。
- ~~マルチスレッド（効果がなく、GitHub Pages では COOP/COEP が要る）~~ **却下の理由の半分は古い（2026-09-24 に訂正）**: 「効かない」は 2026-09-19 の見立てで、2026-09-21 の測り直しでは約 2 倍まで伸びる（AGENTS.md）。残る理由は「GitHub Pages が COOP/COEP を送れないので `SharedArrayBuffer` が無い」ことと、Pyodide がスレッドなしのビルドであること。前者には回避策（`coi-serviceworker`、またはヘッダを付けられるホスティング）があり、後者は Worker 側を素の WASM カーネルにすれば避けられる。→ **T93 で再検討する**。T51（2026-09-19 却下）の番号は使わない。
- Pyodide のバージョン固定やフォールバックの連鎖。
- 「MobileLLM」への改名（Meta の既存モデル名と衝突）。
- T49 プロンプトの一括処理（prefill のバッチ化）。2026-09-19 に却下: このデモのプロンプトは数トークン（「昔々、」は 4 トークン、最初のトークンまで 0.03〜0.05 秒）で、効果が見えない。会話の履歴が長くなったら（T46）再検討の価値はある。
- T50 オフライン対応（PWA）。2026-09-19 に却下: 古い `worker.js` のキャッシュで 404 を出した前例があり、Service Worker はその種の事故をもっと起こしやすい。「Pyodide は常に最新を実行時に解決する」方針とも噛み合わない。
- T46 会話の継続（ターンをまたぐ KV キャッシュ、文脈があふれたときの作り直し、リセット）。2026-09-19 に却下: 実験の範囲の外。このプロジェクトの価値は「WASM Python でどこまでできるかを測って公開する」ことで、状態を持つチャットの作り込みは測れるものを増やさない。150M の指示モデルの受け答えは弱く、チャットボットとして評価される土俵に自分から上がることになる。T46 のもう半分、チャットテンプレート（1 ターンだけ、状態なし）は、T60 で HF の一覧に指示モデルを入れる場合の付属としてだけ残した。「チャット風の UI なのに続きを書くだけ」という違和感は文言で解く。長い入力の処理速度が問題になったら、見直すのは T49（prefill のバッチ化）。
